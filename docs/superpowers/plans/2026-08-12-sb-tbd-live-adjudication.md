# SB-TBD Live Adjudication Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task by task. Do not delegate or spawn subagents unless Jauhar explicitly authorizes that in the execution turn.

**Goal:** Reverify every one of the 66 live `SB-TBD` requirements against the accepted Gate 1 tree, record one evidence-backed as-built classification and pilot disposition per row, route all 66 acceptance-test intentions, and preserve every unresolved endpoint, tensor, dip, saturation-dispatch, summation, unit, uncertainty, source, legal, and naming question without changing thin-bed, LRLC, import, plotting, cutoff, permeability, reporting, or Monte-Carlo behavior.

**Architecture:** This is a documentation-only evidence pass over the Thomas-Stieber module and picker, generic parameter-write custody, curve dictionary and import paths, RtC/IMTS modules, module-runner and run-summary surfaces, pay/cutoff and Monte-Carlo engines, report/export paths, IPC types, manual matrix, and reachable Git history. The immutable PRD supplies the intended contract, original priority, historical chapter status, 52 parameter rows, 11 open items, 6 escalations, 15 defect refusals, 4 independent-derivation items, 327 traceability dispositions, and 66 named test intentions. Current source, independently sourced or derived tests, manual evidence, and reachable history supply the separate live verdict. One algebraic corner test, generic provenance infrastructure, a source-text characterization, or a correct bulk-saturation module does not prove the whole observable thin-bed contract.

**Tech Stack:** Markdown, RFC 4180 CSV, PowerShell 5.1, Git, `rg`, Node.js `node:test`, TypeScript, Rust `cargo test`, the takeover-ledger validator, and the existing SandiBumi full gate.

## Global Constraints

- This planning increment may create this plan and update `docs/takeover/STATUS.md` only. It MUST NOT modify a ledger verdict, evidence receipt, Rust, TypeScript, CSS, test, `REVIEW.md`, generated verification output, any file under `docs/PRD_v2/**`, or any file under `docs/research_2026-08/**`.
- Execute the later adjudication on the session model with `superpowers:executing-plans`; do not delegate or spawn subagents unless Jauhar explicitly authorizes that in the execution turn. Parameter custody, petrophysical interpretation, legal boundary judgment, and final sign-off remain with the primary session.
- Work only in `D:\XX. SandiBumi`. The sole registered Git worktree MUST remain that path. `D:\XX. SandiBumi-check` is not a repository or evidence source and MUST remain untouched.
- The accepted implementation evidence anchor is `b332026cb498c105f36eade0bf7899bc0c1309f0`. At plan freeze, `HEAD` is `95d58e0c955dbdc2f89188fd785d260241d227c6`; fetched `origin/master` and the merge base are both `29833735816d9e5be954afafd9ceb71fd856e3f0`; the accepted anchor is reachable. Reverify all four before execution and stop to reconcile if any moves.
- The local planning branch is `codex/g1-sb-tbd-plan`. The serial Gate 1 chain remains intentionally local and unpushed; do not merge, rebase, rewrite history, push, or open a pull request. After the planning commit, create the serial execution branch in the same worktree.
- The codebase-index MCP server is not callable in this task. Targeted filesystem search is the explicit fallback. A consequential negative result MUST be confirmed in the expected Rust and TypeScript files, exact tests, and reachable history.
- Before adjudicating, read `AGENTS.md`, all of `CLAUDE.md`, `docs/PRD_v2/CONTRACT.md`, all of `docs/PRD_v2/17_thinbed-laminated.md`, applicable portions of `docs/record_calibration.md`, all of `docs/method_lrlc_rtc_imts.md`, the current manual matrix, and existing takeover receipts/status.
- Preserve the ledger's source-owned fields byte-for-byte: `requirement_id`, `chapter`, `title`, `original_priority`, `chapter_status`, and `owned_tests`. Their frozen SHA-256 over the 66 ordered rows is `0b99e05d500648a64c4bcd6e35c1ce12c05f8151a9913a11eb9f16be00e0a422`, calculated as PowerShell `Import-Csv`, ordered `Select-Object` of those six fields, `ConvertTo-Csv -NoTypeInformation`, LF join, and UTF-8 without BOM. The immutable chapter SHA-256 is `faa63688610b1c938da9696772372ead5881fc7c4a5a1fd8a0ee780ff94ebd3f`.
- The chapter and ledger agree on 66 contiguous requirements: `P0=4`, `P1=45`, `P2=14`, `P3=3`. Chapter states are `ABSENT=58`, `PARTIAL=2`, and `PRESENT-DIVERGENT=6`. Reverify live behavior independently; do not copy these historical states into the live verdict.
- The chapter defines exactly 66 contiguous acceptance-test IDs, `SB-TBD-T01` through `SB-TBD-T66`, with no suffixes and no omissions. The ledger carries 86 references to those 66 unique tests because compound requirements share intentions. Route every test intention exactly once to one primary receipt owner and list every secondary requirement it constrains without double-counting it.
- Section 5 contains exactly 52 parameter rows. Nine read `ABSENT - ships with no default`, two read `WITHDRAWN`, one reads `SEAM`, zero read `NON-ADOPTABLE`, and forty are cited; two of the forty are advisory bands rather than defaults. Five rows are structural `spec`/`derived` items rather than petrophysical parameters. Preserve those classifications.
- A petrophysical value is cited or absent. Never adopt the live `PHI_SD_MAX=0.30`, `PHI_SH=0.15`, a vendor demo endpoint, the unrecovered `TSFLG` codes, the pole warning `0.9`, either disputed `B` constant, a remembered anisotropy threshold, a fitted `Vcm` coefficient, or any plausible endpoint as authority.
- `E-5` is already answered conservatively for the execution lane: the replacement endpoint values ship absent. The corpus supplies the derivation method, not a pair of project defaults. Gate 1 records the current divergence; it does not choose or implement replacement values.
- Keep all 11 open items `O-1` through `O-11`, all 6 escalations `E-1` through `E-6`, all 15 defect refusals `R-1` through `R-15`, and all 4 independent-derivation items. `C-3-1`, `C-3-2`, and `C-3-3` are specified obligations; `C-1-1` is deliberately not specified because the granted patent is not cleared. Do not create a requirement, formula, workaround, or implementation route for `C-1-1`.
- `E-1` keeps the uncited microscopic shale-anisotropy invalidity threshold absent. `E-2` remains a named-paper acquisition gap. `E-3` blocks the multi-well dip-fit result because the cited paper's shale-volume input is ambiguous. `E-4` is an out-of-scope knowledge-note correction. `E-6` leaves ownership of the legally sensitive resistivity-independent route unallocated.
- Preserve all 327 section 8 traceability items: 156 individually dispositioned and 171 block-dispositioned with enumerated exceptions. A dossier row is evidence custody, not an automatic live status.
- Current source still ships `thin_bed_ts` with `PHI_SD_MAX=0.30`, `PHI_SH=0.15`, `VLAM`, `VDISP`, `VSAND`, and `PHIE_LAM`; it clamps derived dispersed fraction and sand porosity, omits structural shale and flags, and computes the Geolog Eq 86 decomposition. `ModuleSpec` still exposes only name/title/category/doc/args and has no machine-readable validity-condition carrier.
- The chapter's historical claim that crossplot endpoint writes carry no provenance is stale. Live `crossplotPanel.ts::crossplotWriteSource` and `plotCommon.ts::writePlotParameter` persist plot/axis revisions, viewport, selection, interval, method, target, OS user and UTC timestamp, reject incomplete provenance, and register exact undo/redo. However `workflow::resolve_param_arrays` consumes only `value_num`; `value_text` provenance is not carried into the resulting curve or deliverable. Adjudicate `SB-TBD-012` as a compound chain, not as wholly absent or wholly closed.
- The picker and module remain different constructions. `drawTsOverlay` renders the kinked 1975 dispersed limb while `thin_bed_ts` executes the linearly extended Eq 86 construction. The existing `characterizes_the_interactive_clay_overlay_as_a_duplicate_formula_matching_batch_endpoints` test explicitly characterizes this duplication and divergence; it is not correctness proof.
- The drag handles still hard-code `[0,0.5]`; workflow range enforcement still rejects out-of-range zone parameters from the `ModuleSpec`. That supporting refusal does not prove the single-source UI range contract.
- `curves::FAMILIES` contains no Thomas-Stieber parameterization family, no `Steiber` alias, and no sand-fraction ambiguity type. Unknown delivered mnemonics are preserved rather than guessed, but no ambiguity record or cross-tool comparison guard exists.
- `sw_rtc` and `sw_imts` remain real bulk-porosity modules. Both resolve `PHIT_SSC` with per-sample `PHIT_SSPW` fallback; neither accepts a sand-fraction porosity/resistivity route. Their 26 passing module/calibration tests are evidence for those bulk contracts only.
- The generic pay summary, cutoff sweep, and Monte-Carlo engines operate on `VSH`, `PHIE`, `SWE`, and optional `PERM`. Their existence does not prove laminar net/pay, sand-fraction cutoff identity, sand-reference renormalization, or interval VLSA uncertainty.
- Fresh focused candidates pass `30/0/0`: two module tests, one generic plot-provenance/undo test, one generic workflow range-refusal test, and 26 `lrlc::tests`. None uses an executable `SB-TBD-Tnn` identifier. Treat them as `CHARACTERIZATION` or `SUPPORTING-ONLY` unless the exact observable contract and an independent source-backed oracle qualify.
- Manual evidence remains separate: `thin-beds=0/1`, `thomas-stieber=0/0` and explicitly not listed, `saturation=2/97`, `cutoffs-pay=0/23`, `Monte Carlo=2/14`, `report=6/53`, `workflow=0/23`, and `processing-history=0/7`. Automated or desktop-harness evidence cannot close an unchecked manual scenario.
- New receipt and ledger text MUST contain no client, field, block, basin, operator, asset, well, or project name. Refer only to physical conditions, generic records, and source classes.

## Baseline and Count Contract

Before any adjudication edit, re-measure and record all of the following:

1. branch `codex/g1-sb-tbd-adjudication`, created serially from the committed planning branch;
2. a clean worktree and the sole registered worktree at `D:\XX. SandiBumi`;
3. current `HEAD`, accepted anchor, fetched `origin/master`, merge base, and accepted-anchor reachability;
4. exactly 66 ledger rows, covering `SB-TBD-001` through `SB-TBD-066` once with no gap or duplicate;
5. priority counts `P0=4`, `P1=45`, `P2=14`, `P3=3`;
6. source-status counts `ABSENT=58`, `PARTIAL=2`, `PRESENT-DIVERGENT=6`;
7. all 66 live `as_built_status=UNADJUDICATED`, `release_disposition=UNDECIDED`, `risk_class=UNCLASSIFIED`, and `commit_state=UNVERIFIED`;
8. exactly 66 defined test IDs and the same 66 unique IDs referenced 86 times by the ledger, with no defined-but-unowned or owned-but-undefined ID;
9. exactly 52 parameter rows, including nine ABSENT, two WITHDRAWN, one SEAM, zero NON-ADOPTABLE, forty cited and two cited advisory bands;
10. exactly 11 open items, 6 escalations, 15 defect refusals and 4 independent-derivation items, including the deliberately unspecified `C-1-1`;
11. exactly 327 traceability items, split 156 individual and 171 block dispositions;
12. takeover summary `541` adjudicated, `390` unadjudicated, and `402` pilot blockers before any TBD edit;
13. the manual capability counts listed in Global Constraints; and
14. the fresh `30/0/0` focused candidate-test receipt listed in Global Constraints.

The only mechanically predictable post-adjudication ledger count is `607` adjudicated and `324` unadjudicated. Do not predict as-built, pilot-blocker, test-class, risk, or manual-evidence totals before row-by-row classification.

## File Structure for the Execution Increment

- Create: `docs/takeover/evidence/sb-tbd.md`
- Modify: `docs/takeover/requirements.csv`
- Modify: `docs/takeover/STATUS.md`
- Read only: `src-tauri/src/modules.rs`, `src-tauri/src/lrlc.rs`, `src-tauri/src/workflow.rs`, `src-tauri/src/plotting.rs`, `src-tauri/src/curves.rs`, `src-tauri/src/parsers.rs`, `src-tauri/src/ingest.rs`, `src-tauri/src/intake.rs`, `src-tauri/src/montecarlo.rs`, `src-tauri/src/distribution.rs`, `src-tauri/src/report.rs`, `src-tauri/src/office.rs`, `src-tauri/src/export.rs`, `src-tauri/src/lib.rs`, `src/ipc.ts`, `src/ui/crossplotPanel.ts`, `src/ui/plotCommon.ts`, `src/ui/summaryDialog.ts`, current tests, reachable Git history, manual evidence, and the immutable source chapter
- Read-only negative-store inventory may inspect `src-tauri/src/db.rs`, but neither planning nor adjudication may modify it or infer a new persistence design from an absent column.
- Read-only seam checks may inspect saturation, cutoff, permeability, and generated overlay files where needed. Do not modify them during Gate 1.
- Never modify during adjudication: production code, tests, `REVIEW.md`, generated verification artifacts, PRD files, research dossiers, protected vendor material, or unrelated takeover receipts.

## Evidence Receipt Schema

Create one `### SB-TBD-NNN` section per requirement in numeric order. Every section MUST include:

- **Specified contract:** every observable limb, separated when compound.
- **Current implementation:** exact symbols and paths, plus explicit negative-inventory scope where absent.
- **As-built status:** one legal ledger state and why a stricter state fails.
- **Release disposition and risk:** pilot relevance independent of implementation status.
- **Automated evidence:** exact test names and commands classified `CORRECTNESS`, `CHARACTERIZATION`, `SUPPORTING-ONLY`, or `MISSING`, with the independent expected-value source named.
- **Manual evidence:** exact checked capability/scenario or `NONE`; never inferred from automation.
- **Source/parameter boundary:** cited, absent, withdrawn, seam-owned, advisory, derived, spec choice, disputed, legally blocked, or not applicable, with `O`/`E`/`R`/`C` identifiers.
- **UI/IPC/provenance surface:** picker, module spec, request, zone parameter, stored source note, run result, computed curve, log-set record, Results QC, plot/track, report, office export, LAS export, and batch/job surface as applicable.
- **History/reachability:** accepted commit evidence or confirmed negative history search where consequential.
- **Blocking decision/dependency:** exact source, legal, product, naming, UI, manual, or implementation dependency.
- **Next action:** the smallest bounded follow-up, or `NONE` only when the whole contract is proved and no field evidence is required.

## Requirement Evidence Map

### Group A - Recognition and routing (`001`-`005`)

| ID | Exact contract focus | Primary live candidates | Adjudication guard |
|---|---|---|---|
| `001` | Six-cause LRLC recognition produces a coded per-level decision plus scenario and evidence. | module inventory; ribbon; workflow result types; negative Worthington/Madjid search. | Prose, a menu label, or RtC/IMTS availability is not a recognition result. |
| `002` | Route by bed thickness relative to declared tool resolution; carry the thickness source. | curve metadata; module args; intake metadata; negative router search. | A fixed scenario from a numeric thickness without resolution/source custody is insufficient. |
| `003` | Cause-to-method route table is machine-readable and surfaces known route defects before compute. | module registry; status/defect records; POR seam; dialog routing. | A prose recommendation or downstream failure after compute is not the pre-dispatch contract. |
| `004` | Two-component applicability limit is machine-readable and enforced. | `ModuleSpec`; module doc; input/result guards. | A doc sentence without an evaluated condition is supporting only. |
| `005` | Excluded lithologies are declared and produce distinct scope flags. | `ModuleSpec`; negative scope-flag inventory. | Generic missing output does not prove a named scope refusal. |

### Group B - Thomas-Stieber construction, outputs, picks, and flags (`006`-`021`, `066`)

| ID | Exact contract focus | Primary live candidates | Adjudication guard |
|---|---|---|---|
| `006` | Picker and module use one named construction; any second construction is separately named and computed. | `drawTsOverlay`; `thin_bed_ts`; overlay characterization; module corner test. | Shared endpoints do not prove shared interior geometry; the current characterization is divergence evidence. |
| `007` | No derived fraction or sand porosity is clamped. | `thin_bed_ts` derived `limit` sites; out-of-model fixtures. | Workflow rejection of input parameters does not excuse clamps inside the module body. |
| `008` | Out-of-model points move PHIT, never VSH, and emit a coded direction plus signed PHIT shift. | module outputs; negative TS flag/shift inventory. | Missing output is not a refusal; a boundary value without the signed shift is incomplete. |
| `009` | Withdraw `PHIE_LAM`; emit distinct sand-fraction total and effective porosity curves. | module spec/body; curve catalog/export/report inventories. | Renaming the current Eq 88 value to effective would preserve the defect; both identities and withdrawal matter. |
| `010` | Below-left points back-solve hypothetical clean-sand porosity and difference without constraint. | negative `PMAXNU`/`PORFIL` inventory; clamp path. | A clamped in-range result is the opposite of the diagnostic. |
| `011` | Property proof equates Eq 89 and Eq 122 over arbitrary in-range inputs. | existing module tests; test inventory. | One or two examples are not the specified property test. |
| `012` | Interactive pick provenance persists beside the value and survives to the resulting curve/deliverable. | `crossplotWriteSource`; `writePlotParameter`; `finalize_plot_write_provenance`; zone `value_text`; workflow; exports. | Current write provenance/undo proves only the first half; prove or disprove run-to-deliverable propagation separately. |
| `013` | Every setter reads one `ModuleSpec` range. | `ArgSpec`; module dialog; hard-coded drag clamp; workflow range rejection; zone/DB surfaces. | A later rejection is not UI single-source consistency. |
| `014` | Analyst-selected laminar-structural branch plus cross-branch sand-porosity identity. | module options/doc/body; negative branch search. | The current explicit absence is honest but does not implement the branch. |
| `015` | Branch is a zone-level decision and cannot switch automatically per sample. | zone option resolution; future branch type; current one-branch module. | The defect is not present today because the capability is absent; do not call that whole-contract present. |
| `016` | Three cutoffs carry action class; cosmetic ramp is labelled in UI and deliverable. | module args/flags; report methodology; negative cutoff inventory. | Numeric thresholds alone would not prove action semantics or disclosure. |
| `017` | T-S and tensor laminar shale remain distinct, with a selected authoritative third curve and recorded choice. | module outputs; curve families; negative tensor/reconciliation inventory. | Current undifferentiated `VLAM` is not one of three governed identities. |
| `018` | Imported VLAM-family curve records parameterization; cross-parameterization difference refuses. | `curves::FAMILIES`; ingest metadata; comparison tools; curve meta. | Preserving a mnemonic alone does not identify zeta/Juhasz/PHIT-VSH parameterization. |
| `019` | One flag scheme covers every chapter guard and run summary counts every condition/suppressed level. | module outputs; `ModuleRunResult`; process/history; summary surfaces. | Per-test flag counting or NaN omission is supporting at most. |
| `020` | Import maps `Steiber` spelling to `Stieber`. | `curves::FAMILIES`; parser aliases; exact current/history search. | GR Stieber methods spelled correctly do not implement the import alias. |
| `021` | Plot draws all active construction boundaries/cutoffs and colors points by depth. | `drawTsOverlay`; Z-color machinery; plot options; manual scenario. | Generic optional Z coloring is not a depth-default T-S contract; classify each missing limb. |
| `066` | Withdraw both uncited endpoint defaults; default shale endpoint is derived from picked wet/dry shale; external path is typed. | module spec/body; picker defaults; endpoint inputs; provenance. | Do not replace the values during Gate 1; absence plus cited derivation method is the safe target. |

### Group C - Resistivity tensor, roots, guards, and validity (`022`-`037`)

| ID | Exact contract focus | Primary live candidates | Adjudication guard |
|---|---|---|---|
| `022` | Series branch is a resistivity mix and round-trips. | module registry; exact `RH_SH`/`RV_SH`/`RT_SS` search; history. | Do not infer a tensor path from generic deep resistivity. |
| `023` | Canonical anisotropic form is the repaired quadratic and every repair is labelled. | expected module/result/report inventory; negative history. | Never transcribe either known defective vendor printing. |
| `024` | Both quadratic roots are retained and reported. | result/IPC/curve inventory. | Selecting one root internally without exposing both is absent. |
| `025` | Quadrant classifier selects root and records the branch. | result flags/provenance; negative classifier inventory. | A fixed sign or unrecorded numeric choice fails even if one fixture is right. |
| `026` | Per-level `RV_SH_flip` is an advisory, not a guard. | result curve/flag inventory. | Never replace the derived level-dependent expression with `RV` or a constant. |
| `027` | Impossible input quadrant refuses before solve. | module validity/guard inventory. | Do not guard on the vendor's non-reproducing output signature. |
| `028` | `RV_SH >= RV` singularity is hard-flagged; approach remains finite and tends to RV. | validity data; result behavior; negative tensor search. | A generic divide-by-zero omission is not a named diagnosis. |
| `029` | Horizontal shale-pick proximity guidance is enforced and sourced. | module parameter validation; source-bearing flag. | It is guidance from the cited reference, not permission to clamp the pick. |
| `030` | `RV_SH >= RH_SH` is validated at parameter entry. | `ArgSpec` cross-parameter capability; dialogs; workflow. | Independent min/max fields cannot express a relation between two parameters. |
| `031` | Tensor sand-resistivity bounds flag but never clamp. | tensor output and bounds; C-3-2 custody. | The bounds are cited; their incumbent action is not. Preserve value and source in the target contract. |
| `032` | Parallel-route `RTLAM_MAX` never applies to tensor results. | branch types; cutoff dispatch; negative route inventory. | One generic `RT` ceiling would silently conflate the two routes. |
| `033` | Root selection never uses a hard-coded sign. | source/history search; future classifier. | Correct results below the branch flip do not prove correct selection above it. |
| `034` | Validity conditions are machine-readable on `ModuleSpec` with severity, message, and citation. | live `ModuleSpec`; validity types; runner preflight. | Doc strings and code comments do not satisfy machine-readable custody. Keep E-1's uncited threshold absent. |
| `035` | Parallel-route pole is detected and not clamped through. | parallel-route inventory; guard/result surfaces. | O-1's 0.9 warning threshold remains unresolved; the exact pole formula is derived and separate. |
| `036` | Negative sand resistivity is a distinct diagnosis. | parallel-route flags; summary counts. | It must not collapse into a saturation-bound or pole flag. |
| `037` | Unsolvable anisotropy ratio is preserved, flagged, and stops that level. | parameter/result custody; negative tensor search. | Silent reduction is an explicit refusal even if it would make the solver converge. |

### Group D - Relative dip (`038`-`043`)

| ID | Exact contract focus | Primary live candidates | Adjudication guard |
|---|---|---|---|
| `038` | Moran-Gianzero relation works forward and inverse. | module/equation registry; exact Moran/Gianzero search; history. | Do not substitute a different anisotropy relation. |
| `039` | Missing relative dip or bedding-frame source refuses; zero is never defaulted. | module params; request types; negative dip search. | Absence of the entire module is not an implemented refusal. |
| `040` | Apparent resistivity ceiling is `sqrt(Rh*Rv)`. | forward/inverse oracle; negative implementation inventory. | The ceiling must be asserted numerically, not just mentioned. |
| `041` | Parameter name carries the degrees-from-bedding-normal convention. | `ModuleSpec` and IPC parameter names. | A help string beside an ambiguous `DIP` field is insufficient. |
| `042` | Multi-well fit recovers Rh/Rv and reports per-well residuals. | batch/job/calibration patterns; negative fit search. | E-3 blocks a releasable result until the paper ambiguity is resolved; do not implement a guessed shale-volume choice. |
| `043` | Multi-well route enforces the cited 40-degree dip-span precondition and reports actual span. | fit preflight; validity data; source custody. | The cited span does not unblock E-3's separate equation-input ambiguity. |

### Group E - Sand-reference dispatch, bookkeeping, and summation (`044`-`053`, `062`-`064`)

| ID | Exact contract focus | Primary live candidates | Adjudication guard |
|---|---|---|---|
| `044` | Saturation dispatch consumes sand-fraction porosity and resistivity, never bulk. | saturation module inputs; workflow chain; negative sand-fraction curves. | A generic curve dropdown that could be manually pointed elsewhere is not governed dispatch. |
| `045` | Explicit curve set includes sand fraction, sand total/effective porosity, and sand resistivity. | module outputs; curve families/export/report. | Exact whole-token searches must distinguish `_SS` from `_SSC` and `_SSPW`. |
| `046` | RtC and IMTS are offered on sand-fraction porosity. | `sw_rtc_spec/body`; `sw_imts_spec/body`; 26 passing tests. | Bulk SSC/SSPW fallback proves the current divergent route, not sand-reference compliance. |
| `047` | Three Poupon-family equations refuse under the laminated model by equation identity. | saturation dispatch/options; model identity; laminated-mode state. | A name-string block or absent laminated mode is not the three observable refusals. |
| `048` | Minimum-Sw guard emits clipped and unclipped curves plus flags. | saturation outputs; clamp sites; option inventory. | A clipped value without its twin is explicitly refused. |
| `049` | Reconciliation classifier distinguishes agreement and both directional mismatches with interpretation/action. | T-S/tensor outputs; result data; negative reconciliation search. | One difference curve without signed classes and prescribed response is incomplete. |
| `050` | Reconciliation track renders the classified mismatch. | plot/track registry; negative track inventory. | A classifier alone does not prove the visual track. |
| `051` | Laminar net sand/pay uses sand-fraction curves and geometric sand fraction. | `run_pay_summary`; curve inputs; summary/result exports. | Generic bulk `VSH/PHIE/SWE/PERM` summary is not laminar summation. |
| `052` | Laminar summation is labelled, explicit opt-in, off by default, and carries reserves warning. | summary UI/defaults/report. | Generic pay summary availability does not prove this separate opt-in mode. |
| `053` | Sand-fraction and bulk cutoff identities cannot be interchanged. | cutoff request types; curve identity/provenance; summary dialog. | Equal numeric values remain different quantities and must not share an untyped field. |
| `062` | Sand-fraction suffix is distinguishable from `_SSC` and `_SSPW` before any sand-reference curve ships. | mnemonic matcher; curve families; UI labels; O-10. | Do not invent the naming decision in Gate 1; record the unresolved product dependency. |
| `063` | Report renormalizes sand-reference saturation to bulk while conserving hydrocarbon volume. | report/summary arithmetic; emitted result identities. | Reporting the sand value under a bulk label is silent wrongness. |
| `064` | Permeability is computed on sand fraction and converted explicitly to formation permeability. | permeability modules; output curves; summary/report. | A generic `PERM` transform without `PERM_SS` and `PERM_FM` identities does not qualify. |

### Group F - Plots, units, imports, uncertainty, and resolution (`054`-`061`, `065`)

| ID | Exact contract focus | Primary live candidates | Adjudication guard |
|---|---|---|---|
| `054` | Klein/butterfly crossplot ships sourced mixing overlays and reference lines. | chart registry/generated overlays; crossplot authorization; exact search. | Generic crossplot capability is not this sourced construction; preserve repaired horizontal law. |
| `055` | Timur coefficient is coupled to fraction/percent input type. | permeability module args; unit registry; transform tests. | Correct arithmetic under one convention does not prove rejection of the other coefficient/unit pairing. |
| `056` | Qv and B convert as a declared pair or import refuses. | curve unit registry; import designation; lrlc Qv/B types. | O-8 keeps the unresolved vendor per-litre meaning open; do not assume a 1000x conversion. |
| `057` | Angle and temperature conventions are typed and range-checked. | module args; unit registry; lrlc temperature field; dip inventory. | A `degC` label on one bulk module does not prove angle custody or paired conversion behavior. |
| `058` | Ambiguous vendor sand-fraction curves import unmapped with an ambiguity record. | generic unknown-mnemonic import; curve metadata; alias decisions. | Preserving both names is partial only if the ambiguity is not explicitly recorded. |
| `059` | Interval Monte-Carlo propagates endpoint/input uncertainty and reproduces published VLSA distribution case. | generic `montecarlo.rs`; plan builder; exact VLSA search. | Generic parameter Monte Carlo over bulk cutoffs is not the specified deconvolution/interval distribution. |
| `060` | Anisotropy track renders both ratios on a common declared scale. | track registry; exact anisotropy search; manual matrix. | T62's 1-10 scale is characterization, not sourced correctness. |
| `061` | Clay-mineral thin-bed correction fits thick-bed donor coefficients and records donor/provenance. | clay/mineral modules; calibration records; exact `Vcm`/Madjid search. | A generic VSH or mineral curve is not the cited regression chain. |
| `065` | Resolution enhancement uses published native derivation and refuses vendor artifacts in every format. | model loaders; artifact guards; history; C-3-3; legal/source notes. | Absence of a loader is not full proof of both future native training and all-format refusal; never consume vendor weights. |

## Acceptance-Test Ownership Map

Every test is routed once below. Secondary requirement links are noted in parentheses rather than counted again.

| Test | Primary owner | Independent oracle and required observable scope |
|---|---|---|
| `T01` | `001` | Six constructed Worthington causes classify at every level; taxonomy is the cited oracle. |
| `T02` | `002` | 40/6/2/0.5 cm route to A/B/C/D with non-default thickness source; also constrains `001`. |
| `T03` | `003` | Machine-readable route surfaces the known open porosity defect before computation. |
| `T04` | `004` | Coal-lamina and carbonate controls raise distinct machine-readable scope flags; also constrains `005`. |
| `T05` | `006` | Laminated line identity follows the chapter's independently derived equation. |
| `T06` | `006` | Dispersed line identity follows the chapter's independently derived equation. |
| `T07` | `008` | Out-of-space input shifts PHIT, never clips derived VSH_LAM, and emits nonzero flag/shift; also constrains `007`. |
| `T08` | `007` | Declared CHARACTERIZATION of four current outputs beside sourced Eq 86; current snapshot is not correctness proof. |
| `T09` | `008` | Constructed 0.04 v/v offset returns signed shift to `1e-6`. |
| `T10` | `009` | Chapter arithmetic yields distinct `PHIT_SS=0.164000`, `PHIE_SS=0.140000`, and no `PHIE_LAM`. |
| `T11` | `011` | Property test equates cited Eq 89 and Eq 122 over the domain; also constrains `009`. |
| `T12` | `010` | Below-left point emits back-solved endpoint and delta without constraint. |
| `T13` | `012` | Drag record contains complete origin/plot/well/interval/date and curve provenance query reaches it. |
| `T14` | `013` | Drag range comes from `ModuleSpec`; 0.48 never persists. |
| `T15` | `014` | Both branches return identical sand total porosity and remain zone-stable; also constrains `015`. |
| `T16` | `014` | Structural-line identity at zero laminar shale follows the derived equation. |
| `T17` | `016` | Cutoff actions differ correctly and cosmetic ramp is labelled in flag and deliverable. |
| `T18` | `017` | T-S, tensor, and selected laminar-shale curves are distinct and selection is recorded. |
| `T19` | `018` | Cross-parameterization imported-curve difference refuses until methods are declared. |
| `T20` | `019` | Forty-percent flagged run reports every condition and suppressed level, unlike clean control. |
| `T21` | `020` | `Steiber` import maps to `Stieber`. |
| `T22` | `021` | Sourced geometry/cutoffs/depth coloring render; only auto-scale portion is CHARACTERIZATION. |
| `T23` | `066` | Fresh spec has neither uncited endpoint default and exposes the cited derivation path. |
| `T24` | `066` | Picked wet/dry shale endpoints derive shale porosity independently to `1e-6` with pick provenance. |
| `T25` | `022` | Series resistivity law round-trips exactly. |
| `T26` | `023` | Isotropic tensor truth case recovers 20 and 0.5 from the chapter derivation. |
| `T27` | `023` | Anisotropic truth case recovers 20/0.5 while wrong-sign control returns about 5.92 and must fail. |
| `T28` | `024` | Delta-conductivity zero degenerates to isotropic form and exposes both roots. |
| `T29` | `025` | Full branch-flip sweep preserves continuous physical root and defeats fixed-sign control; also constrains `026`, `033`, `034`. |
| `T30` | `025` | Cited/derived anisotropy-sensitivity sweep matches chapter values without inventing a threshold. |
| `T31` | `026` | Derived flip-threshold quadrant cases prove it is level-dependent, not `RV` or a constant. |
| `T32` | `027` | Impossible quadrant refuses before solve and explicitly rejects the non-reproducing vendor signature; also constrains `034`. |
| `T33` | `028` | Equality singularity refuses with no infinity or large finite escape; also constrains `034`. |
| `T34` | `028` | Near-singularity control tends to `RV` at the chapter value. |
| `T35` | `029` | Ten-percent shale-pick perturbation reproduces chapter sensitivity and raises sourced proximity warning. |
| `T36` | `030` | `RV_SH < RH_SH` rejects at entry with named sourced relation; also constrains `034`. |
| `T37` | `031` | Values beyond tensor bounds are preserved with advisory and parallel 2000 bound is excluded; also constrains `032`. |
| `T38` | `034` | Every documented validity row is machine-readable, evaluated pre-solve, and carries severity/message/citation. |
| `T39` | `035` | Pole refusal and negative-resistivity diagnosis remain distinct; also constrains `036`. |
| `T40` | `035` | Parallel mixing reproduces 50.00/4.13/2.83 and records the vendor-print discrepancy. |
| `T41` | `037` | Unsolvable ratio remains unchanged and flagged; no silent reduction. |
| `T42` | `038` | Dip endpoints produce `Rh` and `sqrt(Rh*Rv)` exactly; also constrains `040`. |
| `T43` | `041` | Complement-angle control differs and parameter names bedding-normal convention; also constrains `038`, `057`. |
| `T44` | `039` | Missing dip/frame source refuses and names both valid supply routes. |
| `T45` | `042` | Multi-well 15-75 degree synthetic recovers Rh/Rv plus per-well residual, only after E-3 closes. |
| `T46` | `043` | Twelve-degree stock refuses and reports actual span against cited 40-degree precondition. |
| `T47` | `044` | Sand-reference and bulk-input saturation answers differ; dispatch must choose the sand route; also constrains `045`. |
| `T48` | `045` | Full sand-reference curve set exists and matcher distinguishes suffix; also constrains `062`. |
| `T49` | `046` | RtC and IMTS consume sand-fraction porosity and differ from the bulk control. |
| `T50` | `047` | Three equation-identity-specific Poupon-family refusals surface vendor reasons. |
| `T51` | `048` | Minimum-Sw guard emits clipped/unclipped twins and fired-level flags. |
| `T52` | `049` | Three reconciliation cases classify with interpretation/action and render mismatch; also constrains `050`. |
| `T53` | `051` | Ten-metre half-sand fixture yields exactly five metres, mode defaults off, and warning reaches deliverable; also constrains `052`. |
| `T54` | `053` | Net sand/pay use only sand-fraction cutoff quantities and differ from bulk control. |
| `T55` | `054` | Sourced overlay laws/lines match the reference fixture; grid spacing alone is CHARACTERIZATION. |
| `T56` | `054` | Horizontal mixing returns `R_SH_H` at shale fraction one; 4-ohm-m control distinguishes repaired division from wrong prose. |
| `T57` | `055` | Fractional 8581 and percent 0.136 Timur routes agree; wrong pair refuses. |
| `T58` | `056` | Qv/B pair converts together or refuses; Qv-only import rejects. |
| `T59` | `057` | Metric/imperial summary invariance plus typed physical-range temperature refusal. |
| `T60` | `058` | Both ambiguous curves remain vendor-named/unmapped with explicit ambiguity record. |
| `T61` | `064` | `PERM_FM = PERM_SS*(1-VSH_LAM)` exactly and both curves emit. |
| `T62` | `060` | Both anisotropy ratios render; 1-10 scale is explicitly CHARACTERIZATION. |
| `T63` | `059` | Published VLSA case yields 2.41/1.41/2.40 ft as a distribution, not scalar. |
| `T64` | `061` | Constructed thin-bed clay overestimate is removed and donor/coefficient provenance is recorded. |
| `T65` | `065` | Vendor model/weight/inference artifacts refuse in every format; native model records training data. |
| `T66` | `063` | Bulk renormalization conserves hydrocarbon volume exactly. |

## Parameter, Open-Item, Escalation, Refusal, Legal, and Traceability Custody

### Parameter rows

- Preserve the cited Thomas-Stieber endpoint/cutoff rows exactly. `PHIT_MAX=0.35` is the cited Geolog value, but Gate 1 does not silently replace the live `PHI_SD_MAX=0.30`; the implementation decision remains a later requirement increment. `PHIT_SH`, wet-shale density, and wet-shale neutron porosity ship absent. The live `PHI_SD_MAX=0.30` and `PHI_SH=0.15` rows remain WITHDRAWN.
- Preserve `TSFLG` 0 and 9 as recovered and codes 1 through 8 as absent. SandiBumi may define its own codes but must not advertise compatibility before O-2 is visually recovered.
- Preserve `RH_SH`, `RV_SH`, relative dip, disputed B constant, and fitted Vcm coefficients as absent. Preserve the shale-anisotropy and CEC ranges as advisory only.
- Preserve the tensor bounds as cited flag thresholds, never clamps; the parallel 2000-ohm-m threshold remains route-specific. Preserve `RV_SH_flip` and the exact pole as derived expressions, not defaults.
- Keep the pole-margin `0.9` marked as an unresolved reporting-spec choice under O-1. Do not promote it to a petrophysical source-backed threshold.
- Preserve the cited laminar summation controls and cutoffs with their sand-fraction quantity identity and the default-off setting. Their existence in the chapter does not authorize applying them to bulk curves.
- Preserve both cited Timur coefficient/unit pairs. Preserve the Qv/B pairing boundary and the unresolved per-litre meaning under O-8; do not assume a factor.
- Preserve the saturation constants as a seam owned by `12_saturation.md`. This adjudication records consumption/dispatch only and must not choose or normalize Archie, RtC, or IMTS values.

### Open items and escalations

- Keep O-1 through O-11 explicit. O-10 is a real product naming decision blocking the first sand-reference curve; Gate 1 does not invent the suffix. O-11 records why no new core requirement is needed.
- Keep E-1 and E-2 open for the missing microscopic anisotropy threshold and named paper. Keep E-3 blocking `SB-TBD-042`. E-4 remains outside repository write scope. E-5 resolves only the safe default disposition: replacement values absent. E-6 keeps the patent-adjacent capability unallocated.

### Defect refusals

- Preserve all R-1 through R-15 as separate controls: no derived clamps; no ratio reduction; no saturation-bound substitution; no tensor clamps; no defective quadratic transcription; no conductivity/resistivity dimensional error; no wrong Klein prose form; no impossible-quadrant output-signature guard; no `RV_SH>=RV` branch-flip shortcut; no unrecovered vendor flag compatibility claim; no automatic per-level branch switch; no mapping ambiguous vendor curves; no clipped Sw without its twin; no default-enabled laminar summation; and no defective Fahrenheit/Celsius printing.
- An implemented absence cannot receive credit for a prospective refusal inside an unbuilt capability. Conversely, a generic product guard does not close a domain-specific observable contract unless it is wired into that route and reported.

### Independent derivation and legal boundary

- Preserve C-3-1 for root selection, C-3-2 for tensor-bound behavior, and C-3-3 for native resolution enhancement. Their independent derivation paths may be specified and later implemented without probing a vendor binary.
- Preserve C-1-1 as deliberately unallocated and unspecified. Do not search for a workaround, write a formula, infer claims, or count the capability as roadmap-ready without legal clearance.
- Preserve the explicit finding that there are no C-2 items in this domain.

### Traceability

- Account for all 327 addressable dossier items: IP inventory 16, Geolog 18, Techlog 19, prior SandiBumi 4, parameter comparisons 44, difference analyses 8, optimal-choice decisions 9, ledger/open dispositions 19, proposed entries 9, adoption modules 10, adoption parameters 28, plots 4, guards/flags 16, regression tests 36, FINDINGS rules 9, open items 15, KB recommendations 3, derivations 27, and critique dispositions 33.
- Preserve the chapter's split of 156 individually dispositioned and 171 block-dispositioned items. Do not rewrite research dossiers during Gate 1.

## Execution Tasks

### Task 1: Refreeze the baseline

- [ ] Create `codex/g1-sb-tbd-adjudication` serially from the committed plan and verify one clean worktree at `D:\XX. SandiBumi`.
- [ ] Re-run anchor, branch, worktree, ledger, source-column hash, chapter hash, test-ID, parameter, open/escalation/refusal/derivation, traceability, manual-evidence, and focused-candidate checks.
- [ ] Stop and reconcile if any frozen count, hash, branch relationship, or source-owned field differs.

### Task 2: Reverify recognition and Thomas-Stieber behavior (`001`-`021`, `066`)

- [ ] Trace recognition/routing surfaces and confirm consequential absence across module registry, UI, IPC, tests, and history.
- [ ] Independently compare the picker geometry and module equation on interior points, not only common endpoints.
- [ ] Inventory every clamp, constraint, flag, output identity, branch, cutoff, diagnostic, and summary count.
- [ ] Trace endpoint picks from rendered axes and selected interval through canonical provenance JSON, zone storage, parameter resolution, result curves, log-set metadata, exports, report and undo history.
- [ ] Reverify import aliases and parameterization metadata without changing the curve dictionary.
- [ ] Keep replacement endpoint values absent and distinguish derived shale porosity method from a direct externally supplied value.

### Task 3: Reverify tensor and dip capability (`022`-`043`)

- [ ] Confirm each missing tensor/dip symbol and surface in current Rust, TypeScript, tests, and reachable history.
- [ ] Route every specified quadratic/root/flip/quadrant/singularity/bound/pole/negative-result oracle without implementing it.
- [ ] Inspect `ModuleSpec` and runner preflight for machine-readable validity support; do not credit doc strings.
- [ ] Keep E-1 threshold absent, E-2 acquisition open, and E-3 blocking multi-well fit; do not infer any value or input convention.
- [ ] Keep C-3 independent-derivation obligations separate from C-1-1's legal prohibition.

### Task 4: Reverify sand-reference dispatch and bookkeeping (`044`-`053`, `062`-`064`)

- [ ] Trace `thin_bed_ts` outputs into every saturation module and prove which porosity/resistivity identities each route consumes.
- [ ] Treat all 26 passing LRLC tests as bulk RtC/IMTS evidence unless a test independently proves sand-fraction dispatch.
- [ ] Trace generic pay/cutoff/summary behavior and explicitly show why it does or does not implement sand-reference net/pay, default-off mode, cutoff typing, bulk renormalization, and permeability conversion.
- [ ] Keep O-10 naming unresolved and verify exact-token negative inventory so `_SSC`/`_SSPW` substring hits cannot masquerade as `_SS` curves.

### Task 5: Reverify plots, units, imports, uncertainty, and resolution (`054`-`061`, `065`)

- [ ] Inventory generated chart definitions read-only, crossplot authorization, track registry, curve unit registry, import alias decisions, Monte-Carlo plan/result types, model loaders, and report/export surfaces.
- [ ] Separate generic capability from the exact Klein, paired Qv/B, typed angle/temperature, ambiguity-record, VLSA, anisotropy-track, clay-correction, and native-resolution contracts.
- [ ] Preserve CHARACTERIZATION labels for T55 grid density and T62 track scale; never promote them to sourced correctness.
- [ ] Confirm every format-bearing model loader refuses vendor artifacts before crediting the C-3-3 refusal limb; absence of the feature alone is not whole-contract proof.

### Task 6: Write the evidence receipt and update all 66 ledger rows

- [ ] Create `docs/takeover/evidence/sb-tbd.md` with one complete section per requirement in numeric order.
- [ ] Add one routing table containing all 66 test intentions exactly once and list shared secondary ownership without double-counting.
- [ ] Update only live adjudication columns in `docs/takeover/requirements.csv`.
- [ ] Recompute the ledger and update `docs/takeover/STATUS.md` with exact as-built, disposition, risk, proof, manual, and remaining-row counts.
- [ ] Verify the six source-owned columns remain byte-identical to the frozen hash.

### Task 7: Run structural and evidence checks

- [ ] Assert 66 receipt sections and 66 matching ledger rows, each legal and non-placeholder.
- [ ] Assert all 66 test intentions, 52 parameters, 11 opens, 6 escalations, 15 refusals, 4 derivation/legal items, and 327 traceability dispositions are accounted for.
- [ ] Search new receipt/ledger text for prohibited identifying names, invented defaults, source-gap fills, patent workarounds, protected data, and accidental claims of field evidence.
- [ ] Run `node --test tools/takeover-ledger.test.mjs`, `node tools/takeover-ledger.mjs --summary-json`, and `git diff --check`.

### Task 8: Run the repository gates and commit the execution increment

- [ ] Run `npx tsc --noEmit` from the repository root.
- [ ] Run `cargo check` from `src-tauri`.
- [ ] Run `powershell -ExecutionPolicy Bypass -File tools\check.ps1` from the repository root. If the live Tauri application owns the default debug executable, use the established isolated `CARGO_TARGET_DIR`; do not stop the live application.
- [ ] Record fresh passed/failed/ignored totals; do not reuse the prior `946/0/36` result.
- [ ] Stage exactly `docs/takeover/evidence/sb-tbd.md`, `docs/takeover/requirements.csv`, and `docs/takeover/STATUS.md`; inspect the cached diff and source-column hash.
- [ ] Commit locally with message `G1-DOM-TBD adjudicate 66 SB-TBD requirements`. Do not push, merge, or open a pull request.

### Task 9: Continue Gate 1 serially

- [ ] Recompute the remaining domain inventory from the post-TBD ledger while keeping all 52 SB-GEO rows deferred to the next product version.
- [ ] Select the next dependency-relevant open-hole-petrophysics domain from live evidence, not chapter order alone.
- [ ] Create the next serial planning branch in the same `D:\XX. SandiBumi` worktree without touching any other folder.
- [ ] Do not begin Gate 2 production remediation until Gate 1 evidence reconciliation is complete or Jauhar explicitly changes the sequence.

## Plan Self-Review Receipt

- **Spec coverage:** all 66 requirements appear exactly once in the Requirement Evidence Map; all 66 test intentions appear exactly once in the Acceptance-Test Ownership Map; all 52 parameters, 11 opens, 6 escalations, 15 refusals, 4 independent-derivation/legal items and 327 traceability dispositions have explicit custody.
- **No placeholders:** the plan contains no guessed endpoint, anisotropy threshold, flag code, B conversion, Vcm coefficient, vendor algorithm, patent workaround, or unspecific error-handling instruction.
- **Live-drift handling:** the plan records that plot-write provenance and undo are now implemented while run-to-deliverable provenance remains unproved, preventing the stale chapter statement from being copied as current truth.
- **Type/interface consistency:** the plan uses the live names `ModuleSpec`, `ArgSpec`, `ModuleRunResult`, `PlotWriteSource`, `PlotWriteProvenanceInput`, `ZoneParamEntry`, `PaySummaryRequest`, `McRequest`, `thin_bed_ts`, `sw_rtc`, and `sw_imts`. It invents no production API.
- **Source discipline:** the two live endpoints remain withdrawn; nine absent parameters remain absent; two advisory bands remain advisory; O-1, O-2, O-8 and O-10 remain open; E-1 through E-6 remain fenced; `C-1-1` remains deliberately unspecified.
- **Evidence discipline:** 30 passing focused tests are candidates only. The source-grep overlay test is CHARACTERIZATION, the generic plot and workflow tests are supporting seams, and bulk RtC/IMTS tests do not prove sand-reference dispatch. Manual thin-bed evidence remains `0/1` and Thomas-Stieber remains `0/0`/not listed.
- **Scope discipline:** planning changes only this plan and STATUS; execution changes only the TBD receipt, ledger, and STATUS. Production remediation remains a later Gate 2 activity.

## Execution Handoff

Commit this planning increment locally, create `codex/g1-sb-tbd-adjudication` serially from it, then execute inline with `superpowers:executing-plans`. Jauhar's standing direction is to continue Gate 1 serially, so no additional approval is required unless execution reaches an unresolved product-owner decision, legal boundary, or source gap that would otherwise be silently guessed.
