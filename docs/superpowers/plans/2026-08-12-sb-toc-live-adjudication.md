# SB-TOC Live Adjudication Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task by task. Do not delegate or spawn subagents unless Jauhar explicitly authorizes that in the execution turn.

**Goal:** Reverify every one of the 43 live `SB-TOC` requirements against the accepted Gate 1 tree, record one evidence-backed as-built classification and pilot disposition per row, route all 58 chapter acceptance-test intentions, and preserve every unit, baseline, maturity, mask, calibration, kerogen, gas-property, standard-condition, naming, source, migration, provenance, and manual-evidence boundary without changing unconventional computation, storage, UI, report, or export behavior.

**Architecture:** This is a documentation-only evidence pass over the four registered unconventional modules (`toc_passey`, `kerogen`, `gip`, and `brittleness`), their shared workflow/versioning path, curve/unit metadata, the custom delta-log-R/Langmuir panel, reports/exports, current tests, manual matrix, and reachable Git history. The immutable PRD supplies the intended contract, 76 parameter rows, 9 open items, 7 escalation bullets, 10 refusals, no Tier-C item, and 58 named test intentions. Current source, independently sourced tests, manual evidence, and reachable history supply the separate live verdict. A published equation, an editable vendor seed, a module-manifest unit, or a passing pure-function test does not by itself prove the compound observable contract.

**Tech Stack:** Markdown, RFC 4180 CSV, PowerShell 5.1, Git, `rg`, Node.js `node:test`, TypeScript, Rust `cargo test`, the takeover-ledger validator, and the existing SandiBumi full gate.

## Global Constraints

- This planning increment may create this plan and update `docs/takeover/STATUS.md` only. It MUST NOT modify a ledger verdict, evidence receipt, Rust, TypeScript, CSS, tests, `REVIEW.md`, generated verification output, any file under `docs/PRD_v2/**`, or any file under `docs/research_2026-08/**`.
- Execute the later adjudication on the session model with `superpowers:executing-plans`; do not delegate or spawn subagents unless Jauhar explicitly authorizes that in the execution turn. Petrophysical method interpretation, parameter custody, source boundaries, and final sign-off remain with the primary session.
- Work only in `D:\XX. SandiBumi`. The sole registered Git worktree MUST remain that path. Other retired or empty SandiBumi directories remain untouched.
- The accepted implementation evidence anchor is `b332026cb498c105f36eade0bf7899bc0c1309f0`. At plan freeze, `HEAD` is `69083fefbdee85fac19147d0d0169b78f7f7bde3`; `origin/master` and the merge base are both `29833735816d9e5be954afafd9ceb71fd856e3f0`; the accepted anchor is reachable. Reverify all four before execution.
- The local planning branch is `codex/g1-sb-toc-plan`. The serial Gate 1 chain remains local and unpushed; do not merge, rebase, rewrite history, push, or open a pull request. After the planning commit, create `codex/g1-sb-toc-adjudication` in the same worktree.
- The codebase-index MCP server is not callable in this task. Targeted filesystem search is the explicit fallback. A consequential Rust/TypeScript absence MUST be confirmed across expected source, tests, UI/IPC, reports/exports, and reachable history.
- Before adjudicating, read `AGENTS.md`, all of `CLAUDE.md`, `docs/PRD_v2/CONTRACT.md`, all of `docs/PRD_v2/19_toc-unconventional.md`, `docs/record_parallel_lanes.md`, `docs/ref_unconventional.md` as historical implementation rationale rather than current parameter authority, the current verification matrix, current takeover receipts/status, and the chapter-routed local dossier only where a consequential source dispute requires it.
- Preserve the ledger's source-owned fields byte-for-byte: `requirement_id`, `chapter`, `title`, `original_priority`, `chapter_status`, and `owned_tests`. Their frozen SHA-256 over the 43 ordered rows is `88c9b05861b3a12d3b7427da8510e4bdf5cf1acbab9d1a3e7c5153365e9f4451`. The immutable chapter SHA-256 is `6c71c7ce81820cfb50dd94fc1ac372d7ba729b1f0d788904e543c8c21ccb7202`.
- The chapter and ledger agree on 43 contiguous requirements: `P0=18`, `P1=15`, `P2=8`, `P3=2`. Historical states are `ABSENT=24`, `PARTIAL=5`, `PRESENT-DIVERGENT=10`, and `PRESENT-OK=4`. Reverify live behavior independently.
- The chapter defines 58 contiguous test IDs, `SB-TOC-T01` through `SB-TOC-T58`. The consolidated index references 67 occurrences covering only 56 unique IDs; T12 and T38 are defined by the chapter but absent from the source-owned `owned_tests` fields. Route all 58 in the evidence receipt. Do not alter the immutable ledger field to repair this PRD/index discrepancy.
- Section 5 contains exactly 76 parameter rows, 23 reading `ABSENT - ships with no default`. Preserve all 23 absences: `LOM`, `VR`, `TOC_BG`, four baselines, `OVERLAY_FINAL`, `RHO_NO_KER`, `TOCLV_GRAD`, measured pressure/temperature, `V_L`, `P_L`, `TOC_LANG`, `VOL_KER_LANG`, `Z`, `M_GAS`, `RHO_ADS`, ash, moisture, measured gas content, and `GWR_MODE`.
- A petrophysical value is cited or absent. Never promote a vendor/example seed, placeholder, regional coefficient, disputed fluid property, baseline, maturity, calibration, isotherm value, mask cutoff, mud-gas mode, or measured input from historical implementation prose into an active default.
- Preserve O-1 through O-9, all 7 escalation bullets, and R-1 through R-10. No Tier-C item exists in this domain. A future source acquisition is not permission to synthesize its result during Gate 1.
- The historical `docs/ref_unconventional.md` calls vendor defaults Tier-A seeds and the code preselects several of them. PRD v2 deliberately supersedes that default policy. Editable is not absent, and a warning to override a placeholder does not make the placeholder safe.
- The current chain is numerically wt%-consistent but has no typed TOC quantity boundary. A `wt%` string in `ArgSpec` or UI prose does not prevent a `w/w` scalar from entering a wt% slot and producing a plausible 100x error.
- The current `gip` module labels intensive scf/ton outputs `GIP_ADS`, `GIP_FREE`, and `GIP_TOTAL`. This is a quantity-identity defect: positive plausible content can be mistaken for extensive Bcf. Do not credit explanatory prose as a type guard, and do not rename or migrate anything in Gate 1.
- `workflow.rs` persists a generic log-set module name, request parameters, and input names. It does not automatically prove the chapter's full method, option, pick interval/revision, unit-conversion, mask, calibration, source-note, flag-count, software-revision, or migration provenance.
- The custom unconventional panel keeps local state and duplicates numeric fallbacks. Layout/view state is not an undoable computation-parameter record, and `dataVersion` invalidation is not proof that visual and compute consume one typed parameter revision.
- Fresh focused candidates pass `26/0/0`, all in `unconventional.rs`. They prove selected local arithmetic, clamps, NaN behavior, monotonicity, and module branches. None uses an executable `SB-TOC-Tnn` identifier, no frontend test exercises `unconventionalPanel.ts`, and none proves the full typed/persisted/migrated/reporting contract.
- Manual unconventional evidence remains `0/4`. Historical unchecked `REVIEW.md` exercises do not become field evidence because the implementation once claimed a green automated suite.
- New receipt and ledger text MUST contain no client, field, block, basin, operator, asset, well, or project name. Refer only to physical conditions, generic records, and source classes.

## Baseline and Count Contract

Before any adjudication edit, re-measure and record:

1. branch `codex/g1-sb-toc-adjudication`, created serially from the committed plan;
2. one clean worktree at `D:\XX. SandiBumi`;
3. current `HEAD`, accepted anchor, `origin/master`, merge base, and anchor reachability;
4. exactly 43 ledger rows, `SB-TOC-001` through `SB-TOC-043`, with no gap or duplicate;
5. priorities `P0=18`, `P1=15`, `P2=8`, `P3=2`;
6. historical source states `ABSENT=24`, `PARTIAL=5`, `PRESENT-DIVERGENT=10`, `PRESENT-OK=4`;
7. all 43 live as-built, implementation-path, test-class, evidence, manual, dependency, commit-state, decision, action, and reverified fields still unadjudicated or placeholder-only;
8. exactly 58 defined chapter test IDs, 67 ledger references, 56 unique ledger IDs, and the two defined-but-unowned IDs named above;
9. exactly 76 parameters and the same 23 absent rows;
10. exactly 9 open items, 7 escalation bullets, 10 refusals, zero Tier-C items, and all section 8.1 through 8.6 traceability blocks;
11. takeover summary `645` adjudicated, `286` unadjudicated, and `432` pilot blockers before SB-TOC;
12. manual unconventional evidence `0/4`; and
13. the fresh `26/0/0` focused candidate-test receipt.

The only mechanically predictable post-adjudication ledger count is `688` adjudicated and `243` unadjudicated. Do not predict as-built, blocker, test-class, risk, or manual-evidence totals before row-by-row classification.

## File Structure for the Execution Increment

- Create: `docs/takeover/evidence/sb-toc.md`
- Modify: `docs/takeover/requirements.csv`
- Modify: `docs/takeover/STATUS.md`
- Read only: `src-tauri/src/unconventional.rs`, `src-tauri/src/modules.rs`, `src-tauri/src/workflow.rs`, `src-tauri/src/equations.rs`, `src-tauri/src/curves.rs`, `src-tauri/src/multimin2.rs`, `src-tauri/src/report.rs`, `src-tauri/src/export.rs`, `src-tauri/src/lib.rs`, `src/ipc.ts`, `src/ui/unconventionalPanel.ts`, `src/ui/moduleDialog.ts`, `src/ui/workspace.ts`, `src/ui/ribbon.ts`, current tests, manual evidence, immutable source chapter, historical implementation record, cited local evidence, and reachable Git history
- Never modify during adjudication: production code, tests, `REVIEW.md`, generated verification artifacts, PRD files, research dossiers, protected vendor material, or unrelated takeover receipts.

## Evidence Receipt Schema

Create one `### SB-TOC-NNN` section per requirement in numeric order. Every section MUST include the specified contract, current implementation, as-built status, release disposition/risk, exact automated evidence class, manual evidence, source/parameter boundary, UI/IPC/provenance surface, history/reachability, blocking decision/dependency, and next action. Compound requirements must have each observable limb separated.

## Requirement Evidence Map

| ID | Exact contract focus | Primary live candidates | Adjudication guard |
|---|---|---|---|
| `001` | Typed wt% versus w/w TOC boundary and named round trip | `unconventional::toc_passey/kerogen`; `modules::ArgSpec`; unit registry; workflow/curve metadata; negative typed-TOC search. | Numeric consistency and `"wt%"` labels do not reject an untagged or fraction-tagged scalar. |
| `002` | All baseline channels ship absent and persist as one interval/revision pick | `toc_passey_spec`; module dialog; unconventional panel; plot/view-state and parameter persistence; history. | Editable independent defaults are not absence, one coupled pick, or persisted curve revisions. |
| `003` | Sonic, density, and neutron separations with explicit sonic-unit conversion | `toc_passey`; module spec; unconventional panel; curve/unit registry; negative NPHI overlay search. | Two branches and one generic `TOC` output do not prove all three alternatives or metric conversion. |
| `004` | Native Passey wt% equation and floor-before-background order | `toc_passey`; seven local Passey tests; module spec. | Credit only the equation/order proved; do not let it close typed units, missing background, picks, flags, or provenance. |
| `005` | Direct/VR LOM, raw value, opt-out 10.5 cap, low warning, and flag | `toc_passey_spec/toc_passey`; negative VR/LOM flag search; local maturity test. | A 6..12 input range is neither the cited cap nor raw/capped observability. |
| `006` | Background TOC ships absent and belongs to the baseline pick | `toc_passey_spec/toc_passey`; module dialog; unconventional panel; workflow persistence. | Default/fallback zero and a separate numeric field do not prove a baseline-bound pick. |
| `007` | Emit three overlay TOCs, min/average/max/spread, with no final preselection | module spec/body; output inventory; negative aggregate/spread search. | One selected overlay under generic `TOC` is not preserved alternatives or uncertainty. |
| `008` | Coal, reservoir, and badhole masks run before every TOC method with stable reason | generic workflow `MASK`; unconventional module inputs/outputs; negative domain-mask/flag search. | A single generic binary mask blanks values but does not encode geology-specific polarity or reason. |
| `009` | Per-zone lab calibration preserves raw prediction, fit quality, revisions, and provenance | unconventional modules/UI; fit/calibration inventory; generic regression facilities; reports/exports. | A neighboring generic fit is not a TOC calibration workflow or persisted lab pairing. |
| `010` | Complete TOC method, pick, maturity, mask, calibration, conversion, and software provenance | workflow log sets; curve catalog; report/export provenance; unconventional UI local state. | Generic module/params/input records are supporting only; enumerate every missing limb. |
| `011` | Cited Schmoker cross-check with method identity | `toc_passey_spec/toc_passey`; density/Passey tests; result persistence/export. | The scalar equation can be correct while raw-clamp or durable method metadata is absent. |
| `012` | Generalized density deficit with semantic densities, harmonic weights, normalization/refusal | unconventional module inventory; negative generalized-density symbols; generic density helpers. | Fixed Schmoker is a different method and cannot substitute. |
| `013` | Uranium TOC with raw/clamped result and environmental warning | module registry; curve families; negative uranium-TOC implementation/search. | Uranium or GR curves elsewhere are inputs, not this sourced validity-gated method. |
| `014` | Forward and inverse TOC/kerogen conversion with shared parameters and flags | `kerogen_spec/kerogen`; five local tests; negative inverse/flag search. | Forward arithmetic and silent clamps do not prove inverse identity or guarded observability. |
| `015` | Keep cited 1.10 endpoint, full response metadata, alternatives/context, and correct provenance | `kerogen_spec`; SandiMin endpoint read-only seam; historical reference; module/curve metadata. | A numerically matching endpoint does not cure a wrong or incomplete source chain. |
| `016` | Optional pyrite coupling while compiled four-component body stays source-gated | module registry; negative pyrite-coupled TOC search; source/open-item boundary. | Held regression coefficients do not authorize reconstruction of the compiled solver. |
| `017` | Typed TOC and kerogen-type HI polynomials produce S2 with provenance | module registry; negative HI/S2 symbols; typed-unit boundary. | No coefficient or neighboring Rock-Eval-style field substitutes for the complete typed path. |
| `018` | Complete RockEval carbon balance, methods, indices, and correct units | module registry; UI/IPC/report/export inventory; negative RockEval symbols. | Partial index arithmetic or names are not the six-index plus carbon-balance contract. |
| `019` | Measured gas properties and isotherm inputs ship absent with units/provenance | `gip_spec`; module dialog; UI isotherm fields; workflow param persistence. | Editable placeholders and warnings to override are still active defaults. |
| `020` | Unit-matched, mutually exclusive TOC/kerogen Langmuir scaling | `gip`; input/output spec; negative `TOC_LANG`/`VOL_KER_LANG` search. | Flat `V_L` and a separate kerogen module do not establish coupled reference scaling. |
| `021` | Opt-in regional Langmuir temperature correction with provenance | `gip`; isotherm UI; negative correction symbols. | Reservoir temperature in Bg is not isotherm temperature correction. |
| `022` | Oil saturation and non-combustible gas derate with phase-sum refusal | `gip`; free-gas tests; negative `SO`/`NCGF`/`PHI_GFP` search. | The two-phase `phi*(1-Sw)` form is not correct when either omitted term is nonzero. |
| `023` | Bg standard pressure/temperature metadata and explicit alternatives | `gip` constant; module spec; curve/result metadata; report/export. | A hard-coded `0.02827` produces plausible values but hides the standard condition. |
| `024` | Sourced Ambrose parameters, correct bracketing, raw companion, and overshoot flag | `gip` deferral text; negative Ambrose symbols; parameter/source boundary. | Historical prose or constant derivation is not an implemented, parameterized correction. |
| `025` | Reserve `GC_*` for scf/ton and `GIP_*` for Bcf with collision refusal | `gip_spec/gip`; curve family/unit registry; output naming; reports/exports; migration inventory. | Describing `GIP_*` as content does not make the mnemonic safe. |
| `026` | Internally consistent areal adsorbed/free/total Bcf using the same corrected pore volume | module registry; negative area/thickness/Bcf outputs; generic summation/report paths. | Intensive content compositing is not extensive GIP. |
| `027` | Guard PCD and require measured ash/moisture while preserving dry basis | `gip`; seven local tests; parameter fallbacks; output inventory. | NaN on invalid PCD and zero-fallback/clamped derates are not explicit refusal or preserved dry basis. |
| `028` | Distinguish linearized and nonlinear isotherm estimators with residuals | module/UI inventory; negative isotherm-fit symbols; generic fit helpers. | Plotting an isotherm from constants is not fitting measured points. |
| `029` | Through-origin TOC-to-Langmuir calibration with acknowledged nonzero experiment | module/UI/fit inventory; negative `TOCLV` symbols. | No fit means no safe default; do not invent a regression. |
| `030` | Dynamic Rickman BI on [0,1] with durable basis and scale metadata | `brittleness_spec/brittleness`; seven local tests; workflow/curve-unit persistence. | Correct arithmetic and manifest prose do not automatically prove stored/exported curve metadata. |
| `031` | Refuse static moduli against dynamic endpoints | brittleness input types/spec; workflow curve identity; negative static-basis guard search. | Computing from DT/DTS is dynamic, but absence of a static input is not a typed basis carried by every incoming curve. |
| `032` | Preserve Jarvie/Wang-Gale identity and distinguish explicit absence from unresolved mapping | brittleness option/body; mineral tests; workflow missing-input behavior. | Treating every NaN as zero conflates a deliberately absent mineral with a mapping failure. |
| `033` | Resolve combined versus split C4/C5 mappings without double count | module registry; intake/curve identity; negative mud-gas symbols. | Generic curve aliases do not implement a one-of channel mapping contract. |
| `034` | Explicit full versus compatibility GWR denominator mode with no default | module/UI registry; negative GWR symbols; source gate. | A threshold table cannot select an unresolved denominator. |
| `035` | Total, disjoint, half-open mud-gas classification with registration validation | module/classifier registry; negative mud-gas classification symbols. | Reproducing incumbent holes/overlaps is characterization, never the release rule. |
| `036` | Only corroborated Haworth thresholds, with explicit boundary convention | module/parameter-source inventory; negative Haworth implementation. | Cited thresholds remain inactive until the classifier and denominator contract exist. |
| `037` | Pixler/OI/GQR stay unavailable until primary equations are held | module registry; UI inventory; source/open-item boundary. | A generic unknown-module error is not an explicit product refusal naming the source gate. |
| `038` | Mud-gas normalization refuses while 5.0028 meaning is unresolved | module registry; unit registry; negative normalizer symbols; source boundary. | Do not turn the 1470.6 discrepancy into a replacement constant. |
| `039` | Component-sum versus measured-total residuals and missing-channel flag | module/QC registry; negative residual symbols; result-summary paths. | Generic statistics do not prove the declared pre-interpretation QC. |
| `040` | Visual picks write the same typed, undoable compute-parameter records | unconventional panel `getState`; workspace persistence; plot parameter writers; workflow params. | View/layout persistence and local redraw are not undoable computation parameters. |
| `041` | Visual and compute consume one registered equation/parameter revision | unconventional panel constants/defaults; module spec/body; data-version invalidation. | Copy-pasted equal constants can drift; invalidating source data does not unify parameter custody. |
| `042` | Stable unconventional QC codes, per-sample curves, and run-summary counts | all unconventional outputs; workflow job state; report/export; negative flag-code search. | NaN, clamp, and a generic warned run lose the sample-level reason and aggregate counts. |
| `043` | Explicit migration preserves legacy `GIP_*` meanings and requires acknowledgement | schema/migration inventory; curve versions; UI acknowledgement; negative domain migration search. | Existing append-only curve history is necessary but does not perform a semantic migration. |

## Acceptance-Test Ownership Map

| Test | Primary receipt owner | Exact proof intention |
|---|---|---|
| `T01` | `001` | wt% to fraction round trip plus untagged refusal. |
| `T02` | `017` | typed TOC forms yield identical S2. |
| `T03` | `004` | native wt% Passey form equals explicit fraction conversion. |
| `T04` | `018` | fraction-tagged TOC refuses in RockEval percent slot. |
| `T05` | `003` | sonic-unit conversion preserves DLR. |
| `T06` | `020` | TOC-matched Langmuir scaling and unit refusal. |
| `T07` | `020` | crossed organic reference pair refuses; valid volume pair computes. |
| `T08` | `003` | all three overlay signs and neutron magnitude. |
| `T09` | `006` | baseline rock produces background TOC exactly. |
| `T10` | `004` | each overlay is monotone in the documented direction. |
| `T11` | `005` | cited LOM factors over the listed values. |
| `T12` | `005` | cited VR polynomial values. **Index-owned-test gap:** route in receipt; do not alter source-owned ledger field. |
| `T13` | `005` | cap-on versus cap-off ratio and flag. |
| `T14` | `002` | any missing baseline refuses with `BASELINE_UNSET`. |
| `T15` | `040` | coupled baseline interval and curve revisions round-trip. |
| `T16` | `011` | Schmoker zero crossing, clamp, and raw companion. |
| `T17` | `012` | generalized and density-deficit forms agree. |
| `T18` | `012` | harmonic density arithmetic and weight-sum refusal. |
| `T19` | `014` | kerogen forward/inverse round trip. |
| `T20` | `015` | full kerogen response metadata and correct provenance. |
| `T21` | `013` | uranium regression plus environmental warning. |
| `T22` | `008` | coal mask returns null plus stable reason. |
| `T23` | `008` | badhole mask returns null plus stable reason. |
| `T24` | `007` | min/average/max/spread invariants. |
| `T25` | `009` | identity calibration preserves raw prediction bit-for-bit. |
| `T26` | `016` | cited pyrite/kerogen regression arithmetic. |
| `T27` | `017` | both HI polynomials at the sourced fixture. |
| `T28` | `018` | both pyrolysed/residual carbon branches retain stoichiometry. |
| `T29` | `018` | all six RockEval indices carry correct quantity units. |
| `T30` | `019` | Langmuir half-capacity and zero-pressure behavior. |
| `T31` | `020` | TOC scaling halves effective Langmuir capacity. |
| `T32` | `019` | every missing measured gas input is named; no placeholder runs. |
| `T33` | `021` | opt-in temperature correction arithmetic. |
| `T34` | `023` | named standard-condition alternatives and 1.3088 percent difference. |
| `T35` | `022` | corrected base free-gas fixture. |
| `T36` | `022` | oil and NCGF phase-volume derate. |
| `T37` | `024` | Ambrose forms agree across unit systems. |
| `T38` | `024` | NCGF bracket placement. **Index-owned-test gap:** route in receipt; do not alter source-owned ledger field. |
| `T39` | `024` | Ambrose overshoot retains raw, guards output, and flags. |
| `T40` | `025` | content/extensive mnemonic collision refuses. |
| `T41` | `026` | corrected content converts consistently to areal free GIP. |
| `T42` | `028` | linearized isotherm estimator recovers the synthetic constants. |
| `T43` | `029` | TOC-zero calibration forces zero capacity unless acknowledged. |
| `T44` | `027` | guarded critical-desorption pressure. |
| `T45` | `027` | measured ash/moisture derate with missing-input refusal. |
| `T46` | `030` | dynamic Rickman endpoints produce 0 and 1 with metadata. |
| `T47` | `031` | static inputs refuse dynamic endpoints. |
| `T48` | `032` | Jarvie and Wang-Gale equations retain method identity. |
| `T49` | `033` | combined and split channels refuse double count. |
| `T50` | `034` | both GWR modes compute and absent mode refuses. |
| `T51` | `036` | all corroborated Haworth boundaries follow the half-open rule. |
| `T52` | `035` | incumbent class holes/overlaps remain explicitly characterized. |
| `T53` | `037` | compiled mud-gas extensions refuse and name the source gate. |
| `T54` | `038` | unsourced normalization refuses while discrepancy stays evidence-only. |
| `T55` | `039` | component-total residuals and missing-channel flags. |
| `T56` | `041` | compute and visual consume the same persisted parameter revision. |
| `T57` | `042` | stable sample codes, run counts, and provenance under all listed conditions. |
| `T58` | `043` | legacy intensive `GIP_*` remains preserved through acknowledged migration. |

## Parameter, Open-Item, Escalation, Refusal, and Traceability Custody

- Preserve all 76 parameter rows and all 23 absent entries exactly. Cited example seeds, optional corrections, masks, and classifier thresholds remain contextual unless the chapter explicitly authorizes an active value.
- Keep O-1 through O-9 explicit. O-1 preserves both GWR denominator modes, O-2 blocks Tmax-to-VR coefficients, O-3 bounds the low-LOM warning evidence, O-4 blocks the compiled density-TOC body, O-5 keeps fluid properties absent, O-6 keeps estimator choice open, O-7/O-8 bound incumbent runtime/UI evidence, and O-9 keeps final overlay unselected.
- Preserve all 7 escalation bullets. A controlled vendor run may characterize runtime behavior but cannot establish SandiBumi correctness or become a parameter source.
- Preserve R-1 through R-10 separately: no untagged TOC, universal baselines, anonymous density regressions, KCF substitution, stoichiometric simplification, content-as-GIP naming, crossed Langmuir pairs, contradictory Bg factor, hole/overlap classifier, or compiled-equation inference.
- Preserve every section 8.1 through 8.6 evidence, inventory, discrepancy, gap, critique, and completeness disposition. Do not edit the dossier or PRD in Gate 1.

## Execution Tasks

### Task 1: Refreeze the baseline

- [ ] Create `codex/g1-sb-toc-adjudication` from the committed plan and verify one clean worktree at `D:\XX. SandiBumi`.
- [ ] Re-run branch, anchor, hashes, row, priority, source-state, test-ID, parameter, open/escalation/refusal, traceability, manual-evidence, and focused-candidate checks.
- [ ] Stop and reconcile any changed hash, count, branch relationship, or source-owned field.

### Task 2: Reverify TOC units, overlays, picks, calibration, and provenance (`001`-`010`)

- [ ] Trace TOC quantity identity through module args, workflow parameters, curve metadata, IPC/UI, report, and export.
- [ ] Distinguish source constants from absent baselines/maturity/background/final selection; trace whether one interval/revision pick exists and is undoable.
- [ ] Inventory all three overlay branches, sonic conversion, raw/aggregate outputs, masks, calibration records, stable flags, and full result provenance.
- [ ] Classify the exact local Passey arithmetic without extending that evidence to missing observable limbs.

### Task 3: Reverify density TOC, kerogen, and RockEval (`011`-`018`)

- [ ] Trace Schmoker method identity and clamp observability.
- [ ] Confirm generalized density, uranium, pyrite-coupled, S2/HI, and complete RockEval paths are absent or identify exact live seams.
- [ ] Reverify forward kerogen conversion, inverse absence, silent clamps, 1.10 response endpoint, alternative/provenance metadata, and cross-module consistency.
- [ ] Keep the compiled four-component source gate closed.

### Task 4: Reverify gas content, standard conditions, Ambrose, naming, and extensive GIP (`019`-`029`)

- [ ] Inventory every active gas/isotherm default and its durable provenance.
- [ ] Trace organic-capacity pairing, temperature correction, So/NCGF phase volume, Bg standard metadata, Ambrose parameters/bracketing/flags, and PCD/derate behavior.
- [ ] Trace `GIP_*` scf/ton names through module spec, persisted curves, catalog, UI, report, export, and migration history.
- [ ] Confirm area/thickness/Bcf, isotherm fit, and TOC-to-Langmuir calibration paths are absent or identify exact live seams.

### Task 5: Reverify brittleness basis and mineral mapping (`030`-`032`)

- [ ] Recheck Rickman arithmetic, endpoint scale, dynamic basis, stored/exported metadata, and static-input refusal.
- [ ] Recheck Jarvie/Wang-Gale identity and distinguish explicit mineral absence from unresolved mapping.
- [ ] Do not infer a static conversion or endpoint; this domain records the boundary only.

### Task 6: Reverify mud-gas, UI parameter custody, flags, and migration (`033`-`043`)

- [ ] Confirm mud-gas identity, ratio, classifier, normalization, extension, and total-gas QC symbols across source, tests, UI, IPC, report/export, and history.
- [ ] Preserve the unresolved denominator and source gates; never compile inferred formulas or substitute 1470.6.
- [ ] Trace unconventional panel state, undo stack, compute parameter records, duplicated constants, source invalidation, result flags/counts, and semantic migration.
- [ ] Preserve all manual evidence as user-owned and unchanged.

### Task 7: Write the evidence receipt and update all 43 ledger rows

- [ ] Create `docs/takeover/evidence/sb-toc.md` with one complete section per requirement.
- [ ] Route all 58 test intentions exactly once, including the two defined-but-unowned IDs, while preserving the ledger's source-owned `owned_tests` field.
- [ ] Update only live adjudication columns in `docs/takeover/requirements.csv`.
- [ ] Recompute and update `docs/takeover/STATUS.md` with exact live counts.
- [ ] Reverify the frozen six-column hash.

### Task 8: Run structural checks, gates, and commit

- [ ] Assert 43 receipt sections, all legal/non-placeholder ledger fields, all 58 test routes, all 76 parameters, 9 opens, 7 escalation bullets, 10 refusals, zero Tier-C items, and section 8 custody.
- [ ] Search new receipt/ledger text for identifying names, invented values, promoted placeholders, inferred compiled equations, false field evidence, and content/GIP ambiguity.
- [ ] Run `node --test tools/takeover-ledger.test.mjs`, `node tools/takeover-ledger.mjs --summary-json`, and `git diff --check`.
- [ ] Run `npx tsc --noEmit`, `cargo check`, and `powershell -ExecutionPolicy Bypass -File tools\check.ps1`, using the established isolated Cargo target if needed.
- [ ] Stage exactly the SB-TOC receipt, ledger, and STATUS; inspect the cached diff and source hash.
- [ ] Commit locally with message `G1-DOM-TOC adjudicate 43 SB-TOC requirements`. Do not push, merge, or open a pull request.

### Task 9: Continue Gate 1 serially

- [ ] Recompute remaining domains after SB-TOC while SB-GEO remains deferred to the next version.
- [ ] Select the next dependency-relevant petrophysics or release domain from live evidence.
- [ ] Create the next serial planning branch in `D:\XX. SandiBumi`.
- [ ] Do not begin Gate 2 production remediation until Gate 1 evidence reconciliation completes or Jauhar explicitly changes sequence.

## Plan Self-Review Receipt

- **Coverage:** all 43 requirements appear once; all 58 chapter tests appear once; T12/T38 ownership gaps are routed without changing immutable source fields; all 76 parameters, 9 opens, 7 escalation bullets, 10 refusals, zero Tier-C items, and section 8.1-8.6 blocks have custody.
- **Live drift:** typed quantity is separated from a unit label; editable seeds from absent parameters; local view state from undoable compute parameters; local arithmetic from durable metadata; generic masking/provenance/history from the complete domain contract.
- **Silent-wrongness:** wt% versus w/w 100x exposure, baseline/default bias, hidden Bg standard condition, So/NCGF omission, missing Ambrose correction, and scf/ton under `GIP_*` are all explicit evidence seams. No plausible-looking output is credited merely because it is finite.
- **Source discipline:** all 23 absent parameters remain absent; O-1 through O-9 and all seven escalations stay open; compiled extensions stay unavailable; no baseline, maturity, fluid property, calibration, isotherm, or classifier choice is invented.
- **Evidence discipline:** 26 focused candidates pass, but no executable `SB-TOC-Tnn` whole-contract test exists and the custom panel has no direct frontend acceptance test. Manual unconventional evidence remains `0/4`.
- **Scope discipline:** planning changes only this plan and STATUS; execution changes only the SB-TOC receipt, ledger, and STATUS. Production remediation remains Gate 2.

## Execution Handoff

Commit this planning increment locally, create `codex/g1-sb-toc-adjudication` serially from it, then execute inline with `superpowers:executing-plans`. Jauhar's standing direction is to continue Gate 1 serially, so no additional approval is required unless execution reaches an unresolved product-owner, source, legal, or performance decision that would otherwise be guessed.
