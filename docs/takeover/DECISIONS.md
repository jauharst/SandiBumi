# SandiBumi takeover decisions

This register separates product-owner policy from engineering fact. `OPEN` means no decision has
been inferred. A decision row changes only from explicit Jauhar direction or named external evidence.

| ID | Decision | State | Current direction | What settles it | Blocks |
|---|---|---|---|---|---|
| DEC-001 | Re-adjudicate original P0 priorities for the paid pilot | DECIDED | Authorized 2026-08-10 | Jauhar authorization | none |
| DEC-002 | Release program | DECIDED | Five gates; Windows-first paid offline pilot | Jauhar authorization | none |
| DEC-003 | Pilot workflow and representative corpus | DECIDED | The representative automated chain is: LAS 2.0 or delimited import; the source/validity and QC/conditioning preflight selected by the exact `QC_CONDITIONING` pilot manifest; linear-GR VSH; density and chart-free analytic density-neutron porosity; effective/total Archie and parameterized Indonesia saturation; fixed per-zone VSH/PHIE/Sw cutoffs with thickness, NTG and pay summary; then log/crossplot review plus PDF and LAS deliverables. Gate 2 uses deterministic, locally controlled repository fixtures; Gate 4 adds at least one sanitized, legally controlled real delivery. Every selected method needs valid and invalid-precondition evidence, and this decision supplies no scientific endpoint or default. | Jauhar approval, 2026-08-12; exact method and requirement membership remains the approved DEC-018 manifest | none |
| DEC-004 | Customer-facing 2,000-well statement | NEEDS-JAUHAR | Design recommends removal until a defined benchmark proves it | Jauhar chooses removal now or an explicitly non-customer-facing hold | G5 |
| DEC-005 | Licence unit and activation | NEEDS-JAUHAR | OPEN | Commercial decision informed by deployment constraints | G5 |
| DEC-006 | Commercial model and support commitment | NEEDS-JAUHAR | OPEN | Commercial decision with written hours and escalation boundary | G5 |
| DEC-007 | Update delivery and supported-version window | NEEDS-JAUHAR | OPEN | Commercial and deployment decision | G5 |
| DEC-008 | Portfolio benchmark operations and thresholds | NEEDS-JAUHAR | OPEN | Named operations, fixture and hardware profile | later scale claim |
| DEC-009 | Lineage granularity beyond the pilot audit need | NEEDS-JAUHAR | OPEN | Audit requirement from the pilot or buyer | later lineage design |
| DEC-010 | Linux product timing and support contract | DEFERRED | Revisit after the Windows pilot | Named opportunity and support capacity | no Windows-pilot block |
| DEC-011 | Geomechanics / PPFG product timing | DEFERRED | Hold SB-GEO for the next product version; keep the current Windows pilot focused on open-hole petrophysics | Revisit after the petrophysics pilot scope is accepted | no current-pilot block |
| DEC-012 | Wyllie compaction behavior when `Cp < 1` | DECIDED | Refuse the run; Help may explain the condition but cannot substitute for the refusal | Jauhar direction, 2026-08-11 | SB-POR-017 implementation and acceptance test |
| DEC-013 | POR output naming and intentional replacement | DECIDED | Output curve names are user-configurable. Distinct names preserve parallel results; deliberately reusing a current name is an explicit replacement and must remain versioned and undoable. Silent name collision is forbidden | Jauhar directions, 2026-08-11 | SB-POR-004 implementation and acceptance tests |
| DEC-014 | POR quick-look, hydrocarbon-response and iterative-method separation | DECIDED | Arithmetic and RMS remain explicit available contracts; the RMS comparison role and SSC/SSPW RMS-conditioning role stay distinct. The Gaymard-Poupon hydrocarbon-response path and the coupled porosity-`Sxo`/`Sw` iterative path are mandatory, separately selected contracts. No route silently stands in for another | Jauhar direction, 2026-08-11; exact equations and parameters remain chapter/source-bound | SB-POR-023, SB-POR-029 through 038, SB-POR-050 through 052, SB-POR-057 and SB-POR-059 |
| DEC-015 | Exact POR-wide common-contract boundary | DECIDED | Porosity corrections and methods use their own source-bound numerical limits, validity domains and correction rules beneath one common POR family/provenance/output-role/flag envelope. The shared contract standardizes custody and observability, not one universal numerical clamp. A method never borrows another method's bound, and an uncited bound remains absent | Jauhar selected option 2, 2026-08-12 | SB-POR-001 through 003 migration |
| DEC-016 | Required POR capability set | DECIDED | Chart-free analytic N-D, the source-gated hydrocarbon-response chain, excavation correction and neutron-sonic all belong in the product. This settles inclusion, not a missing source, parameter, implementation sequence or an otherwise deferred pilot-timing row | Jauhar direction, 2026-08-11; each chapter source gate still applies | SB-POR-021, SB-POR-027 and SB-POR-029 through 042 |
| DEC-017 | Genuine sonic RHG product path | DECIDED | Implement the original three-segment Raymer-Hunt-Gardner 1980 transform as SandiBumi's genuine RHG product path; do not preserve the current approximation under an RHG label merely by renaming around it. The public identity must name RHG80 and its primary source. The current `0.625*(DT-DT_MA)/DT` branch may survive only as a separately named `FIELD_OBSERVED` method with a cited coefficient and one correct shale convention. Exact equation typography must be reverified against the original scan before production code because `docs/method_sonic_porosity.md` explicitly is not implementation authority. This closes SP-013's rename-versus-RHG80 product choice but does not silently choose the separate IP, Geolog or Techlog vendor rendering requested by SB-POR-020 | Jauhar chose “RHG rendering” from the previously stated rename-versus-RHG80 decision, 2026-08-12; the original-paper verification gate remains binding | SB-POR-013 through 020 implementation; original-scan verification; separate SB-POR-020 vendor-rendering decision |
| DEC-018 | Exact first-pilot capability manifest | DECIDED | APPROVED: the exact 242 requirement IDs in `docs/takeover/pilot-scope.json`, SHA-256 `0412de0cc43fabbe0c5e32d4c831d65e90536ee1c348802ab67cb0f3dcd70b6b`, are the first paid Windows offline open-hole petrophysics blocker program; the other 689 requirements are `DEFERRED`, not deleted, and no product-inclusion decision, scientific value, equation or evidence claim changes. Any older row-level “if included” wording outside the manifest now applies only to post-pilot reconsideration; any ID, disposition or hash drift invalidates approval | Jauhar approval, 2026-08-12 | none |
| DEC-019 | Exact Gate 1 SB-GEO adjudication exception | DECIDED | APPROVED: only the exact 52 DEC-011-deferred SB-GEO rows identified by SHA-256 `e1eed8f713f5449926b7e5c840d06d9c061eef8c7f420a868434dc57c2978ffc` may remain visibly UNADJUDICATED for Gate 1; all remain mandatory next-version work | Jauhar approval, 2026-08-12; any row or hash drift invalidates the approval | none |
| DEC-020 | Complete computed-curve custody | DECIDED | The session operator is explicit and classified HUMAN or AUTOMATED, never inferred from the Windows account. Every run requires a non-empty source/reference note for its explicit values; a narrower stored source remains attached when one exists. No production computed-curve write or undo may fall back to ancestry-free storage. The record is per well, visible on demand, preserved with current/archive versions and Save As, and embedded in every number-carrying deliverable. This decides the SB-CORE-010 minimum only; DEC-009 still governs additional lineage beyond it. | Jauhar direction, 2026-08-13 | none |
| DEC-021 | Build-derived module identity source | NEEDS-JAUHAR | OPEN — current run ancestry stores hand-maintained `CARGO_PKG_VERSION`, which SB-DBM-002 explicitly rejects. No executable digest, per-module source digest, build id, algorithm, encoding or cross-machine stability rule is inferred. | Choose the exact artefact boundary, derivation mechanism, stored representation and reproducibility/stability rule; alternatively adopt a named architecture standard that fixes them. | SB-DBM-002; the module-version arms of SB-DBM-004 and SB-DBM-015 |
| DEC-022 | Legacy provenance timestamp migration | NEEDS-JAUHAR | OPEN — pre-migration `log_sets.created_at` values are unqualified local timestamps with no recorded authoring zone. Engineering recommends classifying every such value `ZONE_UNKNOWN`, storing all new timestamps as unambiguous UTC instants, and converting only at display; guessing a zone would invent history, while retaining old local and new UTC meanings in one field would preserve the SB-DBM-041 ambiguity. | Jauhar chooses `ZONE_UNKNOWN` for every pre-migration timestamp, or explicitly accepts the mixed-meaning legacy alternative named by 22_database-model.md §7.2 item 7. | SB-DBM-009 and the timestamp arm of SB-DBM-T11 |
| DEC-023 | Structured-audit zone-set dependency versus immutable pilot scope | NEEDS-JAUHAR | OPEN — approved SB-DBM-011 and exact T11 require a zone-set identity/version, but the owning SB-DBM-008 row is outside DEC-018's immutable 242-row manifest. DEC-020 settles explicit HUMAN/AUTOMATED operator identity, not zone-set identity. Engineering will neither weaken T11 nor silently implement a deferred product contract as if it were approved. | Authorize the zone-set identity/version seam as necessary SB-DBM-011 infrastructure while SB-DBM-008 remains deferred, or explicitly revise and re-approve DEC-018's manifest/hash to include SB-DBM-008. | SB-DBM-011 and the zone-set arm of SB-DBM-T11 |
| DEC-024 | Complete re-run manifest identity seams versus immutable pilot scope | NEEDS-JAUHAR | OPEN — approved SB-DBM-015 and exact T15 require one complete stored manifest, including conditional stochastic and learned-model identities, but the owning SB-DBM-014 and SB-DBM-019/020 rows are outside DEC-018's immutable 242-row manifest. The zone-set arm is already recorded by DEC-023 and the build identity by DEC-021. Engineering will not omit a required manifest arm, fabricate an identity or silently enable a deferred capability. | Authorize versioned identity schemas/resolvers as SB-DBM-015 infrastructure while their stochastic/model capabilities remain deferred, or explicitly revise and re-approve DEC-018's manifest/hash; also settle DEC-021 and DEC-023 before implementation. | SB-DBM-015 and SB-DBM-T15 |
| DEC-025 | Physics-driving neutron metadata versus immutable pilot scope | NEEDS-JAUHAR | OPEN — approved SB-DBM-017 and SB-POR-024 require a declared neutron matrix-scale attribute, run-time custody, stale-output invalidation and absent-value refusal, but the owning SB-ENV-012 metadata/persistence/consumer contract is outside DEC-018's immutable 242-row manifest. The cited closed enum exists in `20_envcorr-qc.md`; no numeric default is authorized. Live `nphimat` choices are explicit module parameters, not stored attributes, and turning them into a hidden header default would reproduce the failure SB-DBM-017 forbids. | Authorize SB-ENV-012's typed neutron-scale metadata/persistence seam as required infrastructure while SB-ENV-012 remains deferred, or explicitly revise and re-approve DEC-018's manifest/hash to include SB-ENV-012. Preserve explicit absence and do not infer a Logging Contractor, tool, salinity or matrix default. | SB-DBM-017, SB-DBM-T17 and SB-POR-024 |
| DEC-026 | PHIE floor source precedence for the pilot registry | NEEDS-JAUHAR | OPEN — `CLAUDE.md` requires `PHIE_FLOOR = 0.001` at density/D-N curve and pay paths, while immutable `11_porosity.md` SB-POR-045 and §5 require the floor to ship `ABSENT` because one held IP manual attests both `0.001` and `0.0001`. The value crosses selected pilot module boundaries and therefore blocks SB-DBM-025's source-bearing constant registry. Registering `0.001` as authority would override the PRD; removing its default would violate the binding project rule. | Jauhar explicitly chooses which contract has precedence and how the choice is source-labelled and recorded in every affected run/pay record. Until then, neither value may be promoted into the registry and the current literal must not be presented as cited closure. | SB-DBM-025, SB-POR-045 and their Gate 2 tests |
| DEC-027 | Required-unset storage and Geolog null-bound authority | NEEDS-JAUHAR | OPEN — SB-DBM-003 requires a declared-but-unsupplied required parameter to remain a queryable `run_parameters` row with `state = REQUIRED_UNSET` and SQL-NULL value/source; SB-DBM-030 instead requires that same “parameter never supplied” state to be absence-of-row, while T30 still requires it to remain named through IPC, UI and export. Separately, T29 requires its bound to derive from the magnitude the export path would emit, while §5 marks both conflicting Geolog magnitudes non-adoptable and delegates the eventual export choice to SB-DIO. | Choose the queryable named row or explicitly correct SB-DBM-003/T05; and either authorize one cited Geolog export magnitude or explicitly make T29 derive from the T1 header verification constant without claiming it is the production export value. Engineering recommends retaining the queryable `REQUIRED_UNSET` row and correcting T30's absence-of-row phrase. | SB-DBM-030, SB-DBM-T29 and SB-DBM-T30 |
| DEC-028 | Legacy single-handle parameter-pack policy | NEEDS-JAUHAR | OPEN — the already-closed P0 SB-INS-015 and exact SB-INS-T18 require a parameter-pack row with no ordinal to stop before activation, while pilot-approved SB-DBM-032 and exact SB-DBM-T32 require a file carrying only one handle to load with a warning. Those reporting surfaces overlap in `parameter_pack.rs`; both outcomes cannot hold for the same input. Engineering recommends retaining the hard refusal for the first pilot because ordinal-only binding is the documented silent-wrongness case and warning-and-continue would weaken an existing P0 guard. | Explicitly choose the accepted legacy shapes and correct the conflicting requirement/test: retain refusal for both one-handle forms, or define separately whether semantic-only and ordinal-only rows refuse or warn. No choice may silently reinterpret an ordinal or downgrade a disagreement to a warning. | SB-DBM-032 and SB-DBM-T32; any change to the closed SB-INS-015/SB-INS-T18 contract |
| DEC-029 | Structural-index file proof versus the immutable pilot import surface | NEEDS-JAUHAR | OPEN — pilot-approved SB-DIO-010 and exact T15 require importing a Geolog flat-ASCII file whose non-first `REFERENCE` declaration becomes the index and is reported. The current test supplies headers/classes directly to `resolve_index_column`; no production reader or command consumes the cited `.flat_ascii_format` declarations. DEC-003 and G2-T04 name LAS 2.0 and delimited text as the pilot import surface. Engineering will neither call the helper a file import nor silently add a format capability outside that stated surface. | Authorize a source-faithful Geolog flat-ASCII reader/import surface as necessary SB-DIO-010 infrastructure while leaving the rest of the format outside the pilot, or explicitly reconcile T15 and the approved manifest with DEC-003. The locally held cited specs establish the structural declaration but do not themselves authorize a hidden test-only reader. | SB-DIO-010 and SB-DIO-T15 |
| DEC-030 | Exact-mnemonic requests versus semantic-family requests | NEEDS-JAUHAR | OPEN — SB-DIO-031 forbids supplying another curve's bytes under a requested name, while the current untyped-string resolver matches either mnemonic or family and existing workflows intentionally depend on family fallback. Plotting already calls its input a semantic request and returns the concrete curve identity; equation/workflow readers instead key returned values under the original request. One string cannot state whether fallback is forbidden or intended. | Approve a typed split: `EXACT_MNEMONIC` returns only that mnemonic and reports absence; `SEMANTIC_FAMILY` may choose a family member only while returning its concrete mnemonic, set/curve identity and resolution rule. Classify every current caller explicitly and make T47 inventory all resolver surfaces. Alternatively supply another explicit non-overlapping request contract; engineering will not preserve the conflation or silently break family workflows. | SB-DIO-031, SB-DIO-T47 and the shared request-type dependency recorded for SB-DIO-034 |
| DEC-031 | Four-state correction channel encoding and per-sample step custody | NEEDS-JAUHAR | OPEN — SB-ENV-007 requires one per-sample correction channel to distinguish full, partial, not-applied and refused states, with the applied step set at partial samples. SB-ENV-030 specifies only binary `1 = true` polarity plus exclusion-versus-diagnostic metadata; it does not define four wire values. `ModuleOutputs` carries only `Vec<f32>`, declared outputs have no flag-type metadata, and SB-ENV-005/OI-4 leaves the correction-step identities and persistence owner open. Exact T13 also requires partial-caliper execution while the closed SB-ENV-006 refusal currently stops any incomplete finite CALI coverage. Engineering will not invent float codes, relabel a unit string as type metadata, split one required channel into several curves without approval, or silently weaken the refusal. | Choose one non-overlapping contract. If SB-ENV-030 remains binary, authorize a one-hot group of typed `1 = true` state channels and define the versioned per-sample step-membership record. If one companion channel is mandatory, define a categorical `CorrectionState` type with exact stable wire codes and clarify that it is not a binary flag curve. In either case select OI-4's persistence owner, define the step identity/version contract and explicitly authorize partial correction for finite covered intervals while all-uncovered input still refuses. Engineering recommends the one-hot typed group if preserving SB-ENV-030 unchanged is the priority. | SB-ENV-007, SB-ENV-T13, SB-ENV-005/OI-4 and the SB-ENV-030 type contract |
| DEC-032 | Bad-hole reason encoding and signed DRHO cause custody | NEEDS-JAUHAR | OPEN — SB-ENV-022 requires a companion reason channel for caliper, DRHO, both and neither-evaluable; SB-ENV-023 requires the DRHO cause to retain its sign. OI-7 explicitly ties the encoded-curve-versus-boolean-group choice to SB-ENV-007, whose representation is still open at DEC-031. SB-ENV-030 defines only binary `1 = true`, not categorical reason codes, while the two SB-ENV-021 availability channels cannot say which criterion fired. Engineering will not invent `f32` enum values, infer causation from availability, or split the singular public channel into multiple curves without approval. | Settle the shared representation family with DEC-031, then either authorize a typed binary cause group whose exact members distinguish caliper, positive DRHO, negative DRHO, both and neither-evaluable, or define one categorical `BadHoleReason` type with every stable wire value and export representation. Engineering recommends the typed binary group to preserve SB-ENV-030, but the exact group remains a product contract rather than an implementation convenience. | SB-ENV-022, SB-ENV-023, SB-ENV-T31, OI-7 and the SB-ENV-030 type contract |
| DEC-033 | Mask repair-exemption declaration and first repair inventory | NEEDS-JAUHAR | OPEN — OI-5 explicitly leaves module-level, category-level and per-output declarations non-equivalent. The current module inventory finds one declared output with repair semantics: `log_predict.SYN`, but its meaning changes by `OPT_COMBINE`. `MAX_RAW` is the exact washout-repair path required by T36/T37; `FILL_MISSING` preserves any finite raw value and only reconstructs an actually missing target; `SYNTHETIC` is a pure model output that OI-5 says arguably is not a repair. A module-wide exemption would silently exempt all three, a `Prep` category exemption would bypass the safety mask for unrelated conditioning modules, and an unconditional output flag would still conflate the modes. The runner currently has no typed reconstructed-sample marker. | Choose an exact, non-overlapping contract. Engineering recommends a conditional per-output declaration whose initial approved inventory contains only `log_predict.SYN` when `OPT_COMBINE = MAX_RAW`; both mask passes are bypassed for that declared repair and a typed binary companion marks only finite values produced at masked depths. `FILL_MISSING` and `SYNTHETIC` remain normally masked until separately enumerated and approved. If another policy is intended, name every exempt module/output/mode and the marker's public identity/type/export behavior. | SB-ENV-027, SB-ENV-T36, SB-ENV-T37 and OI-5 |
| DEC-034 | Four-sample Hampel fallback versus existing refusal | NEEDS-JAUHAR | OPEN — exact SB-ENV-T42 requires a four-sample window to run and report per sample that it used the declared fallback, while the shipped `MIN_HAMPEL_SAMPLES` guard and its existing regression test refuse that same under-minimum window because its decision is set by the judged sample. A refused `Result` has no `ModuleOutputs` flag channel, and reusing `OUT_FLAG` would conflate replaced samples with fallback use. Engineering will neither weaken the refusal nor claim a per-sample flag exists on an error. | Recommended: retain the under-five refusal and explicitly correct/re-adjudicate T42 so zero-MAD windows emit a separate typed binary fallback-used diagnostic while undersized windows return a structured named refusal. Alternative: explicitly authorize removing the guard and accepting the declared fallback for four samples, including the estimator-risk tradeoff. | SB-ENV-033 and SB-ENV-T42 |
| DEC-035 | Exact recovery record versus absent culling and immutable pilot scope | NEEDS-JAUHAR | OPEN — pilot-approved SB-ENV-037 and exact T45 require despike, cull, clip and gap fill to restore bit-identically from each operation's emitted record, but the owning cull capability SB-ENV-036 is ABSENT, DEFERRED and outside DEC-018's immutable 242-row manifest. Current batch Condition outputs may identify changed samples but do not emit their original values; interactive undo proves only one partial path. Engineering will neither implement the deferred cull silently nor omit T45's cull arm and call the whole contract complete. | Choose one explicit route: revise and re-approve DEC-018's manifest/hash to include SB-ENV-036, then implement all four recovery paths; or re-adjudicate SB-ENV-037/T45 so the first pilot names only the shipped operation set while cull recovery remains deferred with cull itself. Either route must define one stable bit-exact emitted record and its persistence owner, including missing-value bit custody. Engineering recommends re-adjudicating the pilot test if culling is intentionally still deferred. | SB-ENV-037, SB-ENV-T45 and the shared interactive-provenance arm owned by SB-ENV-042 |
| DEC-036 | CLY categorical provenance custody versus immutable pilot scope | NEEDS-JAUHAR | OPEN — pilot-approved SB-CLY-001 and exact T01 require a persisted/exportable per-sample `ENDPOINT_INVALID` provenance token distinct from missing input, plus a zone-bearing run message; pilot-approved SB-CLY-055 and exact T35 require the same typed tokens to survive LAS export/re-import. The approved CLAY_LINEAR_GR group includes SB-CLY-001/034/035/041/042/043/046/050/051/054/055 but excludes SB-CLY-031 and SB-CLY-032, which own the provenance curve, its closed vocabulary and the separate substitution field. `ModuleOutputs` and LAS carry numeric curve samples; the current generic precondition companion is a binary condition-agnostic flag and has no zone identity or categorical token table. Engineering will not invent float codes, call `1` the specified token, silently implement excluded whole-domain provenance, or write T01/T35 against an internal result or absent output. | Choose one explicit route. Recommended: revise and re-approve DEC-018's manifest/hash to include SB-CLY-031 and SB-CLY-032, then approve one versioned categorical CLY provenance registry with stable numeric wire/LAS codes, token metadata, unknown-code refusal and a separate substitution channel. Alternative: explicitly authorize that schema as narrow infrastructure for the already-approved CLY rows while 031/032 remain deferred, naming the exact tokens, wire codes and export representation that are in scope. Either route must cover at least method identity, missing input, masked/disabled input, `ENDPOINT_INVALID`, `COAL`, and substitution independence before dependent rows can claim closure. | SB-CLY-001, SB-CLY-055, SB-CLY-T01/T24/T32/T35 and dependencies SB-CLY-031/SB-CLY-032/SB-CLY-046 |
| DEC-037 | CLY undeclared `-999` screening versus DIO `NoNull` precedence | NEEDS-JAUHAR | OPEN — exact SB-CLY-T44 requires a LAS value equal to the known IP bad-hole sentinel `-999` to become absent and raise a warning even without `~W NULL`; adopted SB-DIO-003/T04 requires a channel explicitly declared `NoNull` to preserve a genuine sentinel-shaped amplitude, and SB-DIO-005/T09 makes null conventions per-channel rather than global. Current LAS readers cannot prove writer identity from the number alone, and the CLY chapter does not enumerate the exact mnemonic/signature scope or precedence over an explicit `NoNull`. Adding `-999` to the global list would silently delete legitimate data and violate the closed DIO contract. T35's provenance-export arm is separately blocked by DEC-036. | Choose one explicit, non-overlapping policy. Recommended: a source-controlled vendor/curve rule identifies only the cited CLY output family, explicit `NoNull` wins, and an undeclared matched `-999` is quarantined behind a blocking import question that names the sentinel and affected curves; confirmation converts those cells to `f32::NAN` and records the rule. Alternative: authorize automatic screening for an exact enumerated mnemonic/signature set, still subordinate to `NoNull`, with the same warning/audit record. Supply the complete accepted identifiers and precedence; do not infer them from magnitude alone. | SB-CLY-034, SB-CLY-055, SB-CLY-T35/T44, SB-DIO-003/T04/T05 and SB-DIO-005/T09 |
| DEC-038 | SSC/SSPW scope and the semantic boundary of an unlimited porosity pair | NEEDS-JAUHAR | OPEN — SB-POR-002 requires unlimited and limited PHIT/PHIE pairs for every porosity method. The common POR registry currently classifies SSC and SSPW as deterministic methods, but both discard pre-limit values inside protected `ssc.rs`; their final porosity also depends on already-clamped fractions, projections and component volumes. Merely exposing the expression before the last clamp would not disclose every binding limit, while duplicating the protected formulas elsewhere would create two scientific implementations. SB-POR-004 collision-safe custody is also not complete. | Decide whether SSC/SSPW are methods governed by SB-POR-002 or separately typed workflows outside its twin requirement. If governed, define whether unlimited means only the final PHIT/PHIE clamp is bypassed or every upstream limit that can bind those outputs; then finish SB-POR-004 and explicitly authorize one narrow `ssc.rs` edit preserving existing limited behavior. Engineering recommends a separately typed workflow role unless a source-backed full pre-limit lineage is required. | SB-POR-002, SB-POR-T11/T19/T31/T39 and SB-POR-004 |
| DEC-039 | Singular POR branch-and-limit stream representation | RULED-2026-08-16 | **RULED: record the branch-and-limit state as a COMMENT ON THE CURVE, carried per CURVE VERSION** — each curve already has versions, and each version gets its own comment reflecting what the user did on that run. Same mechanism as the SB-POR-026 ruling, so the two share one answer. This REPLACES the engineering recommendation below (a categorical class curve backed by a closed registry) and dissolves its hardest parts: no vocabulary to enumerate, no combination rule when several limits bind at one sample — the text states all of them — no unknown/fractional-code refusal to design, and no categorical-export contract through reframe. The per-version record already exists: `log_sets` is one row per run holding set_id, well_id, set_name, version, module, params_json, inputs_json, created_at, frame. *What was originally asked, retained for the record:* SB-POR-003 requires one per-sample stream identifying both the producing branch and every binding limit. Current module flags are typed binary `NaN/0/1`, which cannot carry those identities; existing categorical method curves use stable integer-valued `f32` classes, but no POR vocabulary, combination rule, token metadata, unknown-code refusal or export representation exists. The chapter cites IP 6/7/9/16 and Geolog `MTH_PHI = SHALE`, but those examples are not a complete cross-method registry. Several limits can bind one sample, and SSC/SSPW sites are protected by DEC-038's unresolved scope. | ONE narrow authorization remains: `log_sets` has no free-text comment column and adding one means editing `src-tauri/src/db.rs`, a prohibited file — authorize that single edit as the narrow `ssc.rs` edit was authorized. `params_json` must NOT be reused: it is the run's PARAMETERS, and mixing a narrative into it makes the two indistinguishable to every reader. DEC-038 remains separately open. | SB-POR-003, SB-POR-T12/T38/T39/T41, SB-POR-047/048 and DEC-038 |
| DEC-040 | Narrow `ssc.rs` edit for the one shared clay-bound-water porosity | RULED-2026-08-16 | **RULED: authorized.** SB-POR-008 needed `sspw` to compute clay-bound-water porosity against FORMATION WATER density rather than the invaded-zone fluid; the two agree at the 1.00 defaults and diverge on salt water (1.10), so the bug was invisible in every existing test. Scope was narrowed after investigation: only `sspw` was rerouted through the shared helper. `ssc`'s own term is a silt-geometry fraction along a fluid-anchored line, so its arithmetic was left untouched and only its local was renamed. | Settled. Recorded here so the protected-file edit is auditable rather than implicit. | SB-POR-008 |
| DEC-041 | Matrix density selection across chained porosity modules | RULED-2026-08-16 | **RULED: 2.65.** Matrix density was split 2.645/2.65 across modules the docs instruct chaining, so a chained run silently mixed two matrix densities. Jauhar selected 2.65; all three sites now agree and the position not taken is still disclosed through the competing-values topic. | Settled. Field check outstanding: the change moves porosity by roughly 0.3 p.u. | SB-POR-011 |
| DEC-042 | Pay eligibility of the neutron-density quick-look shortcuts | RULED-2026-08-16 | **RULED: ruling (b).** The arithmetic and RMS neutron-density shortcuts stay labelled quick-look comparisons and are NOT a crossplot porosity method, but their limited curve IS a pay candidate. | Settled in code. **`PILOT_SCOPE.md` item 6 still says the opposite** — *excluded from pay by default* — and is hash-bound to DEC-018, so it needs Jauhar's own edit plus a note that (b) superseded it. | SB-POR-023, DEC-018 |
| DEC-043 | Value `PHIE` is set to when the floor binds | RULED-2026-08-16 | **RULED: 0.001**, i.e. the later direct product record supersedes the chapter's unresolved position. IP's own manual gives 0.001 and 0.0001 for the same quantity in three places (F17), which is why the chapter withheld a default. | Adjudication settled. One clause still needs confirming: `11_porosity.md:1052-1053` states TWO requirements — no default AND documented user decision. The ruling satisfies the second. Working assumption is **(A)** a cited parameter DEFAULTING to 0.001 with the ruling as its source, matching DEC-041 and DEC-042; the alternative **(B)** is a `param_open` that refuses unsupplied. Recorded as a deviation from a literal MUST rather than buried. | SB-POR-045 |
| DEC-044 | Where a porosity run's conditioning record is written | RULED-2026-08-16 | **RULED: a direct comment on the curve's own description**, not a new flag curve and not a flag-shaped provenance key — the text an analyst reads in the curve catalogue and the exported LAS header. Consumes `condflag`'s flag rather than recomputing it, because `condflag`'s rule already excludes coal and washout and a local re-derivation would call coal gas. | Same mechanism as DEC-039, so the two share one implementation. Blocked on the same narrow `db.rs` authorization: neither `computed_curves` nor `curve_meta` has a free-text description column, and the manifest's `log_out` description is per-MODULE, identical every run. | SB-POR-026 |
