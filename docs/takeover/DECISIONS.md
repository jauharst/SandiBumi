# SandiBumi takeover decisions

This register separates product-owner policy from engineering fact. `OPEN` means no decision has
been inferred. A decision row changes only from explicit Jauhar direction or named external evidence.

| ID | Decision | State | Current direction | What settles it | Blocks |
|---|---|---|---|---|---|
| DEC-001 | Re-adjudicate original P0 priorities for the paid pilot | DECIDED | Authorized 2026-08-10 | Jauhar authorization | none |
| DEC-002 | Release program | DECIDED | Five gates; Windows-first paid offline pilot | Jauhar authorization | none |
| DEC-003 | Pilot workflow and representative corpus | DECIDED | The representative automated chain is: LAS 2.0 or delimited import; the source/validity and QC/conditioning preflight selected by the exact `QC_CONDITIONING` pilot manifest; linear-GR VSH; density and chart-free analytic density-neutron porosity; effective/total Archie and parameterized Indonesia saturation; fixed per-zone VSH/PHIE/Sw cutoffs with thickness, NTG and pay summary; then log/crossplot review plus PDF and LAS deliverables. Gate 2 uses deterministic, locally controlled repository fixtures; Gate 4 adds at least one sanitized, legally controlled real delivery. Every selected method needs valid and invalid-precondition evidence, and this decision supplies no scientific endpoint or default. | Jauhar approval, 2026-08-12; exact method and requirement membership remains the approved DEC-018 manifest | none |
| DEC-004 | Customer-facing 2,000-well statement | NEEDS-JAUHAR | Design recommends removal until a defined benchmark proves it | Jauhar chooses removal now or an explicitly non-customer-facing hold | G5 |
| DEC-005 | Licence unit and activation | NEEDS-JAUHAR | OPEN | Commercial decision informed by deployment constraints | G5 |
| DEC-006 | Commercial model and support commitment | NEEDS-JAUHAR | OPEN | Commercial decision with written hours and escalation boundary | G5 |
| DEC-007 | Update delivery and supported-version window | NEEDS-JAUHAR | OPEN | Commercial and deployment decision | G5 |
| DEC-008 | Portfolio benchmark operations and thresholds | NEEDS-JAUHAR | OPEN | Named operations, fixture and hardware profile | later scale claim |
| DEC-009 | Lineage granularity beyond the pilot audit need | NEEDS-JAUHAR | OPEN | Audit requirement from the pilot or buyer | later lineage design |
| DEC-010 | Linux product timing and support contract | DEFERRED | Revisit after the Windows pilot | Named opportunity and support capacity | no Windows-pilot block |
| DEC-011 | Geomechanics / PPFG product timing | DEFERRED | Hold SB-GEO for the next product version; keep the current Windows pilot focused on open-hole petrophysics | Revisit after the petrophysics pilot scope is accepted | no current-pilot block |
| DEC-012 | Wyllie compaction behavior when `Cp < 1` | DECIDED | Refuse the run; Help may explain the condition but cannot substitute for the refusal | Jauhar direction, 2026-08-11 | SB-POR-017 implementation and acceptance test |
| DEC-013 | POR output naming and intentional replacement | DECIDED | Output curve names are user-configurable. Distinct names preserve parallel results; deliberately reusing a current name is an explicit replacement and must remain versioned and undoable. Silent name collision is forbidden | Jauhar directions, 2026-08-11 | SB-POR-004 implementation and acceptance tests |
| DEC-014 | POR quick-look, hydrocarbon-response and iterative-method separation | DECIDED | Arithmetic and RMS remain explicit available contracts; the RMS comparison role and SSC/SSPW RMS-conditioning role stay distinct. The Gaymard-Poupon hydrocarbon-response path and the coupled porosity-`Sxo`/`Sw` iterative path are mandatory, separately selected contracts. No route silently stands in for another | Jauhar direction, 2026-08-11; exact equations and parameters remain chapter/source-bound | SB-POR-023, SB-POR-029 through 038, SB-POR-050 through 052, SB-POR-057 and SB-POR-059 |
| DEC-015 | Exact POR-wide common-contract boundary | DECIDED | Porosity corrections and methods use their own source-bound numerical limits, validity domains and correction rules beneath one common POR family/provenance/output-role/flag envelope. The shared contract standardizes custody and observability, not one universal numerical clamp. A method never borrows another method's bound, and an uncited bound remains absent | Jauhar selected option 2, 2026-08-12 | SB-POR-001 through 003 migration |
| DEC-016 | Required POR capability set | DECIDED | Chart-free analytic N-D, the source-gated hydrocarbon-response chain, excavation correction and neutron-sonic all belong in the product. This settles inclusion, not a missing source, parameter, implementation sequence or an otherwise deferred pilot-timing row | Jauhar direction, 2026-08-11; each chapter source gate still applies | SB-POR-021, SB-POR-027 and SB-POR-029 through 042 |
| DEC-017 | Genuine sonic RHG product path | DECIDED | Implement the original three-segment Raymer-Hunt-Gardner 1980 transform as SandiBumi's genuine RHG product path; do not preserve the current approximation under an RHG label merely by renaming around it. The public identity must name RHG80 and its primary source. The current `0.625*(DT-DT_MA)/DT` branch may survive only as a separately named `FIELD_OBSERVED` method with a cited coefficient and one correct shale convention. Exact equation typography must be reverified against the original scan before production code because `docs/method_sonic_porosity.md` explicitly is not implementation authority. This closes SP-013's rename-versus-RHG80 product choice but does not silently choose the separate IP, Geolog or Techlog vendor rendering requested by SB-POR-020 | Jauhar chose “RHG rendering” from the previously stated rename-versus-RHG80 decision, 2026-08-12; the original-paper verification gate remains binding | SB-POR-013 through 020 implementation; original-scan verification; separate SB-POR-020 vendor-rendering decision |
| DEC-018 | Exact first-pilot capability manifest | DECIDED | APPROVED: the exact 242 requirement IDs in `docs/takeover/pilot-scope.json`, SHA-256 `0412de0cc43fabbe0c5e32d4c831d65e90536ee1c348802ab67cb0f3dcd70b6b`, are the first paid Windows offline open-hole petrophysics blocker program; the other 689 requirements are `DEFERRED`, not deleted, and no product-inclusion decision, scientific value, equation or evidence claim changes. Any older row-level “if included” wording outside the manifest now applies only to post-pilot reconsideration; any ID, disposition or hash drift invalidates approval | Jauhar approval, 2026-08-12 | none |
| DEC-019 | Exact Gate 1 SB-GEO adjudication exception | DECIDED | APPROVED: only the exact 52 DEC-011-deferred SB-GEO rows identified by SHA-256 `e1eed8f713f5449926b7e5c840d06d9c061eef8c7f420a868434dc57c2978ffc` may remain visibly UNADJUDICATED for Gate 1; all remain mandatory next-version work | Jauhar approval, 2026-08-12; any row or hash drift invalidates the approval | none |
| DEC-020 | Complete computed-curve custody | DECIDED | The session operator is explicit and classified HUMAN or AUTOMATED, never inferred from the Windows account. Every run requires a non-empty source/reference note for its explicit values; a narrower stored source remains attached when one exists. No production computed-curve write or undo may fall back to ancestry-free storage. The record is per well, visible on demand, preserved with current/archive versions and Save As, and embedded in every number-carrying deliverable. This decides the SB-CORE-010 minimum only; DEC-009 still governs additional lineage beyond it. | Jauhar direction, 2026-08-13 | none |
| DEC-021 | Build-derived module identity source | RULED-2026-08-17 | **RULED: a per-module SOURCE DIGEST, computed at build time.** Each module carries a digest of its own source text, so editing one module changes only that module's identity and a re-run of an untouched module still matches. That REPLACES the hand-maintained `CARGO_PKG_VERSION`, which SB-DBM-002 explicitly rejects because it does not move when a module's arithmetic does - two curves computed by different code could claim the same producer. | **What implementation still has to fix, and none of it is a petrophysical choice:** the artefact boundary (which source spans one module - a file, or a named span within one, since several modules share `modules.rs`), the digest algorithm and its encoding, and the cross-machine stability rule so the same source gives the same digest on Jauhar's machine and a build server - which means hashing normalised source bytes rather than anything path- or timestamp-dependent. **Accepted cost, recorded rather than buried:** the digest also changes on a comment or formatting edit. That is noise, never a lie - it over-reports change and never under-reports it, which is the right direction for a provenance field. | SB-DBM-002; the module-version arms of SB-DBM-004 and SB-DBM-015 |
| DEC-022 | Legacy provenance timestamp migration | RULED-2026-08-17 | **RULED: the legacy timestamps are WIB (UTC+7) and are converted to UTC.** Jauhar states that every pre-migration `log_sets.created_at` value was written on machines set to Western Indonesia time, so the zone is recoverable rather than lost. That REPLACES the engineering recommendation below (`ZONE_UNKNOWN` for every legacy value), which was the correct default only while the zone was unknown - it is now known, and declaring it keeps legacy and new timestamps comparable instead of permanently splitting the audit trail at the migration boundary. New values are stored as unambiguous UTC instants and converted only at display, which was never in question. *What was originally asked, retained for the record:* pre-migration values are unqualified local timestamps with no recorded authoring zone; guessing a zone would invent history, while retaining old local and new UTC meanings in one field would preserve the SB-DBM-041 ambiguity. | **The risk this ruling accepts, recorded rather than buried:** the conversion is only correct if EVERY legacy record was written on a UTC+7 machine. One written elsewhere lands a fixed number of hours off with nothing in the data to catch it, because a timestamp carries no evidence of the clock that made it. Implementation must therefore record the declared zone and this ruling as the converted values' SOURCE, so a later reader sees the offset was declared by the product owner rather than measured. | SB-DBM-009, SB-DBM-011, SB-DBM-015, SB-DBM-041 and the timestamp arm of SB-DBM-T11 |
| DEC-023 | Structured-audit zone-set dependency versus immutable pilot scope | RULED-2026-08-17 | **RULED: authorize the narrow seam.** The zone-set identity and version the structured audit needs are built now, while SB-DBM-008's fuller contract stays DEFERRED and outside the manifest. Same shape and same reasoning as DEC-025: build the field the approved rows actually need rather than revise the manifest hash for it. Why it matters at all - rename or move a top and the same run over the same well means something different, so an audit entry that cannot name its zone set cannot be replayed or defended. | **Constraints that survive the authorization.** The seam is the identity and version ONLY; it is not licence to implement SB-DBM-008's wider contract early or to treat it as approved. When 008 is scheduled it inherits what is here rather than designing freely, and that constraint is the accepted cost of the narrow route - recorded so 008's author is not surprised by it. DEC-020 already settles HUMAN/AUTOMATED operator identity and is untouched by this. | SB-DBM-011, SB-DBM-015, SB-DBM-041 and the zone-set arm of SB-DBM-T11 |
| DEC-024 | Complete re-run manifest identity seams versus immutable pilot scope | RULED-2026-08-17 | **RULED: authorize both remaining seams.** The stochastic-draw identity and the fitted-model identity are built as SB-DBM-015 infrastructure, while SB-DBM-014, 019 and 020 stay DEFERRED and outside the manifest. With DEC-021 (build identity) and DEC-023 (zone set) already settled, all four arms of the re-run manifest now have an owner and SB-DBM-015 can be completed rather than shipped with a hole. The ML half is closer to USING what exists than inventing: `ml_models` already stores a model id, the algorithm, the ORDERED feature curves, the training wells and the scikit-learn version that wrote the artifact, and a refit deliberately makes a NEW model rather than replacing one. | **Constraints that survive the authorization.** Identity fields ONLY - this is not licence to implement 014/019/020's wider contracts early or to treat them as approved, and each inherits what is built here when it is scheduled. The stochastic identity must capture what actually determines a draw (seed, sampling scheme, correlations, iteration count) rather than a run label, or it identifies nothing. Nothing here may weaken the existing model contracts: the scaler still travels with the estimator, and feature ORDER stays part of the artifact's own record. | SB-DBM-015 and the stochastic/learned-model arms of SB-DBM-T15 |
| DEC-025 | Neutron matrix-scale metadata custody | RULED-2026-08-17 | **RULED: authorize the narrow metadata seam.** A cited neutron-matrix-basis field is added to the curve metadata as a scoped edit, the same shape as the DEC-045 `db.rs` authorization rather than a manifest revision. The physics is why it cannot wait: a limestone-unit neutron read against a sandstone matrix is about 0.04 v/v low in clean water sand, and nothing today records the basis, so nothing can refuse a wrong one. | **Constraints that survive the authorization.** ABSENT stays absent: the basis is never inferred from the contractor, the tool, the formation-water salinity or a matrix default, and a module that needs it and does not have it REFUSES by name rather than assuming. The seam is for this field only and is not licence to widen curve metadata generally. Import must be able to state the basis without guessing it, and a converted value records the declared basis and this ruling as its source. | SB-POR-024, SB-DBM-017 |
| DEC-026 | PHIE floor source precedence for the pilot registry | SUPERSEDED-BY-DEC-043 | **Not a live decision. DEC-043 already answered it on 2026-08-16.** DEC-026 asks which contract has precedence for the PHIE floor - `CLAUDE.md`'s `0.001` or `11_porosity.md` SB-POR-045's requirement that it ship ABSENT. DEC-043 ruled **0.001**, on the stated grounds that *the later direct product record supersedes the chapter's unresolved position*, and DEC-047 fixed the shape: a cited parameter DEFAULTING to 0.001 with the ruling as its source. That is the same question and the same answer, so this row is closed by reference rather than re-asked. | Nothing further from Jauhar. SB-DBM-025 registers `0.001` with DEC-043 as its cited source, and the deviation from the chapter's literal MUST is already logged at DEC-047 rather than buried. Flagged to Jauhar 2026-08-17 in case he reads the two questions as distinct. | SB-DBM-025, and by reference SB-POR-045 |
| DEC-027 | Required-unset storage and Geolog null-bound authority | RULED-2026-08-17 | **RULED: absence of row.** A declared-but-unsupplied required parameter stores NO `run_parameters` row. SB-DBM-030 wins over SB-DBM-003's queryable `state = REQUIRED_UNSET` row, and SB-DBM-003 is corrected to match rather than both being carried. | **Where the naming requirement lands, because it does not disappear.** SB-DBM-030's own test still requires the parameter to stay NAMED through IPC, the UI and export. Under this ruling the NAME comes from the module manifest, which already declares every required argument, and *never supplied* becomes **declared-and-required in the manifest with no stored row** - derivable rather than stored. That makes the answer depend on knowing WHICH manifest version ran, which is exactly what DEC-021's per-module source digest now pins; the two rulings have to be implemented as a pair. **Accepted cost:** absence of a row cannot by itself separate *never supplied* from *never declared* - only the manifest join can - so the join is the contract, not an optimisation. **Still open and NOT settled here:** the separate Geolog null-bound magnitude question in this row, which section 5 marks non-adoptable; any bound must be a cited production magnitude or a clearly test-only constant. | SB-DBM-030, SB-DBM-003, SB-DBM-T29 and SB-DBM-T30 |
| DEC-028 | Legacy single-handle parameter-pack policy | RULED-2026-08-17 | **RULED: refuse BOTH one-handle forms.** The existing P0 refusal in SB-INS-015 / SB-INS-T18 stands, and pilot-approved SB-DBM-032 with its test SB-DBM-T32 is CORRECTED to match rather than both being carried. Ordinal-only binding is the documented silent-wrongness case: a pack that binds by position attaches parameters to the wrong names the moment the order shifts, and nothing in the result shows it. Warning-and-continue would have weakened a P0 guard that already exists and already passes, and a warning emitted during a batch of hundreds of wells is a line nobody reads. | **Implementation must correct, not delete.** SB-DBM-032 and T32 are rewritten to expect the refusal, with this ruling cited as why the expectation changed - the test is not marked ignored and the existing guard is not touched. The refusal must NAME what is missing and which file it came from, so a user with an older pack can act on it rather than guessing; a refusal an analyst cannot act on gets worked around instead of obeyed. **Accepted cost, stated plainly:** any genuinely one-handle legacy pack Jauhar still holds will stop loading and must be re-exported or hand-corrected. | SB-DBM-032, SB-DBM-T32, SB-INS-015 and SB-INS-T18 |
| DEC-029 | Structural-index file proof versus the immutable pilot import surface | RULED-2026-08-17 | **RULED: keep the rule, move the proof.** SB-DIO-010's requirement stands unchanged - the depth index is whichever column the FILE declares, never an assumed first column - and it is proven on the two formats DEC-003 fixed as the pilot import surface: LAS 2.0 and delimited text. The Geolog flat-ASCII arm is dropped from the PILOT, not refused as unsupported forever; it is a later-gate format. *What was originally asked, retained for the record:* pilot-approved SB-DIO-010 and exact T15 required importing a Geolog flat-ASCII file whose non-first `REFERENCE` declaration becomes the index, while no production reader or command consumes the cited `.flat_ascii_format` declarations and the current test supplies headers and classes directly to `resolve_index_column`. | **Implementation must move the proof, not weaken the rule.** T15 is rewritten to drive the resolver from a real parsed file on each pilot format, asserting that a declared non-first index is honoured AND that a first-column assumption would fail it - both sides, because a test that only checks the declared column passes for an implementation that ignores the declaration whenever it happens to agree. The Geolog arm is removed with this ruling cited as why the expectation changed. No test-only reader is added, no format capability is claimed that the product does not ship, and `resolve_index_column`'s existing behaviour is not touched. | SB-DIO-010 and SB-DIO-T15 |
| DEC-030 | Exact-mnemonic requests versus semantic-family requests | RULED-2026-08-17 | **RULED: approve the typed split.** A request DECLARES which it is. `EXACT_MNEMONIC` returns only that mnemonic and reports absence rather than substituting; `SEMANTIC_FAMILY` may choose a family member but returns that member's concrete mnemonic, its set/curve identity and the rule that selected it. Every current caller is classified explicitly, one way or the other. *What was originally asked, retained for the record:* SB-DIO-031 forbids supplying another curve's bytes under a requested name, while the untyped-string resolver matches mnemonic-or-family and existing workflows intentionally depend on that fallback - plotting already calls its input a semantic request and returns the concrete identity, whereas equation and workflow readers key returned values under the ORIGINAL request, so one string cannot state whether fallback is forbidden or intended. | **Four implementation constraints, all consequences of the ruling rather than further decisions.** (1) **The request type is a REQUIRED argument, with no default** - a default is how a caller gets classified by omission, which is the conflation this ruling removes; the compiler must refuse an unclassified call rather than picking for it. (2) **Set RAW's absolute priority is untouched** in both arms; the split says which NAMES may answer a request, never which SET wins, and CLAUDE.md rule 10a's byte-identical guarantee for pre-set-era projects still holds. (3) **A `SEMANTIC_FAMILY` result is keyed under its CONCRETE mnemonic, not the request** - returning the real identity while still filing the bytes under the requested name would leave the substitution unrecorded at exactly the reader that consumes it. (4) **Every classification is recorded as a deliberate choice**, and SB-DIO-T47 inventories all resolver surfaces so a caller cannot be missed silently. Existing family workflows keep working; none is broken to satisfy the exact arm. | SB-DIO-031, SB-DIO-T47 and the shared request-type dependency recorded for SB-DIO-034 |
| DEC-031 | Four-state correction channel encoding and per-sample step custody | RULED-2026-08-17 | **RULED, in three parts.** (a) **ONE CODED STATE CURVE**, declared a categorical type and explicitly NOT a binary flag: stable wire codes `0 = full`, `1 = partial`, `2 = not applied`, `3 = refused`, with a decode table shipped beside it. SB-ENV-030's binary `1 = true` contract is untouched and this curve is simply not one of its curves. (b) **The applied-step manifest lives in the LOG-SET ARCHIVE**, riding with the versioned interpretation output it describes; per `20_envcorr-qc.md:2538-2541` that one answer also settles `SB-ENV-028`'s mask record and `SB-ENV-042`'s interactive-edit provenance. (c) **PARTIAL CORRECTION IS AUTHORIZED**: the correction runs over the intervals the caliper actually covers, the uncovered gap is marked `2 = not applied`, and `SB-ENV-006`'s refusal survives ONLY for an input with no finite caliper anywhere. *What was originally asked, retained for the record:* SB-ENV-007 requires one per-sample channel distinguishing four states with the applied step set at partial samples, while SB-ENV-030 defines only binary polarity, `ModuleOutputs` carries only `Vec<f32>`, declared outputs have no flag-type metadata, and OI-4 left the persistence owner open. | **Five constraints, all consequences of the ruling rather than new decisions.** (1) **The state curve is REFUSED as a `MASK` input, by name and with the reason stated.** CLAUDE.md rule 11's mask treats any non-zero sample as flagged; under these codes that reads `2 = not applied` as flagged and `0 = full` as clean, which inverts the meaning of the best state. A categorical curve must not enter that machinery at all, and the refusal is the guard. (2) **Absence stays `f32::NAN`** - a depth with no sample carries NaN, never a code, or "no data here" silently becomes `0 = fully corrected`. (3) **The state curve is emitted on EVERY run, never optionally**, because part (c) lets a raw value pass through under a corrected name and the state channel is the only thing that records it; a corrected curve presented without its state channel is the silent substitution this ruling exists to prevent. (4) **Part (c) is pinned from BOTH sides** - the correction runs over a covered interval AND still refuses an all-uncovered input - because a test that only proves the partial run passes for an implementation that has stopped refusing anything. SB-ENV-006's guard is NARROWED to its stated case, never deleted, and SB-ENV-T13 cites this ruling as why the expectation changed. (5) **Codes are stable and versioned with the decode table**; a renumbering is a migration, not an edit. | SB-ENV-007, SB-ENV-T13, SB-ENV-005/OI-4, SB-ENV-006, SB-ENV-028, SB-ENV-042, the SB-ENV-030 type contract, and DEC-032 via OI-7 |
| DEC-032 | Bad-hole reason encoding and signed DRHO cause custody | RULED-2026-08-17 | **RULED: one coded `BADHOLE_REASON` curve, with the DRHO sign retained in EVERY combination.** The representation follows DEC-031 by OI-7's own instruction (`20_envcorr-qc.md:2554-2556` ties this choice to SB-ENV-007's), so it is a categorical curve and explicitly not one of SB-ENV-030's binary flag curves. The table is `0 = not flagged`, `1 = caliper`, `2 = DRHO positive`, `3 = DRHO negative`, `4 = caliper + DRHO positive`, `5 = caliper + DRHO negative`, `6 = neither evaluable`, and `NaN` where there is no sample at all. **The two combined codes are the ruling's substance**: a five-code table with one undifferentiated "both" discards the DRHO sign on exactly the samples where two criteria fired, which is where a reader most needs it, and SB-ENV-023's requirement to retain the sign is not met by retaining it only when DRHO fires alone. *What was originally asked, retained for the record:* SB-ENV-022 requires a companion reason channel for caliper, DRHO, both and neither-evaluable; SB-ENV-023 requires the DRHO cause to retain its sign; SB-ENV-030 defines only binary polarity, and the two SB-ENV-021 availability channels cannot say which criterion fired. | **Four constraints, inherited from DEC-031 and not restated as new decisions.** (1) **Refused as a `MASK` input by name**, for DEC-031's reason: rule 11's mask reads any non-zero sample as flagged, and here `0` means NOT flagged while `6 = neither evaluable` would read as a bad-hole flag - the exact inversion. Use `SB-ENV-021`'s binary channel for masking; this curve is the reason, never the trigger. (2) **`NaN` for absence, never a code** - otherwise a depth with no data reads as `0 = not flagged`, asserting a clean hole where nothing was measured. (3) **Causation is never inferred from availability**: a code is written only where the criterion actually fired, and a sample with both criteria unavailable is `6`, not `0`. (4) **The sign is the DRHO correction's own sign**, carried through rather than re-derived at the reason channel, so a change to the DRHO convention cannot leave the two disagreeing. Codes are stable and versioned with the decode table. | SB-ENV-022, SB-ENV-023, SB-ENV-T31, OI-7 and the SB-ENV-030 type contract |
| DEC-033 | Mask repair-exemption declaration and first repair inventory | RULED-2026-08-17 | **RULED: a CONDITIONAL PER-OUTPUT declaration, and the first approved inventory contains exactly one entry** - `log_predict.SYN` when `OPT_COMBINE = MAX_RAW`, the one mode that is genuinely a washout repair. `FILL_MISSING` preserves any finite raw value and only reconstructs an actually missing target; `SYNTHETIC` is a pure model output that OI-5 itself says arguably is not a repair. **Both remain normally masked until separately enumerated and approved.** Module-wide and `Prep`-category exemptions are rejected by name: the first would exempt all three modes at once and would silently extend to any output `log_predict` gains later, the second would bypass the safety mask for unrelated conditioning modules. *What was originally asked, retained for the record:* OI-5 leaves module-level, category-level and per-output declarations non-equivalent; the runner has no typed reconstructed-sample marker; and an unconditional output flag would still conflate the three modes. | **Four constraints.** (1) **The declaration is per output AND per mode**, so the exemption cannot be satisfied by matching the output name alone - a `SYN` produced under `SYNTHETIC` is masked normally, and that must be pinned from BOTH sides, since a test proving only the MAX_RAW bypass passes for an implementation that exempts every mode. (2) **Both mask passes are bypassed for a declared repair**, not one, or a repair blanked at the second pass is a repair that did not happen. (3) **A typed binary companion marks ONLY finite values produced at masked depths** - it is the disclosure that makes the exemption honest, and it says "this number was reconstructed, not measured"; it is not the same channel as any replaced-sample flag and must not be reused as one. (4) **Adding an entry to the inventory is a decision, never an implementation convenience**: a new module or output does not inherit an exemption, and enumerating one for `FILL_MISSING` or `SYNTHETIC` returns here rather than being settled in code. | SB-ENV-027, SB-ENV-T36, SB-ENV-T37 and OI-5 |
| DEC-034 | Four-sample Hampel fallback versus existing refusal | RULED-2026-08-17 | **RULED: the under-five refusal STANDS, and T42's two situations are separated.** An undersized window returns a structured, named refusal - the shipped `MIN_HAMPEL_SAMPLES` guard and its existing regression test are untouched. A **zero-MAD window is a different situation and DOES run**: all samples identical means there is no scale to judge against, not too little evidence to judge with, so the declared fallback applies and its use is reported per sample on a separate typed binary diagnostic. SB-ENV-T42 is CORRECTED to expect that split, citing this ruling as why the expectation changed. **The reason the floor holds:** a Hampel filter judges a sample against the median and MAD of its own window, so at four samples the spike contributes a quarter of the scale used to condemn it - the estimator is least reliable exactly where spikes are most likely. *What was originally asked, retained for the record:* exact T42 required a four-sample window to run and report declared-fallback use, while a refused `Result` carries no `ModuleOutputs` flag channel at all and reusing `OUT_FLAG` would conflate replaced samples with fallback use. | **Three constraints.** (1) **The fallback-used diagnostic is its own typed channel** and is never `OUT_FLAG` - a sample whose value was replaced and a sample judged on a fallback scale are different statements, and one channel cannot carry both. (2) **A refusal has no per-sample channel and none is claimed** - the undersized case returns a named structured refusal, not a run that reports itself as degraded, because a `Result` error carries no `ModuleOutputs`. (3) **Pinned from BOTH sides**: the four-sample window refuses AND the zero-MAD window runs with the diagnostic set, since a test proving only the refusal passes for an implementation that refuses both, and one proving only the fallback passes for an implementation that has lost the floor. **Named cost, recorded rather than softened:** despike still will not run where a window cannot reach five samples - a very coarsely sampled log, or the ends of a curve. | SB-ENV-033 and SB-ENV-T42 |
| DEC-035 | Exact recovery record versus absent culling and immutable pilot scope | RULED-2026-08-17 | **RULED: the recovery contract covers the operations the pilot actually SHIPS, and cull's recovery ships with cull.** SB-ENV-037 is re-adjudicated to name despike, clip and gap fill; SB-ENV-T45's cull arm is REMOVED with this ruling cited as why the expectation changed, and the requirement to restore a cull bit-exactly is written into the deferred SB-ENV-036 so it cannot be lost. **DEC-018's 242-row manifest and its hash are untouched** - the immutable pilot scope stays immutable, and nothing is built speculatively for a capability that does not exist. **Storage is not re-decided here**: DEC-031(b) put the ENV provenance family in the LOG-SET ARCHIVE, and this record belongs to that family, including the shared interactive-provenance arm owned by SB-ENV-042. *What was originally asked, retained for the record:* pilot-approved SB-ENV-037 and exact T45 required all four operations to restore bit-identically from each operation's emitted record, while the owning cull capability SB-ENV-036 is ABSENT, DEFERRED and outside the immutable manifest, and current batch Condition outputs may identify changed samples without emitting their original values. | **Three constraints.** (1) **"Bit-identical" is read literally: the record stores the original 32 bits and restores the original 32 bits.** That is what makes missing-value custody derivable rather than a further decision - a missing value is `f32::NAN`, NaN has many bit patterns, and only exact-bit custody restores one without electing a canonical NaN that the original may not have had. (2) **A record that identifies WHICH samples changed is not a recovery record** - the original VALUE must be emitted, or restoration is a re-run rather than a restoration, and the current batch Condition outputs do not meet the contract as they stand. (3) **Pinned from BOTH sides**: restoration returns the exact original bits AND a record missing its original values is refused rather than silently treated as recoverable. **Named limit:** cull recovery is deferred WITH cull, so a future SB-ENV-036 arrives owing a record whose shape differs from its three siblings - despike, clip and gap fill change a value at a depth that still exists, while a cull removes the sample and must restore its DEPTH as well as its value. | SB-ENV-037, SB-ENV-T45, SB-ENV-036 and the shared interactive-provenance arm owned by SB-ENV-042 |
| DEC-036 | CLY categorical provenance custody versus immutable pilot scope | RULED-2026-08-17 | **RULED: authorize a NARROW, VERSIONED CLY provenance registry as infrastructure for the already-approved CLY rows, with DEC-018's 242-row manifest untouched.** SB-CLY-031 and SB-CLY-032 remain DEFERRED and later own the full closed vocabulary as a versioned EXTENSION of this registry, not a replacement for it. **Registry v1 carries exactly six things and no more:** method identity, missing input, masked/disabled input, `ENDPOINT_INVALID`, `COAL`, and substitution - the last as its own separate field, independent of the provenance token, because "which method ran and why it could not" and "what was substituted in its place" are different statements. Unlike DEC-035's deferred cull, which nothing approved depended on, these deferred rows own infrastructure that pilot-approved SB-CLY-001 and SB-CLY-055 cannot be built without, so deferring wholesale would leave approved rows unimplementable. *What was originally asked, retained for the record:* SB-CLY-001/T01 require a persisted and exportable per-sample `ENDPOINT_INVALID` token distinct from missing input plus a zone-bearing run message; SB-CLY-055/T35 require the typed tokens to survive LAS export and re-import; the approved CLAY_LINEAR_GR group excludes SB-CLY-031 and SB-CLY-032; and the current generic precondition companion is a binary condition-agnostic flag with no zone identity or token table. | **Five constraints.** (1) **The registry is VERSIONED from v1**, because the vocabulary is now decided twice by construction - a later extension by SB-CLY-031/032 must be a migration with a version bump, never a silent re-use or renumbering of an existing code. (2) **It follows the house categorical pattern set by DEC-031**: a coded categorical curve, explicitly not one of the binary `1 = true` flag curves, `NaN` where there is no sample rather than a code, and **refused as a `MASK` input by name** - rule 11's mask reads any non-zero sample as flagged, which would read a provenance token as a bad-hole flag. (3) **An unknown code on re-import REFUSES, naming the code and the registry version it could not resolve** - a token whose meaning is not in the reader's table is not a token, and silently passing it through would let a code from a later vocabulary be read as whatever this version happens to assign. (4) **The zone-bearing run message is part of the contract, not the curve** - SB-CLY-001 requires the zone named in the message, and a per-sample token does not discharge that. (5) **Substitution independence is pinned from BOTH sides**: a substituted sample carries its substitution field AND its provenance token, and neither is derivable from the other. | SB-CLY-001, SB-CLY-055, SB-CLY-T01/T24/T32/T35 and dependencies SB-CLY-031/SB-CLY-032/SB-CLY-046 |
| DEC-037 | CLY undeclared `-999` screening versus DIO `NoNull` precedence | RULED-2026-08-17 | **RULED: quarantine and ASK - no undeclared sentinel is ever converted without a human saying so.** An undeclared value matching the sentinel pattern is detected, the import BLOCKS, and the question names the value, every affected curve and the sample count; on confirmation those cells become `f32::NAN` and the rule that matched is recorded in provenance. **An explicit `NoNull` declaration always WINS and is never offered for conversion**, so SB-DIO-003/T04's preserved sentinel-shaped amplitude is untouched and SB-DIO-005/T09's per-channel convention is unweakened. `-999` is NOT added to any global null list. **Why not automatic screening:** it would require an exact enumerated mnemonic set, that set cannot be derived from the data, and inventing one is precisely the unsourced parameter this program forbids - while a mnemonic missing from it would silently import sentinels as measurements. *What was originally asked, retained for the record:* exact SB-CLY-T44 requires a LAS value equal to the known IP bad-hole sentinel `-999` to become absent and raise a warning even without `~W NULL`, current LAS readers cannot prove writer identity from the number alone, and the CLY chapter does not enumerate the mnemonic or signature scope or its precedence over an explicit `NoNull`. | **Four constraints.** (1) **A blocking import question is correct here and matches the house pattern** - CLAUDE.md keeps import wizards modal precisely because they are filled once and dismissed; this is not a tool pane. (2) **Both answers are recorded, not just the conversion.** Choosing to keep the values as measured is itself a finding about the delivery and must appear in provenance and the process history, or a later reader cannot tell the question was ever asked. (3) **Precedence is pinned from BOTH sides**: an undeclared match is offered AND a `NoNull`-declared channel carrying the same value is NOT offered, since a test proving only the offer passes for an implementation that ignores `NoNull` entirely. (4) **The detector reports, it never decides** - it identifies candidate cells and the rule that matched them; conversion happens only on confirmation, and no code path may convert on magnitude alone. **Note:** SB-CLY-T35's provenance-export arm is separately governed by DEC-036, now ruled. | SB-CLY-034, SB-CLY-055, SB-CLY-T35/T44, SB-DIO-003/T04/T05 and SB-DIO-005/T09 |
| DEC-038 | SSC/SSPW scope and the semantic boundary of an unlimited porosity pair | RULED-2026-08-17 | **RULED: SSC and SSPW are separately typed WORKFLOWS, not single-equation porosity methods, and sit OUTSIDE SB-POR-002's twin requirement.** They keep emitting exactly what they emit today. **`ssc.rs` is not edited** - the narrow authorization DEC-040 holds for it remains unused by this row - and the protected formulas are not duplicated anywhere, so there is never a second scientific implementation of them. **Why the twin requirement does not fit:** the pair is a DIAGNOSTIC - a negative unlimited PHIE says the parameters are wrong where a clamped `0.001` hides it - and that reading depends on "unlimited" meaning the bare expression. SSC and SSPW clamp at several stages before the final porosity (component volumes, projections, fractions), so a companion built by bypassing only the last clamp would be labelled unlimited while still bound upstream, which is a worse statement than no companion at all. *What was originally asked, retained for the record:* SB-POR-002 requires unlimited and limited PHIT/PHIE pairs for every porosity method; the common POR registry classified SSC and SSPW as deterministic methods; both discard pre-limit values inside protected `ssc.rs`; and the choice was between a final-clamp-only reading, a full pre-limit lineage, and a separately typed workflow role. | **Three constraints, and one correction to this row's own text.** (1) **The re-typing is a registry change, not a capability change** - SSC and SSPW keep their present outputs, names and numbers, and no existing behaviour moves; a test must pin that the outputs are unchanged, or a re-typing that quietly altered them would pass. (2) **Pinned from BOTH sides**: a registered METHOD is refused unless it declares its unlimited/limited pair, AND a registered WORKFLOW is not required to - so the workflow role cannot become a way for a genuine method to skip the requirement. (3) **The full pre-limit lineage is not abandoned, it is unsourced** - if a documented lineage of every clamp in the SSC/SSPW chain is ever produced, re-typing them back as methods returns here rather than being settled in code. **Correction:** this row's original text records SB-POR-004 collision-safe custody as incomplete; it landed earlier in this program at commit `12d03b8b`, so that dependency is discharged. | SB-POR-002, SB-POR-T11/T19/T31/T39 and SB-POR-004 |
| DEC-039 | Singular POR branch-and-limit stream representation | RULED-2026-08-16 | **RULED: record the branch-and-limit state as a COMMENT ON THE CURVE, carried per CURVE VERSION** — each curve already has versions, and each version gets its own comment reflecting what the user did on that run. Same mechanism as the SB-POR-026 ruling, so the two share one answer. This REPLACES the engineering recommendation below (a categorical class curve backed by a closed registry) and dissolves its hardest parts: no vocabulary to enumerate, no combination rule when several limits bind at one sample — the text states all of them — no unknown/fractional-code refusal to design, and no categorical-export contract through reframe. The per-version record already exists: `log_sets` is one row per run holding set_id, well_id, set_name, version, module, params_json, inputs_json, created_at, frame. *What was originally asked, retained for the record:* SB-POR-003 requires one per-sample stream identifying both the producing branch and every binding limit. Current module flags are typed binary `NaN/0/1`, which cannot carry those identities; existing categorical method curves use stable integer-valued `f32` classes, but no POR vocabulary, combination rule, token metadata, unknown-code refusal or export representation exists. The chapter cites IP 6/7/9/16 and Geolog `MTH_PHI = SHALE`, but those examples are not a complete cross-method registry. Several limits can bind one sample, and SSC/SSPW sites are protected by DEC-038's unresolved scope. | ONE narrow authorization remains: `log_sets` has no free-text comment column and adding one means editing `src-tauri/src/db.rs`, a prohibited file — authorize that single edit as the narrow `ssc.rs` edit was authorized. `params_json` must NOT be reused: it is the run's PARAMETERS, and mixing a narrative into it makes the two indistinguishable to every reader. DEC-038 remains separately open. | SB-POR-003, SB-POR-T12/T38/T39/T41, SB-POR-047/048 and DEC-038 |
| DEC-040 | Narrow `ssc.rs` edit for the one shared clay-bound-water porosity | RULED-2026-08-16 | **RULED: authorized.** SB-POR-008 needed `sspw` to compute clay-bound-water porosity against FORMATION WATER density rather than the invaded-zone fluid; the two agree at the 1.00 defaults and diverge on salt water (1.10), so the bug was invisible in every existing test. Scope was narrowed after investigation: only `sspw` was rerouted through the shared helper. `ssc`'s own term is a silt-geometry fraction along a fluid-anchored line, so its arithmetic was left untouched and only its local was renamed. | Settled. Recorded here so the protected-file edit is auditable rather than implicit. | SB-POR-008 |
| DEC-041 | Matrix density selection across chained porosity modules | RULED-2026-08-16 | **RULED: 2.65.** Matrix density was split 2.645/2.65 across modules the docs instruct chaining, so a chained run silently mixed two matrix densities. Jauhar selected 2.65; all three sites now agree and the position not taken is still disclosed through the competing-values topic. | Settled. Field check outstanding: the change moves porosity by roughly 0.3 p.u. | SB-POR-011 |
| DEC-042 | Pay eligibility of the neutron-density quick-look shortcuts | RULED-2026-08-16 | **RULED: ruling (b).** The arithmetic and RMS neutron-density shortcuts stay labelled quick-look comparisons and are NOT a crossplot porosity method, but their limited curve IS a pay candidate. | Settled in code. **`PILOT_SCOPE.md` item 6 still says the opposite** — *excluded from pay by default* — and is hash-bound to DEC-018, so it needs Jauhar's own edit plus a note that (b) superseded it. | SB-POR-023, DEC-018 |
| DEC-043 | Value `PHIE` is set to when the floor binds | RULED-2026-08-16 | **RULED: 0.001**, i.e. the later direct product record supersedes the chapter's unresolved position. IP's own manual gives 0.001 and 0.0001 for the same quantity in three places (F17), which is why the chapter withheld a default. | Adjudication settled. One clause still needs confirming: `11_porosity.md:1052-1053` states TWO requirements — no default AND documented user decision. The ruling satisfies the second. Working assumption is **(A)** a cited parameter DEFAULTING to 0.001 with the ruling as its source, matching DEC-041 and DEC-042; the alternative **(B)** is a `param_open` that refuses unsupplied. Recorded as a deviation from a literal MUST rather than buried. | SB-POR-045 |
| DEC-044 | Where a porosity run's conditioning record is written | RULED-2026-08-16 | **RULED: a direct comment on the curve's own description**, not a new flag curve and not a flag-shaped provenance key — the text an analyst reads in the curve catalogue and the exported LAS header. Consumes `condflag`'s flag rather than recomputing it, because `condflag`'s rule already excludes coal and washout and a local re-derivation would call coal gas. | Same mechanism as DEC-039, so the two share one implementation. Blocked on the same narrow `db.rs` authorization: neither `computed_curves` nor `curve_meta` has a free-text description column, and the manifest's `log_out` description is per-MODULE, identical every run. | SB-POR-026 |
| DEC-045 | Narrow `db.rs` edit for the per-version curve comment | RULED-2026-08-16 | **RULED: explicitly authorized.** DEC-039 and DEC-044 both write a comment carried per curve version, and `log_sets` - the per-version record - has no free-text comment column. `db.rs` is otherwise a prohibited file; this authorization is narrow and for that column only. | Settled. Constraints that survive the authorization: `params_json` must NOT be reused - it is the run's PARAMETERS, and mixing a narrative into it makes the two indistinguishable to every reader; and `computed_curves` stays deliberately PRIMARY-KEY-LESS, so nothing here may add a PK, an upsert or a duplicate-tolerant path. | SB-POR-003, SB-POR-026, SB-POR-028, SB-POR-047, SB-POR-048, DEC-039, DEC-044 |
| DEC-046 | What a porosity method does with a set conditioning flag | RULED-2026-08-16 | **RULED: MASK.** For `COAL_FLAG`, `TIGHT_FLAG` and `COND_FLAG`, a set flag masks the sample rather than selecting a different branch or merely annotating the answer. This is the per-flag policy SB-POR-048 required be *defined*; the chapter specifies the inputs but not the behaviour. | Settled. Implementation must mask to `f32::NAN` - never 0 - so a masked sample is MISSING rather than a computed zero, and must not delete any existing guard. The masking event is recorded through the DEC-039 curve comment, so a reader can see that a sample was masked and why. | SB-POR-048 |
| DEC-047 | Shape of the `PHIE` floor parameter | RULED-2026-08-16 | **RULED: reading (A).** The floor becomes a cited parameter DEFAULTING to 0.001, with Jauhar's DEC-043 adjudication as its source, matching the shape of DEC-041 (2.65) and DEC-042. It stops being the compile-time `PHIE_FLOOR` constant and becomes source-labelled run configuration. | Settled. Recorded as a deliberate DEVIATION from `11_porosity.md:1052-1053`, which states the value must ship with NO default: the chapter withheld one because the question was unresolved, and DEC-043 resolved it. The deviation is logged here rather than buried so a later reader sees the chapter was overridden knowingly. | SB-POR-045 |
| DEC-048 | Narrow authorization over the five solver and summation files | RULED-2026-08-16 | **RULED: authorized.** Jauhar authorized narrow edits to `multimin2.rs`, `lrlc.rs`, `multimin.rs`, `satheight.rs` and `montecarlo.rs`, which are otherwise prohibited. Nine Gate 2 rows were blocked on them and none needed a petrophysical decision - every equation, default, guard value and formula involved is already cited in its chapter. | Settled. The standing contracts survive the authorization and are NOT relaxed by it: `computed_curves` stays deliberately primary-key-less (no PK, no upsert, no duplicate-tolerant path); missing continuous data stays `f32::NAN` and never `Option<f32>`; and each edit stays scoped to the row that needs it rather than becoming general licence to refactor these files. | SB-SAT-002, SB-SAT-023, SB-SAT-025, SB-SAT-026, SB-SAT-027, SB-SAT-028, SB-SAT-034, SB-CUT-001, SB-CUT-002 |
| DEC-049 | Canonical spelling of the two-endpoint porosity identity | RULED-2026-08-17 | **RULED, in Jauhar's own words: "both true, its different type of porosity."** Density porosity and sonic porosity are not two spellings of one quantity - they are DIFFERENT TYPES of porosity, each with its own tool, its own matrix and fluid endpoints, and its own conventional form. `(matrix - log)/(matrix - fluid)` is the density convention and `(log - matrix)/(fluid - matrix)` is the sonic convention, and each is correct for its own type. **This REPLACES the framing the row was written under**, which treated the divergence as an unstated convention to be collapsed into one spelling; the question put to him offered exactly that choice and he rejected its premise. SB-POR-054's MUST is therefore satisfied by DECLARING each type's convention at the typed boundary, not by forcing one form on both. *What was originally asked, retained for the record:* `modules.rs:3160` writes density as `(rho_ma - r)/(rho_ma - rho_fl)` while `:3392` writes sonic as `(d - dt_ma)/(dt_fl - dt_ma)`; identical numbers, two spellings, with no stated convention. The stale `SB-POR-005`/`SB-POR-040` dependency this row carried appears in neither requirement text. | **The engineering consequence, derived rather than decided - flagged here so it can be corrected.** The real silent-wrongness case in this row is not either published form; it is the MIXED ordering - numerator from one convention, denominator from the other - which returns `-0.2121212` on the same inputs, a negative porosity from an expression that looks like both textbooks at a glance. The way to make that unwritable is a single shared two-endpoint helper taking `(log_value, matrix_endpoint, fluid_endpoint)` with one internal ordering, where **each porosity TYPE supplies its own endpoints and declares its own convention** and no caller ever writes the subtraction. That honours the ruling - the two types stay distinct, with distinct endpoints - while making a numerator-only flip impossible by construction rather than caught by review. The identity test needs no decision and is ready: both published forms give `0.2121212` at `rho_ma` 2.65 / `rho_fl` 1.0 / `rho_b` 2.3, and the mixed form giving `-0.2121212` must be caught. Pinned from BOTH sides - each declared type reproduces its own published form AND the mixed ordering is unreachable. | SB-POR-054 |
| DEC-050 | Source-cell-state custody for empty versus explicitly nulled cells | RULED-2026-08-17 | **RULED: carry the distinction, and EXPORT PRESERVES IT.** A versioned source-cell-state mask rides beside the numeric array as `bytemuck` bytes; an empty cell exports empty and an explicitly nulled cell exports as the null the file declared, so a delivery round-trips unchanged. "Nothing was delivered here" and "the contractor measured this and rejected it" are different provenance statements and both survive. *What was originally asked, retained for the record:* empty and explicitly nulled cells collapse to the same arithmetic and export state, while the project forbids `Option<f32>`, so the distinction cannot live inside the numbers. | **Five constraints.** (1) **Rule 2 is untouched and is the reason for the design**: the numeric array stays `f32` with `f32::NAN` for every absence, branch-free; the state is a SEPARATE array, never a widened element type, and no code path may branch on cell state inside the arithmetic. (2) **The mask crosses IPC as `bytemuck` bytes** per rule 3, never as JSON. (3) **It is VERSIONED from v1** - an unknown state byte refuses by name rather than being read as any current state. (4) **Where a format cannot express a state, the export says so rather than inventing one**: LAS 2.0 has no blank cell, so an EMPTY exports as the declared null there, and that lossy step is recorded rather than silent - a per-deliverable preservation, sidecar or refusal is declared for each export path. (5) **Pinned from BOTH sides**: a CSV round-trip returns an empty cell empty AND a declared null as that null, since a test proving only one passes for an implementation that writes one form everywhere. **Related:** DEC-037 governs the opposite direction - an UNDECLARED sentinel-shaped value on import - and the two must agree; a value quarantined and confirmed absent under DEC-037 is recorded as an explicit null here, because that is what the user declared it to be. | SB-DIO-007 |
| DEC-051 | Output-name collision policy across the module registry | RULED-2026-08-17 | **RULED, in Jauhar's own framing: a DEFAULT-name collision is a registry bug; a USER-TYPED collision is a supported workflow.** Every module already carries its own distinct default output names - `PHIE_DEN`, `PHIE_SON` - so the registry check is uniqueness of DECLARED DEFAULTS, made by inspecting manifests with nothing executed, which is also the only check possible for a module whose required parameters are absent. A user may deliberately type `PHIE` to REPLACE an existing calculated result, and that is a first-class workflow, not a collision to refuse. **This dissolves the row's original difficulty rather than answering it**: if every default name is unique regardless of kind, no rule has to distinguish canonical results from working aliases from categorical flags, and nothing forces unlike outputs equal. **Same-run clash, ruled separately in the same exchange: WARN, but run.** Two steps of one chain writing the same user-typed name execute with the last step winning, and the overwrite is reported in the run result and the processing history naming both steps and the name - keeping a deliberate supersede-cascade available while making a destroyed intermediate visible. *What was originally asked, retained for the record:* a no-parameter execution fixture cannot run producers with required absent parameters, and raw repeated output names conflate canonical results, working aliases and categorical flags. | **Four constraints.** (1) **The registry check is DECLARATION INSPECTION, never execution** - it runs at catalog build beside `validate_parameter_sources`, `validate_domain_defaults` and `validate_saturation_methods`, and a duplicate default fails the build rather than surfacing at run time. (2) **The uniqueness rule applies to defaults ONLY**; a user-supplied name is never checked against the registry, or the replace-an-existing-result workflow would be refused as a collision. (3) **Pinned from BOTH sides**: a duplicate DEFAULT is refused AND a user-typed name matching an existing curve is accepted, since a test proving only the refusal passes for an implementation that refuses every collision and breaks the workflow. (4) **The same-run warning is a WARNING, counted at the chain and never inferred from the outputs** - it is raised because two steps DECLARED the same target, which is knowable before execution, not by noticing afterwards that a curve changed. The existing write discipline is unchanged: `write_computed_curves_batch` still DELETEs a well's target curve names before appending, and `computed_curves` stays deliberately primary-key-less. | SB-CORE-007 |
| DEC-052 | Whole-file allocation and the reader size contract | RULED-2026-08-17 | **RULED: read big files PIECE BY PIECE rather than declaring a maximum.** LAS and delimited text are written line by line, so a reader takes a bounded chunk, parses it, drops it and takes the next - memory stays flat regardless of file size and there is no size ceiling for the formats that matter most. DLIS already runs through the `dlisio` subprocess, so its memory is not in our address space and needs no contract here. **No maximum file size is cited, because none is needed for these readers**, which is the stronger of the two routes the requirement allowed. *What was originally asked, retained for the record:* every reader currently loads the whole file into memory before parsing, the corpus matrix is not a universal reader diagnostic proof, and no sourced maximum whole-file allocation exists - so on a field machine with DuckDB already capped at 1-4 GiB a large delivery exhausts memory with no message, presenting as a freeze rather than an error. | **Four constraints.** (1) **The complete reader inventory is still published** - location, rule and affected count per reader - because the requirement asks for the inventory whichever route is taken, and a reader missing from it is a reader nobody proved bounded. (2) **`parsers::read_text_file` keeps its encoding contract intact** while it stops returning one whole `String`: bytes are still interpreted, never rejected, and DEC-053's declared order is unchanged - a chunked reader must not split a multi-byte character across a chunk boundary. (3) **Any reader NOT converted declares a cited maximum and refuses above it by name**, so the inventory has no silent entries; a reader is either proven bounded or carries a stated limit. (4) **Pinned by memory, not by success**: the test asserts that peak allocation stays bounded as input size grows, since a test that only proves a large file parses passes for the whole-file reader it replaced. | SB-DIO-061 |
| DEC-053 | The published text-encoding inventory and its selection rule | RULED-2026-08-17 | **RULED: publish exactly what ships, as a stated contract, and change nothing.** The declared inventory v1 is, in this fixed order: a BOM if the file carries one (UTF-8, UTF-16LE, UTF-16BE), then valid UTF-8, then **cp1252 as a fallback that cannot fail** because every byte maps to something in it. **No ambiguity rule is needed and none is invented, because the order is total and deterministic - there is no point at which the reader chooses between two candidates.** No additional regional code page is added; Indonesian and English deliveries are fully covered by the three above, and adding a page later is a deliberate versioned change rather than a detection improvement. *What was originally asked, retained for the record:* the chapter names plural Windows code pages but not the exact supported pages or how ambiguous bytes select one, and the requirement asked for a published page inventory with a deterministic selection rule, or an explicit ambiguity decision or refusal, retaining the existing UTF and CP1252 controls. | **Three constraints.** (1) **The fallback's inability to fail is the FEATURE and must be pinned, not merely preserved.** It is what makes bytes interpreted rather than rejected - the real Duri core table was 330 KB of ASCII with two `0x95` bullets in a comment field, and a strict decode refused all 3,045 plugs over them. A test must assert that an arbitrary byte sequence decodes rather than erroring, or a future "stricter" reader would pass a test that only checks the happy path. (2) **The BOM is believed over content sniffing**, because Excel's "Unicode text" export is UTF-16 and decoding it as cp1252 yields NUL-riddled nonsense instead of an error - a silent corruption, not a visible failure. (3) **This inventory is VERSIONED**, so a later page addition is a v2 with its own selection rule rather than an edit that quietly changes how existing files decode. **Related:** DEC-052 converts these readers to bounded chunks; a chunked reader must not split a multi-byte character across a chunk boundary, which is a constraint on that work, not a change to this contract. | SB-DIO-062 |
| DEC-054 | DLIS export scope for the first pilot | RULED-2026-08-17; SOURCE FOUND 2026-08-17 | **RULED: DLIS export IS in scope for the first pilot.** **The API RP66 V1 specification has since been located and verified reachable** - Jauhar supplied `https://energistics.org/sites/default/files/RP66/V1/Toc/main.html` the same day. It is the FULL normative document, not an overview: sections 1-7 (Introduction, Data Organization, Logical Record Syntax, Semantic Terminology and Rules, Semantics for Static and Frame Data, Transient Data, Dictionaries) plus appendices A-F, of which **Appendix B Representation Codes** and **Appendix E Checksum Algorithm** are the two a writer cannot be built without. Section 2 was spot-checked and carries the physical format normatively: the Storage Unit Label and its five fields (4 + 5 + 6 + 5 + 60 bytes), Visible Record structure, and Logical Record Segment header, attributes and trailer. **A DLIS writer is built from this source or not at all** - a reverse-engineered writer produces files that open in some readers and not others, and the failure surfaces at the client rather than here. *What was originally asked, retained for the record:* LAS semantic self-round-trip is covered, but exact SB-CORE-T15 requires an unshipped DLIS writer and the then-withheld normative RP66 writer source; SandiBumi imports DLIS through the `dlisio` subprocess and has no writer at all. | **SB-CORE-015 is no longer document-blocked; it is now a SUBSTANTIAL ENGINEERING ROW and is sized as one.** A conforming writer means the storage-unit and visible-record framing, logical record segmentation with its attribute bits and trailers, EFLR/IFLR construction, the Appendix B representation codes, and the Appendix E checksum - not an afternoon's work, and deliberately not started at the tail of a session. Four constraints when it is taken: (1) **cite section and appendix numbers in the code**, per collaboration rule 6, and **do not copy specification text into the repository** - the URL and a section reference are the citation, and bulk-quoting a standards body's document is a provenance problem `docs/IP_PROVENANCE.md` would have to carry. (2) **Nothing partial ships**: no placeholder encoder, no disabled button, no test-only stub, because a half-built writer is the shape that later gets enabled by someone who assumes it was finished. (3) `dlisio` is an IMPORTER, so the writer needs its own route; whether that is native Rust or a subprocess under rule 7 is a fresh engineering question and not settled here. (4) SB-CORE-T15's round-trip is proven on DLIS **in addition to** LAS, never instead of it - the LAS arm passes today and is not weakened. | SB-CORE-015 and SB-CORE-T15 |
| DEC-055 | Whole-index STEP comparison in the LAS writer | RULED-2026-08-17 | **RULED: EXACT EQUALITY across every interval, and refuse to declare a `STEP` when they differ.** No tolerance is invented. The writer compares all intervals rather than only the first; if any differs, the log is not evenly sampled and the writer says so, naming the depth where the spacing changes and both values. **An unevenly sampled log is a real thing and LAS has an honest way to express it** - by not claiming a uniform step - so the refusal is the correct output, not a limitation. *What was originally asked, retained for the record:* the writer checks only the first interval and the chapter supplies no whole-index `STEP` comparison tolerance; the requirement's instruction was to supply a cited tolerance or explicitly adopt exact equality, and never to insert a plausible epsilon. | **Three constraints.** (1) **No epsilon anywhere in this path**, however small and however plausible - an unsourced tolerance is exactly the invented parameter this program forbids, and it would silently declare a uniform step for a log that drifts. (2) **The refusal NAMES the depth and both spacings**, so the user can see whether it is real drift or a delivery defect; a bare "not evenly sampled" leaves them to find it. (3) **Pinned from BOTH sides**: a genuinely uniform log still writes its `STEP` AND a log that drifts after the first interval is refused - a test proving only the refusal passes for a writer that has stopped declaring `STEP` at all. If real deliveries later prove to carry harmless floating-point jitter, the answer is a CITED tolerance returning here, never a quietly added one. | SB-DIO-056 |
| DEC-056 | Legacy depth-frame typing and datum custody | RULED-2026-08-17 | **RULED, in Jauhar's words across two exchanges: the legacy default is MD, and "default not tied to any datums."** Pre-tracking depths are typed as measured depth - measured along the hole - with NO datum attached and none inferred. Nothing is back-filled: a legacy depth does not acquire KB, RT, GL or MSL by assumption, because the KB-to-GL difference is commonly 5-15 m and a wrongly declared reference offsets a whole well with nothing in the numbers to show it. He was offered the declare-one-convention route - the same shape as his DEC-022 WIB ruling - and deliberately did not take it here. *What was originally asked, retained for the record:* untyped legacy and imported depth frames have no recoverable datum or source sign, so nothing can tell whether two wells' depths are comparable. | **Four constraints, following the requirement's own unblocking text.** (1) **An explicit datum is REQUIRED at every remaining boundary** - new imports and new interfaces state it; the untied default is a statement about legacy rows, never a licence for new ones to omit it. (2) **Only source-declared rows migrate**; a row whose datum is recorded somewhere is migrated to that datum, and a row whose datum is genuinely unknown keeps its unknown meaning rather than being swept into a default. (3) **Cross-DATUM comparison is refused by name** - two rows declaring DIFFERENT datums are not comparable and the refusal says which two. Two untied MD frames are not a cross-datum case and are not refused; they are simply not a datum-aware comparison, which is what a quick-look correlation on MD has always been. (4) **The operation that genuinely cannot proceed is TVDSS**, which needs an elevation reference that an untied frame does not have; it refuses by name rather than computing from an assumed KB. Pinned from BOTH sides: an untied legacy frame stays untied AND a new boundary without a declared datum is refused. | SB-DBM-031 |
| DEC-057 | Source adjudication for SB-ENV-004's 19 shipped-uncited ENV parameters | PARTIAL-2026-08-17 | **RULED on 7 of the 19, and one reconciliation declined.** (a) **The lithology and conditioning flag thresholds are sourced to PHM** - `COAL_RHOB` 1.9, `COAL_NPHI` 0.35, `COAL_DT` 100.0, `TIGHT_PHI` 0.05, `XOVER_MIN` 0.04, `RHO_MA` 2.645 and `RHO_FL` 1.0 - **with the express qualification, in Jauhar's own words, that they "can be subjective/different between studies."** That qualification is part of the source, not a footnote to it. (b) **The two temperature conventions are NOT reconciled**: asked whether `precalc` should be corrected to match `ftemp_grad`, he answered "no need". They stand as they are. *What was originally asked, retained for the record:* section 5 records 29 `SHIPPED-UNCITED` rows, split by the chapter itself into 10 to be emptied to `ABSENT` and 19 that must ACQUIRE a source rather than be emptied, and the chapter supplies no admissible citation for those 19. | **The subjectivity is a CONTRACT, not a caveat, and this is the substance of (a).** A default that varies between studies must not present as determined. The source string for those 7 must say both things - that the value comes from PHM AND that it is study-dependent and expected to be set per study - because a citation that reads like a measured constant invites an interpreter to accept 1.9 g/cc as the coal ceiling for a basin nobody checked. This is the same failure `gr_normalize` is pinned against: a regional calibration shipped as a default always looks plausible, so the user gets no warning. **Two things remain outstanding and this row does NOT close.** (1) **PHM must be expanded to a traceable identity** - a title, author, year or file path. Collaboration rule 6 requires a default to trace to a documented source, and a three-letter token is not one a later reader can follow. (2) **12 of the 19 are still uncited**: the six temperature values `TSURF`, `TGRAD`, `SURF_TEMP`, `TEMP_GRAD`, `BHT`, `TD_BHT` - declining to RECONCILE them is not the same as supplying their source - and the six conditioning values `MIN_THICK`, `SHOULDER`, `SENS`, `K`, `MIN_HAMPEL_SAMPLES`, `DEFAULT_DIVERGENCE`. **The recorded divergence is retained rather than dropped:** `ftemp_grad` ships 26.7 C with 0.03 C/m while `precalc` ships 77.0 F (= 25.0 C) with 0.026 /ft (~0.047 C/m), so the two disagree on both surface and gradient; at 2000 m that is roughly 87 C against 119 C, and temperature drives Rw and therefore Sw. Left as the owner ruled, documented so it cannot be rediscovered as a surprise. | SB-ENV-004, SB-ENV-T06, SB-ENV-T07 |
