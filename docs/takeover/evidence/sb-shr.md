# SB-SHR live-adjudication evidence receipt

## Execution baseline

- Date: `2026-08-12` (`Asia/Jakarta`).
- Worktree and branch: `D:\XX. SandiBumi`; `codex/g1-sb-shr-adjudication`; planning commit `4d4a7935897c49e5bd294add2f29a410a33f3d8b`.
- Accepted implementation anchor `b332026cb498c105f36eade0bf7899bc0c1309f0` is reachable. `origin/master` and the merge base are both `29833735816d9e5be954afafd9ceb71fd856e3f0`.
- Immutable chapter SHA-256: `4567929c51a96d63222eb5c59aa4e1beb3de2fcc27a98ceecf4b094056bba29b`. Frozen six-column ledger SHA-256: `f19ac95bc0e09df63e7c3a1f617b2d0bd418d0d4cef4d444a3e6938003728433`.
- Scope guard: exactly 42 rows, `SB-SHR-001` through `SB-SHR-042`; `P0=13`, `P1=23`, `P2=6`; all 42 live verdict fields were unadjudicated at entry.
- Fresh direct-domain execution on unchanged production bytes: saturation height 7, SHF fit 10, Thomeer 4, HFU 8, Lorenz 10, rock typing 13 and units 6: `58 passed / 0 failed / 0 ignored`.
- Manual evidence remains source-owned and unchanged: saturation-height `0/6`, rock-typing `0/26`, saturation `2/97`, report `6/53`, workflow `0/23`, and processing-history `0/7`.
- This increment changes no production code, test, PRD, research dossier, parameter source, manual-evidence state or generated artifact.

## Evidence and custody boundaries

- A passing helper or self-generated fit is supporting evidence, not proof of a compound observable contract. No executable test uses an `SB-SHR-Tnn` identifier; the strongest applicable current tests are classified below without pretending they implement the missing test intention.
- Section 5 contains 61 parameter rows: 14 height/pressure, 9 fluid-system, 12 model-coefficient, 14 rock-typing, 5 capillary-pressure/permeability and 7 partitioning rows. Nine contain `ABSENT`, six `NON-ADOPTABLE`, fourteen `UNSOURCED`, and one `PRESENT-UNVERIFIED`. None is normalized into a default here.
- `ESC-SHR-1` remains open for primary fluid-property evidence; `ESC-SHR-2` blocks Swanson without a cited apex basis; `ESC-SHR-3` leaves the conflicting hydrocarbon-density defaults unresolved; `ESC-SHR-4` leaves the Lucia transcription unverified; `ESC-SHR-5` preserves the FWL-uncertainty product requirement.
- The three section 7.1 observations, twelve refusals `(a)` through `(l)`, independent derivations for `SB-SHR-030` and `SB-SHR-039`, eight acquisition gaps, six coverage gaps and four uncertainty notes remain open or closed exactly as the chapter records. The implemented refusals do not receive credit for adjacent missing provenance or UI contracts; the three prospective refusals are not credited before their capabilities exist.
- Acquisition remains required for primary laboratory/reservoir fluid properties, the original Lucia paper, the Swanson primary and apex basis, alternative MICP permeability primaries, cutoff-selection papers, full Amaefule text, a mixed-method SCAL set and one licensed correction run.
- Live values such as the current J and gradient literals, fluid defaults, FWL, fused interfacial term, forward-law placeholders, rock-type cutoffs and Swanson pair are implementation evidence only. This receipt neither adopts nor rounds them.

## Acceptance-test routing

| Test intention | Primary receipt owner |
|---|---|
| SB-SHR-T01 | SB-SHR-001 |
| SB-SHR-T02 | SB-SHR-001 |
| SB-SHR-T03 | SB-SHR-002 |
| SB-SHR-T04 | SB-SHR-003 |
| SB-SHR-T05 | SB-SHR-004 |
| SB-SHR-T06 | SB-SHR-005 |
| SB-SHR-T07 | SB-SHR-006 |
| SB-SHR-T08 | SB-SHR-007 |
| SB-SHR-T09 | SB-SHR-008 |
| SB-SHR-T10 | SB-SHR-009 |
| SB-SHR-T11 | SB-SHR-009 |
| SB-SHR-T12 | SB-SHR-010 |
| SB-SHR-T13 | SB-SHR-011 |
| SB-SHR-T14 | SB-SHR-012 |
| SB-SHR-T15 | SB-SHR-013 |
| SB-SHR-T16 | SB-SHR-014 |
| SB-SHR-T17 | SB-SHR-014 |
| SB-SHR-T18 | SB-SHR-015 |
| SB-SHR-T19 | SB-SHR-016 |
| SB-SHR-T20 | SB-SHR-017 |
| SB-SHR-T21 | SB-SHR-018 |
| SB-SHR-T22 | SB-SHR-019 |
| SB-SHR-T23 | SB-SHR-020 |
| SB-SHR-T24 | SB-SHR-021 |
| SB-SHR-T25 | SB-SHR-022 |
| SB-SHR-T26 | SB-SHR-023 |
| SB-SHR-T27 | SB-SHR-025 |
| SB-SHR-T28 | SB-SHR-026 |
| SB-SHR-T29 | SB-SHR-027 |
| SB-SHR-T30 | SB-SHR-028 |
| SB-SHR-T31 | SB-SHR-029 |
| SB-SHR-T32 | SB-SHR-030 |
| SB-SHR-T33 | SB-SHR-031 |
| SB-SHR-T34 | SB-SHR-032 |
| SB-SHR-T35 | SB-SHR-033 |
| SB-SHR-T36 | SB-SHR-034 |
| SB-SHR-T37 | SB-SHR-035 |
| SB-SHR-T38 | SB-SHR-036 |
| SB-SHR-T39 | SB-SHR-037 |
| SB-SHR-T40 | SB-SHR-038 |
| SB-SHR-T41 | SB-SHR-039 |
| SB-SHR-T42 | SB-SHR-040 |
| SB-SHR-T43 | SB-SHR-041 |
| SB-SHR-T44 | SB-SHR-042 |

`SB-SHR-T17` also constrains `SB-SHR-024`; it is routed once above rather than double-counted.

## Requirement receipts

### SB-SHR-001

- **Specified contract:** every shipped height family converts project-unit height into its declared coefficient unit, and adding a family without that declaration is impossible.
- **Current implementation:** `satheight::sw_height` converts Leverett height to feet and Skelt height to metres. `shf_fit::fit_height_method` converts only the Leverett branch; the other fitted families consume project-unit height and return untyped coefficient tuples. No family-registration type forces a coefficient unit.
- **As-built status:** `PARTIAL`; both forward families have live parity, but fitted-family custody and the new-family guard are absent.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. `saturation_height_is_identical_whichever_unit_the_project_declares` and `skelt_harrison_is_identical_whichever_unit_the_project_declares` use exact international-foot arithmetic and pass; they do not prove every fit family or the static registration guard required by T01/T02.
- **Manual evidence:** none; saturation-height is `0/6` and workflow is `0/23`.
- **Source/parameter boundary:** exact metre/foot conversion is cited; no coefficient unit is inferred for an untyped fit result.
- **UI/IPC/provenance surface:** `ShfFitResult.params` and the SHF dialog show names and numbers without coefficient units; no persisted run record adds them.
- **History/reachability:** `bb807ca` and `e662b9c` are reachable closures for forward-family parity; no later registration guard was found.
- **Blocking decision/dependency:** define one typed family descriptor covering coefficient dimensions before admitting another family.
- **Next action:** type every current family, convert fit inputs accordingly, and implement T01/T02 across fit, apply and a compile/static new-family control.

### SB-SHR-002

- **Specified contract:** FWL and every length-bearing shape parameter carry explicit units and re-express when the project unit changes.
- **Current implementation:** `sw_height_spec` hard-codes metres on FWL, Skelt B/D and HAFWL. Brooks-Corey `he` and Thomeer `hd` are untyped tuple entries. The unit carrier refuses a declaration change over committed data; it does not re-express fitted or registered coefficients.
- **As-built status:** `PRESENT-DIVERGENT`; some labels exist, but the required typed re-expression contract does not.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `MISSING`; T03 has no observable parameter-re-expression test. The unit-change refusal test proves a different data-I/O policy.
- **Manual evidence:** none.
- **Source/parameter boundary:** the exact conversion is cited; current numeric defaults remain unsourced and are not used as expected values.
- **UI/IPC/provenance surface:** SHF prompts and result tuples have no project-unit-aware parameter type or conversion event.
- **History/reachability:** the current hard-coded and untyped paths are integrated; no re-expression implementation exists in reachable history.
- **Blocking decision/dependency:** depends on the typed family and coefficient schema from SB-SHR-001.
- **Next action:** add unit-bearing coefficient fields and prove the chapter's exact 30 m to 98.4252 ft control without reinterpreting stored depths.

### SB-SHR-003

- **Specified contract:** every height-domain entry point refuses an undeclared project depth unit by name and computes nothing.
- **Current implementation:** `workflow::run_workflow_module_into` refuses undeclared units before running `sw_height` and writes no curve. `run_shf_fit` still calls `project_depth_unit_or_default`, substituting metres; the fit commands therefore do not share the refusal.
- **As-built status:** `PARTIAL`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. `a_depth_dependent_module_refuses_when_the_project_depth_unit_is_undeclared` proves the forward workflow refusal and zero writes, not every entry point required by T04.
- **Manual evidence:** none.
- **Source/parameter boundary:** no numeric value is needed; the prohibited fallback itself is the finding.
- **UI/IPC/provenance surface:** the workflow returns the named error, while SHF IPC still returns a fit under an implicit metre unit.
- **History/reachability:** the workflow guard is reachable through `d25c274`; the fit fallback remains live.
- **Blocking decision/dependency:** all depth-dependent commands need one shared required-unit resolver.
- **Next action:** route every domain command through the required resolver and test each refusal plus one declared-unit control.

### SB-SHR-004

- **Specified contract:** height outputs, prompts, plot axes and export headers use the declared project unit.
- **Current implementation:** HAFWL is numerically converted to metres and declared as metres even in a foot project; SHF plots say only height/log H; prompts omit or imply a fixed unit; generic curve export uses stored/canonical metadata rather than a domain-wide declared-unit contract.
- **As-built status:** `PRESENT-DIVERGENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `MISSING`; the general LAS depth-header tests do not exercise HAFWL, FWL, the SHF axis and export together as T05 requires.
- **Manual evidence:** none.
- **Source/parameter boundary:** project depth unit is stored state; no display unit is guessed.
- **UI/IPC/provenance surface:** the manifest, dialog axis text, IPC tuples and exported curve unit can disagree or omit the height unit.
- **History/reachability:** numerical forward parity was fixed, but no complete label propagation followed it.
- **Blocking decision/dependency:** depends on SB-SHR-001/002 typed units and one shared display/export label.
- **Next action:** carry the declared unit through result types and render/export all four T05 surfaces from that field.

### SB-SHR-005

- **Specified contract:** one shared water density, hydrocarbon density and reservoir fluid-system object drives fit and apply; disagreement refuses.
- **Current implementation:** the forward module and SHF fitting request own different scalar defaults, including conflicting hydrocarbon-density values; both accept hand-entered fused interfacial terms and no object identity is compared before apply.
- **As-built status:** `PRESENT-DIVERGENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `MISSING`; synthetic fit/apply helpers reuse their own inputs and do not prove shared object identity or disagreement refusal under T06.
- **Manual evidence:** none.
- **Source/parameter boundary:** `ESC-SHR-1` and `ESC-SHR-3` remain open; neither conflicting density nor an interfacial default is chosen.
- **UI/IPC/provenance surface:** SHF dialog inputs, `ShfFitRequest` and `sw_height` parameters are separate scalar channels with no stored identity.
- **History/reachability:** both paths are integrated; no shared fluid object or mismatch guard was found.
- **Blocking decision/dependency:** requires primary source custody or explicit user-supplied values plus a typed stored fit.
- **Next action:** introduce one source-bearing fluid object and refuse fit/apply identity mismatch before computing.

### SB-SHR-006

- **Specified contract:** the Leverett constant and hydrostatic gradient are evaluable first-principles derivations, single-sited and machine-source-bearing.
- **Current implementation:** `J_CONST` and `PSI_PER_FT_PER_SG` are numeric literals with prose comments; the code contains no evaluable derivation or machine-readable citation.
- **As-built status:** `PRESENT-DIVERGENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `MISSING`; no T07 independently evaluates the chapter's two derivation routes and source metadata.
- **Manual evidence:** none.
- **Source/parameter boundary:** the chapter supplies derivations for verification, but current rounded literals remain unsourced implementation values.
- **UI/IPC/provenance surface:** formulas and literal values appear in help text, while results carry no constant source.
- **History/reachability:** unit conversion was fixed without changing literal/source custody.
- **Blocking decision/dependency:** adopt only an independently evaluated expression with its cited source; do not select extra precision from inference.
- **Next action:** replace each transcription with one evaluable derived definition and add the two-route T07 proof.

### SB-SHR-007

- **Specified contract:** shared physical constants have one definition site, including RQI_C and PERM_C, with no duplicate literal in code, UI or fixtures.
- **Current implementation:** SHF imports the saturation constants correctly, but `hfu.rs` and `rocktyping.rs` duplicate the RQI and permeability-transform literals; UI text repeats them and an ingest fixture repeats the Leverett literal.
- **As-built status:** `PRESENT-DIVERGENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `MISSING`; T08's repository-wide inventory and independently derived fixture oracle do not exist.
- **Manual evidence:** none.
- **Source/parameter boundary:** the cited published constants remain distinct where the source publishes rounded pairs; single-siting must not derive one published value from the other.
- **UI/IPC/provenance surface:** module descriptions and dialog help embed secondary literals instead of rendering a canonical source-bearing definition.
- **History/reachability:** the duplicate sites remain reachable; `bb1e4d9` unified Ward logic, not these constants.
- **Blocking decision/dependency:** centralization must preserve source-published rounding and keep fixtures independent.
- **Next action:** export canonical source-bearing definitions, render help from them and make T08 fail on any second literal.

### SB-SHR-008

- **Specified contract:** interfacial tension and contact angle remain separate for every laboratory and reservoir system; no fused product ships.
- **Current implementation:** SCAL rows, SHF requests and `sw_height` store a fused interfacial term; Thomeer additionally ships one fused mercury-air target constant. No contact-angle field exists.
- **As-built status:** `PRESENT-DIVERGENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `MISSING`; `run_thomeer_standardizes_fluid_systems` characterizes the fused ratio and cannot prove T09's separated storage or angle response.
- **Manual evidence:** none.
- **Source/parameter boundary:** all four system defaults stay `ABSENT` under `ESC-SHR-1`; the fused live targets remain unsourced.
- **UI/IPC/provenance surface:** importer, SHF dialog, IPC and result records expose system plus fused value, never tension plus angle.
- **History/reachability:** the fused architecture is integrated in import, fit and apply.
- **Blocking decision/dependency:** a typed fluid-system record and cited/user-entered components must precede conversion.
- **Next action:** separate the two quantities end to end and prove changing angle changes every derived radius without editing tension.

### SB-SHR-009

- **Specified contract:** pooled/per-rock-type SHF, Thomeer, HFU and Lorenz fits persist as named, versioned objects with complete training provenance and cannot apply when incomplete.
- **Current implementation:** all four commands return transient JSON result structs and dialogs hold them locally. No domain fit store, name/version, reopen path or complete provenance object exists.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `MISSING`; T10/T11 have no close/reopen or incomplete-object refusal test.
- **Manual evidence:** none; processing-history is `0/7` and does not substitute for fitted-object custody.
- **Source/parameter boundary:** persistence metadata requires no petrophysical default; fluid and FWL values must remain explicit fields.
- **UI/IPC/provenance surface:** `CuddyFoilResult`, `ShfFitResult`, `ThomeerResult`, `HfuResult` and `LorenzResult` omit a persistent object id/version and the complete enumerated training record.
- **History/reachability:** negative current and reachable-history searches found persistence only for unrelated model classes.
- **Blocking decision/dependency:** define one fitted-object envelope without delegating write discipline or reusing an unrelated model store blindly.
- **Next action:** persist all four classes transactionally and prove name/version/provenance survives project reopen.

### SB-SHR-010

- **Specified contract:** forward apply consumes a stored fit; an override records both replaced and replacement values; hand transcription is unsupported.
- **Current implementation:** `sw_height` accepts free scalar coefficients, and the SHF dialog explicitly tells the user to export/carry fit values into the forward law. No stored-fit id or override record reaches apply.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `MISSING`; T12 has no stored-object acceptance, hand-entry refusal or old/new override proof.
- **Manual evidence:** none.
- **Source/parameter boundary:** current forward defaults are unsourced placeholders and cannot stand in for a stored fit.
- **UI/IPC/provenance surface:** module parameters, SHF result tuples and run provenance are disconnected.
- **History/reachability:** no domain fit id appears in the apply request or stored run parameters.
- **Blocking decision/dependency:** depends on SB-SHR-009's fitted-object envelope and version identity.
- **Next action:** require a fit id, validate its complete provenance, and record a typed override delta when explicitly authorized.

### SB-SHR-011

- **Specified contract:** FWL is uncertain first-class custody; scanning is mandatory, returns an interval and per-zone confidence, and refuses unconstrained fits.
- **Current implementation:** FOIL optionally returns a residual scan and one best FWL. Other SHF methods accept one scalar FWL. No interval, statistical indistinguishability rule, per-zone confidence or unconstrained refusal exists.
- **As-built status:** `PARTIAL`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DEGRADED-RESULT`.
- **Automated evidence:** `CHARACTERIZATION`. `fwl_scan_finds_the_true_contact` uses a self-generated sharp optimum and proves only current argmin behavior, not T13's wide/narrow interval and confidence controls.
- **Manual evidence:** none.
- **Source/parameter boundary:** `ESC-SHR-5` preserves the specified product requirement; no confidence threshold or interval rule is invented.
- **UI/IPC/provenance surface:** the dialog checkbox makes scanning optional and shows a single best line; results carry no interval/confidence.
- **History/reachability:** the residual-curve implementation is integrated; no uncertainty object appears later.
- **Blocking decision/dependency:** an independently specified statistical equivalence rule is required before implementation.
- **Next action:** define the source-backed interval rule, make the scan mandatory and prove flat versus sharp residual surfaces from both sides.

### SB-SHR-012

- **Specified contract:** Brooks-Corey emits labelled lambda and reciprocal N and refuses import/export without a convention.
- **Current implementation:** fitting uses the published lambda form and returns only a `lambda` tuple; no N field, convention type or coefficient import/export refusal exists.
- **As-built status:** `PARTIAL`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. `brooks_corey_recovers_synthetic_curve` characterizes the current lambda fit from its own equation; it does not independently pin the cited saturation stake or T14 surfaces.
- **Manual evidence:** none.
- **Source/parameter boundary:** the published convention and reciprocal relation are cited; no coefficient is adopted as a default.
- **UI/IPC/provenance surface:** result tuples and dialog show lambda only, while generic export has no convention schema.
- **History/reachability:** the correct arithmetic branch is integrated; the declaration/refusal contract is absent.
- **Blocking decision/dependency:** add a convention-bearing result/import type before any portable coefficient exchange.
- **Next action:** emit both labelled fields and refuse missing convention on both import and export, with an independent T14 oracle.

### SB-SHR-013

- **Specified contract:** Thomeer emits labelled base-10 and natural-log G and refuses an imported G without a base.
- **Current implementation:** both height and MICP fits use base-10 arithmetic but emit one unqualified `g`; no second form or import refusal exists.
- **As-built status:** `PARTIAL`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. The two synthetic recovery tests share the implementation's equation; they support base-10 behavior but do not prove T15's dual labelled forms and refusal.
- **Manual evidence:** none.
- **Source/parameter boundary:** the chapter cites base 10 and the exact logarithm conversion; no fitted coefficient becomes a default.
- **UI/IPC/provenance surface:** IPC and both dialogs label the field only `G`.
- **History/reachability:** current base-10 code is reachable; no base-bearing type exists.
- **Blocking decision/dependency:** one convention type must serve both SHF and MICP Thomeer results.
- **Next action:** emit both forms from one stored base-10 value and refuse an untagged imported coefficient.

### SB-SHR-014

- **Specified contract:** Swanson has no apex-basis default; an explicit source-bearing basis travels with every result, and no selection yields missing permeability.
- **Current implementation:** `run_thomeer_fit` always selects one implicit bulk-volume treatment and computes Swanson permeability with an unverified literal pair whenever fused-fluid standardization is available. Result and dialog carry neither basis nor coefficient source.
- **As-built status:** `PRESENT-DIVERGENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. Current Thomeer tests prove the computation and unstandardized null branch, thereby pinning the divergence; T16/T17's no-basis refusal and three-basis controls are missing.
- **Manual evidence:** none.
- **Source/parameter boundary:** `ESC-SHR-2` remains blocking; the live pair is `NON-ADOPTABLE`, and none of the three candidate bases is selected.
- **UI/IPC/provenance surface:** `swanson_k` is a clean nullable number in IPC and table output; no unverified flag reaches export/report.
- **History/reachability:** the default computation is integrated from the Thomeer increment and remains reachable.
- **Blocking decision/dependency:** acquire the primary source and settle the apex basis before any clean result is possible.
- **Next action:** remove implicit computation, require an explicit sourced basis and propagate basis/source/unverified state through every consumer.

### SB-SHR-015

- **Specified contract:** both published port-size schemes are named and selectable, no default is silent, and every result records the active scheme.
- **Current implementation:** `PORT_BOUNDS` defines one fixed scheme; `port_class` and Pittman output use it without a scheme field. The Pittman apex selector changes saturation radius, not the port-boundary scheme.
- **As-built status:** `PRESENT-DIVERGENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. `winland_r35_and_port_class` and Pittman selector tests pin the current single scheme, not T18's two-scheme choice and travelling identity.
- **Manual evidence:** none.
- **Source/parameter boundary:** both published schemes are cited; Gate 1 does not choose one as default.
- **UI/IPC/provenance surface:** module specs expose a class method/apex but no port-scheme control or result field.
- **History/reachability:** one scheme is integrated; no second scheme or scheme identity exists in reachable history.
- **Blocking decision/dependency:** product needs a no-default choice surface and a typed scheme id.
- **Next action:** implement both cited boundary tables and prove the chapter's boundary-changing sample plus result/export identity.

### SB-SHR-016

- **Specified contract:** the two RQI quantities are namespace-disambiguated by type, and cross-consumption refuses independently of mnemonic.
- **Current implementation:** rock typing/HFU emits mnemonic `RQI`; SHF Leverett internally carries an unlabelled square-root term. Curve resolution is mnemonic/family based and no quantity type distinguishes them.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `MISSING`; T19's wrong-quantity refusal and cited scaling stake are not executable.
- **Manual evidence:** none.
- **Source/parameter boundary:** the two published definitions remain distinct; neither may be renamed into the other.
- **UI/IPC/provenance surface:** result tuples, curve metadata and module inputs contain no RQI quantity discriminator.
- **History/reachability:** no namespace or wrong-family guard was found in current source or history.
- **Blocking decision/dependency:** requires quantity-family metadata at both curve and fitted-object boundaries.
- **Next action:** define separate typed identities and refuse a same-mnemonic curve carrying the wrong identity.

### SB-SHR-017

- **Specified contract:** every log-porosity correlation enforces declared porosity unit/range, fails the whole run, and names the offending curve.
- **Current implementation:** rock-typing modules expect v/v and convert to percent where needed, but invalid samples are silently left missing. Workflow may still report success if other samples are finite; no domain-wide named run refusal inventories every correlation.
- **As-built status:** `PARTIAL`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. Current missing/out-of-range tests prove local NaN behavior and paper-backed Pittman arithmetic, not T20's whole-run unit refusal from both sides.
- **Manual evidence:** none.
- **Source/parameter boundary:** each correlation's published porosity basis is cited; no cross-method range is invented.
- **UI/IPC/provenance surface:** module requests name curves but carry no verified porosity-unit contract or structured per-run unit error.
- **History/reachability:** local guards are integrated; the domain-wide precondition is absent.
- **Blocking decision/dependency:** typed unit metadata must reach module input resolution before arithmetic.
- **Next action:** inventory every log-porosity consumer, validate its declared basis up front, and test one accepted and one named rejected curve per basis.

### SB-SHR-018

- **Specified contract:** conflicting incumbent gradients are surfaced at the choice point as height consequences in the user's unit.
- **Current implementation:** no gradient chooser or consequence calculator exists; fit/apply paths instead expose separate scalar defaults.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `MISSING`; T21's cited three-consequence display is absent.
- **Manual evidence:** none.
- **Source/parameter boundary:** the hydrocarbon-gradient default remains `ABSENT`; candidate values are evidence for divergence, not defaults.
- **UI/IPC/provenance surface:** the SHF dialog shows densities and a fused fluid term but no competing-source disclosure or height consequence.
- **History/reachability:** negative source and history searches found no choice surface.
- **Blocking decision/dependency:** depends on sourced fluid candidates and project-unit propagation.
- **Next action:** after source custody is resolved, render each candidate's derived consequence without selecting one automatically.

### SB-SHR-019

- **Specified contract:** sigma-scoped coefficients are recorded `NON-ADOPTABLE`, unreachable as defaults/seeds, and display their scope when shown.
- **Current implementation:** the foreign scoped coefficient set is not present as a default or seed, and current Thomeer standardizes each row explicitly. There is no typed `NON-ADOPTABLE` record, repository guard or verification display carrying scope.
- **As-built status:** `PARTIAL`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `MISSING`; T22's repository-wide unreachability check does not exist.
- **Manual evidence:** none.
- **Source/parameter boundary:** the scoped values remain non-adoptable evidence only; no value is copied into this receipt or code.
- **UI/IPC/provenance surface:** no verification-only coefficient object or scope label exists.
- **History/reachability:** current source contains the explicit-ratio architecture but not the required custody guard.
- **Blocking decision/dependency:** define a machine-readable admissibility state before any verification catalogue can expose such values.
- **Next action:** add a non-adoptable evidence type and a repository guard proving it cannot feed defaults or fit seeds.

### SB-SHR-020

- **Specified contract:** Lucia RFN enforces both calibrated limits and RFN/class outputs carry the same out-of-band state.
- **Current implementation:** `lucia_class` rejects only above the high bound; values below the calibrated floor become class 1. `lucia_rfn_module` writes the finite RFN even when the class is missing on the high side.
- **As-built status:** `PRESENT-DIVERGENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. `lucia_rfn_round_trips_and_classes` pins current in-band behavior from the same transform; T23's below-floor and RFN/class agreement controls are missing.
- **Manual evidence:** none.
- **Source/parameter boundary:** coefficients remain `PRESENT-UNVERIFIED` under `ESC-SHR-4`; the unsupported high-side threshold remains absent.
- **UI/IPC/provenance surface:** two output curves can tell different validity stories and carry no unverified-source flag.
- **History/reachability:** the one-sided classifier is integrated and unchanged.
- **Blocking decision/dependency:** acquire the original paper before field release and implement one shared validity state.
- **Next action:** apply the cited two-sided band to both outputs and prove below, within and above controls independently.

### SB-SHR-021

- **Specified contract:** every sample in a published extrapolation regime carries a flag, and no unpublished monotonic clamp is applied.
- **Current implementation:** Pittman preserves the published independent regressions and correctly does not clamp their tight-rock crossover. It documents the regime only in module help; output curves have no per-sample extrapolation flag.
- **As-built status:** `PARTIAL`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DEGRADED-RESULT`.
- **Automated evidence:** `CHARACTERIZATION`. `every_shipped_pittman_row_is_a_published_one` and `the_published_pittman_rows_cross_over_in_tight_rock_and_that_is_the_papers_own_arithmetic` are source-backed correctness evidence for the no-clamp limb; T24's per-sample flag is missing.
- **Manual evidence:** none.
- **Source/parameter boundary:** the expected radii and extrapolation statement come from the cited paper; no monotonic replacement values are invented.
- **UI/IPC/provenance surface:** help text warns generally, while RAPEX and radius curves carry no flag or source-regime metadata.
- **History/reachability:** `565fd2b` is the reachable paper-correction commit and preserves the non-monotone controls.
- **Blocking decision/dependency:** define a per-sample quality/validity channel shared by computed curves and exports.
- **Next action:** emit the cited extrapolation flag without modifying numeric radii and prove flagged/unflagged controls.

### SB-SHR-022

- **Specified contract:** every domain module returns and persists a named count for every excluded sample; reduced coverage says why.
- **Current implementation:** SHF fits return several named exclusion counts and preserve failed rock-type groups. Thomeer, HFU and Lorenz return only aggregate skipped counts; deterministic curve modules leave NaNs without a persisted reason ledger.
- **As-built status:** `PARTIAL`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DEGRADED-RESULT`.
- **Automated evidence:** `CHARACTERIZATION`. SHF group/exclusion, HFU skip and Lorenz missing-curve tests prove isolated honesty limbs; T25's every-module reconciliation and persistence proof is missing.
- **Manual evidence:** none; processing-history is `0/7`.
- **Source/parameter boundary:** reason counts require no numeric default.
- **UI/IPC/provenance surface:** SHF renders reasons, while other dialogs show one skipped total and computed curves carry no exclusion ledger in ancestry.
- **History/reachability:** the SHF reason ledger is integrated; no domain-wide result envelope exists.
- **Blocking decision/dependency:** depends on a shared run-result and curve-provenance schema.
- **Next action:** standardize named reasons, reconcile candidate counts, persist them with outputs and test every registered domain module.

### SB-SHR-023

- **Specified contract:** a classifier distinguishes unclassified from the lowest class, and each boundary is cited or absent and user-entered.
- **Current implementation:** `rt_cutoff` maps every finite sample missing both higher criteria to class 3 using four unsourced defaults; Lucia maps below-floor RFN to class 1. Port classification also has a fixed silent scheme.
- **As-built status:** `PRESENT-DIVERGENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. `rt_cutoff_ladders_by_vsh_and_phie` and `an_inverted_cutoff_ladder_is_accepted_and_scatters_the_middle_class` pin current behavior, including the divergence; T26's unclassified control and source-or-entry rule are missing.
- **Manual evidence:** none.
- **Source/parameter boundary:** all four reservoir-flag cutoffs remain `UNSOURCED`; no house boundary is approved.
- **UI/IPC/provenance surface:** module parameters appear as defaults and output only a numeric class with no source/unclassified state.
- **History/reachability:** the classifier is integrated and remains directly reachable.
- **Blocking decision/dependency:** the cutoff values must remain absent until cited or explicitly entered per run.
- **Next action:** remove silent boundary adoption, add an unclassified state and prove it is distinct from every valid class.

### SB-SHR-024

- **Specified contract:** every result derived from an unverified constant carries that state through dialog, plot, export and report.
- **Current implementation:** Swanson and Lucia source comments warn developers, but result structs expose clean numbers/classes with no machine-readable verification state.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `MISSING`; T17's four-surface propagation proof does not exist.
- **Manual evidence:** none.
- **Source/parameter boundary:** Swanson remains `NON-ADOPTABLE`; Lucia remains `PRESENT-UNVERIFIED`; comments do not elevate either.
- **UI/IPC/provenance surface:** dialogs may show prose warnings but IPC, plot data, generic export and reports cannot carry the state.
- **History/reachability:** no result-level verification flag was found.
- **Blocking decision/dependency:** requires one provenance state shared by domain results and deliverables.
- **Next action:** add the state to source-bearing result objects and refuse a clean presentation path.

### SB-SHR-025

- **Specified contract:** the deliverable names the actual saturation-height and rock-typing methods, parameters and FWL and omits methods not used.
- **Current implementation:** the shared PDF/Word methodology template is fixed and contains no saturation-height or rock-typing provenance; users may edit free-text rows, but the report does not derive them from run records.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DEGRADED-RESULT`.
- **Automated evidence:** `MISSING`; report page-order and shared-template tests do not prove T27's actual-method selection or unused-row omission.
- **Manual evidence:** report is `6/53`, with no qualifying SHR methodology scenario.
- **Source/parameter boundary:** reporting must quote the selected stored fit; it may not infer a method from curve mnemonic.
- **UI/IPC/provenance surface:** editable methodology text is not linked to fit/run identity, coefficients, scheme or FWL.
- **History/reachability:** no domain-method report binding exists in current or reachable source.
- **Blocking decision/dependency:** depends on stored fits and curve ancestry from SB-SHR-009/010.
- **Next action:** generate methodology rows from the actual stored objects and prove one used and one unused method from the rendered PDF and Word output.

### SB-SHR-026

- **Specified contract:** laboratory Pc converts to reservoir conditions using separately declared tension and angle for both named systems and refuses either undeclared system.
- **Current implementation:** Thomeer standardization scales by one fused target divided by each row's fused import value. Separate components, reservoir-system input and two-sided undeclared refusal are absent.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. `run_thomeer_standardizes_fluid_systems` proves the current fused-ratio path; T28's published relation, round trip and both undeclared controls are missing.
- **Manual evidence:** none.
- **Source/parameter boundary:** four system defaults remain absent under `ESC-SHR-1`; no fluid value is supplied here.
- **UI/IPC/provenance surface:** importer stores system plus fused value; result stores only a standardized boolean.
- **History/reachability:** current standardization is integrated; no reservoir conversion object exists.
- **Blocking decision/dependency:** requires cited or explicit per-run system components and SB-SHR-008 separation.
- **Next action:** implement typed lab/reservoir systems and independently prove the relation plus both refusal sides.

### SB-SHR-027

- **Specified contract:** all four published closure/conformance corrections are named choices with no default and travel into corrected curves and derived entry/Thomeer results.
- **Current implementation:** no capillary-pressure closure correction request, result, dialog or provenance type exists.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `REQUESTED-CAPABILITY`.
- **Automated evidence:** `MISSING`; T29 is absent.
- **Manual evidence:** none.
- **Source/parameter boundary:** the four treatment identities are specified; no numeric correction endpoint or silent default is invented.
- **UI/IPC/provenance surface:** SCAL import, Thomeer fit, plots and exports have no correction-choice field.
- **History/reachability:** negative source/test/history inventory found no implementation.
- **Blocking decision/dependency:** correction provenance must be defined before corrected data can be accepted under SB-SHR-038.
- **Next action:** implement the four typed treatments without a default and carry the selected identity into every derivative.

### SB-SHR-028

- **Specified contract:** net-overburden and clay-bound-water corrections act on the non-wetting phase and record identity; wetting-phase application is structurally impossible.
- **Current implementation:** neither correction exists in the domain's SCAL/Thomeer path.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `DEFERRED`; `REQUESTED-CAPABILITY`.
- **Automated evidence:** `MISSING`; T30 is absent.
- **Manual evidence:** none.
- **Source/parameter boundary:** no correction equation or parameter is adopted from adjacent modules or barred material.
- **UI/IPC/provenance surface:** no typed phase or correction field exists.
- **History/reachability:** confirmed absent across current source, tests and reachable history.
- **Blocking decision/dependency:** requires primary correction sources and typed phase custody before later-version implementation.
- **Next action:** retain as a sourced later increment; do not approximate from a neighboring correction.

### SB-SHR-029

- **Specified contract:** pore-throat distributions are tested for modality and a multimodal finding is reported as evidence against one saturation-height law.
- **Current implementation:** the HFU histogram helper can find a valley in FZI values, but Thomeer fits one pore system per sample and explicitly defers multimodal throat systems. No throat-modality result exists.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `DEFERRED`; `REQUESTED-CAPABILITY`.
- **Automated evidence:** `MISSING`; `histogram_finds_the_bimodal_valley` concerns FZI clustering and cannot substitute for T31's throat-distribution modality contract.
- **Manual evidence:** none.
- **Source/parameter boundary:** no modality threshold or law-selection rule is inferred.
- **UI/IPC/provenance surface:** Thomeer result/dialog return one system and no modality field or cross-law warning.
- **History/reachability:** the one-system limitation is explicit and no later implementation was found.
- **Blocking decision/dependency:** needs a source-backed modality method and representative multi-system SCAL evidence.
- **Next action:** defer until those inputs exist; then report modes as evidence rather than silently stacking one law.

### SB-SHR-030

- **Specified contract:** Lorenz-inflection partitioning is selectable beside exact Ward and the active method is recorded.
- **Current implementation:** Lorenz uses the shared exact Ward dynamic program in depth order, with caller-set or hard-capped automatic K. There is no inflection implementation or method selector/result identity.
- **As-built status:** `PARTIAL`.
- **Release disposition and risk:** `DEFERRED`; `REQUESTED-CAPABILITY`.
- **Automated evidence:** `CHARACTERIZATION`. Lorenz segmentation tests and the shared-Ward commit prove deterministic Ward behavior; T32's independent three-regime inflection oracle and selectable disagreement are missing.
- **Manual evidence:** none; rock-typing is `0/26`.
- **Source/parameter boundary:** the inflection tolerance remains absent; independent derivation in section 7.4 must not borrow a vendor cap or tuned threshold.
- **UI/IPC/provenance surface:** the dialog exposes only unit count; `LorenzResult` has no partitioner field.
- **History/reachability:** `bb1e4d9` single-sites Ward; no inflection path appears in current or reachable source.
- **Blocking decision/dependency:** derive the method independently and cite any introduced tolerance or keep it absent.
- **Next action:** implement a typed second partitioner and T32 only after the derivation yields a defensible boundary rule.

### SB-SHR-031

- **Specified contract:** Dykstra-Parsons VDP is available and reported beside the Lorenz coefficient.
- **Current implementation:** `LorenzResult` returns only the Lorenz coefficient and capacity summaries; no percentile-permeability or VDP field exists.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `DEFERRED`; `REQUESTED-CAPABILITY`.
- **Automated evidence:** `MISSING`; T33 is absent.
- **Manual evidence:** none.
- **Source/parameter boundary:** the chapter gives the definition; no percentile interpolation convention is invented here.
- **UI/IPC/provenance surface:** backend, IPC, Lorenz table/plot, reports and exports omit VDP.
- **History/reachability:** no VDP implementation was found.
- **Blocking decision/dependency:** define a cited percentile convention before computing it.
- **Next action:** implement as a separately labelled companion metric in a later increment with the layered synthetic oracle.

### SB-SHR-032

- **Specified contract:** at least Purcell and Katz-Thompson permeability routes appear beside Swanson, each separately sourced and named.
- **Current implementation:** only the implicit Swanson route exists.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `DEFERRED`; `REQUESTED-CAPABILITY`.
- **Automated evidence:** `MISSING`; T34 is absent.
- **Manual evidence:** none.
- **Source/parameter boundary:** alternative-route constants remain `ABSENT`; primary sources are an acquisition gap and no remembered equation is adopted.
- **UI/IPC/provenance surface:** Thomeer IPC/dialog contain only `swanson_k`.
- **History/reachability:** negative source/test/history inventory confirms no alternative route.
- **Blocking decision/dependency:** acquire the primary papers before designing parameters or expected values.
- **Next action:** keep both routes absent until sourced, then implement side-by-side typed outputs rather than a silent selector.

### SB-SHR-033

- **Specified contract:** Winland R35, Aguilera R35 and Permadi-Susilo PGS are separately named typed indicators with no substitution route.
- **Current implementation:** `rocktyping` emits Winland `R35` plus `PGEOM` and `PSTRUC` PGS components. Aguilera R35 is absent, and the outputs have mnemonics/units but no stronger quantity identity preventing substitution.
- **As-built status:** `PARTIAL`.
- **Release disposition and risk:** `DEFERRED`; `REQUESTED-CAPABILITY`.
- **Automated evidence:** `CHARACTERIZATION`. `winland_r35_and_port_class` and PGS-related formula tests cover current outputs only; T35's three-identity and no-substitution proof is missing.
- **Manual evidence:** none.
- **Source/parameter boundary:** existing Winland/PGS sources remain as recorded; no Aguilera equation or value is supplied without its primary source.
- **UI/IPC/provenance surface:** module help names current methods, but curve family metadata cannot distinguish two future R35 quantities by type.
- **History/reachability:** Winland and PGS are integrated; Aguilera is absent.
- **Blocking decision/dependency:** acquire/verify the missing source and add quantity typing before adding a second R35 mnemonic.
- **Next action:** defer the missing method, preserve separate current names and forbid mnemonic-only substitution in the eventual typed registry.

### SB-SHR-034

- **Specified contract:** one derived fluid-gradient service serves the cutoff domain, uses stored-fit densities and refuses an undeclared unit.
- **Current implementation:** saturation height owns local gradient literals/conversion calls; the cutoff/HCPV path exposes no call to a shared service, and stored SHR fits do not exist.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `MISSING`; T36 has no cross-domain call, stored-density or undeclared-unit proof.
- **Manual evidence:** none.
- **Source/parameter boundary:** the gradient must come from SB-SHR-006's derived expression and explicit stored densities; no local fallback is authorized.
- **UI/IPC/provenance surface:** no service request/result or HCPV provenance field links the domains.
- **History/reachability:** repository search finds the gradient only in local saturation paths and unrelated module literals.
- **Blocking decision/dependency:** depends on SB-SHR-003, 006 and 009 before the cutoff seam can be safe.
- **Next action:** expose one typed service and make the cutoff caller prove unit refusal and stored-density use.

### SB-SHR-035

- **Specified contract:** candidate cutoffs are derived from rock partition, flow boundaries and Pc evidence, retain that evidence, and are handed off without selection.
- **Current implementation:** rock-type classes, HFU/Lorenz boundaries and Pc fits exist as separate transient outputs. No evidence-bearing candidate-cutoff object or handoff to the cutoff machinery exists.
- **As-built status:** `PARTIAL`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `CHARACTERIZATION`. Existing source-backed Pittman and deterministic partition tests prove inputs only; T37's derived object and no-selection control are missing.
- **Manual evidence:** none; rock-typing is `0/26` and cutoff evidence remains separate.
- **Source/parameter boundary:** selection papers remain an acquisition gap owned by the cutoff chapter; this domain does not invent a chosen cutoff.
- **UI/IPC/provenance surface:** no result type, Results QC card or cutoff request carries the evidence bundle.
- **History/reachability:** component outputs are integrated; no typed cross-domain handoff was found.
- **Blocking decision/dependency:** requires stored partition/Pc objects and the receiving cutoff evidence schema.
- **Next action:** define a candidate-only handoff with source links and prove this domain cannot mark it selected.

### SB-SHR-036

- **Specified contract:** Lambda becomes a sixth family only after its parameter sources are established; until then it stays absent and is never approximated.
- **Current implementation:** Lambda is not listed in the forward module, SHF request union or dialog, and no adjacent family is labelled as its approximation.
- **As-built status:** `ABSENT`; the current refusal boundary is correct, while the source-dependent capability is unbuilt.
- **Release disposition and risk:** `DEFERRED`; `LATER`.
- **Automated evidence:** `MISSING`; T38's registered-family inventory and no-approximation assertion do not exist.
- **Manual evidence:** none.
- **Source/parameter boundary:** Lambda parameters remain absent by design; this is not a gap to fill from training or a neighboring family.
- **UI/IPC/provenance surface:** all current selectors omit the family cleanly.
- **History/reachability:** negative current and history search confirms no hidden Lambda route.
- **Blocking decision/dependency:** a cited parameter source is the explicit unblocker.
- **Next action:** retain absence; after source acquisition, implement as its own typed family with independent tests.

### SB-SHR-037

- **Specified contract:** any saturation at a reference pressure carries typed pressure plus laboratory/reservoir state and refuses an undeclared reference.
- **Current implementation:** Swirr appears as scalar fit coefficients/curves and elsewhere by mnemonic; no pressure/state type is attached and no domain refusal validates the reference condition.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `MISSING`; T39's missing-field and same-pressure/different-state controls are absent.
- **Manual evidence:** none.
- **Source/parameter boundary:** the reference-pressure default remains absent pending a mixed-method SCAL dataset; no lab/reservoir interpretation is guessed.
- **UI/IPC/provenance surface:** fit parameters, curve metadata, SCAL records and dialogs omit typed reference state.
- **History/reachability:** no `Swirr` reference object was found in current source or reachable history.
- **Blocking decision/dependency:** requires a typed reference-condition schema before storing or comparing Swirr.
- **Next action:** make pressure and state jointly mandatory and prove undeclared, laboratory and reservoir cases separately.

### SB-SHR-038

- **Specified contract:** corrected Pc import requires correction provenance and resolves module identity from the declared manifest name, not free text.
- **Current implementation:** SCAL import stores format, system, fused interfacial term and set identity but no correction tag/implementation identity. It cannot distinguish corrected from uncorrected Pc and therefore accepts an untagged corrected delivery.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `MISSING`; current SCAL import tests prove accepted uncorrected controls only, not T40's refusal and manifest-identity conflict.
- **Manual evidence:** none.
- **Source/parameter boundary:** no correction is inferred from filename, format or specification prose.
- **UI/IPC/provenance surface:** importer request/result and stored SCAL rows omit correction provenance.
- **History/reachability:** set versioning and fluid fields are reachable, but no correction tag exists.
- **Blocking decision/dependency:** define a correction manifest and retain uncorrected as an explicit control state.
- **Next action:** require the tag only when corrected Pc is declared, key identity on the manifest, and test refusal/acceptance from both sides.

### SB-SHR-039

- **Specified contract:** a deterministic exhaustive uncapped sweep spans the declared model-choice grid, uses all depths, records reproducible inputs and retains the full ranking.
- **Current implementation:** users run one fit/partition selection at a time. No SHR sweep request, Cartesian enumerator, ranking result, persistence record or UI exists.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `DEFERRED`; `REQUESTED-CAPABILITY`.
- **Automated evidence:** `MISSING`; T41 is absent.
- **Manual evidence:** none.
- **Source/parameter boundary:** section 7.4 requires independent derivation; no vendor algorithm, sampling cap or grid default is reconstructed.
- **UI/IPC/provenance surface:** no sweep surface or full-ranking export exists.
- **History/reachability:** negative source/test/history searches found no domain model-selection runner.
- **Blocking decision/dependency:** product must first define a declared source-bearing grid and control data without inventing choices.
- **Next action:** defer until the component objects exist; then enumerate the explicit grid deterministically and retain every scored combination.

### SB-SHR-040

- **Specified contract:** numerical results are invariant to plot open/closed state, axis scale, zoom and theme; a required log domain belongs to the method.
- **Current implementation:** existing fits, entry pressure and rock classes are computed in Rust before canvas rendering, so current plots do not feed their numeric routines. Closure picking is absent, and no product-wide invariant or plot-state regression guards future capability.
- **As-built status:** `PARTIAL`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `MISSING`; backend unit tests support separation but T42's closed/linear/log bit-identity proof across closure, entry and class is absent.
- **Manual evidence:** none; plot/manual state is not exercised for this contract.
- **Source/parameter boundary:** no log-domain threshold is introduced; the current Thomeer log base is method math, not plot state.
- **UI/IPC/provenance surface:** canvases consume result arrays, but the architecture has no typed method/display boundary contract.
- **History/reachability:** current numeric-before-render pattern is integrated; the prospective closure refusal remains unimplemented.
- **Blocking decision/dependency:** establish the invariant before adding interactive closure picking.
- **Next action:** add a display-independent command test now and require any future pick to persist method-domain inputs rather than canvas transforms.

### SB-SHR-041

- **Specified contract:** published prediction direction is preserved and recorded; algebraic inversion is never presented as a fitted regression.
- **Current implementation:** Pittman correctly evaluates the published forward radius-from-permeability/porosity equations. No inverse permeability route is offered, and outputs do not carry a prediction-direction field.
- **As-built status:** `PARTIAL`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. The paper-backed table and radius tests are correctness evidence for the current forward limb, but T43's forward/inverse discriminator and travelling direction identity are missing.
- **Manual evidence:** none.
- **Source/parameter boundary:** only the cited forward equations are used; no inverse coefficient set is invented.
- **UI/IPC/provenance surface:** curve names and help imply radius output, while result metadata/export do not record fitted direction.
- **History/reachability:** `565fd2b` corrected the forward table; no inverse route or direction field appears later.
- **Blocking decision/dependency:** add typed direction provenance before any inverse prediction capability.
- **Next action:** record `radius_from_k_phi` on current results and use independently cited forward/inverse equations for T43 if an inverse route is added.

### SB-SHR-042

- **Specified contract:** contact angle has one range and representation, every Pc expression uses absolute cosine, and stored angle/cosine cannot diverge.
- **Current implementation:** the product stores fused interfacial terms and no angle field, validation range or derived-cosine representation. Absolute-cosine behavior therefore cannot be enforced or audited.
- **As-built status:** `ABSENT`.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `MISSING`; T44's complementary-angle and single-editable-source controls are absent.
- **Manual evidence:** none.
- **Source/parameter boundary:** the angle range/default remains absent under `ESC-SHR-1`; the chapter's complementary-angle arithmetic is a test oracle, not permission to choose a default.
- **UI/IPC/provenance surface:** importer, fit/apply requests, SCAL rows and Thomeer result contain no angle/cosine field.
- **History/reachability:** no contact-angle type exists in current source or history.
- **Blocking decision/dependency:** depends on SB-SHR-008 and a cited validation convention.
- **Next action:** store one angle, derive absolute cosine everywhere and prove complementary-angle equality plus rejection of independently editable cosine.

## Live adjudication totals

- As-built: `18 ABSENT`, `14 PARTIAL`, `10 PRESENT-DIVERGENT`, `0 PRESENT-OK`, `0 PRESENT-UNVERIFIED`.
- Release: `34 PILOT-BLOCKER`, `8 DEFERRED`, `0 UNDECIDED`, `0 OUT`.
- Test evidence: `17 CHARACTERIZATION`, `25 MISSING`, `0 CORRECTNESS`; supporting source-backed controls are named in their rows and are not promoted to whole-contract proofs.
- Commit state: `24 INTEGRATED`, `18 UNIMPLEMENTED`.
- Manual evidence remains unchanged; no field/manual scenario was closed.
