# Gate 1 baseline receipt — 2026-08-10

## Identity

- Branch: `codex/sandibumi-takeover-gate1`.
- HEAD: `706fe59d50fa673066a1dbfb87f0948074656f4e` (`G1-I001 establish the takeover tracker`).
- Local `master...HEAD`: `0` commits on `master` not in HEAD; `6` commits on HEAD not in local `master`. Remote refs were not fetched in this increment; that inventory belongs to G1-I003.
- Worktree paths present before Gate 1 and still excluded from takeover commits:
  - `src-tauri/Cargo.toml` — `git status --short` reports modified. `git diff -- src-tauri/Cargo.toml` is empty, and the index and filtered worktree hashes are both `047546710f33ca583080fa972ae585e6b3cfd5d6`.
  - `claude-usage-widget/` — untracked.
  - `src-tauri/target-ignored-tests/` — untracked.
  - `src-tauri/target-plt-p0/` — untracked.

## Requirement ledger

- Source rows: `931`.
- Unique requirement IDs: `931`.
- Original priority counts: blank `15`; `P0` `266`; `P1` `401`; `P2` `199`; `P3` `46`; `P4` `4`.
- Raw chapter-status counts: blank `62`; `ABSENT` `507`; `ABSENT — designed, parked` `1`; `PARTIAL` `124`; `PRESENT-DIVERGENT` `110`; `PRESENT-OK` `114`; `PRESENT-UNVERIFIED` `12`; `UNMEASURED` `1`.
- Live-adjudication count: `0` adjudicated and `931` explicitly `UNADJUDICATED`.
- Pilot-disposition count: `0` classified as `PILOT-BLOCKER`; all `931` remain `UNDECIDED`.

## Manual and field evidence

- Checked scenarios in `REVIEW.md`: `78`.
- Unchecked scenarios in `REVIEW.md`: `1,392`.
- Total scenarios in `REVIEW.md`: `1,470`.
- Checked ratio: `5.3%`.
- Capability matrix: `14 / 54` capabilities have recorded exercise; `1 / 54` is fully exercised.
- `node tools/generate-verification-matrix.mjs --check`: exit `0`; the generated matrix is current.

## Automated gate

- Command: `powershell -ExecutionPolicy Bypass -File tools\check.ps1`.
- Exit code: `0`; `GATE GREEN in 69s`.
- Takeover-ledger result: `9` passed, `0` failed.
- Frontend result: `12` passed, `0` failed; TypeScript and Vite production build succeeded.
- Rust result across the Cargo test binaries: `910` passed, `0` failed, `36` ignored.
- Aggregate named-test result across tracker, frontend and Rust stages: `931` passed, `0` failed, `36` ignored. This aggregate is not a requirement-coverage percentage.
- Commit tested: `706fe59d50fa673066a1dbfb87f0948074656f4e`, with the four pre-existing worktree paths listed under Identity still present and excluded from takeover staging.

## Interpretation boundary

This receipt describes repository and evidence state. It does not declare a petrophysical result
correct and does not mark an unchecked manual scenario complete.
