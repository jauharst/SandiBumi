# SB-TOC live-adjudication evidence receipt

## Execution baseline

- Date: `2026-08-12` (`Asia/Jakarta`).
- Worktree and branch: `D:\XX. SandiBumi`; `codex/g1-sb-toc-adjudication`; planning commit `ef970bfa079bb80d9baa8eba1cf66065b2d46584`.
- Accepted implementation anchor `b332026cb498c105f36eade0bf7899bc0c1309f0` is reachable. `origin/master` and the merge base are both `29833735816d9e5be954afafd9ceb71fd856e3f0`.
- Immutable chapter SHA-256: `6c71c7ce81820cfb50dd94fc1ac372d7ba729b1f0d788904e543c8c21ccb7202`. Frozen six-column ledger SHA-256: `88c9b05861b3a12d3b7427da8510e4bdf5cf1acbab9d1a3e7c5153365e9f4451`.
- Scope guard: exactly 43 rows, `SB-TOC-001` through `SB-TOC-043`; `P0=18`, `P1=15`, `P2=8`, `P3=2`; all 43 live verdict fields were unadjudicated at entry.
- Fresh focused execution on unchanged production bytes: `26 passed / 0 failed / 0 ignored` in `unconventional.rs`.
- Manual evidence remains source-owned and unchanged: unconventional `0/4`, workflow `0/23`, report `6/53`, LAS export `0/2`, processing history `0/7`, workspace shell `0/159`.
- This increment changes no production code, test, PRD, research dossier, parameter source, manual-evidence state, or generated verification artifact.

## Evidence, custody, and harsh-truth boundaries

- The chapter defines 58 contiguous test intentions. The consolidated source index references 67 occurrences covering 56 unique IDs. `SB-TOC-T12` and `SB-TOC-T38` are chapter-defined but absent from every source-owned `owned_tests` field; this receipt routes them without altering that immutable discrepancy.
- Section 5 contains exactly 76 parameter rows and 23 explicit absences. Every absent maturity, baseline, background, final-selection, measured-gas, Langmuir-reference, Ambrose-property, and denominator value remains absent.
- O-1 through O-9, all seven escalation bullets, R-1 through R-10, the zero-Tier-C finding, and every section 8.1 through 8.6 disposition remain explicit.
- A unit string beside a bare `f32` is not a typed quantity. The current wt%-consistent chain can still accept a fraction-tagged meaning as a plain scalar and produce a plausible 100x result.
- Editable placeholders are still defaults. Current baseline, maturity, pressure, temperature, z, Langmuir, ash, moisture, and measured-content seeds can produce plausible outputs without a measured/source-bearing value.
- `GIP_ADS`, `GIP_FREE`, and `GIP_TOTAL` currently carry intensive scf/ton. This is a silent quantity-identity defect, not a cosmetic naming issue: a finite positive content curve can be read as extensive reserve volume.
- Generic versioned log sets preserve useful module, numeric-parameter, input, set, and time evidence. They do not automatically preserve option/method, pick interval/revision, conversions, domain masks, calibration, standard condition, stable flags/counts, or software revision.
- The custom panel duplicates compute constants and stores local view state. Matching values today are not shared custody; `dataVersion` invalidation is not a persisted parameter/equation revision.
- Twenty-six green local tests prove selected arithmetic, clamp, NaN, and branch behavior. They do not prove the full typed, persisted, migrated, reported, exported, or manually exercised compound contracts.

## Acceptance-test routing

| Test intention | Primary receipt owner | Index ownership |
|---|---|---|
| SB-TOC-T01 | SB-TOC-001 | source index references this ID |
| SB-TOC-T02 | SB-TOC-017 | source index references this ID |
| SB-TOC-T03 | SB-TOC-004 | source index references this ID |
| SB-TOC-T04 | SB-TOC-018 | source index references this ID |
| SB-TOC-T05 | SB-TOC-003 | source index references this ID |
| SB-TOC-T06 | SB-TOC-020 | source index references this ID |
| SB-TOC-T07 | SB-TOC-020 | source index references this ID |
| SB-TOC-T08 | SB-TOC-003 | source index references this ID |
| SB-TOC-T09 | SB-TOC-006 | source index references this ID |
| SB-TOC-T10 | SB-TOC-004 | source index references this ID |
| SB-TOC-T11 | SB-TOC-005 | source index references this ID |
| SB-TOC-T12 | SB-TOC-005 | chapter-defined; absent from source-index `owned_tests` |
| SB-TOC-T13 | SB-TOC-005 | source index references this ID |
| SB-TOC-T14 | SB-TOC-002 | source index references this ID |
| SB-TOC-T15 | SB-TOC-040 | source index references this ID |
| SB-TOC-T16 | SB-TOC-011 | source index references this ID |
| SB-TOC-T17 | SB-TOC-012 | source index references this ID |
| SB-TOC-T18 | SB-TOC-012 | source index references this ID |
| SB-TOC-T19 | SB-TOC-014 | source index references this ID |
| SB-TOC-T20 | SB-TOC-015 | source index references this ID |
| SB-TOC-T21 | SB-TOC-013 | source index references this ID |
| SB-TOC-T22 | SB-TOC-008 | source index references this ID |
| SB-TOC-T23 | SB-TOC-008 | source index references this ID |
| SB-TOC-T24 | SB-TOC-007 | source index references this ID |
| SB-TOC-T25 | SB-TOC-009 | source index references this ID |
| SB-TOC-T26 | SB-TOC-016 | source index references this ID |
| SB-TOC-T27 | SB-TOC-017 | source index references this ID |
| SB-TOC-T28 | SB-TOC-018 | source index references this ID |
| SB-TOC-T29 | SB-TOC-018 | source index references this ID |
| SB-TOC-T30 | SB-TOC-019 | source index references this ID |
| SB-TOC-T31 | SB-TOC-020 | source index references this ID |
| SB-TOC-T32 | SB-TOC-019 | source index references this ID |
| SB-TOC-T33 | SB-TOC-021 | source index references this ID |
| SB-TOC-T34 | SB-TOC-023 | source index references this ID |
| SB-TOC-T35 | SB-TOC-022 | source index references this ID |
| SB-TOC-T36 | SB-TOC-022 | source index references this ID |
| SB-TOC-T37 | SB-TOC-024 | source index references this ID |
| SB-TOC-T38 | SB-TOC-024 | chapter-defined; absent from source-index `owned_tests` |
| SB-TOC-T39 | SB-TOC-024 | source index references this ID |
| SB-TOC-T40 | SB-TOC-025 | source index references this ID |
| SB-TOC-T41 | SB-TOC-026 | source index references this ID |
| SB-TOC-T42 | SB-TOC-028 | source index references this ID |
| SB-TOC-T43 | SB-TOC-029 | source index references this ID |
| SB-TOC-T44 | SB-TOC-027 | source index references this ID |
| SB-TOC-T45 | SB-TOC-027 | source index references this ID |
| SB-TOC-T46 | SB-TOC-030 | source index references this ID |
| SB-TOC-T47 | SB-TOC-031 | source index references this ID |
| SB-TOC-T48 | SB-TOC-032 | source index references this ID |
| SB-TOC-T49 | SB-TOC-033 | source index references this ID |
| SB-TOC-T50 | SB-TOC-034 | source index references this ID |
| SB-TOC-T51 | SB-TOC-036 | source index references this ID |
| SB-TOC-T52 | SB-TOC-035 | source index references this ID |
| SB-TOC-T53 | SB-TOC-037 | source index references this ID |
| SB-TOC-T54 | SB-TOC-038 | source index references this ID |
| SB-TOC-T55 | SB-TOC-039 | source index references this ID |
| SB-TOC-T56 | SB-TOC-041 | source index references this ID |
| SB-TOC-T57 | SB-TOC-042 | source index references this ID |
| SB-TOC-T58 | SB-TOC-043 | source index references this ID |

Every chapter test is counted once above. Shared ownership remains visible in the immutable ledger; the two source-index gaps are neither hidden nor repaired.

## Requirement receipts

### SB-TOC-001

- **Specified contract:** Every TOC input, parameter, and output carries a typed `wt%` or `w/w` identity; canonical computation stays in wt% and named conversions round-trip.
- **Current implementation:** Passey and kerogen are numerically consistent in wt%, and manifest strings label units, but `ModuleOutputs` remains `HashMap<String, Vec<f32>>`; no typed TOC quantity or conversion/refusal API exists.
- **As-built status:** `PARTIAL`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T01, SB-TOC-T04: Current Passey and kerogen tests characterize the untyped numeric path; no test proves a typed round trip or untagged/fraction-tag refusal.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** The chapter's wt%-canonical boundary governs; no inferred conversion convention or untagged default is introduced.
- **UI/IPC/provenance surface:** `ArgSpec.unit` is display metadata, `ModuleOutputs` is bare arrays, no TOC family exists in `curves.rs`, and generic computed-curve export cannot infer this quantity safely.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** SB-CORE-001, SB-CORE-006, and SB-CORE-010 remain prerequisites for typed quantity and provenance custody.
- **Next action:** Introduce a typed TOC value/conversion boundary and prove both valid round-trip directions plus untagged and wrong-tag refusals.

### SB-TOC-002

- **Specified contract:** All four delta-log-R baselines ship absent and one interval/depth pick persists every available channel plus its curve revision.
- **Current implementation:** The module preselects numeric resistivity, sonic, and density baselines; the custom panel duplicates them as independently editable local values, with no neutron baseline or coupled persisted pick.
- **As-built status:** `PRESENT-DIVERGENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `MISSING`. SB-TOC-T14, SB-TOC-T15: No executable test refuses every missing baseline or round-trips one coupled interval and curve-revision record.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** All four baseline parameters remain `ABSENT` under chapter section 5; current seeds are evidence of divergence, not authorized defaults.
- **UI/IPC/provenance surface:** Module-dialog defaults and panel-local controls are separate surfaces; `getState` is view persistence, not an undoable compute-parameter record.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** A coupled pick schema and revision identity are unresolved implementation prerequisites, not values to guess.
- **Next action:** Remove active defaults only in Gate 2, add one coupled provenance-bearing pick, and prove missing-baseline refusal and reopen round-trip.

### SB-TOC-003

- **Specified contract:** Sonic, density, and neutron separations use their cited native coefficients, preserve separate outputs, and metric sonic enters only through explicit conversion.
- **Current implementation:** Sonic and density branches are implemented and tested; neutron, an `NPHI_BASE` input, explicit sonic-unit conversion, and separate overlay output family are absent.
- **As-built status:** `PARTIAL`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `UNDECIDED`; `DEGRADED-RESULT`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T05, SB-TOC-T08, SB-TOC-T34: Existing sonic and density tests characterize two branches; they do not prove metric conversion, neutron sign/magnitude, or three distinct outputs.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Only the chapter-cited coefficients may be used; the historical negative-neutron note is superseded and is not an implementation source.
- **UI/IPC/provenance surface:** The panel offers only sonic/density; module output is one selected `TOC` plus `DLOGR`, so the third branch and output identity never reach IPC/export.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P1 pilot inclusion remains undecided; implementing neutron still depends on the already-cited chapter contract, not a new local calibration.
- **Next action:** Add the third branch, typed sonic conversion, and separate outputs, then prove both unit paths and all three signs from independent fixtures.

### SB-TOC-004

- **Specified contract:** Passey TOC equals the cited native-wt% expression with the Passey term floored before background is added.
- **Current implementation:** `toc_passey` implements the exact exponent, multiplication, floor, then background order for its active sonic and density branches.
- **As-built status:** `PRESENT-OK`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `FIELD-EVIDENCE`.
- **Automated evidence:** `CORRECTNESS`. SB-TOC-T03, SB-TOC-T09, SB-TOC-T10: `sonic_overlay_recovers_known_toc`, `density_overlay_recovers_known_toc_and_schmoker`, and `non_source_interval_floors_toc_to_background_not_zero` independently pin the cited arithmetic and order for active branches.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Expected literals and arithmetic come from chapter sections 4 and 6; this row does not authorize missing baselines, background defaults, neutron, or typed-unit shortcuts.
- **UI/IPC/provenance surface:** The correct scalar equation is registered, but broader typed/provenance/manual surfaces remain owned by other rows.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** Manual unconventional evidence remains 0/4, so first-sale evidence is still open even though this narrow arithmetic contract is automated.
- **Next action:** Retain the equation and order unchanged; close the release row only after surrounding typed custody and representative manual evidence exist.

### SB-TOC-005

- **Specified contract:** Direct LOM or the cited VR polynomial emits raw maturity, applies the defeatable cited cap, warns below the lower evidence boundary, and emits a stable cap flag.
- **Current implementation:** Only direct LOM exists; the manifest validates 6 through 12, while raw maturity, VR conversion, a 10.5 cap, cap opt-out, low warning, and flag are absent.
- **As-built status:** `PRESENT-DIVERGENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T11, SB-TOC-T12, SB-TOC-T13: `toc_decreases_with_maturity_lom` characterizes current direct-LOM monotonicity only; cap, VR polynomial, raw value, warning, and flag remain unproved.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** LOM and VR ship absent; only the chapter-cited polynomial, cap, and warning evidence may be implemented. T12 is chapter-defined but absent from source-owned `owned_tests`.
- **UI/IPC/provenance surface:** The module dialog presents a preselected numeric LOM and range; neither current curves nor run summaries expose raw/capped maturity or reason code.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** No new maturity default is selected; the cited direct/VR methods and opt-out behavior must be explicit.
- **Next action:** Implement raw/capped maturity with source-bearing selection and stable flag, preserving cap-on and cap-off results.

### SB-TOC-006

- **Specified contract:** Background TOC ships absent and persists as part of the same baseline interval rather than as a global scalar.
- **Current implementation:** The manifest and panel preselect zero; the compute path also substitutes zero when `TOC_BG` is non-finite, and no coupled baseline record exists.
- **As-built status:** `PRESENT-DIVERGENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T09, SB-TOC-T14, SB-TOC-T15: Background-offset and floor-order tests characterize arithmetic once a number exists; they do not prove absent-by-default or baseline-bound persistence.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** `TOC_BG` remains absent; the cited seed is contextual only and is not promoted.
- **UI/IPC/provenance surface:** The module scalar, panel view state, and generic numeric `params_json` do not represent one interval/revision pick.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** Depends on the SB-TOC-002 coupled baseline schema; no global background value may be inferred.
- **Next action:** Require an explicit baseline-bound background value and prove missing refusal plus persisted reopen identity.

### SB-TOC-007

- **Specified contract:** All three overlay TOCs plus minimum, average, maximum, and spread are emitted together while final selection ships absent.
- **Current implementation:** One selected sonic or density branch writes generic `TOC`; neutron, simultaneous alternatives, aggregates, spread, and an absent final-selection record do not exist.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `UNDECIDED`; `DEGRADED-RESULT`.
- **Automated evidence:** `MISSING`. SB-TOC-T24: No executable test proves simultaneous alternatives or the aggregate/spread invariants.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Final selection remains absent; no incumbent default or house weighting is chosen.
- **UI/IPC/provenance surface:** Neither module outputs, catalog families, panel, report, nor export carries the ensemble.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P1 pilot inclusion and the final selection decision remain open; proprietary weighted combinations stay refused.
- **Next action:** Implement the unselected ensemble and exact aggregate identities without introducing a final preselection.

### SB-TOC-008

- **Specified contract:** Coal, reservoir/sand, and badhole exclusions execute before every TOC method with one polarity, null output, and stable reason.
- **Current implementation:** The generic workflow `MASK` blanks flagged inputs before any module and blanks outputs afterward, so a user-selected badhole-style flag can exclude TOC samples. Domain-specific coal/reservoir masks, combined policy, and reason codes are absent.
- **As-built status:** `PARTIAL`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T22, SB-TOC-T23: The generic workflow mask test characterizes the universal pre/post blanking seam; no TOC test proves the three domain masks or stable reasons.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** The cited mask cutoffs remain contextual and no new geological cutoff or polarity is invented.
- **UI/IPC/provenance surface:** Generic `MASK` is selected outside the manifest and produces NaN without a domain reason curve or run-summary code.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** The generic wrapper is supporting infrastructure only; domain mask definitions and stable observability remain required.
- **Next action:** Register explicit domain-mask inputs/policy and prove coal and badhole cases from both masked and valid sides.

### SB-TOC-009

- **Specified contract:** Per-zone lab calibration preserves raw and calibrated outputs, pairing, fit statistics, revisions, and provenance; identity calibration is explicit.
- **Current implementation:** No TOC calibration command, fit object, UI, result pair, statistics, or persisted lab pairing is registered.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `UNDECIDED`; `DATA-INTEGRITY`.
- **Automated evidence:** `MISSING`. SB-TOC-T25: No executable TOC calibration test exists; neighboring fit infrastructure is not domain acceptance evidence.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Calibration coefficients ship absent unless fitted from supplied data; the cited identity pair is a declared no-calibration state, not a universal fit.
- **UI/IPC/provenance surface:** No calibration payload reaches module IPC, curves, report, or export.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** A typed lab-pairing and revision model is required before any slope/intercept can exist.
- **Next action:** Implement an explicit per-zone fit artifact and prove identity calibration bit-preserves the raw prediction.

### SB-TOC-010

- **Specified contract:** Every TOC output carries complete method, pick, maturity, mask, calibration, conversion, and software provenance.
- **Current implementation:** Versioned workflow runs persist module name, numeric request parameters, input names, set/version, and creation time. They omit option/method choices from `params_json` and do not cover pick interval/revisions, conversions, masks, calibration, source note, flags/counts, or software revision.
- **As-built status:** `PARTIAL`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T57: Existing workflow/log-set tests characterize generic versioning and parameter/input persistence; no TOC whole-contract provenance test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Chapter sections 4, 6, and SB-CORE-010 define the required fields; unknown provenance remains unknown rather than inferred.
- **UI/IPC/provenance surface:** LAS export can emit the generic log-set record, but reports and curves do not acquire the missing domain fields and options automatically.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** Generic history is a prerequisite, not complete TOC provenance; the missing schema fields are implementation work.
- **Next action:** Define one immutable unconventional run record and link curves, plots, reports, and exports to it.

### SB-TOC-011

- **Specified contract:** The cited density cross-check retains its exact equation and durable method identity, including observable clamp behavior.
- **Current implementation:** The exact scalar equation writes `TOC_SCHMOKER`; negatives are silently clamped to zero. The raw pre-clamp value and complete per-output method metadata are absent.
- **As-built status:** `PARTIAL`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `UNDECIDED`; `DATA-INTEGRITY`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T16: The density-overlay test characterizes the equation at one positive fixture; it does not prove zero crossing, raw retention, or persisted/exported identity.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Only the cited cross-check constants are used; this row does not authorize a regional density regression.
- **UI/IPC/provenance surface:** The output mnemonic carries local identity, while generic run provenance names the parent module and lacks per-output raw/clamp metadata.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P1 inclusion remains undecided; raw companion and durable identity are still required for whole-contract closure.
- **Next action:** Retain the equation, add raw/clamped observability and per-output method metadata, then test both sides of the zero crossing.

### SB-TOC-012

- **Specified contract:** The generalized density-deficit method uses semantic densities, harmonic weight mixing, explicit sub-unity normalization, and over-unity refusal.
- **Current implementation:** Only the fixed density cross-check exists; no generalized-density arguments, harmonic organic-density calculation, normalization, or refusal exists.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `DEFERRED`; `LATER`.
- **Automated evidence:** `MISSING`. SB-TOC-T17, SB-TOC-T18: No executable generalized-density test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Only the chapter-cited densities and weights may be offered; the full compiled four-component body remains source-gated separately.
- **UI/IPC/provenance surface:** No module, UI, IPC, output, report, or export path exists.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P2 capability is deferred; do not substitute the fixed cross-check or infer a compiled equation.
- **Next action:** Implement later from the cited physical form with semantic parameters and two-sided weight-sum tests.

### SB-TOC-013

- **Specified contract:** Uranium TOC uses the cited gain, preserves raw/clamped values, and warns when uranium is not an admissible organic proxy.
- **Current implementation:** No uranium-TOC module, method option, warning, raw curve, or flag exists.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `DEFERRED`; `LATER`.
- **Automated evidence:** `MISSING`. SB-TOC-T21: No executable uranium-TOC test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** The cited gain/offset and environmental limitation remain the only authorized basis.
- **UI/IPC/provenance surface:** Uranium curve-family support elsewhere is an input seam, not this method or its validity warning.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P2 capability is deferred and must not be inferred from generic uranium handling.
- **Next action:** Implement later with a named validity gate, raw companion, and warning fixture.

### SB-TOC-014

- **Specified contract:** TOC-to-kerogen and kerogen-to-TOC are inverse under one parameter set; invalid results are flagged rather than silently propagated.
- **Current implementation:** Forward TOC-to-kerogen arithmetic exists. Inverse conversion is absent; forward bulk volume and corrected porosity are silently clamped/floored without stable flags.
- **As-built status:** `PARTIAL`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T19: Five kerogen tests characterize forward arithmetic, monotonicity, missing RHOB, and current clamps; no inverse round-trip or flag test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** `KCF` and `RHO_KER` remain source-bound; no alternate factor is substituted.
- **UI/IPC/provenance surface:** The module returns three bare curves; inverse identity and clamp reasons do not reach IPC/provenance/export.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** The active forward path is pilot-reachable and silent clamps must become observable before release.
- **Next action:** Add inverse operation under the same parameters, preserve raw values, and prove forward/inverse plus invalid-bound cases.

### SB-TOC-015

- **Specified contract:** The cited 1.10 kerogen-density endpoint remains aligned with the mineral response and carries correct provenance, alternatives, and maturity context.
- **Current implementation:** The value 1.10 is active and aligns numerically with the mineral endpoint, but the module documentation compares/cites an unrelated density context; alternatives and complete response metadata are not durable output provenance.
- **As-built status:** `PRESENT-DIVERGENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `MISSING`. SB-TOC-T19, SB-TOC-T20: No executable test loads the complete density/neutron/sonic response metadata and rejects the unrelated provenance.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** The cited 1.10 value is retained exactly; no endpoint or maturity alternative is invented.
- **UI/IPC/provenance surface:** Manifest prose is not curve metadata, and the generic run record does not carry the endpoint response/provenance set.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** Correcting attribution and durable metadata is required; production math must not be changed merely because the old prose was wrong.
- **Next action:** Retain 1.10, replace the wrong provenance in Gate 2, expose sourced alternatives/context, and test the full response record.

### SB-TOC-016

- **Specified contract:** The cited pyrite-to-kerogen regression may be offered, while the compiled four-component TOC body stays unavailable until primary equations are held.
- **Current implementation:** No pyrite-coupled TOC option or explicit source-gate refusal exists; the compiled body also remains absent.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `DEFERRED`; `LATER`.
- **Automated evidence:** `MISSING`. SB-TOC-T26: No executable regression-option or named source-gate test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** O-4 remains open. Held regression parameters do not authorize reconstruction of the compiled solver.
- **UI/IPC/provenance surface:** No registered capability or refusal surface exists.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P2 is deferred; compiled behavior must not be inferred from binaries or outputs.
- **Next action:** If later needed, expose only the cited regression as a named option and keep the compiled body behind an explicit source gate.

### SB-TOC-017

- **Specified contract:** Typed TOC and selected kerogen-type HI polynomials compute S2 with type and maturity provenance.
- **Current implementation:** No HI polynomial, S2 output, typed-TOC input, kerogen-type selection, or provenance path exists.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `DEFERRED`; `LATER`.
- **Automated evidence:** `MISSING`. SB-TOC-T02, SB-TOC-T27: No executable typed-S2 or polynomial test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Only the two chapter-cited polynomials may be implemented; LOM and type provenance must remain explicit.
- **UI/IPC/provenance surface:** No module/UI/IPC/report/export surface exists.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P2 capability is deferred and depends on SB-TOC-001 typed TOC custody.
- **Next action:** Implement later after typed TOC, proving percent/fraction identity and both sourced polynomial fixtures.

### SB-TOC-018

- **Specified contract:** All six RockEval indices and the complete pyrolysed/residual carbon branches ship together with correct units and reconstructed TOC.
- **Current implementation:** No RockEval module, branch selector, index output, carbon balance, unit metadata, or refusal surface exists.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `DEFERRED`; `LATER`.
- **Automated evidence:** `MISSING`. SB-TOC-T04, SB-TOC-T28, SB-TOC-T29: No executable RockEval test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** The chapter-cited stoichiometric forms remain exact; no simplified denominator or partial index subset is authorized.
- **UI/IPC/provenance surface:** No module/UI/IPC/report/export surface exists.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P2 capability is deferred; shipping only familiar indices would violate the compound contract.
- **Next action:** Implement the complete branch later with typed TOC and explicit ppk/wt%/mass-ratio outputs.

### SB-TOC-019

- **Specified contract:** Measured pressure, temperature, z, Langmuir values, ash, moisture, and measured gas content ship absent and carry units plus sample/lab provenance.
- **Current implementation:** The module preselects all eight as ordinary numeric parameters, and the panel duplicates pressure and Langmuir values; no sample/lab provenance is captured.
- **As-built status:** `PRESENT-DIVERGENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T30, SB-TOC-T32: Langmuir and gas tests characterize arithmetic with supplied numbers; no test proves every missing measured input is named and no placeholder runs.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** All eight measured properties remain absent under chapter section 5; active seeds are not transferable defaults.
- **UI/IPC/provenance surface:** Manifest ranges and editable fields do not provide measurement provenance; generic `params_json` records values only after they were already preselected.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** The active module can produce plausible gas content from placeholders, so this is a first-sale blocker.
- **Next action:** Make each measured input explicitly supplied/source-bearing and prove a complete missing-input refusal list.

### SB-TOC-020

- **Specified contract:** Langmuir capacity scales through exactly one unit-matched TOC or kerogen-volume reference pair, with crossed pairs refused.
- **Current implementation:** Current Langmuir capacity is a flat scalar and consumes neither TOC nor kerogen volume; no pair selector or unit guard exists.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `MISSING`. SB-TOC-T06, SB-TOC-T07, SB-TOC-T31: No executable scaling, matched-unit, or crossed-pair refusal test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** `TOC_LANG` and `VOL_KER_LANG` remain absent; no reference pair is chosen automatically.
- **UI/IPC/provenance surface:** The custom panel plots the same unscaled scalar and does not consume a typed organic input.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** The active gas-content method lacks its required organic coupling; the pair must remain explicit.
- **Next action:** Implement mutually exclusive typed routes and prove identity, half-capacity, and crossed-pair refusal.

### SB-TOC-021

- **Specified contract:** The cited regional Langmuir temperature correction is opt-in, off by default, retains native coefficients, and carries source context.
- **Current implementation:** No isotherm temperature-correction option, coefficients, provenance, or output exists; reservoir temperature is used only for Bg.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `DEFERRED`; `LATER`.
- **Automated evidence:** `MISSING`. SB-TOC-T33: No executable temperature-correction test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** The cited coefficients remain contextual and the option remains off; no universal default is inferred.
- **UI/IPC/provenance surface:** Neither module nor panel distinguishes measurement temperature from reservoir-temperature Bg use.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P2 capability is deferred; reservoir temperature is not a substitute for isotherm correction.
- **Next action:** Implement later as an explicit source-labelled option with the chapter fixture.

### SB-TOC-022

- **Specified contract:** Free gas uses porosity times `(1-Sw-So)`, derates by `(1-NCGF)`, and refuses invalid phase sums.
- **Current implementation:** The active path uses only porosity times `(1-Sw)`; oil saturation, non-combustible fraction, phase-sum refusal, and flags are absent.
- **As-built status:** `PRESENT-DIVERGENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T35, SB-TOC-T36: The current free-gas fixture characterizes the two-phase special case where omitted terms are zero; it cannot prove nonzero oil/NCGF behavior or refusal.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Only the chapter-cited phase expression is authorized; no saturation or gas fraction is invented.
- **UI/IPC/provenance surface:** Bare output curves and generic warnings do not expose phase-sum reason or derate provenance.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** The active formula over-calls gas whenever either omitted term is nonzero, so this remains a pilot blocker.
- **Next action:** Add typed So/NCGF inputs, explicit phase validation, and two-sided base/nonzero fixtures.

### SB-TOC-023

- **Specified contract:** Bg carries one z-factor and explicit standard pressure/temperature metadata; alternative standards are converted and the contradictory factor stays refused.
- **Current implementation:** The active formula hard-codes `0.02827` and emits `BG`, but no standard-condition fields, alternative conversion identity, or refusal metadata travel with the curve.
- **As-built status:** `PRESENT-DIVERGENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T34: The free-gas test characterizes the current coefficient at one fixture; no test proves named standards, conversion, or extra-factor refusal.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** The chapter-cited standard condition governs; the contradictory factor is not adopted.
- **UI/IPC/provenance surface:** `BG` has a manifest unit only; generic run/export metadata cannot recover the hidden standard condition.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** A plausible Bg with unnamed standards can be mixed silently, so this is a first-sale blocker.
- **Next action:** Make standard conditions explicit typed metadata and test both named alternatives plus refusal of the contradictory factor.

### SB-TOC-024

- **Specified contract:** Ambrose correction uses sourced, absent-by-default gas molar mass and sorbed density, correct bracketing, raw companion, and overshoot flag.
- **Current implementation:** The module explicitly defers Ambrose; no parameters, correction, raw value, bracket option, or flag exists while active free gas remains uncorrected.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `MISSING`. SB-TOC-T37, SB-TOC-T38, SB-TOC-T39: No executable Ambrose unit-equivalence, bracket, raw-overshoot, or flag test exists. T38 is chapter-defined but absent from source-owned `owned_tests`.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** `M_GAS` and `RHO_ADS` remain absent under O-5; no plausible fluid values are selected.
- **UI/IPC/provenance surface:** No module, UI, IPC, report, or export surface states whether free gas is corrected.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** Because the active free-gas output can be interpreted as complete, the missing correction remains a pilot blocker until the product scope explicitly excludes it.
- **Next action:** Implement only after sourced properties are supplied, retaining raw/guarded values and stable overshoot code.

### SB-TOC-025

- **Specified contract:** Intensive scf/ton outputs use `GC_*`; `GIP_*` is reserved for extensive Bcf and mnemonic aliases cannot override quantity.
- **Current implementation:** The module explicitly says gas content and units scf/ton but emits `GIP_ADS`, `GIP_FREE`, and `GIP_TOTAL`; no TOC/gas quantity family or collision refusal exists.
- **As-built status:** `PRESENT-DIVERGENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `MISSING`. SB-TOC-T40: No executable quantity-collision test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** The chapter's `GC_*` versus `GIP_*` reservation governs; explanatory prose is not a type guard.
- **UI/IPC/provenance surface:** Catalog/export may carry scf/ton beside the mnemonic, but a bare curve name and downstream selection can still misstate intensive content as volume.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** This is the harshest current semantic defect: finite positive values look credible under a reserve-volume name.
- **Next action:** Introduce typed quantity identity and an explicit migration; refuse mnemonic/quantity collisions rather than silently aliasing.

### SB-TOC-026

- **Specified contract:** Areal adsorbed, free, and total Bcf use declared area/thickness units and the same corrected pore volume as intensive free gas.
- **Current implementation:** No area, thickness, rock-mass conversion, Bcf output, or extensive result type exists.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `UNDECIDED`; `REQUESTED-CAPABILITY`.
- **Automated evidence:** `MISSING`. SB-TOC-T41: No executable intensive-to-extensive identity test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Only the chapter-cited conversion constants may be used; area/thickness inputs have no default.
- **UI/IPC/provenance surface:** No module/UI/report/export surface exists for extensive GIP.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P1 pilot inclusion remains undecided; this absence must not be hidden by current `GIP_*` names.
- **Next action:** Implement only with typed geometry and shared corrected pore volume, then prove intensive/extensive consistency.

### SB-TOC-027

- **Specified contract:** Critical desorption is guarded, ash/moisture require measured fractions, dry-basis content is preserved, and negative remaining fraction refuses.
- **Current implementation:** PCD is emitted only for finite `0<GC<VL`. In CBM mode, missing ash/moisture silently become zero, remaining fraction is clamped to [0,1], and only the derated curve survives.
- **As-built status:** `PRESENT-DIVERGENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T44, SB-TOC-T45: PCD and ash/moisture tests characterize valid arithmetic; they do not prove missing-input refusal, negative-fraction refusal, or dry-basis preservation.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Ash, moisture, and measured gas content remain absent unless supplied; sulfur remains an explicit gap and is not invented.
- **UI/IPC/provenance surface:** No stable refusal or dry-basis companion reaches output/provenance/reporting.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** Silent zero substitution changes active CBM results and is therefore a pilot blocker.
- **Next action:** Require measured fractions, preserve dry and in-situ outputs, and test valid, missing, and negative-remaining cases.

### SB-TOC-028

- **Specified contract:** Isotherm fitting exposes linearized and direct nonlinear estimators as different methods with residuals and no silent inheritance.
- **Current implementation:** No isotherm-fit command, estimator selector, residual output, or fit artifact exists; the panel plots supplied constants only.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `DEFERRED`; `LATER`.
- **Automated evidence:** `MISSING`. SB-TOC-T42: No executable isotherm-fit test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** O-6 remains open; neither estimator becomes a default without measured-data comparison.
- **UI/IPC/provenance surface:** The visual isotherm is not a fitting UI and persists no estimator or residuals.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P2 capability is deferred pending estimator evaluation on supplied desorption data.
- **Next action:** Implement both named estimators later and keep the method explicit in the fit artifact.

### SB-TOC-029

- **Specified contract:** TOC-to-Langmuir calibration is through-origin by default and any nonzero intercept is visibly nonphysical-at-zero and acknowledged.
- **Current implementation:** No TOC-capacity calibration, slope/intercept fit, acknowledgement, or persisted experiment exists.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `UNDECIDED`; `REQUESTED-CAPABILITY`.
- **Automated evidence:** `MISSING`. SB-TOC-T43: No executable calibration-at-zero test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** The slope ships absent and zero intercept is only the constraint once a fit exists; no regression is invented.
- **UI/IPC/provenance surface:** No module/UI/IPC/report/export surface exists.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P1 pilot inclusion remains undecided and depends on supplied laboratory pairs.
- **Next action:** Implement a through-origin fit artifact and explicit nonzero-intercept acknowledgement path.

### SB-TOC-030

- **Specified contract:** Dynamic Rickman arithmetic emits BI on [0,1] and durable curve metadata states both dynamic basis and scale.
- **Current implementation:** The module computes dynamic E/nu and BI correctly and manifest prose names dynamic/[0,1]. The written `BI` curve lacks a registered brittleness/BI quantity family and the selected `METHOD` option is not persisted in workflow `params_json`, so per-curve basis/scale custody is incomplete.
- **As-built status:** `PARTIAL`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T46: Elastic tests characterize current arithmetic and guards; no test proves both cited endpoints plus stored/exported dynamic/[0,1] metadata.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Only the cited dynamic endpoints and scale are credited; no static conversion is selected.
- **UI/IPC/provenance surface:** Manifest text can disappear from a curve copied or exported independently; the durable curve/run record is incomplete.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** Correct arithmetic does not close the P0 metadata and field-evidence contract.
- **Next action:** Persist method basis and scale per output, then prove endpoints and export/reopen metadata.

### SB-TOC-031

- **Specified contract:** Static moduli cannot use dynamic endpoints without supplied static calibration/endpoints.
- **Current implementation:** The module accepts DT/DTS/RHOB and computes dynamic moduli; it has no typed precomputed-modulus input, static-basis selector, calibrated static endpoints, or explicit static-input refusal.
- **As-built status:** `PARTIAL`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T47: Elastic tests characterize the dynamic-only route and shear guards; no test submits static moduli and observes the required refusal.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Static conversion and endpoints remain absent; none is inferred from a range or textbook relationship.
- **UI/IPC/provenance surface:** The current input names imply dynamic computation, but basis does not travel as a type on every incoming/result curve.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** An unreachable static path is not a named refusal; dynamic custody must be explicit before first sale.
- **Next action:** Add typed modulus basis and explicit static refusal, leaving calibration absent until supplied.

### SB-TOC-032

- **Specified contract:** Jarvie and Wang-Gale identities remain separate, and zero for a missing mineral is allowed only when absence is explicit rather than unresolved mapping.
- **Current implementation:** Both equations exist and local tests distinguish dolomite treatment, but every NaN/missing mapped curve is converted to zero and the chosen `METHOD` option is not persisted in the generic run record.
- **As-built status:** `PRESENT-DIVERGENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `CHARACTERIZATION`. SB-TOC-T48: Mineralogical tests characterize the two equations and all-missing behavior; they do not distinguish explicit absence from unresolved mapping or durable method identity.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** No mineral fraction or method is inferred beyond the selected option; unresolved mapping must remain unresolved.
- **UI/IPC/provenance surface:** Both methods write generic `BI`; lost option provenance plus NaN-to-zero can produce a plausible number under ambiguous identity.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** The active path silently treats mapping failure as geology, so it is a first-sale blocker.
- **Next action:** Carry explicit per-input mapping state, persist method identity, and prove absent versus unresolved controls.

### SB-TOC-033

- **Specified contract:** Combined versus split C4/C5 channel mappings are mutually exclusive so every component is counted once.
- **Current implementation:** No mud-gas input map, ratio module, duplicate-channel guard, or product refusal exists.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `UNDECIDED`; `REQUESTED-CAPABILITY`.
- **Automated evidence:** `MISSING`. SB-TOC-T49: No executable channel double-count test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** No channel alias or combination rule is inferred from generic curve families.
- **UI/IPC/provenance surface:** No module/UI/IPC/report/export surface exists.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P1 inclusion remains undecided; generic import identity is not a mud-gas mapping contract.
- **Next action:** Define typed one-of mappings and prove combined/split valid paths plus double-count refusal.

### SB-TOC-034

- **Specified contract:** Full and compatibility GWR denominators are separately named and mode selection ships absent.
- **Current implementation:** No GWR implementation, mode selector, result, or explicit missing-mode refusal exists.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `UNDECIDED`; `REQUESTED-CAPABILITY`.
- **Automated evidence:** `MISSING`. SB-TOC-T50: No executable dual-mode arithmetic or absent-mode refusal test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** O-1 keeps the denominator unresolved; `GWR_MODE` remains absent and neither mode becomes a silent default.
- **UI/IPC/provenance surface:** No module/UI/IPC/report/export surface exists.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P1 inclusion remains undecided and primary-source acquisition still bounds any single-mode claim.
- **Next action:** Implement both explicit modes only; preserve absent selection and test the chapter fixture.

### SB-TOC-035

- **Specified contract:** Every finite ratio triple maps to exactly one half-open class; missing input is unclassified and rule gaps/overlaps fail registration.
- **Current implementation:** No mud-gas classifier or rule-registration validator exists.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `UNDECIDED`; `REQUESTED-CAPABILITY`.
- **Automated evidence:** `MISSING`. SB-TOC-T51, SB-TOC-T52: No executable total/disjoint classifier test or incumbent-table characterization exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** The defective class table is evidence only and must not be copied.
- **UI/IPC/provenance surface:** No module/UI/IPC/report/export surface exists.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P1 inclusion remains undecided; classification cannot precede resolved ratios and channel identity.
- **Next action:** Implement a validated half-open partition with an explicit missing state and a separate characterization of the rejected table.

### SB-TOC-036

- **Specified contract:** Only corroborated Haworth thresholds may seed a named classifier and every boundary follows the explicit half-open rule.
- **Current implementation:** No Haworth classifier, threshold registry, or boundary convention exists.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `UNDECIDED`; `REQUESTED-CAPABILITY`.
- **Automated evidence:** `MISSING`. SB-TOC-T51: No executable boundary sweep exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** The cited thresholds remain contextual seeds; denominator mode and primary-paper questions remain open.
- **UI/IPC/provenance surface:** No module/UI/IPC/report/export surface exists.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P1 inclusion remains undecided and depends on SB-TOC-034/035.
- **Next action:** Implement only after the ratio mode is explicit, then sweep exact boundaries from both sides.

### SB-TOC-037

- **Specified contract:** Pixler, oil-indicator, and gas-quality extensions remain unavailable until public primary equations are independently documented and tested.
- **Current implementation:** No extension or explicit request/refusal surface exists; compiled behavior has not been reconstructed.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `DEFERRED`; `LATER`.
- **Automated evidence:** `MISSING`. SB-TOC-T53: No executable request that names the source gate exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** The primary-equation acquisition gap remains open; compiled behavior is never inferred.
- **UI/IPC/provenance surface:** A generic unknown-module error is not the domain-specific source-gate refusal.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P3 capability is deferred and legally/source bounded.
- **Next action:** Keep absent; if exposed later, add an explicit refusal until independently sourced specifications exist.

### SB-TOC-038

- **Specified contract:** Mud-gas normalization refuses while the meaning of the disputed reference constant remains unresolved.
- **Current implementation:** No normalizer or explicit normalization refusal exists.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `DEFERRED`; `LATER`.
- **Automated evidence:** `MISSING`. SB-TOC-T54: No executable refusal preserves the discrepancy as evidence only.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** The unresolved constant is not replaced by geometric arithmetic; the discrepancy remains evidence, not a default.
- **UI/IPC/provenance surface:** No module/UI/IPC/report/export surface exists.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P3 capability is deferred pending the primary normalization source.
- **Next action:** Keep normalization unavailable and add a named refusal only when the request surface exists.

### SB-TOC-039

- **Specified contract:** Measured total gas is compared with component sum using absolute/relative residuals and a missing-channel flag before interpretation.
- **Current implementation:** No mud-gas QC module, residual curves, missing-channel flag, or run summary exists.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `DEFERRED`; `LATER`.
- **Automated evidence:** `MISSING`. SB-TOC-T55: No executable residual fixture exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** The expected arithmetic is chapter-derived; no missing component is treated as zero.
- **UI/IPC/provenance surface:** No module/UI/IPC/report/export surface exists.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** P2 capability is deferred and depends on typed channel mapping.
- **Next action:** Implement later before ratio interpretation and prove complete versus missing-component cases.

### SB-TOC-040

- **Specified contract:** Visual baseline and Langmuir picks write the same typed, undoable compute-parameter records with zone, revisions, viewport, and source note.
- **Current implementation:** The custom panel edits local controls and returns `getState`; it never invokes a compute-parameter write or `pushUndo`, and its state lacks curve revisions, viewport provenance, and source note.
- **As-built status:** `PRESENT-DIVERGENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `MISSING`. SB-TOC-T15, SB-TOC-T56: No frontend acceptance test exercises the unconventional panel's parameter custody or undo.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Parameter values remain absent until explicitly picked; view/session state is not reclassified as compute provenance.
- **UI/IPC/provenance surface:** The plot can look authoritative while compute uses different module-dialog values.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** The visual/compute split is pilot-reachable and must close before release.
- **Next action:** Replace local-only picks with one typed undoable record and prove compute/visual reopen the identical revision.

### SB-TOC-041

- **Specified contract:** Visual and compute surfaces consume one registered method and parameter revision; source revisions invalidate the plot.
- **Current implementation:** The panel duplicates baseline and Langmuir equations/default fallbacks. `dataVersion` refetches curve samples but does not bind the visual to the module's parameter or equation revision.
- **As-built status:** `PRESENT-DIVERGENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `SILENT-WRONGNESS`.
- **Automated evidence:** `MISSING`. SB-TOC-T06, SB-TOC-T56: No frontend or integration test proves one shared persisted parameter/equation revision.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** No duplicated fallback becomes authoritative merely because it currently matches one module seed.
- **UI/IPC/provenance surface:** The UI and Rust can drift independently while both render plausible curves.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** This is a direct silent-divergence path on a user-facing interpretation surface.
- **Next action:** Make the panel consume the registered payload and test parameter and source-revision invalidation end to end.

### SB-TOC-042

- **Specified contract:** Stable unconventional QC codes exist for every named condition, with per-sample curves where applicable and matching run-summary counts.
- **Current implementation:** No unconventional code registry, flag curve, condition-specific reason, or aggregate count exists; NaN, clamp, and generic warned-run states lose the cause.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `MISSING`. SB-TOC-T57: No executable multi-condition flag/count test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Codes must describe the chapter's conditions; no threshold, flag meaning, or count is inferred from output NaNs.
- **UI/IPC/provenance surface:** Generic workflow status is run-level and cannot reconstruct per-sample reasons.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** Silent failure observability is P0 and remains a first-sale blocker.
- **Next action:** Define stable codes and prove sample curves, counts, and provenance across every listed condition.

### SB-TOC-043

- **Specified contract:** A domain migration preserves legacy intensive `GIP_*` values/units/provenance and requires acknowledgement before creating `GC_*` or recomputing.
- **Current implementation:** No TOC/unconventional semantic migration, acknowledgement UI, or legacy quantity marker exists. Generic versioned archives preserve bytes but do not reinterpret or relabel them safely.
- **As-built status:** `ABSENT`; neighboring helpers, manifest prose, or a numerically correct subset do not receive credit for unimplemented observable limbs.
- **Release disposition and risk:** `PILOT-BLOCKER`; `DATA-INTEGRITY`.
- **Automated evidence:** `MISSING`. SB-TOC-T58: No executable legacy-project migration test exists.
- **Manual evidence:** unconventional `0/4`; workflow `0/23`; report `6/53`; LAS export `0/2`; processing history `0/7`; no checked unconventional scenario closes this row.
- **Source/parameter boundary:** Legacy meanings must be preserved exactly; no old curve may be silently treated as Bcf or silently renamed.
- **UI/IPC/provenance surface:** Current project open/migration infrastructure has no domain-specific branch for this semantic change.
- **History/reachability:** Current source, exact-symbol tests, and reachable history contain the original four module increments plus the custom panel and no later whole-contract closure.
- **Blocking decision/dependency:** SB-TOC-025 and removal of active defaults require an explicit migration before production remediation can ship.
- **Next action:** Design a versioned acknowledged migration and prove old curves remain intact until the user elects the new quantity identities.

## Parameter, open-item, escalation, refusal, and traceability custody

- All 76 parameter rows remain unchanged. Twenty-three remain explicitly absent; historical/example seeds are not promoted.
- O-1 through O-9 remain open: denominator mode, maturity conversion/range, compiled density body, fluid properties, estimator choice, runtime unit behavior, and final overlay selection are not guessed closed.
- All seven escalation bullets remain source-acquisition or controlled-runtime questions. A future source or vendor run may characterize behavior but does not establish correctness by itself.
- R-1 through R-10 remain separate defect refusals: no untagged TOC, universal baselines, anonymous regressions, factor substitution, simplified carbon balance, content-as-GIP naming, crossed Langmuir pairs, contradictory gas factor, defective class partition, or compiled-equation inference.
- No Tier-C item falls in this domain.
- Every section 8.1 through 8.6 requirement, inventory, discrepancy, gap, critique, and completeness disposition retains chapter custody.

## Adjudication summary

- As-built: `22 ABSENT`, `8 PARTIAL`, `12 PRESENT-DIVERGENT`, `1 PRESENT-OK`, `0 PRESENT-UNVERIFIED`.
- Release disposition: `23 PILOT-BLOCKER`, `10 UNDECIDED`, `10 DEFERRED`, `0 OUT`.
- Acceptance evidence: `1 CORRECTNESS`, `15 CHARACTERIZATION`, `27 MISSING`.
- Manual evidence remains unchanged and open. No automated result is presented as field evidence.
- After these 43 rows are written, the mechanical global rollup must be `688 adjudicated`, `243 unadjudicated`, and `455 pilot blockers`.
