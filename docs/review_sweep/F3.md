# F3 review — survivors

Run 2026-07-24 under `docs/engineering_review_prompt.md`. **36 findings survived** adversarial refutation; 5 refuted (listed at the end).

**These are static findings** — read from the code, verified by a second agent re-reading the code. **None has been reproduced by running the app.** Reproduce before fixing.

## [High] var(--bg) is undefined in all 7 palettes and has no fallback — the log-view depth-scale select loses its background entirely

`F3a` · THEME-VIOLATION · effort **S** · worth **fix-now** · verifier confidence high

**Where** `src/styles.css:850`

**Evidence**

```
.lv-scale {
  height: 20px;
  font-size: 10.5px;
  color: var(--text);
  background: var(--bg);        /* <- --bg is defined in NO theme block */
  border: 1px solid var(--border);
}

/* the control it is meant to match: */
.form-control { ... background: var(--bg-app); color: var(--text); ... }   /* styles.css:1863 */
```

**Failure** — --bg is not among the 17 vars defined in :root or any [data-theme] block (verified by diffing defined-vs-used names across styles.css: --bg, --bg-active, --bg-inset, --danger, --font-ui, --mono are all used and never defined; --bg is the only one with NO fallback value). A var() referencing an undefined property with no fallback is guaranteed-invalid, so `background: var(--bg)` is invalid-at-computed-value-time and the whole background shorthand resets to unset -> background-color: transparent. .lv-scale is a native <select> (logViewPanel.ts:167-168, the 1:20…1:5000 depth-scale picker in the log-view toolbar). In every palette it therefore renders with no field background while every sibling control in that toolbar (.form-control, styles.css:1863) gets `background: var(--bg-app)`; in the dark palette the styled select's dropdown popup composites its cream `color: var(--text)` (#e8dfd0) against the UA's white popup surface — ~1.3:1 contrast, i.e. the 1:N options are effectively unreadable. This is the exact class of defect the review brief flags as High: a var consumed by the app but defined in zero palettes.

**Fix** — Either add --bg-inset to all 7 palette blocks (it is already referenced with a fallback at styles.css:4607 and 4677, so one token covers three call sites), or change line 850 to `background: var(--bg-app);` to match .form-control. Then add a CI/lint step that diffs `var(--X` names against the names declared in the :root blocks so a phantom token cannot land again.

**Verifier correction** — styles.css:850 `.lv-scale { background: var(--bg); }` does reference a token defined in no palette and with no fallback, so the depth-scale <select> computes background-color: transparent and shows the toolbar's var(--bg-panel) through instead of an inset field colour. Severity Low, not High: the control stays legible in every palette (var(--text) on var(--bg-panel)), its real toolbar siblings (.lv-btn, styles.css:828) are intentionally transparent too, and the claimed unreadable dark-mode dropdown popup is an unverified engine behaviour, not a code-provable failure. The cited .form-control comparison (styles.css:1863) is not a sibling — those instances (logViewPanel.ts:409/418/429) belong to openBordersDialog(), a separate modal. Fix remains: change line 850 to var(--bg-app) or var(--bg-inset, var(--bg-app)).

**Priority rationale** — Verified real and trivially cheap: `var(--bg)` appears exactly once in the whole codebase (src/styles.css:850, `.lv-scale`), `--bg` is declared in zero palette blocks (only --bg-app/--bg-panel/--bg-panel-alt/--bg-hover exist), and there is no global `select` background rule to mask it (src/styles.css:261 only sets font-family/weight), so the declaration is invalid-at-computed-value-time and the control falls to transparent. The affected widget is the 1:20…1:5000 depth-scale picker in the log-view toolbar (src/ui/logViewPanel.ts:167-168) — the log view is the app's primary workspace and a petrophysicist touches that scale picker on essentially every well, so this is not a corner the user never sees; in the dark palette the cream `--text` (#e8dfd0) composited over WebView2's default light popup surface makes the option list borderline unreadable, and even in the light palettes the field loses the affordance every sibling `.form-control` (styles.css:1863) has. The fix is a one-character-class edit — `background: var(--bg-app);`, exactly the pattern already used with a fallback at styles.css:4607/4677 — touching a single declaration in a single rule, so the fix is strictly less risky than the defect and churns nothing. It is not a data-honesty issue, so it is not cardinal-rule urgent, but at ~1 minute of work in the app's most-used panel there is no reason to queue it. Scope the fix to line 850 only: the proposed CI/lint token-diff step is separate scope-creep and belongs in the backlog at best, since the other five undefined names (--bg-active, --bg-inset, --danger, --font-ui, --mono) all carry fallbacks and are therefore harmless today.

---

## [High] Vega panel is the only plot panel with no dataVersion subscription — it keeps plotting pre-run values after a module run

`F3b` · CODE-BUG · effort **S** · worth **fix-now** · verifier confidence high

**Where** `src/ui/vegaPanel.ts:735`

**Evidence**

```
vegaPanel.ts:735-738 is the full subscription set: `const unsubBrush = appState.brushedDepths.subscribe((sel) => applyBrush(sel)); const unsubTheme = appState.themeVersion.subscribe(() => { if (embedded && lastRows) void repaint(); });` — no dataVersion. Its sibling plots all have one: crossplotPanel.ts:1990, histogramPanel.ts:769, pickettPanel.ts:422, correlationPanel.ts:1076. workspace.createPlot (workspace.ts:1005-1067) only subscribes to `appState.selectedWell`, so nothing rebuilds it either. Curve names are a build-time snapshot: vegaPanel.ts:315 `const curveNames = await loadCurveNames();`, consumed once at :341-343.
```

**Failure** — Open a Vega scatter of PHIE vs SW for well A, then run SandiMin (multiminDialog.ts:1211 calls bumpDataVersion()) to recompute SW. The crossplot, histogram, Pickett, correlation and log-view panels all re-fetch and redraw with the new SW; the Vega panel silently keeps rendering the pre-run values with no visual cue that it is stale — the two panels sit side by side showing contradictory clouds for the same two curves. Newly written curves (MM_PHIE, MM_SW) are also absent from the X/Y/colour dropdowns until the panel is closed and reopened or the user switches wells and back.

**Fix** — Mirror the crossplot: add a primed dataVersion subscription that calls `void render()` (which re-fetches through getCurveData at vegaPanel.ts:546) and refills the three curve selects from a fresh loadCurveNames(), then release it in the dispose at vegaPanel.ts:749-762.

**Verifier correction** — Claim stands as written. Only implementation nuance for the fix: the sibling panels guard the subscription with a `dataPrimed` flag (crossplotPanel.ts:1989-1996, histogramPanel.ts:768-775) to swallow Observable.subscribe's synchronous first fire; a fix that omits that flag would double-load on panel build.

**Priority rationale** — Confirmed real and it is a data-honesty defect, which is this app's fix-now class. `src/ui/vegaPanel.ts:735-738` subscribes only to `brushedDepths` and `themeVersion`, while `crossplotPanel.ts:1990`, `histogramPanel.ts:769`, `pickettPanel.ts:422` and `correlationPanel.ts:1076` all carry the same primed `appState.dataVersion.subscribe(... void reload(true))` block, and `workspace.createPlot` only rebuilds on `selectedWell` — so after a SandiMin/equation run the Vega panel keeps rendering pre-run values with no stale cue, sitting next to a crossplot of the same two curves that has already redrawn with the new SW. Two contradictory clouds of PHIE-vs-SW on screen is exactly "a degraded result presented as a clean success", and Vega is the panel with the SVG/PNG export path (`exportSvg` at :587, which even calls `recordProcess("Export", ...)`), so a stale cloud can walk straight into a client deliverable and be logged as a legitimate process step. The fix is not risky: it is the copy of a pattern already proven in four sibling panels — a primed `dataVersion` subscription calling the existing `render()` (which re-fetches through `getCurveData` at :546 and preserves `specOverride`, since only a chart-type change clears it), plus a refill of the three curve selects from a fresh `loadCurveNames()` keeping the current values, released alongside `unsubBrush`/`unsubTheme` in the dispose at :749-762. About ten lines, no churn in the spec-editor or brush machinery. The only user-visible cost is that a data-version bump resets the vega zoom/pan, which the file already accepts explicitly for theme repaints (:565-566). It also unblocks nothing-in-particular, but it removes the trap where newly written MM_PHIE/MM_SW never appear in the X/Y/colour dropdowns until the panel is closed and reopened, which reads as "the run did not write the curves".

---

## [High] The VSH/PHIE/SWE cutoff quartet is copy-pasted into five panes; Monte Carlo's defaults differ, and four of five ignore the saved default that Cutoff Sensitivity writes

`F3c` · INCONSISTENCY · effort **S** · worth **fix-now** · verifier confidence high

**Where** `D:\XX. Arshilla\src\ui\monteCarloDialog.ts:459`

**Evidence**

```
monteCarloDialog.ts:458-461
  const vshMax = numField("VSH ≤", 0.5, 0, 1);
  const phieMin = numField("PHIE ≥", 0.08, 0, 1);
  const sweMax = numField("SWE ≤", 0.5, 0, 1);

Every other consumer of the same quantity uses 0.5 / 0.1 / 0.6:
  cutoffDialog.ts:110-112     numInput("0.5"); numInput("0.1"); numInput("0.6");
  summaryDialog.ts:42-44      saved?.vsh_max ?? "0.5" / phie_min ?? "0.1" / swe_max ?? "0.6"
  reportDialog.ts:122-124     mkNum("0.5",…); mkNum("0.1",…); mkNum("0.6",…)
  dashboardPanel.ts:56-58     num("0.5"); num("0.1"); num("0.6")

And the persisted pick is read by exactly one of the five. cutoffDialog.ts:754 writes it:
  await saveDocument("cutoffs", "__default__", JSON.stringify(payload));
summaryDialog.ts:9-17 is the only reader:
  const docs = await listDocuments("cutoffs");  const doc = docs.find((d) => d.name === "__default__");
grep for `"cutoffs"` across src/ui returns only cutoffDialog.ts and summaryDialog.ts — reportDialog, dashboardPanel and monteCarloDialog never call it, and cutoffDialog does not re-read its own saved value on reopen.
```

**Failure** — A petrophysicist runs Cutoff Sensitivity on DST-tested rock, lands on VSH≤0.42 / PHIE≥0.13 / SWE≤0.55, and clicks "Save as pay-summary default" (cutoffDialog.ts:224). Pay Summary then correctly opens preloaded with 0.42/0.13/0.55. But Report (reportDialog.ts:122) opens at 0.5/0.1/0.6 and Field Dashboard (dashboardPanel.ts:56) at 0.5/0.1/0.6, so the PDF deliverable's FLAG_PAY curves and pay tables are computed against cutoffs the study explicitly rejected — silently, because both panes show a plausible-looking number. Worse, Monte Carlo defaults to PHIE≥0.08 / SWE≤0.50: the P10/P50/P90 HPV distribution is built on a *third* net-pay definition, so the reported P50 volume cannot be reconciled with the Pay Summary net-pay number in the same report. Reopening Cutoff Sensitivity also loses the pick (it never reads back its own document), so the user has no way to see what was saved.

**Fix** — Extract `loadDefaultCutoffs()` from summaryDialog.ts:9-17 plus a `buildCutoffFields()` helper into a shared module (e.g. src/ui/cutoffFields.ts) exporting one DEFAULTS constant, and have cutoffDialog, summaryDialog, reportDialog, dashboardPanel and monteCarloDialog all construct their quartet from it. At minimum, change monteCarloDialog.ts:459-460 to 0.1 / 0.6 so the five panes agree on the unsaved-default case.

**Verifier correction** — Corrected claim (severity Medium, not High): "The VSH/PHIE/SWE cutoff quartet is duplicated across SIX panes with two conflicting default sets for one pay predicate." Four sites use 0.5/0.1/0.6 (cutoffDialog.ts:110-112, summaryDialog.ts:42-44, reportDialog.ts:122-124, dashboardPanel.ts:56-58); TWO use 0.5/0.08/0.5 — monteCarloDialog.ts:458-460 and the site the reporter missed, resultsQcPanel.ts:605-607 (`const vshMaxIn = numInput(0.5, "0.01"); const phieMinIn = numInput(0.08, "0.01"); const sweMaxIn = numInput(0.5, "0.01");`). So Monte Carlo is not a lone outlier. Two sub-claims must be dropped or softened: (a) "four of five ignore the saved default" is not a broken contract — cutoffDialog.ts:224 labels the button `"Save as pay-summary default"` and the status at :758 says "Open Cutoffs & Pay Summary to use them", so a single Pay Summary reader is the stated scope; (b) "silently" is overstated — the cutoffs are visible editable fields in Report (reportDialog.ts:164) and Dashboard (dashboardPanel.ts:88-90) and are stamped into the PDF itself at report.rs:414-416 (`"Pay Summary  (VSH ≤ {:.2}, PHIE ≥ {:.2}, SWE ≤ {:.2})"`), so a P50-vs-net-pay mismatch is auditable from the deliverable. The genuinely defective residue: (1) two numerically different unsaved defaults for the same pay flag across six panes, and (2) cutoffDialog.ts never reads back documents "cutoffs"/"__default__" (no listDocuments import), so reopening Cutoff Sensitivity loses the saved pick with no way to view it. The proposed shared-module fix and the minimum fix (align monteCarloDialog.ts:459-460 and resultsQcPanel.ts:606-607 to 0.1/0.6) remain valid.

**Priority rationale** — Fix-now, because this lands squarely on the app's cardinal rule (numbers a deliverable presents must be honest and reconcilable) and the fix is mechanical, near-zero-risk, and small. I verified every cited site: monteCarloDialog.ts:458-461 builds `numField("VSH ≤", 0.5…)`, `numField("PHIE ≥", 0.08…)`, `numField("SWE ≤", 0.5…)` while cutoffDialog.ts:110-113 (`numInput("0.5"); numInput("0.1"); numInput("0.6")`), reportDialog.ts:122-124 (`mkNum("0.5"…); mkNum("0.1"…); mkNum("0.6"…)`), dashboardPanel.ts:56-58 (`num("0.5"); num("0.1"); num("0.6")`) all use 0.5/0.1/0.6, and only summaryDialog.ts:9-16 `loadDefaultCutoffs()` ever reads the `"cutoffs"/"__default__"` document that cutoffDialog.ts:754 writes. For this specific user — an Indonesian petrophysicist whose deliverable IS the cutoff-driven net-pay table — the concrete cost is a client PDF in which the Pay Summary net pay (0.42/0.13/0.55, honoured) and the Monte Carlo P50 HPV (0.5/0.08/0.5, a third definition never chosen by anyone) cannot be reconciled, with no on-screen warning that three different net-pay definitions are in play; the fields are visible and editable in each pane, so this needs user inattention, but the app itself creates the expectation of propagation by offering "Save as pay-summary default" and then losing the pick on reopen of Cutoff Sensitivity, so the user cannot even audit what was saved. Against that, the fix touches no algorithm, no schema, and no plotting code: one exported DEFAULTS constant plus moving the existing 8-line `loadDefaultCutoffs()` into a shared `src/ui/cutoffFields.ts` and calling it from five input-construction sites — a well-scoped seam that also removes the trap where a sixth cutoff consumer (a planned QC or export pane) silently forks a seventh set of defaults. Even the 10-second floor (monteCarloDialog 0.08→0.1, 0.5→0.6) removes the worst of it, so there is no version of "too expensive right now" here; the only reason to defer would be pure style, and inconsistent net-pay definitions inside one report are not style.

---

## [High] Pay summary reports a fabricated Net 0.0 / N/G 0.00 / HPV 0.00 for wells whose VSH/PHIE/SWE were never computed

`F3d` · CODE-BUG · effort **M** · worth **fix-now** · verifier confidence high

**Where** `src-tauri/src/workflow.rs:651`

**Evidence**

```
let gross = zone.bottom_depth - zone.top_depth;
all_rows.push(PaySummaryRow {
    ...
    gross,
    net: net as f32,
    ntg: if gross > 0.0 { (net / gross as f64) as f32 } else { 0.0 },
    ...
    hpv: hpv as f32,
});

// and the only frontend guard, src/ui/summaryDialog.ts:105 —
if (rows.length === 0) {
  container.textContent = "No results — check that VSH/PHIE/SWE have been computed for the selected wells.";
```

**Failure** — Import a LAS with GR/RES_DEEP/NPHI/RHOB only (no interpretation run yet) and press Compute Summary, or Compute on the Field Dashboard. fetch_curve_frame (equations.rs:277) reads `depth` from standard_curves so it is non-empty; VSH/PHIE/SWE resolve to all-NaN columns; classify_sample (workflow.rs:700) returns (NaN,NaN,NaN) for every sample, so `net` stays 0.0 and `hpv` stays 0.0. The engine still pushes 3 rows per zone. The user sees a fully populated table reading Net 0.0, N/G 0.00, HPV 0.00 — identical to a genuine wet/shaly result — and the status line says "Pay summary: N rows; FLAG curves written". The `rows.length === 0` guard that was written for exactly this case can never fire. report.rs:368 calls the same engine, so `export_report_batch` writes a client-deliverable PDF containing a "Pay Summary" table of hard zeros for every uninterpreted well in the batch. The sibling cutoff-sweep engine already carries `n_samples` per series and cutoffDialog.ts:551 uses it ("no pay at any cutoff — check VSH/PHIE/SWE are computed"), proving the convention exists and was simply not applied here.

**Fix** — Add an evaluated-sample count to PaySummaryRow (e.g. `n_classified: usize`, the count of samples where flags[i] is not NaN, plus per-curve live counts). When it is 0, render net/ntg/hpv as "—" with a "VSH/PHIE/SWE not computed for this well" note in summaryDialog/dashboardPanel, and skip or flag the zone block in report.rs rather than printing zeros.

**Verifier correction** — Corrected claim [CODE-BUG, Medium] — workflow.rs:651-668 (run_pay_summary): for a well whose VSH/PHIE/SWE were never computed, the engine emits SAND/RESERVOIR/PAY rows carrying net 0.0 / ntg 0.00 / hpv 0.00, which are byte-identical to a genuine zero-net result, so the dialog (summaryDialog.ts:104), the Field Dashboard (dashboardPanel.ts:240) and the client PDF (report.rs:368-381, batch via report.rs:467) cannot tell "not interpreted" from "no pay". Corrections to the original report: (1) the row is NOT fully populated — avg_vsh/avg_phie/avg_swe are f32::NAN (workflow.rs:664-666), crossing IPC as null and rendering "—" (summaryDialog.ts:101) or "-" (fmt_num, report.rs:278-284); the ambiguity is against a genuine zero-net row, not against a normal result. (2) "the rows.length === 0 guard can never fire" is false: it fires when fetch_curve_frame returns empty depth or list_zones errors (workflow.rs:510-517 `continue`); it merely does not cover the missing-VSH case. (3) The cutoff-sweep precedent is misread: n_samples is n_incl, the count of samples with geometric zone/DST overlap (workflow.rs:1048-1057, 1085), not a live-curve count; cutoffDialog.ts:538 derives its "check VSH/PHIE/SWE are computed" hint from `peak <= 0`, the exact analogue of net == 0 — so the sibling engine does not already carry an evaluated-sample count and does not distinguish the two cases either. (4) Severity is Medium, not High: no panic, no incorrect arithmetic, and the persisted FLAG_* curves are correctly all-NaN; the concrete cost is confined to the rendered table and the PDF deliverable. A minimal fix is to count samples where flags[i] is finite (or where VSH/PHIE/SWE are non-NaN) and either suppress the row or render net/ntg/hpv as "—" when that count is 0.

**Priority rationale** — This is a data-honesty violation of exactly the kind the app's cardinal rule forbids, and it escapes into a client deliverable. Verified: with no interpretation run, every sample's flag is NaN, `flags[i] != 1.0` skips it (workflow.rs:630), so `net`/`hpv` stay 0.0 and the row is still pushed at workflow.rs:651 with `ntg: 0.0`. Worse than the original claim, the row is not even weakly self-revealing: `net_vsh`/`net_phie`/`sum_phie_w` accumulate only inside the same flag guard, so a zone with zero net always emits `Avg VSH/PHIE/SWE = —` — meaning a genuinely evaluated wet/shaly zone and a never-computed well render *identically* (`Net 0.0`, `N/G 0.00`, `—`, `—`, `—`, `HPV 0.00`) in `summaryDialog.ts` and, via `fmt_num`'s NaN→"-" at report.rs:278, in the PDF from `report.rs:368`. So the user has no way to tell the two apart, and the status line reports success ("N rows; FLAG curves written"). The realistic trigger is not the single-well case an expert would catch — it is `export_report_batch` over a well list where one or two wells were never interpreted (a re-run after adding wells, a group scoped wider than the interpretation run): the batch PDF silently carries a fully-formatted "Pay Summary" table of zeros for those wells, and zero net pay is a conclusion a client will act on. The fix is additive and low-risk: one extra count field on `PaySummaryRow` (live VSH/PHIE/SWE samples within the zone, counted *outside* the flag guard so badhole NaN gaps don't false-positive), plus a render branch in two frontends and a skip/flag in report.rs — it does not touch the pay integration math, which is the delicate part. The sibling cutoff engine already carries `n_samples` (workflow.rs:859/1085) and cutoffDialog.ts:551 already prints the exact warning this case needs, so the convention and its wording exist and only need to be applied here. The dead `rows.length === 0` guard in summaryDialog.ts:105 is direct evidence this case was intended to be handled and simply was not.

---

## [High] Results-QC scorecard status is a 9px colour dot only, and the theme mapping makes "ok" render bright red under the Halliburton skin

`F3e` · A11Y · effort **S** · worth **fix-now** · verifier confidence high

**Where** `src/ui/resultsQcPanel.ts:148`

**Evidence**

```
function checkRow(status: CheckStatus, label: string, detail: string, tooltip?: string): HTMLElement {
  const row = document.createElement("div");
  row.className = "rqc-check";
  const dot = document.createElement("span");
  dot.className = `rqc-dot rqc-dot-${status}`;   // <- the ONLY carrier of ok/warn/alert/na
  ...
  if (tooltip) row.title = tooltip;

// styles.css:2629-2649 — four identical circles, fill colour is the only difference:
//   .rqc-dot { width: 9px; height: 9px; border-radius: 50%; }
//   .rqc-dot-ok    { background: var(--accent); }
//   .rqc-dot-warn  { background: var(--accent2); }
//   .rqc-dot-alert { background: var(--warn); }
// styles.css:112-117 (halliburton theme):
//   --accent: #e31b23;  /* Halliburton red */
//   --accent2: #3f4249; /* graphite */
//   --warn: #b3141b;
```

**Failure** — Under `[data-theme="halliburton"]` a PASSING check paints #e31b23 (bright red) and a FAILING check paints #b3141b (dark red) — at 9px diameter these are indistinguishable to anyone, so a petrophysicist reviewing a 12-zone scorecard (5 checks per zone = 60 dots) reads every clean zone as an alarm and cannot pick out the genuinely failing ones. Under the DEFAULT light theme the mapping is inverted: ok = #b5651d (orange), warn = #5f7350 (green) — green means caution. The `detail` string is pure numbers ("BVW 0.045 · CV 22% · n=340") and the thresholds (BVW_CV_OK 0.15 / BVW_CV_WARN 0.30 at lines 53-54) are never shown, so there is no text fallback and, unlike healthPanel.ts:49, no legend. The differentiating text lives only in `row.title` — mouse-hover only. A deuteranope, or anyone on the branded client themes the app ships for Pertamina/Halliburton/SLB demos, cannot read the scorecard at all.

**Fix** — Put the verdict in text: set `dot.textContent` to the glyph the codebase already uses for exactly this in processingPanel.ts:30-36 (`ok:"✓", warned:"⚠", failed:"✗", pending:"⏳"`), or prepend the status word to `detail`. Decouple the semantic colours from the brand tokens — add dedicated `--qc-ok/--qc-warn/--qc-alert` variables per theme instead of reusing `--accent`/`--accent2`/`--warn`, whose hues are chosen for branding, not for meaning.

**Verifier correction** — The defect is real but the mechanism needs three corrections and the severity is arguably Medium, not High. (1) The tooltip is a PARTIAL fallback, not absent: computeBuckles:199-202, computeRecon:282-285, computeMc:342-346 and computeCutoff:409-413 all branch only on `status === "ok"`, so warn and alert emit the IDENTICAL tooltip string — hover separates ok from not-ok but can never separate warn from alert, in any theme. (2) The "no text fallback" claim applies only to ok/warn/alert; "na" rows are fully readable from text (resultsQcPanel.ts:177 "no SWE/PHIE curve", :254 "run SandiMin recon QC first", :302 "run Monte Carlo (persist) first"). (3) The cited glyph map is processingPanel.ts:29-35 (not 30-36) and also contains `running: "▶"`. Additionally missed: under halliburton, warn (--accent2 #3f4249 graphite) is also near-identical to na (--text-dim #63636b at opacity 0.5, styles.css:2646-2649), so a warn can read as "not run" as well. Severity: Medium — it only degrades on the branded skins or for colour-vision-deficient users, hover recovers ok/not-ok, and the CSV export (scorecardCsv:425-449) carries every raw number.

**Priority rationale** — This is a data-honesty defect on the one panel whose entire job is to tell the petrophysicist his result is degraded, and it fails on the DEFAULT theme, not just the branded demo skins. styles.css:2640 maps `warn` to `var(--accent2)`, which in the shipped default light theme (styles.css:28) is `#5f7350` — olive green. So a Buckles BVW check that trips BVW_CV_WARN, or an MC-uncertainty check that trips MC_REL_WARN, paints green next to a passing check painted ochre-orange (`--accent: #b5651d`): the degraded result reads as the clean one and the clean one reads as the caution. That is exactly the failure mode the app's cardinal rule forbids, and it happens on the everyday screen with no theme switch needed. The Halliburton case (#e31b23 pass vs #b3141b fail at 9px) is the demo-day version of the same bug: the aggregate line at resultsQcPanel.ts:822 says "N check(s) flagged" but gives no way to find which N of 60 dots they are, so recovering the information costs a hover-by-hover sweep of every row in front of a client. The fix is cheap and low-risk: one line setting `dot.textContent` to the glyphs processingPanel.ts:30-36 already defines, plus additive `--qc-ok/--qc-warn/--qc-alert` variables per theme block. It is purely additive CSS and one DOM line in a single panel — it touches no computation, no thresholds, no shared component, so it cannot churn a working area. Decoupling the semantic triple from `--accent`/`--accent2`/`--warn` also removes a standing hazard for planned work: every future client skin added to the theme system silently re-rolls the meaning of the QC colours, because brand palettes are chosen for branding, never for pass/fail legibility.

---

## [High] themeVersion is only emitted by the ribbon <select>, so the default "system" theme never repaints any canvas when the OS flips to dark mode

`F3b` · CODE-BUG · effort **S** · worth **backlog** · verifier confidence high

**Where** `src/theme.ts:49`

**Evidence**

```
theme.ts:49-56 — `function applyTheme(choice){ if (choice==="system") delete root.dataset.theme; else root.dataset.theme = choice; }` and the ONLY bump site, ribbon.ts:191-195 — `themeSelect.addEventListener("change", () => { setTheme(...); bumpThemeVersion(); ... })`. `grep -rn "matchMedia|prefers-color-scheme" src/**/*.ts` returns nothing outside a comment, yet styles.css:56 defines `@media (prefers-color-scheme: dark) { :root:not([data-theme="light"]) { --bg-app: #1c1610; ... } }` and theme.ts:37 defaults to "system" when nothing is stored.
```

**Failure** — Fresh install (no localStorage key) → getTheme() returns "system". The user works with the app open past sunset; Windows 11 auto dark-mode switches. Every DOM surface repaints instantly via the media query, but themeVersion never fires. LogCanvasRenderer.ts:97-100 caches the clear colour (`private cachedBgColor: string | null = null;` — "we read it once and only re-read on a theme change (repaint() clears this)") and repaint() (LogCanvasRenderer.ts:629-632) is called from exactly one place: the themeVersion handler at logViewPanel.ts:333. So the log view keeps a cream #f2ebdc WebGPU background inside a now-dark UI until the user reloads a well; crossplot/histogram/Pickett/correlation/map/Vega keep the light palette until a pan, resize or well switch.

**Fix** — In theme.ts, attach `window.matchMedia("(prefers-color-scheme: dark)").addEventListener("change", ...)` once at module init and call bumpThemeVersion() from it when getTheme() === "system". Move the bump into setTheme()/applyTheme() at the same time so the ribbon is not the sole owner of the signal.

**Verifier correction** — Mechanism and file:line are right; three details are wrong and the severity is overstated. (1) The stale WebGPU clear colour is whatever was cached from --bg-panel at first render, and the hard-coded fallback is "#fbf7ee" (LogCanvasRenderer.ts:690), not "#f2ebdc". (2) Windows 11 has no built-in time-of-day auto dark mode, so the "past sunset" trigger is inaccurate — the reachable path is the user manually toggling Settings > Personalization > Colors (or a third-party auto-dark tool) while the app is open. (3) Explicitly selecting "system" in the ribbon does bump themeVersion once, so canvases are correct at that moment; only later OS flips are missed. Effect is purely cosmetic — canvases keep the previous palette until any pan/resize/well-reload — so this is Medium, not High.

**Priority rationale** — Technically confirmed and cheap to fix, but the consequence is cosmetic and self-healing, so it does not clear the fix-now bar. Concrete impact: if the OS theme flips mid-session (Windows 11 has no built-in sunset scheduler, so this means a manual Settings toggle or a third-party Auto Dark Mode helper — narrower than the "works past sunset" story in the evidence), every DOM surface goes dark via styles.css:56 while the WebGPU log view keeps its cached cream clear colour (LogCanvasRenderer.ts:100/630) and the nine other canvas panels keep the light palette until their next pan/resize/well-switch redraw. No number is wrong, nothing is presented as a clean result that is not one, and the whole thing clears the moment the user touches the ribbon theme select or interacts with a plot — so the cardinal data-honesty rule is not engaged. It earns backlog rather than drop on two counts: (1) the app is used to produce client-facing deliverables, and a screenshot of a half-dark UI with cream plot backgrounds reads as "the tool is broken" to a Pertamina/SLB audience; (2) the second half of the proposed fix is a genuine maintenance seam — because ribbon.ts:193 is the sole owner of bumpThemeVersion(), the next feature that sets the theme programmatically (very plausible given the client-brand palettes already exist: auto-select the Pertamina skin when opening a Pertamina project) will silently leave all ten canvas subscribers stale, and that failure will look like a canvas bug, not a theme bug. Fix risk is near zero and does not churn a tested area: theme.ts is a 56-line leaf imported from only main.ts:12 and ribbon.ts:35, state.ts imports nothing but a type from ipc so there is no import cycle, and the new trigger fires the exact same signal the ribbon path already exercises (a leftover double-bump from ribbon.ts is harmless — subscribers just redraw twice). Batch it with the next theme/UI-polish pass rather than interrupting current work.

---

## [High] buildWellScope snapshots the well list and group list at build time, so a run launched from an already-open panel silently skips wells imported since

`F3b` · CODE-BUG · effort **M** · worth **backlog** · verifier confidence high

**Where** `src/ui/wellScope.ts:40`

**Evidence**

```
wellScope.ts:40-43 `const wells: WellSummary[] = await listWells().catch(() => []); const groups: WellGroupEntry[] = await listWellGroups().catch(() => []); const wellById = new Map(...); const allIds = wells.map((w) => w.well_id);` — and the only live subscriptions are wellScope.ts:249-250 `unsub.push(appState.pinnedWellIds.subscribe(...)); unsub.push(appState.multiSelectedWellIds.subscribe(...));`. No dataVersion, no wellGroupsVersion, despite state.ts:79-80 documenting wellGroupsVersion as bumped "so the Wells pane and batch dialogs reload their group list". `resolveIds()` case "all" (wellScope.ts:185-187) returns the stale `allIds`. moduleDialog.ts:262-263 even asserts the opposite: "the well scope tracks live state on its own, so nothing well-related to re-tick here". 12 long-lived panes are affected (cutoff, ml, montecarlo, shf, thomeer, hfu, faciesTie, multimin, module, workflow, paysummary — all asyncPane, which never rebuilds).
```

**Failure** — User opens the SandiMin pane with 12 wells in the project; the scope header reads "12 wells". They import 3 more LAS files from the ribbon (bumps dataVersion), then press Run with scope "All". The run covers the original 12 — the 3 new wells get no multimin output, and the confirmation text still says 12 wells, so nothing looks wrong. Second failure: with zero groups at build time, `hasGroups` (wellScope.ts:50) is false so the "Group" mode button is never even created (wellScope.ts:92 `if (!m.show) continue;`); creating the project's first group in the Wells pane leaves every open tool pane permanently unable to scope to it.

**Fix** — Subscribe to dataVersion and wellGroupsVersion inside buildWellScope, re-fetch listWells()/listWellGroups(), rebuild wellById/allIds/groupSel and the MODES visibility, preserve the current mode and customIds, then updateCount() — pushing both unsubs onto the existing `unsub` array so scope.dispose() still releases them. Then delete the now-false comment at moduleDialog.ts:262-263.

**Verifier correction** — Mechanism, file:line and both failure scenarios are correct. Two adjustments: (1) the affected long-lived set is 11 asyncPane sites (cutoff, ml, montecarlo, shf, thomeer, hfu, faciesTie, multimin, module, workflow, paysummary), not 12 — the 12th buildWellScope caller, reportDialog.ts:56, is mounted through wellPane (workspace.ts:571-578) which rebuilds on each well selection and so self-heals. (2) The staleness is broader than the report states: because `groups` (with its `well_ids`) is also frozen, an activeWellGroup switch or a group-membership edit made while a pane is open is likewise not reflected in "group" mode, and the option list in groupSel (wellScope.ts:105-111) never gains renamed/new groups. Additional supporting evidence the report missed: moduleDialog.ts:33-35 explicitly documents that the pane's well list refreshes on data changes, which the code does not do — so the defect contradicts the module pane's own stated contract, not just the line-262 comment.

**Priority rationale** — Verified real: wellScope.ts:40-43 snapshots wells/groups once, hasGroups (line 50) gates the "Group" button at line 91, and asyncPane (workspace.ts:610-631) never rebuilds — while LAS import does bump dataVersion (ribbon.ts:1065 -> workspace.ts:1442). But the concrete damage is under-coverage, not a wrong answer: the 12 wells that run get correct output, no bad data is written, and reopening the pane plus a re-run fully repairs it, so it does not trip the "degraded result presented as clean success" rule the way a silently-defaulted parameter would — the header and the confirmation both honestly say 12 wells, they are just stale. The group-button symptom (create the project's first group, every open pane is permanently unable to scope to it) is the more irritating one for a Geolog/Techlog-trained user, but closing and reopening the pane is a complete workaround. The fix is contained to one 273-line file with no test surface to churn, yet it is not a five-minute patch: dataVersion is bumped from ~20 sites (highlightsOverlay.ts alone bumps it ten times), so a naive subscribe means 12 open panes each doing two extra IPC fetches of a 2000-row well list on every curve rename — it needs a diff-and-skip or a dedicated wells-changed signal, plus preservation of mode/customIds/groupId. Schedule it high in the backlog and delete the now-false moduleDialog.ts:261-262 comment in the same change, since that comment is precisely what would mislead a future edit into trusting live tracking that does not exist; it does not preempt work in flight.

---

## [High] Five docked analysis panels fill their canvas with theme.bg but never subscribe to themeVersion

`F3b` · CODE-BUG · effort **S** · worth **backlog** · verifier confidence high

**Where** `src/ui/hfuDialog.ts:231`

**Evidence**

```
hfuDialog.ts:231-232 `const theme = readTheme(canvas); const s = setupCanvas(canvas, theme.bg);` — setupCanvas (hfuDialog.ts:215-228) paints the FULL canvas with that bg. Its dispose (hfuDialog.ts:112-115) is `{ detachRender?.(); scope.dispose(); }` — no theme unsub, and `grep -n "appState" src/ui/hfuDialog.ts` returns nothing. Identical pattern with no themeVersion subscription in: lorenzDialog.ts:273-274 (drawSmlp), thomeerDialog.ts:154 + :230 (drawPcCurve/drawPdG), shfDialog.ts:420 + :483 (drawFoil/drawScan), multiminDialog.ts:1295 (drawReconScatter). All six are long-lived dock panels registered at workspace.ts:487-516 (`case "thomeer"`, `case "hfu"`, `case "lorenz"`, `case "multimin"`, `case "shf"`), and asyncPane (workspace.ts:610-632) rebuilds them on nothing.
```

**Failure** — Run the Thomeer Pc fit in dark theme, then switch the ribbon theme to Pertamina to screenshot for a client deck. The panel chrome, table and captions go white-on-Pertamina-blue, but the two plot canvases stay solid #1c1610 dark rectangles with #a08d70 axis text — a black box in the middle of a light report page. Thomeer and SHF are the worst: neither calls attachResizeRedraw (unlike hfu/lorenz/multimin, which at least recover if you drag the pane edge), so the only repaint paths are a table-row click (thomeerDialog.ts:126-129) or the FWL drag (shfDialog.ts:304-330). Resizing the window does not fix it; the user must press Run again and re-wait the fit.

**Fix** — Add `const unsubTheme = appState.themeVersion.subscribe(() => redraw());` in each builder and release it in dispose — the same three lines already present at cutoffDialog.ts:815 and resultsQcPanel.ts:852. For thomeerDialog/shfDialog, hoist the existing `redraw`/render closure so it is reachable from the builder scope (thomeerDialog.ts:94-99 `const redraw = () => {...}` currently lives inside renderThomeer), and add attachResizeRedraw while you are there.

**Verifier correction** — The mechanism, file:line and reachability are all correct, but two details are off. (1) shfDialog has THREE stale-background draw functions, not two: drawFoil (420), drawScan (483) and drawShfFit (636) all repeat `ctx.fillStyle = theme.bg; ctx.fillRect(0, 0, w, h)` — so the stale-canvas count is 8 across the five panels (hfu 2, lorenz 1, thomeer 2, shf 3, multimin 1), not 6. (2) Severity should be Medium, not High: the defect is purely cosmetic and self-heals on the next redraw trigger (table-row click, re-Run, or a pane resize for hfu/lorenz/multimin which do call attachResizeRedraw). No computed value, saved curve or export path is affected — only the on-screen bitmap and anything screenshotted from it.

**Priority rationale** — Real, verified, and cheap — but cosmetic, not a correctness or data-honesty failure, so it does not clear the fix-now bar. The canvas keeps a self-consistent snapshot (bg and axis text both from the same readTheme call at src/ui/hfuDialog.ts:230-232), so the plot stays legible and every plotted number stays correct; it just sits as a dark rectangle inside light-themed chrome. That said, this is worth real backlog priority rather than a drop, for two concrete reasons. First, the client-brand palettes (Pertamina/Halliburton/SLB/LAPI-ITB) exist specifically so results can be screenshotted into client decks, so "compute, then switch theme, then capture" is an intended workflow, and these five panels defeat it — worst on thomeerDialog.ts and shfDialog.ts, which lack attachResizeRedraw entirely, so even a pane drag will not recover and the user must re-run the fit. Second, and the stronger argument, is the maintenance seam: 10 other panels already carry the exact three-line pattern (cutoffDialog.ts:815, resultsQcPanel.ts:852, etc.), which makes these five the outliers against an established convention; the next client theme added to the 15-var CSS contract, or the next panel copy-pasted from hfuDialog.ts, silently inherits the same hole. The fix is mechanical and near-zero risk (subscribe to appState.themeVersion, call the existing redraw, release in dispose), and it does not churn tested logic — only thomeerDialog.ts (hoisting the redraw closure out of renderThomeer, currently at :96-99) and shfDialog.ts need more than three lines. Batch all five plus the missing attachResizeRedraw calls into one sitting the next time the theme system is touched; nothing else blocks on it.

---

## [High] formRow() emits a <label> with no for/htmlFor and the control as a sibling — 179 call sites, zero label associations in the codebase

`F3e` · A11Y · effort **M** · worth **backlog** · verifier confidence high

**Where** `src/ui/modal.ts:103`

**Evidence**

```
export function formRow(label: string, control: HTMLElement, hint?: string): HTMLElement {
  const row = document.createElement("div");
  row.className = "form-row";
  const lab = document.createElement("label");
  lab.className = "form-label";
  lab.textContent = label;
  if (hint) lab.title = hint;
  row.appendChild(lab);      // <- no lab.htmlFor
  row.appendChild(control);  // <- sibling, not nested
  return row;
}

// `grep -rn '\.htmlFor|for="|setAttribute("for"' src/` => 0 matches across the whole app.
// 179 formRow() calls across 26 files (moduleDialog, multiminDialog, monteCarloDialog,
// reportDialog, crossplot/histogram/pickett properties, cutoffDialog, shfDialog, ...).
```

**Failure** — Every parameter field in every module and plot-properties dialog is announced with no name. A screen-reader user opening the SHF dialog (shfDialog.ts:102, `formRow("Free-water level (TVDSS)", fwlInput, …)`) Tabs into the field and hears "spin button, 0" — they cannot tell FWL from Rw from m from n, and entering a value in the wrong box silently produces a wrong saturation-height law. Sighted mouse users also lose the click-the-label-to-focus target on 5.5em-wide number inputs. Worst instance: reportDialog.ts:122-129 packs four visually identical number boxes into one flex row under a single `formRow("Cutoffs VSH/PHIE/SWE/PERM", cutoffWrap, …)` — the only per-field identification is `el.title`, so which box is VSH≤ and which is SWE≤ is discoverable only by hovering each one, and swapping them silently ships a wrong net-pay table into a client PDF.

**Fix** — In formRow, if `control` has no id, assign a generated one (`form-ctl-${counter++}`) and set `lab.htmlFor`; when `control` is a wrapper div, fall back to `lab.id` + `control.setAttribute('aria-labelledby', lab.id)` and give the inner fields their own visible micro-labels (reportDialog's four cutoff boxes especially).

**Verifier correction** — formRow() (src/ui/modal.ts:103-113) emits a <label> with no htmlFor and appends the control as a sibling, so all 178 formRow call sites across 26 files produce form controls with no label association and no click-the-label-to-focus target - confirmed at src/ui/shfDialog.ts:102 (fwlInput, no title of its own) and src/ui/moduleDialog.ts:102/108/119 (every module argument). But this is a formRow-specific defect, NOT a codebase-wide absence of labeling: ~19 files (multiminDialog.ts, shfDialog.ts's own mkField at :92-97, cutoffDialog.ts:163, mlDialog.ts, monteCarloDialog.ts, wellScope.ts) already use the correct IMPLICIT pattern of nesting the control inside the <label>, which a `for=` grep cannot detect. multiminDialog.ts has zero formRow calls and is not affected. reportDialog.ts:113-129's four cutoff boxes are the wrong "worst instance" - mkNum sets el.title, which IS the accessible-name fallback, so screen readers announce them correctly; only the sighted-user ambiguity (four identical boxes discoverable by hover) is a real cost there. The right fix is to make formRow use the same implicit-nesting pattern the rest of the codebase already uses, or assign a generated id + htmlFor; severity is closer to Medium than High for a single-operator desktop app with no stated AT user.

**Priority rationale** — The defect is real but its headline harm is the one that doesn't apply here: SandiBumi is a Tauri desktop plotting/interpretation tool whose entire value is visual (composite plots, crossplots, Pickett), used by Indonesian petrophysicists on Geolog/Techlog-style workflows — there is no screen-reader user in this user base, so the "hears 'spin button, 0'" scenario is theoretical. The claimed worst case also overstates: D:\XX. Arshilla\src\ui\reportDialog.ts:164 passes `formRow("Cutoffs VSH/PHIE/SWE/PERM", cutoffWrap, "Pay summary flags: VSH ≤, PHIE ≥, SWE ≤, PERM ≥ (blank = off)")` — the visible label enumerates the left-to-right order and the hint spells out each direction, on top of the per-box `el.title` at reportDialog.ts:113-126, so a wrong net-pay table is not "silently" reachable and this is not a data-honesty fix-now. What keeps it off the drop pile is the sighted-user ergonomics plus the cost shape: the fix lives entirely inside one 12-line function (src/ui/modal.ts:103), needs zero edits at the 179 call sites, and restores click-the-label-to-focus for every bare checkbox (e.g. `formRow("Tables only (no composite)", tablesOnly)` at reportDialog.ts:165, whose only hit target today is a ~13px box) and every 5.5em number input across moduleDialog/multiminDialog/shfDialog. One caveat for whoever picks it up: `control` is sometimes a wrapper `<div>` (scalePageWrap, cutoffWrap at reportDialog.ts:103-129), so a blind `lab.htmlFor = control.id` would produce an invalid association — the wrapper branch needs the aria-labelledby fallback, which is exactly the part that makes this a 30-minute task rather than a 5-minute one. Do it the next time modal.ts is open for another reason; don't schedule a dedicated pass.

---

## [High] Wells tree and Tops list are click-only <div>s — no keyboard path to change the active well or interval

`F3e` · A11Y · effort **M** · worth **backlog** · verifier confidence high

**Where** `src/ui/objectTree.ts:75`

**Evidence**

```
const node = document.createElement("div");
node.className = "tree-node tree-well" + ...;
const star = document.createElement("span");
star.className = "tree-pin" + (isPinned ? " tree-pinned" : "");
star.addEventListener("click", (e) => { e.stopPropagation(); void this.togglePin(...); });
...
node.addEventListener("click", (e) => this.handleWellClick(e, well, index, node));

// grep of the whole file: zero hits for tabindex / tabIndex / role / keydown (225 lines)
// same pattern in src/ui/topsPanel.ts:45-52:
//   const node = document.createElement("div");
//   node.className = "tree-node top-node" + ...;
//   node.addEventListener("click", () => this.toggle(i));
```

**Failure** — `appState.selectedWell.set()` has exactly one interactive call site — workspace.ts:922, driven by `tree.onSelectWell` (the other two are session restore at 1472/1495 and a null-reset at ribbon.ts:784). Every consumer panel follows it (logViewPanel.ts:320 subscribes; crossplot/histogram/pickett/resultsQc/moduleDialog all read it) and no panel has its own well picker. So a keyboard-only or switch-access petrophysicist opens a project with 40 Mahakam wells, Tabs through the ribbon into the Wells pane, and finds nothing focusable: the well rows, the ★ pin toggle, and the tops rows are all bare divs/spans. They can never change the active well or window a plot to a top after startup — they are locked to whichever well the restored session had, which means Log View, Crossplot, Pickett, Results QC and every module dialog scoped to 'selected well' are unusable for all other wells.

**Fix** — In objectTree.ts give the container `role="listbox"`, each `node` `tabIndex = 0` + `role="option"` + `aria-selected`, and a keydown handler for Enter/Space (activate, forwarding ctrlKey/shiftKey to handleWellClick) and ArrowUp/ArrowDown (roving focus). Rebuild `star` as a real `<button>` with an aria-label and `aria-pressed={isPinned}` so it lands in the tab order on its own. Apply the identical treatment to topsPanel.ts:44-53.

**Verifier correction** — Correct claim: "Wells tree rows/pin toggle (objectTree.ts:75-96) and Tops rows (topsPanel.ts:45-52) are non-focusable click-only divs/spans, so there is no keyboard path to change the ACTIVE well or the selected top interval." Impact is narrower than reported: it locks a keyboard-only user out of Log View (logViewPanel.ts:320), the Crossplot/Histogram/Pickett panels (wired at workspace.ts:1052), the Tops pane (workspace.ts:967), DB Inspector (dbInspectorPanel.ts:101) and the ~10 selected-well ribbon actions (ribbon.ts:1013-1612). It does NOT make module dialogs unusable: moduleDialog, multiminDialog, mlDialog, monteCarloDialog, reportDialog, workflowDialog, cutoffDialog, shfDialog, hfuDialog, summaryDialog, thomeerDialog, faciesTieDialog and lorenzDialog all resolve their run set via buildWellScope (wellScope.ts:39), which is fully keyboard-operable (real <button> mode chips at line 93, <select> at 103, search input at 122, checkboxes at 159-167) and whose "Custom…"/"All"/"Group" modes are independent of the Wells pane — moduleDialog.ts:261-263 states this explicitly. Two minor factual slips: crossplotPanel.ts contains no reference to selectedWell (it is wired externally), and moduleDialog only subscribes to selectedWell to refresh constants pickers, not to scope the run. Severity is better rated Medium than High for a single-developer local desktop tool with no stated accessibility requirement.

**Priority rationale** — Backlog. The technical claim checks out — `objectTree.ts:75-97` builds bare `<div>`/`<span>` nodes with only click listeners, `topsPanel.ts:44-52` does the same, and neither file contains tabindex/role/keydown — but the claimed victim (a keyboard-only or switch-access petrophysicist) is theoretical for this app in a way the rest of the app makes decisive: SandiBumi's core surfaces are Canvas-2D domain plots (crossplot lasso, Pickett line placement, histogram, log-view depth picking) that have no keyboard path either, so making the Wells tree focusable would not deliver a keyboard-operable app — it would deliver a keyboard-operable well list feeding mouse-only plots. Nobody gets unblocked. There is no legal/procurement a11y gate here (single developer, local desktop tool, own well data, users trained on Geolog/Techlog/IP, all of which are equally mouse-driven). It is not a correctness or data-honesty issue: nothing renders wrong and no degraded result is shown as clean. What keeps it off "drop" is the ergonomic half of the same fix rather than the a11y framing — Up/Down arrow roving focus to cycle the active well is exactly the interaction Techlog/Petrel users expect when scanning 40 Mahakam wells one at a time, and it lands in a 225-line self-contained file with one seam (`handleWellClick(e, well, index, node)` already takes the modifier keys off the event, so a synthetic keydown handler forwarding `ctrlKey`/`shiftKey` reuses the existing anchor/multi-select logic untouched). Risk is contained but non-zero: `refresh()` blows away `container.innerHTML` on every pin toggle and multi-select change, so a roving-tabindex implementation must re-apply focus after re-render or the keyboard user loses their place mid-list — worth doing carefully, not worth doing today. Schedule it alongside the next intentional pass on the Wells pane; do the `<button>`-ification of the ★ pin and `aria-selected` in the same edit since they cost nothing once the file is already open.

---

## [Med] Six analysis panes style their Run button with a bare `.primary` class that has no CSS rule — the primary CTA renders as an unstyled native button

`F3c` · THEME-VIOLATION · effort **S** · worth **fix-now** · verifier confidence high

**Where** `D:\XX. Arshilla\src\ui\thomeerDialog.ts:22`

**Evidence**

```
thomeerDialog.ts:19-22  const runBtn = document.createElement("button");  runBtn.type = "button";  runBtn.textContent = "Fit Thomeer";  runBtn.classList.add("primary");

Identical in hfuDialog.ts:46-49 ("Cluster HFUs"), lorenzDialog.ts:87-90 ("Build Lorenz Plot"), shfDialog.ts:175-178 ("Fit FOIL"), faciesTieDialog.ts:39-42 ("Compare"), mlDialog.ts:360-363 ("Run Model").

Every `primary` rule in styles.css is scoped and none of them match these buttons:
  styles.css:1735  .lp-btn.primary { background: var(--accent); border-color: var(--accent); color: #fff; }
  styles.css:3319  .workflow-run-row .primary { font-weight: 600; }
  styles.css:4703  .guard-confirm button.primary { ... }
There is no bare `.primary` rule (grep "primary" styles.css returns exactly those 3 hits + 1735's :hover), and no global `button {}` rule beyond styles.css:259 which sets only font-family/font-weight. index.html loads only /src/styles.css.

The accent-filled majority: monteCarloDialog.ts:502-503 `runBtn.className = "form-run-btn mc-run-btn";`, cutoffDialog.ts:201-203 `runBtn.className = "form-run-btn"`, summaryDialog.ts:53-54, moduleDialog.ts:222-223, multiminDialog.ts:1063 `runBtn.classList.add("primary", "mm-run-btn")` (styled via .mm-dialog button.mm-run-btn, styles.css:4485), dashboardPanel.ts:70-72 `runBtn.className = "btn btn-accent"`.
```

**Failure** — Open Monte Carlo and Thomeer Pc Fit side by side in the same dock group. Monte Carlo's "Run Monte Carlo" is a full-width accent-brown button (styles.css:3634 `.mc-run-row` comment literally reads "Full-width brown run button (matches the Cutoff panel's Compute)"). Thomeer's "Fit Thomeer" sits in the same `.mc-run-row` container but renders as a flat grey Chromium default button — no accent fill, no hover, and it does not repaint when the user switches to a client-brand theme (Pertamina/SLB/Halliburton), because none of the 15 theme CSS vars reach it. Six of the twelve tool panes are affected, so the client-brand skin looks half-applied on exactly the control the user is meant to press.

**Fix** — Add a global `button.primary` rule in styles.css that carries `background: var(--accent); border-color: var(--accent); color: #fff` (i.e. promote the `.lp-btn.primary` body to an unscoped `.primary`), or — cheaper and more consistent with the count — change the six `classList.add("primary")` calls to `className = "form-run-btn mc-run-btn"` to match monteCarloDialog.ts:503. Then delete `.workflow-run-row .primary` (styles.css:3319) as redundant.

**Verifier correction** — Count and fix advice need correcting: the affected set is SEVEN, not six. workflowDialog.ts:761-762 is the same pattern (`const runBtn = button("Run chain"); runBtn.classList.add("primary");` where the local `button()` helper at workflowDialog.ts:890-895 sets only type and textContent). Its only matching rule is `.workflow-run-row .primary { font-weight: 600; }` (styles.css:3319) — bold but still no accent fill, no border, no hover, no theme var. Consequently the reporter's closing step "delete .workflow-run-row .primary (styles.css:3319) as redundant" is wrong: that rule is currently the ONLY rule reaching that button. It becomes redundant only under the reporter's FIRST option (add a global `button.primary` carrying background/border-color var(--accent) + color #fff), not the second (rewriting the six classList.add calls to "form-run-btn mc-run-btn"), which would leave workflowDialog's Run chain button still unstyled. Separately, autosave.ts:84 `restore.className = "primary"` is correctly NOT in scope — its container is class "guard-confirm" (autosave.ts:66-68), so styles.css:4703 `.guard-confirm button.primary` covers it.

**Priority rationale** — Real, visible on every session, and the fix is a one-line CSS addition with provably zero specificity regression. Confirmed: `.mc-run-row` (styles.css:3634) is `flex-direction: column; align-items: stretch`, so the bare-`.primary` button in thomeerDialog/hfuDialog/lorenzDialog/shfDialog/faciesTieDialog/mlDialog is stretched full-width but carries no fill — a full-width flat grey Chromium button sitting in the exact container whose CSS comment reads "Full-width brown run button (matches the Cutoff panel's Compute)". workflowDialog.ts:762 is a seventh case (`.workflow-run-row .primary` supplies only `font-weight: 600`). That is 7 of ~12 tool panes rendering their primary CTA unthemed, and because no `var(--accent)` reaches them they are the one control that refuses to repaint when the user switches to a Pertamina/SLB/Halliburton brand skin — precisely the client-demo scenario the 15-var theme system exists for. Not a correctness or data-honesty issue, but the cost side is near zero: promoting the `.lp-btn.primary` body (styles.css:1735-1739) to an unscoped `button.primary` cannot regress anything, since every existing consumer outranks it — `.lp-btn.primary` (0,2,0) with identical declarations, `.guard-confirm button.primary` (0,2,1), and `.mm-dialog button.mm-run-btn` (0,2,1, styles.css:4481). The alternative of editing seven TS files to `className = "form-run-btn mc-run-btn"` is the worse trade: more churn for the same pixels. One CSS rule, no build risk, no test surface — it costs less to fix than to triage a second time out of a backlog.

---

## [Med] Batch report export: minutes-long run shown as a single 0% job item, and its Cancel button marks a fully-completed batch "Cancelled"

`F3d` · CODE-BUG · effort **M** · worth **fix-now** · verifier confidence high

**Where** `src-tauri/src/lib.rs:345`

**Evidence**

```
let label = format!("{} report(s)", well_ids.len());
jobs::run_simple_job(jobs_reg.inner().clone(), "Report batch", label, move || {
    let (written, errors) = report::export_report_batch(&conn, &spec, &well_ids, &dest_dir)?;

// run_simple_job's own doc (jobs.rs:262) says:
//   "Coarse single-unit off-thread job for a MONOLITHIC op that has no natural per-well/per-file
//    progress ... Use [`run_job`] instead whenever per-item progress is available."
// but report.rs:475 is a plain per-well loop that never polls cancellation:
for wid in well_ids { ... match render_report_pdf(db, &s) { ... } }
```

**Failure** — Export reports for a 40-well field. The dialog shows a static "Exporting 40 report(s)…" (reportDialog.ts:409) and the Processing panel shows one card, "Report batch — 40 report(s)", stuck at 0% with 0/1 for the whole multi-minute run (each well renders composite pages, runs the pay summary and writes FLAG_* curves). The user cannot tell whether it is working or hung. Worse: the panel offers a Cancel button (processingPanel.ts:203, shown whenever the phase is running). Clicking it sets the flag, but export_report_batch never reads it, so all 40 PDFs are still written; when the loop returns, run_job's `if finalize.is_cancelled() { finalize.cancelled(); }` (jobs.rs:246) marks the job Cancelled. The Processing panel then says "Cancelled" while the dialog simultaneously says "Wrote 40 report PDF(s) to <dir>" — two contradictory reports of the same run. LAS import (lib.rs:159) already uses run_job with per-file items, so the pattern is established.

**Fix** — Switch to jobs::run_job with one item per well_id; thread a JobHandle into report::export_report_batch so it can start_item/finish_item per well and break out of the loop on is_cancelled(), returning the partial written list.

**Verifier correction** — Claim stands as written; only the report.rs line number is slightly off — the uncancellable per-well loop is at approximately report.rs:471 (inside pub fn export_report_batch starting ~report.rs:466), not 475.

**Priority rationale** — Real, not theoretical, and cheap to fix. A 40-well field export is a normal Mahakam-scale run for this user, and every such run shows a frozen "Report batch — 40 report(s)" card at 0%/0-of-1 for several minutes with a static dialog line — the user has no way to distinguish "working" from "hung", and the app's own convention (LAS import at lib.rs:159 already uses run_job with per-file items) means the frozen card reads as a hang, not as a design choice. The worse half is the Cancel button: processingPanel.ts:203 renders it whenever the phase is active, cancelJob flips the flag, but report::export_report_batch (report.rs:472-499) is a bare `for wid in well_ids { ... match render_report_pdf(db, &s) }` that never reads it, so all 40 PDFs are still written and — because these renders run the pay summary and write FLAG_* curves back into DuckDB (reportDialog.ts:414 calls bumpDataVersion for exactly that reason) — the user's database is mutated for all 40 wells after they told the app to stop. When the loop returns, run_job's `if finalize.is_cancelled() { finalize.cancelled(); }` (jobs.rs:246) stamps the job Cancelled while the dialog simultaneously reports "Wrote 40 report PDF(s)"; the two UI surfaces contradict each other about the same run, and the plausible user response to "Cancelled" is to re-run the whole batch or to distrust the 40 PDFs that are in fact complete. That is a control-integrity/state-honesty defect, not cosmetics — the UI asserts an outcome that did not happen. Against that, the fix is contained and low-risk: export_report_batch is a single ~35-line function with one call site, the change is add a JobHandle param, start_item/finish_item per well, `if h.is_cancelled() { break }` at the top of the loop, and swap run_simple_job for run_job with one item per well_id. No rendering, PDF-assembly or pay-summary logic is touched, and run_job is already exercised by the import path, so there is no new machinery to validate. Small blast radius, established pattern, user-visible lie removed — do it now rather than letting it sit.

---

## [Med] SQL console reports the LIMIT-truncated row count as the true total

`F3d` · CODE-BUG · effort **S** · worth **fix-now** · verifier confidence high

**Where** `src-tauri/src/db.rs:1294`

**Evidence**

```
let limit = limit.clamp(1, 5000);
let wrapped = format!("SELECT * FROM ({trimmed}) __sandibumi_q LIMIT {limit}");
...
let total = rows_out.len();
Ok(TablePage { columns, rows: rows_out, total_rows: total })

// consumed unqualified at src/ui/sqlQueryPanel.ts:44 —
info.textContent = `${page.total_rows} row(s)`;
```

**Failure** — Run `SELECT w.well_name, s.depth FROM standard_curves s JOIN wells w USING (well_id) WHERE s.gr > 120` on a project with 200 wells. sqlQueryPanel calls runQuery(sql, 1000); the backend wraps the query in LIMIT 1000, so rows_out.len() is exactly 1000 and total_rows is reported as 1000. The panel prints "1000 row(s)" with no truncation marker anywhere, and the grid shows exactly those 1000. A user counting shaly samples, or checking how many rows a cleanup query would touch, reads 1000 as the answer when the true count may be 400,000. The DB Inspector next door gets this right ("${from}–${to} of ${page.total_rows}", dbInspectorPanel.ts:141) because its total is a real COUNT.

**Fix** — Return a `truncated: bool` (rows_out.len() == limit) or run a separate COUNT(*) over the wrapped query, and have the panel render "1000 row(s) (first 1000 — result truncated)" when set.

**Verifier correction** — Mechanism confirmed, but this is not a backend CODE-BUG at db.rs:1294 and not Med. The backend behaves as the developer documented: docs/manual_test_plan.md:2381 (T-REP-17, expected item 3) states "Result silently caps at the display limit (\"1000 row(s)\") rather than freezing the UI" - the cap and the bare count are a known, accepted state. The label is also literally true of rows returned, the true count is one COUNT(*) query away in the same console, and total_rows from run_query feeds nothing but that info label (runQuery has a single consumer). The correct finding is a Low-severity UI affordance gap at src/ui/sqlQueryPanel.ts:44: the panel should distinguish "exactly at the cap" from "complete result" - e.g. render "1000 row(s) (display limit - result may be truncated)" when page.rows.length === the requested limit - so the SQL console's count is not read with the same "of N total" semantics the adjacent DB Inspector legitimately carries. No backend change is required; the frontend already has enough information (rows.length vs the limit it passed).

**Priority rationale** — This is a data-honesty violation on the app's cardinal rule, and it is the common case rather than an edge case: the SQL console hard-codes `runQuery(sql, 1000)` (sqlQueryPanel.ts:42) and any row-level query against `standard_curves` blows past 1000 rows on a single well (a 2000 m interval at 0.1524 m sampling is ~13,000 samples), so essentially every non-aggregate query a petrophysicist types silently truncates and then reports "1000 row(s)" as if that were the answer. The user's own workflows are exactly the ones that get burned — counting shaly samples above a GR cutoff, sizing how many rows a cleanup would touch, sanity-checking a join before trusting a module run — and the number is not merely missing, it is confidently wrong and indistinguishable from a genuine 1000-row result. The DB Inspector sitting in the next dock renders `${from}–${to} of ${page.total_rows}` from a real COUNT (dbInspectorPanel.ts:141), which actively trains the user to read `total_rows` in this app as a true total. The fix is close to free and carries no churn risk: `run_readonly_query` has exactly one caller (lib.rs:1382) and the panel is ~90 lines, so either add a `truncated: bool` to `TablePage` or, cheaper still, have the panel compare `page.rows.length` against the limit it itself passed and render "first 1000 — truncated". Nothing well-tested is disturbed; the existing db.rs tests assert row contents, not the total semantics.

---

## [Med] ML dialog reports a green success and writes a History entry claiming every scoped well, when only some wells were written

`F3d` · CODE-BUG · effort **S** · worth **fix-now** · verifier confidence high

**Where** `src/ui/mlDialog.ts:501`

**Evidence**

```
if (res.error) {
  statusLine.textContent = `Failed: ${res.error}`;
} else {
  statusLine.textContent = `Done in ${ms} ms → ${res.outputs.join(", ")}`;
  setStatus(`${algo.label}: wrote ${res.outputs.join(", ")} to ${applyIds.length} well(s)`);
  recordProcess("ML", `${algo.label}: wrote ${res.outputs.join(", ")} to ${applyIds.length} well(s)`);
  bumpDataVersion();
}
```

**Failure** — Run k-means electrofacies on a 12-well group where only 2 wells have NPHI+RHOB (common with mixed-vintage Mahakam deliveries). ml.rs:344-353 pushes ApplyWell{error: Some("missing input curve data")} for the 10 wells without the features; because n_apply > 0 the run proceeds and ml.rs:473 returns `MlResult { ..., error: None }` with 10 of 12 well entries carrying errors. The dialog therefore takes the else-branch: status line "Done in 1240 ms → FACIES_ML", global status "K-means: wrote FACIES_ML to 12 well(s)", and a PERMANENT processing-history entry asserting 12 wells. The backend was honest (it marked those 10 Failed/Warned in the Processing panel, ml.rs:421 and 447) and moduleDialog.ts:311 does this correctly (`${ok}/${results.length} well(s) computed — ${failed} need attention`); only this dialog's status/History lie, and the History entry is the artefact that survives the session.

**Fix** — Compute `ok = res.wells.filter(w => !w.error).length` and use `${ok}/${res.wells.length}` in the status line, the global status and recordProcess; skip recordProcess entirely when ok === 0, and dispatch "sandibumi:open-processing" like moduleDialog does.

**Verifier correction** — The finding stands at src/ui/mlDialog.ts:501-503 with two refinements. (1) "only some wells were written" is precise only for wells whose ApplyWell carries an error (ml.rs:419-424 — missing input curve data, fetch error, or write error): those get no curve at all. Wells that reach the write path with m == 0 (ml.rs:444-458) DO get an all-NaN curve written and are only ItemState::Warned, yet they too carry `error: Some("no complete samples in this well")`, so they also inflate any naive ok-count — the proposed `ok = res.wells.filter(w => !w.error).length` correctly excludes both classes. (2) The user is not fully blind in-session: renderResults (mlDialog.ts:550-568) renders a per-well "Error" column. The lasting defect is therefore the global status line and, above all, the permanent History entry asserting the full scoped well count.

**Priority rationale** — This is squarely the app's cardinal-rule case: a partially-failed run presented as a clean success, and the lie is written into the permanent processing History, which is exactly the artefact used later to reconstruct what was done to a study. I confirmed the mechanism — ml.rs pushes `MlWellResult { rows_predicted: 0, error: Some("missing input curve data") }` per bad well (and a `Warned` "no complete samples in this well" case) while still returning `MlResult { ..., error: None }`, so mlDialog.ts:497-503 unconditionally takes the else-branch and hardcodes `applyIds.length` in the status line, the global status and `recordProcess("ML", ...)`. The trigger is not theoretical for this user: mixed-vintage Mahakam group deliveries where only some wells carry NPHI+RHOB are the normal case, and ML is almost always run group-wide. The concrete harm is that a 2-of-12 electrofacies run is remembered as "wrote FACIES_ML to 12 well(s)" — the interpreter then trusts FACIES_ML exists on wells where it silently does not, and any later audit of the History reads a false coverage claim. The only mitigation is the per-well table rendered below the fold, which the summary line actively contradicts. The fix is about four lines in one dialog, entirely local, with the correct pattern already sitting in moduleDialog.ts:311 (`${ok}/${results.length} well(s) computed — ${failed} need attention`) to copy verbatim; there is no large or well-tested area to churn, so the fix carries far less risk than the defect. Do the counting fix and the `ok === 0` skip now; the "sandibumi:open-processing" dispatch is a nice-to-have that can ride along but is not what makes this urgent. Files: D:\XX. Arshilla\src\ui\mlDialog.ts:497-505, D:\XX. Arshilla\src\ui\moduleDialog.ts:311, D:\XX. Arshilla\src-tauri\src\ml.rs:344-353 and 418-473.

---

## [Med] Pickett panel has no empty-state message at all — a well with no PHIE renders a clean, labelled log-log grid

`F3d` · INCONSISTENCY · effort **S** · worth **fix-now** · verifier confidence high

**Where** `src/ui/pickettPanel.ts:261`

**Evidence**

```
// drawPickett (line 75) unconditionally ends with:
  return plot;   // line 150 — never null

// so redraw has no empty branch at all:
plot = drawPickett(canvas, rt, phi, currentLine(), n, picks, hoverIdx, viewRef.current, {
  rtMin: props.rtMin, rtMax: props.rtMax, phiMin: props.phiMin, phiMax: props.phiMax, ...
});
```

**Failure** — Open Pickett on a freshly imported well. The panel defaults to RT=RES_DEEP, Porosity=PHIE (line 179), and PHIE only exists after a porosity module runs. reload() (line 320) sets `phi = new Float32Array(0)`. drawPickett fits the backing store, constructs the PlotCanvas over the fixed 0.1–1000 / 0.01–1 window and draws the frame, decade gridlines and both axis labels — a textbook-perfect, entirely empty Pickett plot. Nothing on screen distinguishes it from a well whose samples are all outside the axis window, from a bad curve selection, or from a backend that returned nothing; the crossplot and histogram siblings in the same folder both print "No valid data…" for the same condition. If the user then types M and Rw manually, the Sw=1/0.5/0.25 lines and the "M = …, Rw = … (N = 2)" readout are drawn over zero data points, which reads as a completed fit.

**Fix** — Have drawPickett return null when there are no (rt>0, phi>0) pairs and add the same centred message branch the crossplot/histogram use; suppress the M/Rw line readout when the point count is 0.

**Verifier correction** — Corrected claim (severity Low, not Med): "Pickett's redraw has no data-dependent empty state — drawPickett (src/ui/pickettPanel.ts:87) returns PlotCanvas unconditionally, so a well without PHIE renders a complete labelled empty grid, and a typed M/Rw draws Sw=1/0.5/0.25 lines plus the 'M = ..., Rw = ...' readout over zero points (line 97-119), reading as a completed fit." Two corrections to the reported mechanism: (1) phi is NOT `new Float32Array(0)` — src-tauri/src/equations.rs:129-139 returns a series for every requested name and fills a missing curve with `*values.get(i).unwrap_or(&f32::NAN)`, so phi is a full-length all-NaN array; the `?? new Float32Array(0)` at pickettPanel.ts:327 never fires for a missing curve (only the catch path at line 333, which does call setStatus). (2) "Nothing on screen distinguishes it" is overstated: pickettPanel.ts:211-217 appends a permanent hint ending "Needs a computed porosity curve (run a Porosity module first).", which covers the headline freshly-imported-well case — it is static, so it still cannot distinguish no-PHIE from all-points-outside-window from a bad curve pick. Also, the sibling comparison is weaker than stated: crossplot's null comes from axis-range resolution (crossplotPanel.ts:679-716), not point count, so an all-NaN NPHI-vs-PHIE crossplot also draws an empty grid because both curves have axisDefaults (lines 411-424).

**Priority rationale** — This is a data-honesty defect, not a cosmetic one, and it sits on the app's most common first-open path. `D:\XX. Arshilla\src\ui\pickettPanel.ts:181` defaults Porosity to `PHIE`, which does not exist until a porosity module has run, and `reload()` (line 326-327) silently falls back to `new Float32Array(0)` on a missing curve — and at line 332-333 the *load-failure* branch does the same, so a backend error also lands on the same clean canvas with only a transient status-bar line as evidence. `drawPickett` returns `PlotCanvas` unconditionally (line 150) and `redraw()` (line 262) has no empty branch, so the user gets a textbook-perfect log-log frame with decade grid and both axis labels. The killer is line 97-118: `currentLine()` is driven purely by the typed M/Rw fields, so as soon as the user types values the panel draws the Sw=1/0.5/0.25 lines plus the readout `Sw=1 line: M = …, Rw = … (N = 2)` over zero data points. Pickett's entire output is m and Rw, which the petrophysicist then carries straight into Archie/Simandoux — a fit-looking readout backed by no samples is exactly "a failed result presented as a clean success," the cardinal rule. Against that, the fix is about as cheap and low-risk as fixes get: the identical centred-message pattern already exists and works in both siblings in the same folder (`crossplotPanel.ts:1138`, `histogramPanel.ts:538`, with the string already translated in `i18n.ts:182`), so it is a ~15-line local change inside one file touching no shared plotting code, no state, and no backend. Cheap, proven pattern, real correctness payoff — do it now rather than letting it sit; scope it to the null-return + message + suppressing the M/Rw readout at zero valid pairs, and resist widening it into a rework of the drawPickett signature.

---

## [Med] Crossplot's "No valid data" empty state is unreachable for NPHI/RHOB/GR/DT/VSH/PHIE/PHIT/SWE/SWT — its own default axes

`F3d` · CODE-BUG · effort **S** · worth **fix-now** · verifier confidence high

**Where** `src/ui/crossplotPanel.ts:694`

**Evidence**

```
const base = axisDefaults(name) ?? (auto(values) ? { ...auto(values)!, invert: false } : null);
if (!base && (manMin === null || manMax === null)) return null;

// axisDefaults (line 409) returns a hard-coded range for exactly the mnemonics that matter:
//   NPHI, RHOB, GR, DT, VSH, PHIE, PHIT, SWE, SWT

// so drawCrossplot never returns null and the guard at line 1131 never runs:
if (!plot) { ... ctx.fillText("No valid data for these curves/zone.", ...); return; }
```

**Failure** — Open a Crossplot on an old GR/SP-only well, or on any well before a porosity module has run. The panel defaults to X=NPHI, Y=RHOB, colour=GR (line 913). reload() sets `xs = byName.get("NPHI")?.value ?? new Float32Array(0)` (line 1492); percentile() on an empty array returns NaN (plotCanvas.ts:945) so `auto()` returns null — but axisDefaults("NPHI") returns {-0.05, 0.6} and axisDefaults("RHOB") returns {1.9, 3.0}, so `resolve` succeeds, drawCrossplot returns a PlotCanvas, and the user gets a fully framed, gridded, axis-labelled, completely empty NPHI-RHOB crossplot with no explanation and no point count anywhere on the panel. Select PERM vs KLOGH instead (not in axisDefaults) and the same panel correctly says "No valid data for these curves/zone." — so the behaviour is inconsistent within one panel. histogramPanel.ts:538 and vegaPanel.ts:501 ("No finite X / Y samples in <well> · <zone>.") both do this correctly.

**Fix** — Before resolving axes, count finite (x,y) pairs; if it is 0, return null from drawCrossplot regardless of axisDefaults, so the existing message fires. Better: mirror vegaPanel's wording and name the well/zone.

**Verifier correction** — Same defect, minor corrections: the panel defaults live at crossplotPanel.ts:914-915 (xSel = "NPHI", ySel = "RHOB") and 924 (z = "GR"), not 913. The trigger set is broader than "a GR/SP-only well": any state where the resolved arrays are empty or all-NaN and both axes are among the 9 hard-coded mnemonics silently renders a fully framed, empty plot — including a zone selection with no samples on a well that does have NPHI/RHOB, and any case where manual xMin/xMax or yMin/yMax are set in Properties (those bypass the null path entirely regardless of mnemonic).

**Priority rationale** — Real, cheap, and on the honesty line. The trigger is not exotic: the panel hard-defaults to X=NPHI / Y=RHOB (crossplotPanel.ts:913) and `axisDefaults` (line 409) hands back {-0.05,0.6} and {1.9,3.0} for those exact two mnemonics, so `resolve` (line 694, `const base = axisDefaults(name) ?? ...`) never fails and `drawCrossplot` never returns null — the guard at line 1131 that would print "No valid data for these curves/zone." is dead for precisely the curve set Jauhar opens most (NPHI/RHOB/GR/DT/VSH/PHIE/PHIT/SWE/SWT). Old GR/SP-only Mahakam wells, or any well opened before a porosity/Sw module has run (PHIE/SWE/SWT genuinely absent, and they are also in axisDefaults), produce a fully framed, gridded, axis-labelled crossplot with zero points and zero explanation. That is indistinguishable from the legitimate case "my data exists but sits outside a manually-set range" — and the panel has SVG/PDF export (`getSvg`/`getPdf`, lines 1123-1126), so a blank-but-professional-looking D-N chart can land in a client deliverable with no marker that the curve was missing rather than empty. That is a failed result rendered as a clean-looking one, which is the cardinal rule. It is also self-inconsistent: PERM vs KLOGH in the same panel correctly reports the message, and histogramPanel.ts:538 / vegaPanel.ts:501 both get it right, so the crossplot is the odd one out and a future reader will "fix" the wrong side. The fix is a finite-pair count before axis resolution returning null — roughly five lines inside one function, no schema, no state, no touching the drawing/regression/overlay code, so the fix is far cheaper and less risky than the defect it removes.

---

## [Med] ΔlogR overlay draws a resistivity-only track with a legend for the missing porosity curve — reads as "no separation, not source rock"

`F3d` · CODE-BUG · effort **S** · worth **fix-now** · verifier confidence high

**Where** `src/ui/unconventionalPanel.ts:614`

**Evidence**

```
overlayRes = resS?.value ?? new Float32Array(0);
overlayPor = porS?.value ?? new Float32Array(0);
overlayDepth = resS?.depth ?? porS?.depth ?? new Float32Array(0);

// drawOverlay only bails when BOTH are gone (n === 0 / xLo non-finite), and unconditionally
// prints the porosity legend entry at line 211:
ctx.fillStyle = theme.accent2;
ctx.fillText(`▬ ${d.porName} (${d.mode})`, padL + 84, 12);
```

**Failure** — Open the Unconventional pane on a well that has RES_DEEP but no DT (sonic mode is the default, and defaultPorCurve falls back to the canonical "DT" name even when the well has none — line 450). `overlayPor` is length 0 while `overlayDepth` is length N, so the n === 0 guard at line 84 does not fire and xLo is finite from the resistivity alone, so the "No overlapping … samples" guard at line 128 does not fire either. Every poroTerm is NaN, so the porosity polyline draws nothing and the ΔlogR shading loop (line 175, requires both xR and xPor finite) fills nothing. The result is a fully framed depth track with decade gridlines, the resistivity curve, and a two-entry legend claiming both "▬ RES_DEEP" and "▬ DT (sonic)" are plotted — with zero shaded separation. For a Passey ΔlogR chart, "no separation" is the literal reading for "not organic-rich", so an absent curve is presented as a negative source-rock result.

**Fix** — Track finite counts for xR and xPor separately; if either is 0, call centeredMessage with e.g. "DT has no data in this interval — ΔlogR needs both curves", and never draw a legend entry for a curve with no finite samples.

**Verifier correction** — Same defect and same severity, corrected mechanism and anchor lines. The ΔlogR overlay renders a resistivity-only track with a legend entry for a porosity curve that has no finite samples, presenting an absent curve as a genuine "no separation / not organic-rich" Passey result. Cause is NOT an empty overlayPor array. src-tauri/src/equations.rs:129-139 (fetch_curve_data) returns a series for EVERY requested curve name, including ones the well does not have: `let values = columns.get(&upper).cloned().unwrap_or_default();` then `point_count = keep.len();` and `packed.extend(keep.iter().map(|&i| *values.get(i).unwrap_or(&f32::NAN)));`. So for a DT-less well, porS at unconventionalPanel.ts:613 IS defined and overlayPor is a full-length Float32Array of NaNs; the `?? new Float32Array(0)` fallback never fires. The load-bearing frontend lines are therefore not 612-614 but: (a) line 84 `if (n === 0)` — n is d.depth.length, full length from resistivity, so it never fires; (b) lines 104-109, where `p` is NaN so poroTerm and xp are NaN; (c) line 127 `if (!Number.isFinite(xLo) || !Number.isFinite(dLo) || dHi <= dLo)` — xLo is finite from `xr` alone (set at 113-116), so the "No overlapping ... samples" message never shows; (d) line 197 drawTrackCurve draws nothing and lines 178-195 shade nothing because both require Number.isFinite(xPor[i]); (e) line 211 `ctx.fillText(\`▬ ${d.porName} (${d.mode})\`, padL + 84, 12)` prints the porosity legend entry unconditionally. Reachability is stronger than the reporter argued: defaultPorCurve at line 449-452 (`wants.find((c) => names.includes(c)) ?? wants[0]`) tests against curveNames from loadCurveNames(), which calls list_curve_catalog (src-tauri/src/lib.rs:506) with NO well_id — a project-global catalog. Its doc comment at 447-448 ("prefer a real sonic / density mnemonic that the well actually has") is therefore inaccurate: on a multi-well project where any OTHER well has DT, "DT" is auto-selected for a well that lacks it, so the bad state is the default on well switch, not just on manual selection. curveSelect (src/ui/plotCommon.ts:231) additionally prepends a missing name as a selectable option. Correct fix: count finite samples of xR and xPor separately in the loop at line 100; if either count is 0, call centeredMessage naming the empty curve, and gate each legend entry (lines 205 and 211) on its curve having at least one finite sample. Optionally also scope defaultPorCurve/curveNames to the active well's curves rather than the global catalog.

**Priority rationale** — This is a data-honesty failure of the exact kind the app's cardinal rule forbids, and the trigger path is routine rather than theoretical. `loadCurveNames()` (src/ui/plotCommon.ts:242) feeds the selector from `list_curve_catalog`, which is a PROJECT-wide catalog, not a per-well one, so in any multi-well project the "DT" option exists in the dropdown for wells that do not have DT — and `defaultPorCurve` (unconventionalPanel.ts:449-452) additionally falls back to the canonical "DT" string when no sonic mnemonic matches at all. `getCurveData` then returns only the resistivity series, `overlayPor = porS?.value ?? new Float32Array(0)` (line 613) is empty while `overlayDepth = resS?.depth` (line 614) is length N, so neither the `n === 0` guard (line 84) nor the `!Number.isFinite(xLo)` guard (line 127) fires, and line 211 unconditionally prints `▬ ${d.porName} (${d.mode})` in accent2. What makes this worse than a cosmetic legend bug is that the failure is visually IDENTICAL to the correct negative result: the panel's own hint (line 516) tells the user the two curves "overlie in non-source rock and separate over organic-rich intervals", so a single drawn polyline with zero shading is precisely what a perfectly-overlying pair looks like. A missing curve is therefore rendered as a confident "not organic-rich" ΔlogR screen, feeding a false negative into a TOC/source-rock or MNK screening conclusion — a wrong answer the user has no on-screen cue to catch. The same hole exists mirrored (well has DT but not RES_DEEP → depth falls through to `porS?.depth`). Against that, the fix is about as cheap and low-risk as fixes get: `drawOverlay` has exactly one caller (line 573), no other module imports it, and the change is two finite-counters inside the loop that already runs at lines 113-120, an early `centeredMessage` (a helper already used twice in this same function), and making each legend `fillText` conditional — roughly ten lines inside one self-contained canvas function with no shared-state or layout churn. Cheap contained fix versus a silently wrong petrophysical verdict: fix now.

---

## [Med] Field Dashboard tells the user "FLAG curves written." on the one code path that deliberately writes nothing

`F3d` · CODE-BUG · effort **S** · worth **fix-now** · verifier confidence high

**Where** `src/ui/dashboardPanel.ts:251`

**Evidence**

```
allRows = await runPaySummary({
  ...
  // Dashboard is read-only: compute the stats, persist nothing. Skips ~1,600 FLAG-curve
  // write transactions per Compute. Persisting flags stays with Cutoffs & Summary.
  stats_only: true,
});
const flags = new Set(allRows.map((r) => r.flag));
statusEl.textContent = `${wellIds.length} well(s) · ${allRows.length} zone-rows across ${flags.size} flag level(s). FLAG curves written.`;
```

**Failure** — Press Compute on the Field Dashboard. workflow.rs:562 (`if !req.stats_only { ... }`) skips every FLAG_* write — this is asserted by the unit test at workflow.rs:1604 ("stats_only must not write any FLAG_* curve", "stats_only must not create a PAYFLAG log set"). The status line nonetheless states "FLAG curves written." The user then opens a Log View or picks FLAG_PAY as the crossplot Z-curve expecting the flags to be there and finds nothing, with no error to explain it. The claim is a leftover from before the stats_only optimisation and directly contradicts the comment three lines above it.

**Fix** — Drop the sentence, or replace it with "stats only — run Cutoffs & Summary to write FLAG curves".

**Verifier correction** — Finding stands at the cited file:line with the cited mechanism. Two refinements: (1) the user-visible failure is not only "flags missing" — if an earlier Cutoffs & Summary run already wrote FLAG_PAY, the dashboard claims "FLAG curves written" after a cutoff tweak while Log View still shows STALE flags computed at the old cutoffs, which is silently wrong rather than merely absent; (2) severity is better rated Low/Med than Med, since it is status text only with no data loss or corruption — the cost is user misdirection, not incorrect persisted results.

**Priority rationale** — This is a data-honesty violation with a one-line, zero-risk fix, which is the cheapest possible ratio in the backlog. The status line at D:\XX. Arshilla\src\ui\dashboardPanel.ts:251 asserts a persistence side effect ("FLAG curves written.") that the code on the same call provably never performs — stats_only: true is passed at line 248, workflow.rs:562 gates every FLAG_* write behind `if !req.stats_only`, and the unit test at workflow.rs:1604 pins that behaviour ("stats_only must not write any FLAG_* curve", "stats_only must not create a PAYFLAG log set"). The user impact is not theoretical: Field Dashboard is a first-class menu panel (workspace.ts:328/:430), FLAG_SAND/RESERVOIR/PAY are real artifacts that other paths genuinely do write (summaryDialog.ts:21, reportDialog.ts:295), so a petrophysicist who reads that line and then opens a Log View or selects FLAG_PAY as a crossplot Z-curve finds nothing, with no error to explain it — the classic "hunt for the bug that is actually a lying status message" time sink, and exactly the pattern this app's cardinal rule forbids. The fix touches one template string in a leaf UI handler: it churns no logic, no schema, no test (the Rust test asserts behaviour, not copy), and cannot regress anything. While in there, the same-vintage stale doc at src/ipc.ts:1482-1483 should go too — it still claims "Field Dashboard sets this true so its field-wide QC pass writes FLAG_* in place", which is now false for the dashboard and is precisely the comment a future maintainer would trust when deciding whether skip_version/stats_only can be collapsed or removed.

---

## [Med] A tops row with color = NULL renders in three different colours across three views (grey chip / gold line / theme-warn connector)

`F3a` · INCONSISTENCY · effort **S** · worth **backlog** · verifier confidence high

**Where** `src/ui/topsPanel.ts:49`

**Evidence**

```
// topsPanel.ts:49 — Wells & Tops tree swatch
<span class="top-color" style="background:${t.color ?? "#8b8f96"}"></span>

// topsEditor.ts:9 + :96 — the same top drawn on the log view
const DEFAULT_COLOR = "#e2b93d";
...
      const color = t.color ?? DEFAULT_COLOR;

// correlationPanel.ts:345 — the same top's connector in the correlation view
      const color = active.flatMap((s) => s.tops).find((t) => t.top_name === name)?.color || theme.warn;
```

**Failure** — TopEntry.color is `string | null` (ipc.ts:104) and the CSV tops importer writes NULL for every row: `db::upsert_top(conn, &well_id, &rec.top_name, rec.depth, None)` (src-tauri/src/ingest.rs:515). Importing a tops CSV out of Geolog/Petrel — the normal way tops get into the app — therefore produces tops with no colour, and the same TOP_BEKASAP then shows as a mid-grey #8b8f96 chip in the Wells & Tops tree, a gold #e2b93d line + label chip on the log view, and a red (--warn, e.g. #ed1a2f under Pertamina) connector in the correlation view. The user cannot colour-match a top between the tree and the plots, which is the whole point of the swatch. The grey and gold literals are also palette-blind: #e2b93d on --bg-panel #fbf7ee is 1.76:1, so the 1.25px tops line is nearly invisible in the light default and in all four white-panel branded skins (only the gold label chip keeps it findable).

**Fix** — Export one DEFAULT_TOP_COLOR from a shared module and use it in all three call sites, or better, have the ingest path assign a deterministic colour per top name so the value is stored once and every view reads the same field. Whichever literal is chosen, verify it against --bg-panel = #ffffff (four branded skins) as well as against the dark panel.

**Verifier correction** — Mechanism is if anything under-stated at correlationPanel.ts:345: `active.flatMap((s) => s.tops).find((t) => t.top_name === name)?.color || theme.warn` takes the FIRST strip's row for that name, so a top the user has explicitly coloured in well B still draws as --warn red in the correlation view whenever well A's row for the same name is NULL (or empty). The inconsistency therefore is not limited to all-NULL imported tops.

**Priority rationale** — Real, reachable on the normal path (CSV tops import writes None for every row, so every imported top takes all three fallbacks), but the severity is lower than the write-up claims and the fix is cheap enough to ride along with the next tops/theme touch rather than jumping the queue. The weak half of the claim is the colour-matching argument: when every top is null-coloured, every chip in the tree is the same grey, every line in the log view is the same gold, and every connector in correlation is the same warn-red — there is no per-top discrimination to lose in any view, so the user is not actually mis-matching TOP_BEKASAP against a differently coloured neighbour. The strong half is legibility: the hardcoded #e2b93d at lineWidth 1.25 (topsEditor.ts:97) sits on --bg-panel #fbf7ee in the light default and #ffffff in the four branded skins, and a ~1.8:1 line is genuinely hard to see when tops are the main zone reference a petrophysicist scans for — the gold label chip is the only thing keeping it findable, which is a usability tax, not a wrong answer. This is not a data-honesty issue: nothing degraded is being shown as clean, no number is wrong, only a stroke colour, so it does not hit the cardinal-rule fix-now trigger. Fix risk is near zero for the cheap variant (export one DEFAULT_TOP_COLOR from a shared module, pick a value or a theme token that clears contrast on both #ffffff and the dark panel, use it at all three sites — three lines, no logic touched, nothing well-tested churned). Deliberately defer the second half of the proposal, assigning deterministic colours per top name in ingest.rs:515: that writes into stored rows, raises a backfill question for tops already imported, and is a bigger change than the problem justifies. The maintenance hazard is real but slow-burning — a fourth tops consumer (e.g. a tops track in the Vega panel or a tops column in an export) would invent a fourth literal, so the shared-constant fix is worth doing before that next tops-rendering surface is added, which is exactly what makes it a backlog item with a natural trigger rather than an interrupt.

---

## [Med] Phantom font tokens --mono and --font-ui: the vega error line and every scatter tooltip render in a different typeface than the rest of the app

`F3a` · THEME-VIOLATION · effort **S** · worth **backlog** · verifier confidence high

**Where** `src/styles.css:2838`

**Evidence**

```
.vega-spec-err {
  color: var(--danger, #d9534f);
  font-size: 11.5px;
  font-family: var(--mono, monospace);       /* contract token is --font-mono */

.plot-tooltip {
  font-family: var(--font-ui, system-ui, sans-serif);   /* styles.css:2732 — no such token */

/* what the contract actually declares, styles.css:34-35: */
  --font-canvas: "Segoe UI Variable Text", "Segoe UI", system-ui, sans-serif;
  --font-mono: "Cascadia Code", Consolas, monospace;
/* and what four other rules correctly use: 1305, 1312, 1690, 4360, 4757 -> var(--font-mono) */
```

**Failure** — Neither --mono nor --font-ui is defined in any theme block, so both always take the fallback. The Vega spec-error line renders in the generic `monospace` (Courier New under WebView2 on Windows) while the SQL editor, DB inspector and multimin cells render in Cascadia Code via var(--font-mono) — two mono faces side by side in the same app, in all 7 palettes. The .plot-tooltip case is worse and is documented by the file itself: styles.css:188-190 explains that `system-ui` resolves to STATIC Segoe UI on Win 11, which snaps the app's base font-weight 500 down to 400, whereas the body and --font-canvas use "Segoe UI Variable Text" which honours 500. So the crossplot/Pickett hover tooltip renders visibly lighter and in a different face than the panel behind it — the exact regression pass #9A was supposed to eliminate. Named future change that trips on this: adding a branded font override to a skin (the comment at styles.css:32-33 promises "a branded skin can restyle text by overriding one variable") will silently miss both of these elements.

**Fix** — Replace var(--mono, monospace) with var(--font-mono) at line 2838 and var(--font-ui, system-ui, sans-serif) with var(--font-canvas) at line 2732 (--font-canvas already carries the body face). Alias-or-delete the other phantoms in the same pass.

**Verifier correction** — The finding is real but UNDER-reported and has one wrong mechanism detail. (a) The same rule carries a THIRD phantom on the line above the one flagged: styles.css:2836 `color: var(--danger, #d9534f);` — `--danger` is likewise declared nowhere, while the real per-theme error token `--warn` IS declared in every block (#a83e2c light, #d96a52 dark, #ed1a2f Pertamina, #b3141b Halliburton, #c0392b SLB, #b33636 LAPI-ITB, #c0392b white). So the Vega spec-error line hardcodes an off-palette bootstrap red in all 7 themes — more visible than the typeface defect reported. Fix should be var(--warn) alongside var(--font-mono). (b) Mechanism nit: styles.css:191 sets the body font-family as a LITERAL stack ('Segoe UI Variable Text', 'Segoe UI', system-ui, sans-serif), not var(--font-canvas). --font-canvas only duplicates that stack; it is not the body's source. The proposed fix still resolves to the same face and is correct, but the branded-skin argument is stronger than stated: overriding --font-canvas would also leave the literal body rule at line 191 unaffected.

**Priority rationale** — Verified real and slightly broader than reported: `src/styles.css:2838` uses `var(--mono, monospace)`, `:2732` uses `var(--font-ui, system-ui, sans-serif)`, and neither token is declared anywhere in the file (only `--font-canvas`/`--font-mono` at :34-35 exist), so both always fall back; the same `.vega-spec-err` rule also carries a third phantom, `color: var(--danger, #d9534f)` at :2836, whose only occurrence in the whole stylesheet is that one usage — so the error line is off-palette as well as off-face in all 7 skins. But nothing here is a correctness or data-honesty problem: no number is wrong, no failure is presented as success. The user-visible cost is small and split unevenly — the vega spec-error line is a transient state that only appears while a hand-edited Vega spec is invalid (rare, and it is *supposed* to look alarming), while `.plot-tooltip` is the one that is genuinely seen constantly, on every crossplot/Pickett hover, rendering in static Segoe UI (weight 500 snapped to 400 per the file's own comment at :186-189) against a body that honours 500 — a subtle lightness/face mismatch on an 11px transient bubble, not something a petrophysicist reading Sw will ever be blocked by. The real argument for doing it is maintenance: the theming contract is a shipped feature with per-client brand palettes, and the comment at :32-33 promises a skin can restyle all text by overriding one variable — a branded-font skin would silently miss both elements, and the bug is invisible precisely because fallbacks make it look fine. The fix is two token renames plus one colour token, zero behavioural risk, no layout reflow, nothing to re-test beyond a glance. That combination — trivial safe fix, negligible present-day user cost, real future-skin trap — is exactly backlog: do it as one grep-driven phantom-token sweep (every `var(--x` cross-checked against declared names) the next time the theme/CSS area is open, not as its own interrupt.

---

## [Med] No --on-accent token: color:#fff hardcoded on background:var(--accent) gives 2.7:1 text on every primary button in the dark palette

`F3a` · THEME-VIOLATION · effort **M** · worth **backlog** · verifier confidence high

**Where** `src/styles.css:1369`

**Evidence**

```
.btn.btn-accent {
  background: var(--accent);
  border-color: var(--accent);
  color: #fff;
}

/* same pattern, 12 more sites: */
1605 .lp-item.selected      1738/1743 .lp-btn.primary     1879 .form-run-btn
2170 .map-btn:hover         2179 .map-btn-on            2876 .tree-group-manage:hover
2945 .wg-btn:hover          3961 .well-scope-mode-active 4095 .plot-export-btn:hover
4108 .tree-lock-btn.active  4489/4495 .mm-run-btn         4617 .cutoff-seg-btn.active
4706 .guard-confirm button.primary      4547 .guard-confirm button.danger (on var(--warn))
```

**Failure** — The contract has no foreground-on-accent token, so 15 rules hardcode white. Under [data-theme="dark"] --accent is #d98c3f (styles.css:47), whose relative luminance is 0.339; white text on it is 2.70:1 — below even the 3:1 large-text floor. This hits the app's most-clicked controls: the primary Run button in every module/multimin dialog (.mm-run-btn, .form-run-btn), the active well-scope segment, the selected layout item, and the active cutoff segment. A Mahakam petrophysicist working in the dark palette sees washed-out white labels on light-orange buttons. The dark palette's own --text (#e8dfd0) would be worse (2.04:1); the correct foreground there is near-black (#1c1610 gives 6.6:1), which is exactly why a per-palette token is needed rather than a literal. Secondary: .guard-confirm button.danger (line 4547) puts #fff on the dark palette's --warn #d96a52 at 3.42:1.

**Fix** — Add --on-accent (and --on-warn) to all 7 palette blocks — #ffffff for pertamina/halliburton/schlumberger/lapi-itb/white/light, #1c1610 for dark — and replace the 15 `color: #fff` declarations with var(--on-accent)/var(--on-warn). This also closes the gap for any future light-accent skin.

**Verifier correction** — The defect is real; two details are off. (1) Count: there are 17 `color: #fff` declarations, not 15 — the reporter's enumerated line list is complete and correct, but the prose undercounts. (2) Scope of the fix: the token must be added to 8 blocks, not 7 — the six named skins (dark, pertamina, halliburton, schlumberger, lapi-itb, white), plus `:root`, plus the duplicated `@media (prefers-color-scheme: dark)` block at styles.css:56-70 which redeclares the whole dark palette. There is no `:root[data-theme="light"]` block; "light" resolves to the `:root` default, whose --accent #b5651d gives 4.33:1 against white — marginally under AA-normal but above the 3:1 floor, so the dark palette (2.70:1) and its --warn (3.42:1) are the only concrete failures.

**Priority rationale** — Real but non-blocking, and the fix is cheap enough that it should ride along with the next theme touch rather than jump the queue. It is a real hit, not theoretical: the dark palette is reachable without opting in (styles.css:56 `@media (prefers-color-scheme: dark) { :root:not([data-theme="light"]) ... }`), so any Windows 11 user with system dark mode — the default on a lot of machines — lands on `--accent: #d98c3f` and gets 2.7:1 white labels on the Run button in every module/multimin dialog and on the active cutoff segment. That said, it degrades legibility, not correctness: the label is washed out, not wrong or missing, no result is misrepresented, so it does not touch the data-honesty cardinal rule and nothing about a run silently reads as a clean success. The fix is the opposite of risky — two new tokens in 7 palette blocks plus 17 mechanical `color: #fff` → `var(--on-accent)` swaps in one file, no logic, no state, visually verifiable in one pass of the theme switcher; there is no "large, working, well-tested area" to churn because CSS declarations are the whole surface. The real reason not to drop it is the maintenance hook: the theme system is a documented fixed-var contract with a documented add-a-theme path and client-brand skins (Pertamina/Halliburton/SLB/LAPI-ITB), and today that contract has no foreground-on-accent slot at all — so the next client skin with a pale or yellow accent (a plausible request from an Indonesian operator brand) produces white-on-pale, i.e. an invisible Run label, and the person adding the skin has no token to reach for. Fixing it now converts a silent trap in a documented extension point into a filled-in slot. Backlog it as a single bundled chore with the `.guard-confirm button.danger` / `--warn` case (4547, 3.42:1) and do it the next time a palette is added or edited.

---

## [Med] Highlight default palette collapses to duplicate colours in every palette — slots 1/4 and 5/6 are the same hue, and the colour is persisted

`F3a` · THEME-VIOLATION · effort **M** · worth **backlog** · verifier confidence high

**Where** `src/ui/highlightsOverlay.ts:33`

**Evidence**

```
function palette(): string[] {
  ...
  return [
    v("--accent", "#e0b64a"),
    v("--accent2", "#5fae5f"),
    v("--warn", "#d9694a"),
    v("--accent-dim", "#4a90d9"),
    v("--border-strong", "#9b6fd0"),
    v("--text-dim", "#3fb0b0"),
  ];
}

// highlightsOverlay.ts:292-293, inside createHighlight():
    const pal = palette();
    const color = pal[this.highlights.length % pal.length];
// ...then persisted:  await upsertHighlight(wellId, id, topDepth, bottomDepth, color, null);
```

**Failure** — --accent and --accent-dim are by contract a light/dark pair of the SAME hue in every palette (light #b5651d/#8f4d15, dark #d98c3f/#b5651d, pertamina #006bb8/#00538f, halliburton #e31b23/#b3141b, schlumberger #0033a0/#00246f, lapi-itb #003c7d/#002a58, white #4a5568/#333b49), and --border-strong/--text-dim are likewise a same-hue pair. Bands are painted at DEFAULT_ALPHA = 0.16 (line 181, ctx.globalAlpha = DEFAULT_ALPHA). Concretely in the default light theme over --bg-app #f2ebdc: band 1 (--accent) composites to rgb(232,214,189) and band 4 (--accent-dim) to rgb(226,210,188) — a 6/4/1 RGB delta, visually identical; band 5 vs band 6 differ by 11/10/7. So a user who drags 4+ highlight bands on one well gets two pairs of indistinguishable bands, in ALL SEVEN palettes, and because createHighlight persists the chosen string those duplicates survive a theme switch and an app restart. The hardcoded fallback array in this very function IS a proper distinct-hue rotation (gold/green/coral/blue/purple/teal) but is unreachable, since all six vars resolve in every palette.

**Fix** — Stop treating the theme vars as a qualitative rotation. Either seed from a fixed distinct-hue set (the FACIES_PALETTE in plotCanvas.ts:791-795 already exists and is exactly this: Tableau-10 categorical), or keep theme-derived seeding but drop --accent-dim and --border-strong from the rotation and generate the remaining slots by hue-rotating --accent so no two slots share a hue.

**Verifier correction** — The rotation in palette() (src/ui/highlightsOverlay.ts:32-39) yields only 3 independent hues, not 6, because slots 1/4 (--accent/--accent-dim) and 5/6 (--border-strong/--text-dim) are same-hue light/dark pairs by the theme contract. The reporter UNDERSTATED two cases: in the halliburton theme slots 3 and 4 are the byte-identical literal #b3141b (styles.css:113 --accent-dim: #b3141b; and styles.css:117 --warn: #b3141b;), not merely the same hue; and in the white theme 5 of 6 slots collapse to the same desaturated slate (#4a5568, #7b8794, #333b49, #b6b9be, #6a6f75), leaving only --warn #c0392b distinct. The reporter also MISSED a second and third call site: draw() line 135 (const color = hl.color ?? pal[i % pal.length];) and line 143 (the dashed drag preview pal[this.highlights.length % pal.length]), so the collision is visible live during the drag, before anything is persisted. Two parts of the description are WRONG: (a) the [THEME-VIOLATION] tag and the "duplicates survive a theme switch" framing are misattributed - the comment at lines 22-25 documents persistence as deliberate ("Values are hex-validated because they seed <input type=color> and are persisted per highlight"), so persistence is intent and the defect is solely the hue collapse; (b) severity is over-called at Med and should be Low, because createHighlight force-opens openEditDialog at line 318 with a live <input type="color"> seeded to the band colour, giving the user an immediate recolour prompt, and labelled bands still get a label chip painted at globalAlpha = 0.92 (lines 204-207) in near-full-strength colour that does distinguish them. The cost is a bad default on unlabelled bands, not an unrecoverable state.

**Priority rationale** — Real but cosmetic, with a cheap and genuinely low-risk fix — so it belongs on the list, not at the front of it. The defect is confirmed: src/styles.css defines --accent/--accent-dim and --border-strong/--text-dim as same-hue lightness pairs in all seven palettes, and highlightsOverlay.ts:32-39 uses all six as if they were a qualitative rotation, so a user who drags a 4th and a 6th highlight band on one well gets visually identical fills at DEFAULT_ALPHA 0.16, persisted by createHighlight (:293) so the duplication survives restart and theme switch. But this is not a correctness or data-honesty issue and does not touch the cardinal rule: highlights are informal user-drawn annotations (grep confirms HighlightEntry/listHighlights are consumed nowhere but this overlay and ipc.ts — no legend, no export, no colour key), so colour never encodes a computed result. Bands are disambiguated by depth position and by the label chip painted at alpha 0.92 (:204-210) where the light/dark pair is actually separable, and any band is recolourable in two clicks via the dblclick edit dialog. Against that, the fix is contained to one 8-line pure function with a single caller, needs no migration (already-saved colours simply stay), and would churn nothing else — so it is an ideal ride-along whenever highlightsOverlay.ts is next opened. When it is done, fix the invisibility too, not just the duplication: slots 5 and 6 (--border-strong #c2ac81, --text-dim #7c6b52) are near-neutral tans that at 0.16 alpha over --bg-app #f2ebdc render as barely-there bands regardless of duplication, which is the more annoying half of the bug. Seeding from FACIES_PALETTE (plotCanvas.ts:791-795) is the right call — it is already a distinct-hue categorical set with adequate saturation, so it solves both halves in one edit.

---

## [Med] Tool panes snapshot listCurveCatalog() at build, so curves written by a run never appear in their pickers

`F3b` · CODE-BUG · effort **M** · worth **backlog** · verifier confidence high

**Where** `src/ui/mlDialog.ts:147`

**Evidence**

```
mlDialog.ts:145-149 `const [wells, catalog] = await Promise.all([listWells()..., listCurveCatalog().catch(() => [])]); const curveNames = catalog.map((c) => c.name);` with dispose `{ scope.dispose() }` (mlDialog.ts:515) and no appState subscription anywhere in the file. Same one-shot snapshot in workflowDialog.ts:52-53, shfDialog.ts:36-37, faciesTieDialog.ts:14, lorenzDialog.ts:16-20 (which also snapshots the Well dropdown: `const scoped = filterByActiveGroup(wells)` at :21). moduleDialog.ts:234-256 already implements the correct fix for exactly this — a generation-guarded `refreshData()` behind a primed `appState.dataVersion.subscribe` that re-runs listCurveCatalog() and refills every select in place.
```

**Failure** — User opens the Lorenz panel, sees no permeability curve, runs the Rock Typing module to produce PERM_RT (bumps dataVersion), returns to the still-open Lorenz panel — the "Permeability (k)" dropdown (lorenzDialog.ts:52 `curveSel(["PERM","KLOGH","PERM_RT","KH","K"])`) still lists only the pre-run names, so it looks as though the module wrote nothing. Same for the ML panel's predictor list after a SandiMin run, the SHF panel's BVW/PHIE pickers after computing PHIE, and the Workflow builder's curve pickers after any step writes a curve. Only remedy today is closing and reopening the pane, which also discards the entered parameters.

**Fix** — Extract moduleDialog's refreshData/dataPrimed pattern into a small shared helper in plotCommon.ts (e.g. `onCurveCatalogChange(refill)` returning an unsub) and call it from mlDialog, workflowDialog, shfDialog, faciesTieDialog and lorenzDialog, refilling the selects with their current values preserved.

**Verifier correction** — The finding is real for mlDialog.ts:145-149, lorenzDialog.ts:16-21 (which additionally freezes its Well dropdown), shfDialog.ts:36-37 and faciesTieDialog.ts:14-15. But the Workflow builder should be dropped from the list as described: workflowDialog.ts:59-62 deliberately unions every module's declared log_out names into inputCurveNames (`const moduleOutputs = [...new Set(modules.flatMap((m) => m.args.filter((a) => a.kind === "log_out").map((a) => a.name)))].filter((n) => !curveNames.includes(n)); const inputCurveNames = [...curveNames, ...moduleOutputs];`) so module-written curves are already selectable before any run — "after any step writes a curve" is false there. Workflow is only stale for non-module-written curves (new imports, ML outputs, curve-editor/crossplot-written curves), which is a much weaker case.

**Priority rationale** — Real, not theoretical: dock panes are built exactly once (workspace.ts:472/484/502 route through asyncPane and dockview keeps the instance alive for the session), so a pane opened before a run keeps its build-time snapshot as long as it stays open — lorenzDialog.ts:16-21 grabs catalog/scoped once and never re-reads, and mlDialog.ts:145-149 does the same with no appState subscription anywhere. Jauhar's own ordering (rock typing writes PERM_RT, then look at the SMLP; multimin writes PHIE, then fit SHF) is exactly what trips it, and the only remedy is closing the pane and retyping parameters. It is not fix-now, though, because it fails in the safe direction: nothing wrong is computed or displayed as clean — the module run itself reports honestly and the picker merely fails to offer the new name, so the cardinal "degraded result must not look like success" rule is not engaged (this makes a success look like a no-op, the conservative error). Fix risk is low but real scope is wider than the five files named: summaryDialog, cutoffDialog, monteCarloDialog, thomeerDialog and hfuDialog are built through the same one-shot asyncPane path, so the sensible move is a single batched pass extracting moduleDialog.ts:234-256's generation-guarded refreshData plus primed dataVersion.subscribe into a shared helper and wiring every tool pane to it — proven and purely additive (no existing logic rewritten), but a ~10-file sweep that deserves a scheduled slot rather than an interrupt. It also unblocks future panes, which today would copy the stale snapshot an eleventh time.

---

## [Med] Monte Carlo histogram and tornado keep the previous theme's bar/grid/label colours after a theme switch

`F3b` · CODE-BUG · effort **S** · worth **backlog** · verifier confidence high

**Where** `src/ui/monteCarloDialog.ts:834`

**Evidence**

```
monteCarloDialog.ts:801-803 `function cssVar(name){ return getComputedStyle(document.documentElement).getPropertyValue(name).trim() || "#888"; }` feeding drawHistogram at :834-837 `const barCol = opts.barColor || cssVar("--accent"); const accent2 = cssVar("--accent2"); const text = cssVar("--text-dim"); const border = cssVar("--border");`. The pane's only teardown is monteCarloDialog.ts:598-601 `dispose: () => { scope.dispose(); disconnectCanvasObservers(); }` — no themeVersion subscription; the sole repaint triggers are the ResizeObserver (:643-654), a table-row click (:777-782) and the ⚙ props panel.
```

**Failure** — Run a 10,000-realization Monte Carlo in dark theme, then switch to the "white" theme to print the HPV distribution. Unlike the other five, the canvas is cleared transparently (`ctx.clearRect` at :832) so the panel background does turn white — but the bars stay dark-theme orange #d98c3f and the axis/percentile labels stay #a08d70 on near-white, i.e. unreadable tick labels on the exported plot. The same stale colours apply to the tornado (drawTornado via renderSensitivity:1018 `observeCanvas(canvas, redraw)`).

**Fix** — Add `const unsubTheme = appState.themeVersion.subscribe(() => { drawHist(); redrawTornado(); });` — the two draw closures already exist (`drawHist` at :710, `redraw` at :1011); expose them through a module-local pair of callbacks the way disconnectCanvasObservers already tracks the canvases, and release the unsub in the dispose at :598.

**Verifier correction** — The defect is real at src/ui/monteCarloDialog.ts:834 (drawHistogram) and :1053-1056 (drawTornado), but the scope is wider than reported: it is THREE stale canvases, not two. drawConvSpark at src/ui/monteCarloDialog.ts:1251-1264 has the identical pattern (ctx.clearRect at :1260 then cssVar("--accent")/("--accent2")/("--text-dim") at :1262-1264, registered via observeCanvas(canvas, draw) at :1246 and rendered by renderConvergence at :581), so the convergence sparkline goes stale too. Also drawHistogram:847 `ctx.font = canvasFont(readTheme(canvas), 10)` snapshots the theme font, so typography — not only colour — is stale. A correct fix must therefore re-run all three draw closures (drawHist at :710, redraw at :1011, draw at :1244) on themeVersion, and release the unsub in the dispose at :598-601 alongside disconnectCanvasObservers().

**Priority rationale** — Real but cosmetic-and-visible, so it does not clear the fix-now bar. Confirmed against the source: nine other canvas panels (crossplotPanel.ts:1985, histogramPanel.ts:763, pickettPanel.ts:409, cutoffDialog.ts:815, correlationPanel.ts:1088, mapPanel.ts:431, resultsQcPanel.ts:852, unconventionalPanel.ts:640, vegaPanel.ts:736) each hold `const unsubTheme = appState.themeVersion.subscribe(...)`, and monteCarloDialog.ts has none — its only redraw hooks are `observeCanvas(canvas, drawHist)` at :798 and `observeCanvas(canvas, redraw)` at :1018 plus the zone/metric selects, so after a switch to the white/client-brand theme the bars stay at the old `cssVar("--accent")` value read at :834 while `ctx.clearRect` at :832 lets the new white panel background show through. That matters here because theme switching is not a novelty in this app — the brand palettes exist so a figure can be recoloured for a Pertamina/SLB deliverable, and the HPV P10/P50/P90 histogram and tornado are exactly the figures that get exported. But the failure is loud, not silent: the petrophysicist is staring at the ugly plot, no number is wrong, and the workaround is one pane drag or one zone-dropdown change (both already trigger a full redraw), so it never presents a degraded result as a clean one. Fix cost is small and contained — `observeCanvas` at :643 already funnels every canvas's draw callback through one place and `disconnectCanvasObservers` at :639 already owns teardown, so the redraw list plus one subscribe/unsub in the dispose at :598-601 is ~6 lines with no touch to the drawing or sampling code and no plausible regression beyond one redundant repaint. Do it the next time monteCarloDialog.ts is open, ideally as a sweep that also greps for any other canvas panel missing the subscribe, rather than as its own interrupt.

---

## [Med] pickettPanel's properties affordance diverges from crossplot/histogram on four axes at once, including a 130 px input-shaped gear button

`F3c` · INCONSISTENCY · effort **S** · worth **backlog** · verifier confidence high

**Where** `D:\XX. Arshilla\src\ui\pickettPanel.ts:188`

**Evidence**

```
pickettPanel.ts:187-189
  const propsBtn = document.createElement("button");
  propsBtn.className = "form-control";
  propsBtn.textContent = "⚙";

The two peers, both canvas-2D plot panels with the same toolbar structure:
  crossplotPanel.ts:936-938  propsBtn.className = "plot-export-btn";  propsBtn.textContent = "⚙ Properties";
  histogramPanel.ts:378-380  propsBtn.className = "plot-export-btn";  propsBtn.textContent = "⚙ Properties";

All three sit in the same flex container (`selRow.className = "plot-toolbar"` — pickettPanel.ts:193, crossplotPanel.ts:944, histogramPanel.ts:386), and styles.css:2234 forces `.plot-toolbar .form-control { width: 130px; flex: 0 0 auto; }` while styles.css `.form-control` (line ~) supplies `background: var(--bg-app); border: 1px solid var(--border); padding: 4px 7px` — i.e. text-input chrome.

Same divergence repeats twice more:
  modal title  pickettPanel.ts:514 openModal("Pickett properties", …)  vs  crossplotPanel.ts:1773 "Crossplot Properties" and histogramPanel.ts:692 "Histogram Properties"
  Apply class  pickettPanel.ts:506 applyBtn.className = "form-run-btn"  vs  crossplotPanel.ts:1768 and histogramPanel.ts:687 applyBtn.className = "lp-btn primary"
```

**Failure** — A user who has learned the "⚙ Properties" button in Crossplot and Histogram opens Pickett and sees, in the same toolbar row, a 130 px-wide box with input-field background and border containing a single small gear glyph — visually a disabled text field, not a button, and it never picks up the `.plot-export-btn:hover { background: var(--accent); color: #fff }` affordance the other two have. It also consumes the width of a full curve selector in a wrap-flex toolbar, so on a narrow docked pane the export buttons wrap to a second row earlier than they do in Crossplot/Histogram.

**Fix** — In pickettPanel.ts:188-189 set `propsBtn.className = "plot-export-btn"` and `propsBtn.textContent = "⚙ Properties"`; change pickettPanel.ts:514 to `openModal("Pickett Properties", body, 340)` and pickettPanel.ts:506 to `applyBtn.className = "lp-btn primary"` to match both peers.

**Verifier correction** — The defect is real but is ONE axis, not four, and the mechanism is worse than described on one point and absent on two others. Real: pickettPanel.ts:188-189 `propsBtn.className = "form-control"; propsBtn.textContent = "⚙";` inside `.plot-toolbar` inherits styles.css:2234 `width: 130px` plus styles.css:1860-1870 input chrome, AND — the reporter missed this — `.form-control` sets no `cursor: pointer`, while `.plot-export-btn` (styles.css:4081-4091) and `.lp-btn` (styles.css:1718-1728) both do. So the gear renders as a 130 px input-shaped box with a default arrow cursor and no hover feedback, versus an ~85 px hoverable pointer-cursor "⚙ Properties" button in Crossplot/Histogram. Not real: the Apply-button divergence (pickettPanel.ts:507 `form-run-btn` vs crossplotPanel.ts:1768 / histogramPanel.ts:687 `lp-btn primary`) is cosmetically inert — styles.css:1877-1886 and 1735-1739 both produce an accent-filled, white-text, pointer-cursor, hover-responsive primary button; and the modal-title divergence (pickettPanel.ts:514 "Pickett properties" vs "Crossplot Properties"/"Histogram Properties") is title-case only in a header seen one at a time. Fix reduces to pickettPanel.ts:188-189: `propsBtn.className = "plot-export-btn"; propsBtn.textContent = "⚙ Properties";`. Severity Low, not Med.

**Priority rationale** — Real, verified, and user-visible — but cosmetic, so it belongs in the next UI-polish batch rather than jumping the queue. I confirmed every link in the chain: pickettPanel.ts:187-189 sets `propsBtn.className = "form-control"` on a `<button>`, styles.css:1860 gives `.form-control` text-input chrome (`background: var(--bg-app); border: 1px solid var(--border); padding: 4px 7px`) with no `cursor: pointer` and no `:hover` rule at all, and styles.css:2234 `.plot-toolbar .form-control { width: 130px; flex: 0 0 auto; }` stretches it to a full curve-selector width. The peers at crossplotPanel.ts:937 and histogramPanel.ts:379 both use `plot-export-btn`, which does have `cursor: pointer` plus `:hover { background: var(--accent); color: #fff }` (styles.css:4081-4096). So in the Pickett toolbar the gear is a 130 px-wide box with input-field fill and border, an arrow cursor, and zero hover feedback — it reads as a disabled text field, not a control. That is a genuine discoverability cost in a workhorse panel (Pickett is where Rw and m get picked), and it eats a curve-selector's worth of width so the export buttons wrap a row earlier in a narrow docked pane. Against that: nothing is wrong or misreported — this is not a data-honesty issue, the plot still computes correctly, and the panel offers two working escape hatches (the `title` tooltip on the button and the right-click-the-plot path that tooltip advertises), so a user who has already been running Pickett has found it. The fix is four string literals in one file (className/textContent at :188-189, `openModal("Pickett Properties", …)` at :514, `applyBtn.className = "lp-btn primary"` at :506), touches no logic, no shared CSS, and no other panel — risk is effectively nil, but so is urgency. Queue it with the next styling sweep so styles.css and the three plot panels get opened once instead of three times. One caveat on the proposed fix: verify `lp-btn primary` renders sanely inside the Pickett modal's `form-row` btnRow (pickettPanel.ts:508-511) before swapping it, since the peers' Apply buttons sit in different modal layouts.

---

## [Med] Only moduleDialog refreshes its curve pickers on dataVersion; the five sibling tool panes hold a build-time snapshot

`F3c` · INCONSISTENCY · effort **M** · worth **backlog** · verifier confidence high

**Where** `D:\XX. Arshilla\src\ui\shfDialog.ts:36`

**Evidence**

```
moduleDialog.ts:254-262 is the only pane that resubscribes:
  const unsubData = appState.dataVersion.subscribe(() => { … void refreshData(); });
(with a race guard at moduleDialog.ts:236 — "a slower refresh resolving after a fresher one must not overwrite it with a stale catalog")

The five siblings fetch once and never again (grep "subscribe" in each returns 0 hits for dataVersion):
  shfDialog.ts:36        const catalog = await listCurveCatalog().catch(() => []);
  faciesTieDialog.ts:14  const catalog = await listCurveCatalog().catch(() => []);
  lorenzDialog.ts:17     listCurveCatalog().catch(() => []),
  mlDialog.ts:147        listCurveCatalog().catch(() => []),
  workflowDialog.ts:52   const catalog = await listCurveCatalog().catch(() => [] as CurveCatalogEntry[]);

workspace.ts:610-633 confirms these panes are built exactly once:
  private asyncPane(className, load, label): IContentRenderer {
    return new DomPanel(className, (host) => { … load().then((content) => { host.appendChild(content.el); … }); … });
  }
— no dataVersion listener, no rebuild path.
```

**Failure** — The standard chain is Vsh → porosity → saturation, run from module panes, then QC'd in the analysis panes. User docks SHF Fit and Workflow Builder alongside the Porosity module, runs Porosity so PHIE appears in the catalog, then switches to the SHF Fit tab to fit a Cuddy FOIL law. Its "Porosity (φ)" dropdown (shfDialog.ts:79) still lists only the pre-run curves — PHIE is absent — so the user concludes the module failed. The Module pane right next to it, built from the same `listCurveCatalog`, shows PHIE correctly (moduleDialog.ts:239). The only recovery is closing and reopening the SHF pane, which discards the FWL, min-φ, rock-typing and scan settings already entered.

**Fix** — Lift moduleDialog.ts:236-262's guarded refresh (generation counter + `preserve current selection if it still exists`) into a shared helper, e.g. `subscribeCurveCatalog(selects: HTMLSelectElement[]): () => void` in plotCommon.ts, and call it from shfDialog, faciesTieDialog, lorenzDialog, mlDialog and workflowDialog, returning the unsubscribe from each pane's existing `dispose()`.

**Verifier correction** — Corrected claim: moduleDialog.ts:233-259 is the ONLY place in the app that refreshes a curve picker on dataVersion; every other pane holds a build-time snapshot of the curve-name list, so a curve created by a module/equation/ML/import run never appears in an already-open pane's dropdown until the pane is closed and reopened (which discards its entered settings — no persistence in shfDialog.ts). Two fixes to the reported scope: (1) workflowDialog is NOT an equivalent instance — workflowDialog.ts:53-62 deliberately unions every module-declared output into inputCurveNames ("must be expressible in a fresh project whose catalog has no computed curves yet"), so PHIE IS selectable there before any run; it is stale only for non-module curves (LAS imports, equation/ML outputs). (2) The set is much wider than five, which inverts the "inconsistency" framing: the canvas panes subscribe to dataVersion but refresh only DATA, never their pickers — crossplotPanel.ts:914-915 (xSel/ySel from curveNames at :907; reload at :1483 refetches data only), correlationPanel.ts:554 (handler at :1083 does refreshWells().then(reload), never touches curveSel), histogramPanel.ts:376, pickettPanel.ts:180-181, vegaPanel.ts:341-342, unconventionalPanel.ts:488-489. Also missed: lorenzDialog.ts:17-18 snapshots the WELL list via listWells(), so a newly imported well never appears in its Well dropdown. The right fix is therefore a shared refreshable curve-picker helper in plotCommon.ts adopted app-wide, not a five-file patch.

**Priority rationale** — Real, not theoretical, but not a data-honesty violation — so it belongs in the backlog rather than jumping the queue. Verified: the five panes fetch once (`shfDialog.ts:36`, `faciesTieDialog.ts:14`, `lorenzDialog.ts:17`, `mlDialog.ts:147`, `workflowDialog.ts:52`) while `moduleDialog.ts:236-262` alone re-subscribes; `workspace.ts:610-633`'s `asyncPane` has no rebuild path. What makes the exposure bigger than the described side-by-side scenario is layout persistence: `workspace.ts:165` saves the dock to localStorage and `:1146` restores it with `this.dock.fromJSON(...)`, so a user whose saved workspace contains SHF Fit / Workflow Builder / ML gets those panes built at app start and frozen at the startup catalog for the entire session — every curve produced by any module run all day is invisible to them. The codebase already knows this is the hazard the modal→pane port created: `workspace.ts:639` comments that `followData` keeps a pane's lists current "the way the modal era re-fetched on every open," and the plot panels (crossplot, histogram, pickett, correlation, unconventional, map, db inspector) all took that subscription — these five were simply missed. The concrete cost is a false negative, not a false positive: the user sees PHIE absent from `shfDialog.ts:79`'s "Porosity (φ)" list, concludes the Porosity module failed, and the only recovery discards the FWL / min-φ / rock-typing entries already typed in. Nothing wrong is presented as clean success, no computed number is corrupted (the fit resolves curves by name against live data at run time), and there is a workaround — which is why this is backlog, not fix-now. The fix is cheap and low-risk: all five builders already return `{ el, dispose }` (`shfDialog.ts:281`, `faciesTieDialog.ts:89`, `mlDialog.ts:515`, `lorenzDialog.ts:153`, `workflowDialog.ts:881`), so a shared `subscribeCurveCatalog` in `plotCommon.ts` reusing moduleDialog's generation-counter + preserve-selection guard is additive plumbing in five small files, touching no working numerical path. The maintenance argument is the real reason to do it rather than drop it: the pattern is currently copy-paste-or-forget, and every new tool pane added to `workspace.ts`'s `asyncPane` list (already 17 cases) repeats the omission until the helper exists.

---

## [Med] #status-bar has no role="status"/aria-live — 274 setStatus() calls are the app's only feedback channel and none of it reaches assistive tech

`F3e` · A11Y · effort **S** · worth **backlog** · verifier confidence high

**Where** `src/state.ts:145`

**Evidence**

```
export function setStatus(text: string): void {
  const el = document.querySelector<HTMLElement>("#status-bar");
  if (el) el.textContent = text;
}

// index.html:414 — the target element carries no live-region semantics:
//   <footer id="status-bar">Ready</footer>
// `grep -rn 'aria-live|role="status"|role="alert"' src/ index.html` => 0 matches.
// 274 setStatus( call sites.
```

**Failure** — Every operation outcome in the app routes here: "Selected well X" (workspace.ts:923), "Results-QC: … 12 zone(s), 4 flagged" (resultsQcPanel.ts:823), "Exported N-zone scorecard" (resultsQcPanel.ts:834), "Nothing to export yet — Recompute first" (resultsQcPanel.ts:830), "Undo: <label>" / "Nothing to undo" (ribbon.ts:119). A screen-reader user activates Export LAS or runs SandiMin across 40 wells and hears absolutely nothing — success, failure, and the well count are all silently written into a footer they are not looking at, so they cannot tell a completed run from a no-op. The same gap hits sighted users of magnification who are focused on a dialog at the top of the screen while the confirmation appears in the footer.

**Fix** — Add `role="status" aria-live="polite" aria-atomic="true"` to `<footer id="status-bar">` in index.html. One attribute set fixes all 274 call sites. Additionally give moduleDialog.ts's `resultBox` (line 268/278, where validation errors like "VSH: value must be between 0 and 1" land next to an `input.focus()`) `role="alert"`, so the reason for the focus jump is announced.

**Verifier correction** — The defect is real at the cited location and the proposed fix is right, but the description overstates one point: setStatus is NOT "the app's only feedback channel". Several surfaces mirror the same message locally — resultsQcPanel.ts:822 writes `statusLine.textContent = \`${targets.length} zone(s) · ${flagged} check(s) flagged\`` in-panel immediately before the setStatus call, and moduleDialog.ts:304 writes `resultBox.textContent = \`Running ${spec.name} on ${wellIds.length} well(s)… see the Processing panel for progress.\`` plus terminal states at :310/:316. That weakens the "40-well SandiMin run produces absolutely nothing" framing, since those runs do surface progress in the Processing panel and the dialog result box. However, since the repo-wide grep for aria-live/role=status/role=alert returns zero matches, none of those mirror surfaces are announced either — so the underlying accessibility gap stands unchanged, and the genuinely footer-only messages (e.g. resultsQcPanel.ts:830 "Nothing to export yet — Recompute first", ribbon.ts:119/122 "Nothing to undo"/"Nothing to redo") remain silent no-op-vs-success confusions for both screen-reader and magnification users. One caveat on the fix: a footer written by 274 call sites will be chatty under aria-live="polite"; the resultBox role="alert" half of the fix is the higher-value part since it sits next to an input.focus() jump.

**Priority rationale** — Technically real and verified (D:\XX. Arshilla\index.html:414 `<footer id="status-bar">Ready</footer>`, D:\XX. Arshilla\src\state.ts:145 `setStatus` writes textContent, 274 call sites, zero aria-live/role in src/ or index.html), but the headline user does not exist in this app: SandiBumi's entire deliverable is visual — multi-track log plots, canvas-2D crossplot/histogram/pickett, Vega panel, dockview layout — so a screen-reader-only petrophysicist cannot use it at all with or without a live region; the "runs SandiMin across 40 wells and hears nothing" scenario is theoretical for a single-developer local tool with a known, sighted Indonesian user base. It is also not a correctness/data-honesty issue: the status text itself is truthful, and a failed run is never rendered as clean success by this gap — it just isn't spoken. What keeps it off "drop" is that the fix is a two-minute, zero-churn markup edit with no code path touched, and the second half of the proposal has independent value even for sighted users: moduleDialog.ts:277-278 does `resultBox.textContent = "${name}: value must be between ..."` followed immediately by `input.focus()`, i.e. the caret jumps while the reason sits in a box the user's eye may not be on — `role="alert"` there is the one genuinely useful piece. Note before implementing that the proposed `aria-live="polite" aria-atomic="true"` on a footer written by 274 call sites (including per-well progress chatter) is a firehose live region and would need throttling or an assertive/polite split rather than being the claimed one-line cure-all. File it to be done opportunistically the next time index.html or moduleDialog.ts is open; do not schedule a session for it.

---

## [Med] The dock ＋ menu and the right-click context menu are appended at the end of <body> with no focus move; the ＋ menu has no Escape dismiss

`F3e` · A11Y · effort **S** · worth **backlog** · verifier confidence high

**Where** `src/ui/workspace.ts:377`

**Evidence**

```
const rect = anchor.getBoundingClientRect();
menu.style.left = ...; menu.style.top = `${rect.bottom + 4}px`;
document.body.appendChild(menu);        // <- 24 <button>s land at the very end of the DOM

// Defer the outside-click closer so the opening click doesn't fire it.
const closeOnOutside = (e: MouseEvent) => { ... };
window.setTimeout(() => document.addEventListener("mousedown", closeOnOutside), 0);
// no menu.focus(), no keydown handler, no arrow-key navigation.
// The only Escape handler that touches menus is ribbon.ts:156, and it is class-scoped:
//   for (const menu of document.querySelectorAll(".ribbon-menu:not([hidden])")) menu.hidden = true;
// this menu's class is "dock-add-menu" (workspace.ts:318), so Escape never reaches it.
```

**Failure** — A keyboard user presses Enter on the ＋ button in a window's tab bar (workspace.ts:258). The 24-item menu (New Log View … Processing History) renders anchored under the button but is inserted at the end of `<body>`, while focus stays on ＋ inside the dock — so reaching "New Crossplot" means Tabbing forward through every remaining control in the dock and status bar. Escape does nothing because the handler is scoped to `.ribbon-menu`, and the only dismissal is a mouse `mousedown` outside — so the menu cannot be cancelled from the keyboard at all and stays floating over the workspace. The same shape applies to contextMenu.ts:60 (`document.body.appendChild(menu)` with no focus move), which is the only route to per-panel actions such as Close Window; there at least the Escape handler at contextMenu.ts:69-71 works.

**Fix** — After appending, focus the first enabled item (`menu.querySelector('button:not([disabled])')?.focus()`) and add a keydown handler for Escape (close + return focus to `anchor`) and ArrowUp/ArrowDown roving. Do the same in contextMenu.ts:60-78, where Escape already exists but focus is never moved in.

**Verifier correction** — Corrected claim: src/ui/workspace.ts:315-387 (showAddPanelMenu) builds a **31**-item menu (not 24), appends it at the end of <body> without moving focus, and gives it no keydown handler, so it cannot be cancelled from the keyboard — ribbon.ts:153-158's Escape handler is scoped to `.ribbon-menu` and cannot match `.dock-add-menu`. Two impact qualifiers the reporter got wrong: (1) "Close Window" is NOT only reachable via the context menu — workspace.ts:281 already exposes a ✕ "Close this window and every panel in it" button in the same tab bar; (2) every panel type in the ＋ menu is also openable from the ribbon, which does honour Escape, so this is a degraded secondary route rather than a keyboard dead-end for the feature itself. That argues for Low/Med rather than Med severity. Additionally the reporter missed a third identical instance: src/ui/correlationPanel.ts:522-551 (the "Wells (n/m)…" picker) uses the same `dock-add-menu` class, `document.body.appendChild(menu)`, mousedown-only close, no Escape and no focus move — any fix should cover all three sites, not two.

**Priority rationale** — Take the cheap half, skip the expensive half. The claimed failure as written — a keyboard-only user Tab-trapping past the whole dock to reach "New Crossplot" — is theoretical for this app: it is a mouse-and-drag dockview desktop tool for Indonesian petrophysicists, there is no VPAT/procurement driver, and nobody is Tab-navigating a floating-window workspace. Building a proper roving-focus ARIA menu (arrow keys, focus return, aria-activedescendant) in showAddPanelMenu is real work with real regression risk against a working 24-entry menu, and it buys a user who does not exist here — that part is a drop. But the same finding exposes a concrete mouse-visible defect worth ~10 lines: contextMenu.ts:69-78 already registers Escape, `window.blur`, and capture-phase `scroll` closers plus a MutationObserver to unhook them, while workspace.ts:381-388 registers only `mousedown` outside. So the ＋ menu — unlike the right-click menu that sits 100 lines away and looks identical to the user — survives Alt-Tab, window resize, and scrolling the tab bar, staying pinned at stale `rect.bottom + 4` coordinates floating over the workspace until you click somewhere. That is reachable with a mouse in three seconds and it is an inconsistency between two sibling menus, which is exactly the kind of thing that gets re-diverged the next time either one is touched. Backlog it as "make showAddPanelMenu reuse showContextMenu's dismissal set (Escape + blur + scroll + observer cleanup)" — ideally by having the ＋ button call showContextMenu with the entry list instead of hand-rolling a second menu implementation, which deletes the divergence rather than patching it. Not fix-now: no computed result is wrong, no degraded output is being presented as clean, nothing downstream is blocked.

---

## [Med] openModal never moves focus into the dialog and never restores it on close; #modal-root is the last element in the document

`F3e` · A11Y · effort **M** · worth **backlog** · verifier confidence high

**Where** `src/ui/modal.ts:78`

**Evidence**

```
const close = () => {
  root.hidden = true;
  root.innerHTML = "";              // destroys whatever had focus; activeElement falls back to <body>
  document.removeEventListener("keydown", onKey);
  dragCleanup?.();
  if (activeClose === close) activeClose = null;
};
...
closeBtn.addEventListener("click", close);
document.addEventListener("keydown", onKey);
activeClose = close;
return close;
// no dialog.focus(), no saved document.activeElement, no restore, no role="dialog",
// no aria-labelledby; closeBtn.textContent = "✕" (modal.ts:31) with no aria-label.
// index.html:416 places the host LAST in the document: <div id="modal-root" hidden></div>
```

**Failure** — The scrim is deliberately pointer-transparent (modeless), so a focus TRAP would be wrong — but initial focus and focus return are still required. 15 of the 24 openModal call sites focus nothing at all (correlationPanel.ts:985 Fluid contacts, crossplotPanel.ts:1773 Crossplot Properties, histogramPanel.ts:692, pickettPanel.ts:514, layoutPropsDialog.ts:333 the 880px Layout Properties, logViewPanel.ts:443, ribbon.ts:959/1285/1401/1570, wellGroups.ts:260, workspace.ts:1318 Help, curveEditDialog.ts:81, topsEditor.ts:321). Concretely: a keyboard user in a Crossplot panel presses Enter on the properties button; the dialog renders at the very end of the DOM while focus stays on the trigger, so reaching the first control requires Tabbing forward past the rest of the ribbon and every open dock panel. On close, `root.innerHTML = ""` removes the focused node, Chromium resets activeElement to <body>, and the next Tab restarts at `#qat-undo` at the top of the ribbon — the user loses their place in the panel entirely, every time. The inconsistency is visible inside one file: topsEditor.ts:244 focuses on "New top", topsEditor.ts:321 "Edit top" does not.

**Fix** — In openModal: capture `const opener = document.activeElement as HTMLElement | null` before rendering; after append, focus the first focusable descendant of `body` (falling back to `dialog` with `tabIndex = -1`); in `close()` call `opener?.focus()` before wiping `root.innerHTML`. Add `dialog.setAttribute("role","dialog")` + `aria-labelledby` pointing at the `<h3>`, and `closeBtn.setAttribute("aria-label", "Close dialog")` (the bare ✕ glyph is not announced at NVDA's default symbol level).

**Verifier correction** — The defect is real at src/ui/modal.ts:78-99 but the failure narrative fuses two disjoint cases. For the 15 call sites that focus nothing (crossplotPanel.ts:1773, layoutPropsDialog.ts:333, correlationPanel.ts:985, histogramPanel.ts:692, pickettPanel.ts:514, logViewPanel.ts:443, ribbon.ts:959/1285/1401/1570, wellGroups.ts:260, workspace.ts:1318, curveEditDialog.ts:81, topsEditor.ts:321), the cost is initial focus only: the dialog renders at the very end of the DOM (index.html:416, after #dock-root) while focus stays on the trigger, so a keyboard user must Tab past the rest of the ribbon and every open dock panel to reach the first control. On close in that same scenario focus was never inside the dialog, so root.innerHTML="" removes nothing focused and the trigger keeps focus — the "activeElement falls back to <body>, next Tab restarts at #qat-undo" half applies instead to the 9 auto-focusing dialogs (ribbon.ts:675/892/1157/1502/1707, mapPanel.ts:558, plotCommon.ts:158, topsEditor.ts:244, highlightsOverlay.ts:350) and to any dialog the user clicks or tabs into. So: 15 sites lack initial focus, and every site lacks focus restore for the case where focus did land inside. Missing role="dialog"/aria-labelledby and the unlabeled "✕" (modal.ts:31) are confirmed as stated. Med severity and the proposed fix are appropriate.

**Priority rationale** — Real but low-severity, and cheap enough to be worth doing once — just not ahead of anything that touches numbers. Concrete impact is ergonomic, not correctness: nothing renders wrong and no result is misreported, so it doesn't hit the data-honesty cardinal rule. The genuine daily cost is initial focus — a user who opens Crossplot Properties or Layout Properties has to click a field before typing, because focus stays on the trigger and the dialog is appended at the very end of `#modal-root` (index.html:416). Escape still works regardless of focus (the handler is on `document`, modal.ts:97), so the dialog is never unreachable or un-closable; the "lose your place on Tab" half only bites a keyboard-only user, and this app's audience (Geolog/Techlog-trained petrophysicists driving canvas plots, dock layouts and drag-to-move dialogs) is overwhelmingly mouse-driven, with no known screen-reader user. That is why it's not fix-now. It's not drop either, because the fix lives entirely inside one 100-line helper and pays out at all 24 call sites at once, retiring the per-site ad-hoc `focus()` calls that already disagree with each other (topsEditor.ts:244 focuses, topsEditor.ts:321 doesn't) — that inconsistency is the maintenance hazard, and it gets worse with every new dialog. One caveat for whoever implements it: the scrim is deliberately pointer-transparent/modeless (modal.ts:1-4), so blind `opener?.focus()` on close would yank focus away from a panel the user clicked into while the dialog was still open; guard the restore with `if (root.contains(document.activeElement))` before wiping `innerHTML`. The `role="dialog"` + `aria-labelledby` + `aria-label="Close dialog"` on the bare ✕ (modal.ts:31) are two lines each and should ride along in the same edit rather than being tracked separately.

---

## [Med] Track-header curve visibility toggle is a click-only <span> with no keyboard equivalent anywhere

`F3e` · A11Y · effort **S** · worth **drop** · verifier confidence high

**Where** `src/ui/viewerChrome.ts:84`

**Evidence**

```
const swatch = document.createElement("span");
swatch.className = "legend-dot" + (isBlocks ? " blocks" : "");
...
swatch.title = "Toggle visibility";
const name = document.createElement("span");
name.className = "legend-name";
...
const toggle = () => {
  row.classList.toggle("hidden");
  const nowHidden = row.classList.contains("hidden");
  if (nowHidden) hiddenCurves.add(curve.curve_name);
  else hiddenCurves.delete(curve.curve_name);
  callbacks.onCurveToggle(curve.curve_name, nowHidden);
};
swatch.addEventListener("click", toggle);
name.addEventListener("click", toggle);   // <- spans: no tabindex, no role, no keydown
```

**Failure** — Track reorder (viewerChrome.ts:200-211 drag), track resize (171-198 mousedown), and scale min/max editing (127-128 click) all have keyboard equivalents in Layout Properties — the ↑/↓ buttons at layoutPropsDialog.ts:102/111, the `width_weight` number input at 230, and the min/max numInputs at 277-278. Curve VISIBILITY does not: the curve table at layoutPropsDialog.ts:255-299 has Curve/Color/Min/Max/Fill/Fill-color/Opacity/✕ columns and no hidden flag. So a keyboard-only user with GR, RHOB, NPHI and DT overlaid in one track who wants to temporarily hide NPHI to read RHOB has exactly one option — layoutPropsDialog.ts:292 `iconBtn("✕", "Remove this curve", …)`, which permanently deletes the curve style from the layout and forces them to re-enter its name, colour, min and max by hand to get it back. Screen-reader users get no signal these spans are interactive at all, since they carry no role.

**Fix** — Make the swatch a `<button type="button" aria-pressed>` (or give the row `tabIndex = 0` + `role="switch"` + Enter/Space keydown) with an accessible name like `Toggle ${curve.curve_name} visibility`. Alternatively add a Show column (checkbox) to the layoutPropsDialog curve table wired to the same `hiddenCurves` set.

**Verifier correction** — Curve-visibility toggling in the track-header legend is mouse-only: viewerChrome.ts:84 and :97 build the swatch and name as bare <span>s and :108-109 bind `click` to the `toggle` closure with no tabIndex, no role, and no keydown, and no alternative trigger exists anywhere (single producer of onCurveToggle at viewerChrome.ts:106). Unlike track reorder/width/scale, the layoutPropsDialog curve table (:255-258) exposes no visibility column, so a keyboard-only user's only route is ✕ Remove (layoutPropsDialog.ts:292) followed by Ctrl+Z to restore — the removal IS undoable (logViewPanel.ts:834-842 pushUndo + undo.ts:88-96 global Ctrl+Z), so nothing is permanently lost and no values are retyped. Corrected severity: Low (mouse-only affordance with a clumsy 3-step keyboard workaround), not Med (data loss). Fix is unchanged: make the swatch a <button type="button" aria-pressed> with accessible name `Toggle ${curve.curve_name} visibility`, or add a Show checkbox column to the layoutPropsDialog curve table wired to the same hiddenCurves set. Minor line-number correction: the width_weight numInput is at layoutPropsDialog.ts:229, not 230.

**Priority rationale** — Technically real but there is no user here who hits it. The finding is confirmed at src\ui\viewerChrome.ts:84-109 — two bare `<span>`s with only `click` handlers and no tabindex/role/keydown — but the thing they toggle is a pure, non-destructive view flag (`hiddenCurves.add/delete` + a CSS class), always reachable with one mouse click, and the surrounding chrome is inherently pointer-driven: the same row is `row.draggable = true` (line 78) for moving curves between tracks, the track resizer is a raw `mousedown` drag (171-198), and scale editing is a click-to-spawn floating input (127-128). A keyboard-only user cannot operate this log viewer regardless, so wiring up one `role="switch"` buys a token of compliance rather than an operable workflow — the "one option is ✕ delete" scenario at layoutPropsDialog.ts:292 only bites someone who is already unable to drag a curve into the track in the first place. This is an internal desktop tool for a solo consultant and a handful of Indonesian petrophysicists on Geolog/Techlog-style mouse workflows, not a shipped web product with an accessibility obligation, and it touches no correctness or data-honesty concern: nothing is presented as clean when it is degraded. The one thing worth remembering is the alternative fix — a "Show" checkbox column in the layoutPropsDialog curve table (255-299) bound to the same `hiddenCurves` set — because if hidden-curve state is ever persisted into saved layouts, that column becomes the natural place to expose it and the a11y gap closes for free as a side effect. Until such a change is actually planned, this stays off the list.

---

## [Low] crossplot/histogram "Theme color" swatch hardcodes light-theme terracotta while layoutPropsDialog already reads --accent live

`F3a` · INCONSISTENCY · effort **S** · worth **backlog** · verifier confidence high

**Where** `src/ui/crossplotPanel.ts:1649`

**Evidence**

```
// crossplotPanel.ts:1649 and histogramPanel.ts:645 — identical:
    colorIn.value = /^#[0-9a-fA-F]{6}$/.test(opts.color) ? opts.color : "#b5651d";
    colorIn.disabled = !opts.color;

// layoutPropsDialog.ts:311-313 — the FIXED version of the same pattern, with a comment saying why:
      // Seed the persisted default from the LIVE theme accent (hex-validated ...), so new curves
      // match the active palette instead of always coming out light-theme terracotta.
      const accent = getComputedStyle(document.documentElement).getPropertyValue("--accent").trim();
        color: /^#[0-9a-fA-F]{3}(?:[0-9a-fA-F]{3})?$/.test(accent) ? accent : "#b5651d",
```

**Failure** — #b5651d is the LIGHT palette's --accent, hardcoded. Under Pertamina the crossplot draws its point cloud in --accent #006bb8 blue but the disabled swatch beside the "Theme color" checkbox shows terracotta orange — the swatch claims to show the theme colour and does not. Worse, when the user unchecks "Theme color" to pick a custom colour, the picker opens seeded at terracotta rather than at the palette accent, so the natural "nudge it slightly" edit jumps the series from Pertamina blue to an off-brand orange and persists it via savePlotProps. Same in Halliburton, Schlumberger, LAPI-ITB, white and dark. layoutPropsDialog.ts:313 already solves exactly this and carries a comment explaining it, so the two-line fix and its rationale are already in the repo.

**Fix** — Extract the layoutPropsDialog seeding into a shared helper (e.g. themeAccentHex() in plotCommon.ts) and use it in crossplotPanel.ts:1649, histogramPanel.ts:645 and logViewPanel.ts:430 (which likewise seeds #888888 instead of --border).

**Verifier correction** — Claim stands as written; two refinements. (1) The repo already contains a generic helper doing exactly the proposed fix, not just the inline layoutPropsDialog.ts:313 case: src/ui/monteCarloDialog.ts:1542 `function propColor(label, current, seedVar, fallback, onChange)` with `inp.value = current ? toHexColor(current, fallback) : toHexColor(cssVar(seedVar), fallback);`, used at :1577 as `propColor("Bars", opts.barColor, "--accent", "#b5651d", ...)`. So the shared-helper shape exists twice and merely needs exporting/reuse. (2) The persistence step should cite crossplotPanel.ts:1785 / histogramPanel.ts:699 (`opts.color = themeChk.input.checked ? "" : colorIn.value;`) rather than savePlotProps generically — that is the exact line that bakes #b5651d in.

**Priority rationale** — Real but bounded. On the default path — "Theme color" checked, which is how nearly every plot is created — the point cloud is drawn from live --accent and is correct; the only wrong pixel is a *disabled* 48px swatch, which is pure cosmetics. The finding only bites on the opt-out path: a user under Pertamina/Halliburton/SLB/LAPI-ITB unchecks the box to nudge the colour, the picker opens at light-theme terracotta #b5651d instead of the palette accent, and savePlotProps persists an off-brand series colour into a client-branded deliverable. That is a genuine user-visible consequence for an app that deliberately ships client-brand palettes, so it is not drop — but it is a colour-of-a-marker issue, never a wrong number or a degraded result dressed up as clean, so the cardinal data-honesty rule does not engage and it is not fix-now either. The fix is also unusually cheap and low-risk: the working, commented implementation already exists at layoutPropsDialog.ts:313, so lifting it into a themeAccentHex() helper in plotCommon.ts and calling it at the three seed sites (including logViewPanel.ts:430's #888888, which should read --border) touches only dialog-construction code, never the render or persistence math, and cannot churn any tested plotting path. Queue it as a single ~15-minute chore to be done the next time the theme system or a plot properties dialog is opened — doing it once as a shared helper also removes the fourth copy hazard, since the next panel that grows a colour picker will otherwise copy the terracotta literal again.

---

## [Low] Vega regression trend line uses --warn over an --accent point cloud; under Halliburton both are the same brand red

`F3a` · THEME-VIOLATION · effort **S** · worth **backlog** · verifier confidence high

**Where** `src/ui/vegaPanel.ts:239`

**Evidence**

```
  const accent = cssVar("--accent", "#b5651d");             // vegaPanel.ts:116
  ...
    : { type: "point", filled: true, size: 20, ...(zName ? {} : { color: accent }) };   // :195
  ...
    const trendColor = cssVar("--warn", "#c0392b");          // :239
          mark: { type: "line", color: trendColor, strokeWidth: 2 },                    // :255
          mark: { type: "text", ..., color: trendColor, fontSize: 11 },                 // :266
```

**Failure** — --accent and --warn are only guaranteed to be distinguishable by convention, not by the contract. Under Halliburton --accent is #e31b23 and --warn is #b3141b (styles.css:112,117) — the same hue at ~20% different lightness. Turning on the trend overlay on a Vega scatter in the Halliburton skin draws a 2px fit line and its R² label in a red that is essentially the point colour, so the fit is unreadable over the cloud it is fitted to. Pertamina is milder but similar in family (accent #006bb8 vs warn #ed1a2f is fine; the failure is specific to Halliburton, where the brand is a single red).

**Fix** — Use --accent2 for the trend line (graphite #3f4249 under Halliburton, cyan under Schlumberger, green under Pertamina — distinct from --accent in all 7 palettes), or keep --warn but add a white/dark halo stroke so the line reads regardless of hue collision.

**Verifier correction** — The defect is real but narrower than described on two points. (1) It only triggers when NO Z colour curve is selected: at :195 `color: accent` is spread in only when zName is falsy, so with a Z curve the cloud is viridis and the red fit line reads fine. (2) The R² label is not the problem — at :266 it is pinned to x/y value 6, i.e. the plot's top-left corner over bare --bg-panel white, giving ~6.9:1 contrast. The actual failure is the 2px fit line at :255 alone: scatter points render at 0.55 opacity (:196-203), so a sparse point resolves to ~#f08286 (~2.7:1 vs #b3141b, marginal), but in overplotted regions the stacked points converge on full #e31b23, where contrast against #b3141b is ~1.47:1 and the line effectively disappears — precisely in the dense part of the cloud the fit is meant to summarize. Fix direction is sound; note src/ui/plotCanvas.ts:377-388 (drawRefPoint) already uses the bg-stroke halo trick for a --warn mark, so the halo variant matches existing house style.

**Priority rationale** — The collision is real and I confirmed it in the palette: under the Halliburton skin `--warn: #b3141b` (styles.css:117) is byte-identical to `--accent-dim: #b3141b` (:113), i.e. the trend line is drawn in the shaded version of the very point colour `--accent: #e31b23` (:112) it sits on top of. So a user who picks the Halliburton skin for a client deliverable, opens a Vega scatter with no z-colour field, and ticks the regression overlay gets a 2px fit line and an R² label that are hard to separate from the cloud — bad on a screenshot pasted into a report. But that is a legibility annoyance, not a data-honesty failure: the fit is still computed and the R² is still printed, nothing degraded is being sold as clean, and it only bites in 1 of 7 palettes on an optional overlay. Against that, the fix is about as cheap and low-risk as a change gets — `cssVar("--warn", ...)` → `cssVar("--accent2", ...)` at vegaPanel.ts:239, one line feeding both the line mark (:255) and the text mark (:266); I checked and `--accent2` is not referenced anywhere else in vegaPanel.ts (only `--text`, `--text-dim`, `--border`, `--accent` at :113-116), so there is no second collision to introduce, and `--accent2` is visually distinct from `--accent` in all seven palettes (graphite vs red under Halliburton, gold vs navy under ITB, cyan vs blue under SLB). Zero churn to a tested area, no plotting logic touched. Backlog rather than fix-now: park it and take it with the next theme/plot-styling pass, where it costs a minute instead of a context switch. File: D:\XX. Arshilla\src\ui\vegaPanel.ts:239; palette source D:\XX. Arshilla\src\styles.css:112-117.

---

## [Low] var(--danger) is a phantom token — the Vega spec-error message uses a Bootstrap red that matches no palette's --warn

`F3a` · THEME-VIOLATION · effort **S** · worth **backlog** · verifier confidence high

**Where** `src/styles.css:2836`

**Evidence**

```
.vega-spec-err {
  color: var(--danger, #d9534f);
/* --danger is defined in no :root block; every other error/failure colour in the app uses --warn: */
  --proc-fail: var(--warn);      /* styles.css:4838 */
  --health-bad: var(--warn);     /* styles.css:5064 */
.catalog-badge.shadow { color: var(--warn); }   /* styles.css:4818 */
```

**Failure** — --danger is defined nowhere, so the fallback #d9534f is used in all 7 palettes. Under Halliburton the app's error red is --warn #b3141b, under Pertamina it is the brand red #ed1a2f, under lapi-itb #b33636 — so when a user types an invalid Vega spec, the error text under the editor appears in a fourth, off-brand red while the processing-panel failure rows and catalog shadow badges next to it use the palette red. On a branded deliverable skin this reads as a stray colour.

**Fix** — Change to `color: var(--warn);`. If a distinct error-vs-warning red is genuinely wanted, add --danger to all 7 palette blocks as a real 16th token rather than leaving it phantom.

**Verifier correction** — The defect is real but the mechanism and scope need fixing. (1) The trigger is malformed JSON in the spec editor, not an "invalid Vega spec" — vegaPanel.ts:674 only fires from the JSON.parse catch (`specErr.textContent = \`Invalid JSON: ${...}\``); a parseable-but-semantically-invalid Vega spec falls through to render() and surfaces elsewhere. Same visual outcome, wrong name for the trigger. (2) The same declaration block contains a SECOND phantom token the reporter missed: styles.css:2838 `font-family: var(--mono, monospace)`. The real token is `--font-mono` (defined at styles.css:35 as "Cascadia Code", Consolas, monospace) and every other mono field in the file uses it (lines 1305, 1312, 1690, 4360, 4757). Line 2838 is the only `var(--mono` in the codebase, so the error text also silently drops to generic monospace instead of the app font. (3) The fix should therefore be both `color: var(--warn)` AND `font-family: var(--font-mono)`, not just the colour. Also, the app defines --danger-equivalent semantics purely through the `.danger` CLASS (4 rules, all -> --warn), so adding a real --danger token to all 7 palettes would be the less consistent of the two proposed options.

**Priority rationale** — Verified real: styles.css:2836 `color: var(--danger, #d9534f)` is the only --danger reference in src and --danger is defined in zero :root blocks, while all 8 --warn: definitions exist (lines 30/52/71/98/117/136/155/174), so the Bootstrap fallback fires under every palette including the Pertamina #ed1a2f and Halliburton #b3141b client skins. But the blast radius is one 11.5px mono error string in one panel (vegaPanel.ts:634), shown only transiently after the user types an invalid Vega spec, in a red that still reads unmistakably as an error — nothing renders wrong, no degraded result is presented as clean, so the cardinal data-honesty rule is untouched and no planned work is blocked. It earns backlog rather than drop because a phantom token silently opts one string out of the palette contract: the next person adding an 8th client skin will define every real token, get an off-brand red here anyway, and have no grep hit to warn them. The right unit of work is not a standalone session but a single ~15-minute phantom-token sweep — repoint .vega-spec-err at --warn and in the same pass decide whether the other phantoms (--mono, --font-ui, --bg-active, --bg-inset, all also used with fallbacks and defined nowhere) should be promoted to real tokens or have their fallbacks inlined, so that var(--x, fallback) stops meaning two different things in this file.

---

## [Low] Avg SWE and N/G are rendered at different decimal places by the three panes that display the same PaySummaryRow

`F3c` · INCONSISTENCY · effort **S** · worth **backlog** · verifier confidence high

**Where** `D:\XX. Arshilla\src\ui\summaryDialog.ts:124`

**Evidence**

```
All three call the same backend `run_pay_summary` and render the same `PaySummaryRow` fields.

summaryDialog.ts:124-125 (fmt default digits = 2, summaryDialog.ts:97):
  `<td>${fmt(r.net, 1)}</td><td>${fmt(r.ntg)}</td><td>${fmt(r.avg_vsh)}</td>` +
  `<td>${fmt(r.avg_phie, 3)}</td><td>${fmt(r.avg_swe)}</td><td>${fmt(r.hpv, 2)}</td>`
  → ntg 2 dp, avg_swe 2 dp

dashboardPanel.ts:27-32:
  { key: "ntg", label: "N/G", digits: 2, num: true },
  { key: "avg_swe", label: "Avg SWE", digits: 3, num: true },
  → ntg 2 dp, avg_swe 3 dp

monteCarloDialog.ts:772-776:
  numCell(z.ntg, 3, false),
  numCell(z.avg_swe, 3, false),
  → ntg 3 dp, avg_swe 3 dp

(avg_phie is 3 dp in all three; net 1 dp in all three; hpv 2 dp in all three — so SWE and N/G are the only two that drift.)
```

**Failure** — Same well, same zone, same cutoffs, three panes open. Zone BS-40 with avg SWE = 0.5951 reads "0.60" in Cutoffs & Pay Summary and "0.595" in Field Dashboard. Against a SWE≤0.60 pay cutoff the Pay Summary rendering looks like the zone sits exactly on the cutoff (an immediate red flag a petrophysicist will chase), while the Dashboard shows it comfortably inside. Copying the Pay Summary table into a report and the Dashboard CSV into a spreadsheet then produces two Avg SWE columns that do not reconcile.

**Fix** — Define the per-field precision once — e.g. `export const PAY_DP = { net: 1, ntg: 2, avg_vsh: 2, avg_phie: 3, avg_swe: 3, hpv: 2 }` next to the PaySummaryRow type in ipc.ts — and drive summaryDialog.ts:124-125, dashboardPanel.ts:21-32 and monteCarloDialog.ts:772-776 from it.

**Verifier correction** — Corrected claim: "PaySummaryRow.avg_swe is rendered at 2 dp by the Cutoffs & Pay Summary dialog but 3 dp by the Field Dashboard grid." Scope is TWO panes and ONE field, not three panes and two fields.

What is confirmed (exact, verified):
- summaryDialog.ts:125 `<td>${fmt(r.avg_swe)}</td>` with `fmt(v, digits = 2)` at summaryDialog.ts:97 → 2 dp.
- dashboardPanel.ts:31 `{ key: "avg_swe", label: "Avg SWE", digits: 3, num: true }` → 3 dp.
- Both files import `PaySummaryRow` and call `runPaySummary` (summaryDialog.ts:1,71; dashboardPanel.ts:1,240), so this is genuinely the same field from the same backend command. Grep confirms these are the ONLY two `PaySummaryRow` consumers in src/.
- Concrete cost stands but is narrow: with the default SWE cutoff of 0.6 (dashboardPanel.ts:58 `sweIn = num("0.6")`), a zone at avg_swe 0.5951 prints "0.60" in the Pay Summary and "0.595" in the Dashboard, so the Pay Summary makes an inside-cutoff zone look like it sits exactly on the cutoff.

What is wrong in the original finding:
1. monteCarloDialog.ts is NOT a third pane over the same data. It imports `runMonteCarlo` / `McZoneResult` (lines 5 and 16) and never imports `runPaySummary` or `PaySummaryRow`. `res.zones[i]` is a stochastic simulation result, and its own headers mark the columns as different quantities: monteCarloDialog.ts:740-743 declare `{ label: "NTG", sub: "P50" }`, `{ label: "Avg PHIE", sub: "P50" }`, `{ label: "Avg SWE", sub: "P50" }`, with Net pay and HPV carrying a percentile `band` subtitle. A Monte Carlo P50 is not the deterministic pay-summary value, so its precision choice is not an inconsistency and must not be unified with the other two.
2. Because the Monte Carlo pane is excluded, the N/G part of the claim is false. `ntg` is 2 dp in BOTH real consumers (summaryDialog.ts:124 `fmt(r.ntg)` defaulting to 2; dashboardPanel.ts:28 `digits: 2`). avg_vsh (2 dp), net (1 dp), hpv (2 dp) and avg_phie (3 dp) also match. avg_swe is the only field that drifts.
3. The CSV half of the failure scenario is wrong. exportCsv at dashboardPanel.ts:359-370 does no rounding — line 367 is `return typeof v === "number" ? (Number.isNaN(v) ? "" : v) : ...`, emitting the raw float. `GRID_COLS.digits` is used only for on-screen cells and the box-plot axis (dashboardPanel.ts:172), never for export. The exported CSV therefore carries full precision, so the described "two Avg SWE columns at 2 dp vs 3 dp that do not reconcile" does not occur via that path.

Also note a third divergence the reporter missed within dashboardPanel itself: the "By zone" aggregate at dashboardPanel.ts:148 renders Mean SWE via `fmt(netW("avg_swe"), 3)` (3 dp), consistent with its own grid but likewise inconsistent with summaryDialog's 2 dp.

Proposed fix is directionally fine but must be scoped: a shared PAY_DP constant should drive summaryDialog.ts:123-125 and dashboardPanel.ts:21-32 (and dashboardPanel.ts:147-148) only. Do not route monteCarloDialog.ts:769-776 through it — those are percentile statistics of a different type.

Severity: Low is correct, arguably Very Low, since only one field is affected and only the display rounding differs (no computed value or export is wrong).

**Priority rationale** — Real but low-stakes, with a near-zero-risk fix — bundle it into the next touch of these files rather than interrupting planned work. The concrete user cost is narrow: it only bites when the same zone is visible in two panes at once, and the only genuinely misleading case is the one named — an Avg SWE of 0.5951 printed as "0.60" in Cutoffs & Pay Summary against a SWE≤0.60 cutoff reads as sitting exactly on the cutoff, which a petrophysicist will stop and chase before realising the Dashboard's 0.595 is the same number. That is a wasted-minutes annoyance, not a data-honesty breach: nothing degraded is being sold as clean, the stored values are correct, and dashboardPanel.ts:359 exportCsv writes the raw f64 (`return typeof v === "number" ? ... : v`), so the claimed "two Avg SWE columns that don't reconcile in a spreadsheet" is actually just full-precision-vs-rounded, which is normal — the deliverable-corruption half of the evidence does not hold. On the other side, the fix is genuinely cheap and safe: three pure-presentation call sites (summaryDialog.ts:124-125, dashboardPanel.ts:21-32, monteCarloDialog.ts:772-776), no computation, no state, no churn in a large tested area, and a shared `PAY_DP` map next to PaySummaryRow in ipc.ts pays forward — any fourth pane that renders PaySummaryRow (a planned export/report template is the obvious candidate) inherits the precision instead of inventing a fourth set. Worth doing; not worth doing today.

---

## [Low] Correlation panel renders a blank strip for a well missing the chosen curve, with no reason shown

`F3d` · INCONSISTENCY · effort **S** · worth **backlog** · verifier confidence high

**Where** `src/ui/correlationPanel.ts:307`

**Evidence**

```
const label = opts.datum && !s.hasDatum ? `${s.well.well_name} (no datum)` : s.well.well_name;
ctx.fillText(label, left + stripW / 2, 12, stripW + gap - 6);
ctx.fillStyle = theme.text;
ctx.font = canvasFont(theme, 10);
ctx.fillText(opts.curve, left + stripW / 2, 24, stripW - 4);

if (!s.series || s.series.depth.length === 0) return;
```

**Failure** — Correlate 12 wells on VSH after running the Vsh module on only 9 of them. The three unprocessed wells get a framed strip headed with the well name and the text "VSH", and nothing inside — visually identical to a well that was logged but has no readings over the displayed window. In a correlation panel the natural reading of a blank column is "nothing logged here", not "this well has no such curve", so a user can conclude a well is unlogged over an interval it was actually never processed for. The same function already annotates the analogous case for flattening — `(no datum)` on the very line above — so the pattern for saying why a strip is degraded exists and is one string away.

**Fix** — When `!s.series || s.series.depth.length === 0`, append "(no <curve>)" to the strip header (mirroring the "(no datum)" suffix) or draw a dimmed "curve not present" label inside the empty frame before returning.

**Verifier correction** — Finding is valid at the cited file:line, mechanism and severity (Low). One refinement: the strip is not entirely blank — the `return` at line 307 exits only the per-strip curve closure, so formation tops (344-375) and fluid contacts (380-430) still render for that well. What is missing without explanation is the curve trace, which remains indistinguishable from a well whose curve simply has no finite samples over the displayed window. The same null-series path is also taken when getTrackData throws (466-468), so the header suffix would need to cover "fetch failed" as well as "curve absent" if the proposed fix is applied literally.

**Priority rationale** — Real and routinely reachable, not theoretical: loadCurveNames (src/ui/plotCommon.ts:242) offers the project-wide curve catalog, while reload() (src/ui/correlationPanel.ts:451-474) builds a strip for every included well and swallows a missing curve (`series = data.find((s) => s.curve_name === opts.curve) ?? null;`), so correlating on VSH after processing a subset of wells is the default outcome. At line 307 `if (!s.series || s.series.depth.length === 0) return;` bails after the frame and headers are drawn, and there is no compensating readout — hover only paints a crosshair (lines 433-442), no per-strip value or status text — so nothing distinguishes "never processed for this curve" from "no readings over this window" in the one panel whose purpose is judging lateral presence/absence of a unit. That can push a wrong stratigraphic call. It is backlog rather than fix-now because nothing numerically wrong is emitted: no computed result is shown as clean, no export or stored curve is affected, and the misread is a transient interpretation-time inference the user can correct by checking the well's curve list. The fix is cheap and near-zero-risk (a string change at lines 301-302 mirroring the existing `(no datum)` suffix, inside a self-contained canvas draw block with no test surface), so pick it up next time correlationPanel.ts is open. Implementation note: the proposed `!s.series` condition only catches the absent-curve case — getTrackData still returns a series when the curve exists but is all-null, so the label should key off "no finite value in the visible window" to actually close the ambiguity.

---

## [Low] Ribbon tabstrip declares role="tablist" but its buttons have no role="tab" or aria-selected; the active tab is signalled by CSS class only

`F3e` · A11Y · effort **S** · worth **drop** · verifier confidence high

**Where** `src/ui/ribbon.ts:373`

**Evidence**

```
for (const tab of tabs) {
  tab.addEventListener("click", () => {
    const target = tab.dataset.tab!;
    for (const t of tabs) t.classList.toggle("active", t === tab);   // <- class only
    for (const [key, el] of panels) el.hidden = key !== target;
    this.updateRibbonOverflow?.();
  });
}

// index.html:16 declares the container as a tablist whose children are plain buttons
// (and which also directly contains a role="toolbar" and a <span> brand):
//   <div class="ribbon-tabstrip" role="tablist">
//     <span class="ribbon-brand">…</span>
//     <div class="qat" role="toolbar" aria-label="Quick access">…</div>
//     <button class="ribbon-tab" data-tab="project">Project</button>   (index.html:43-48)
```

**Failure** — A screen-reader user Tabs the ribbon and hears "tab list … Project button, Data button, Petrophysics button, Advance button, Plot button, View button" — a tablist that reports zero tabs, and no indication which panel is currently showing. After activating "Plot" they get no confirmation that the switch happened (the ribbon panel swap is a `hidden` toggle with no announcement, and nothing goes to the status bar), so they cannot tell whether the Crossplot/Pickett buttons they are now Tabbing past belong to Plot or to the previously-active Petrophysics panel. Contrast dockview 7.0.2, which sets role="tab" + aria-selected + aria-controls on every dock tab (tab.js:67-77, 192) — the ribbon is the one tabstrip in the app that does not.

**Fix** — Add `role="tab"` + `aria-controls="<panel id>"` to the six `.ribbon-tab` buttons in index.html (and give each `.ribbon-panel` an id and `role="tabpanel"`), then in attachTabs set `t.setAttribute("aria-selected", String(t === tab))` alongside the existing classList.toggle. Move the `role="toolbar"` QAT and the `.ribbon-brand` span out of the tablist container so it owns only tabs.

**Verifier correction** — Claim stands as written. Two minor citation fixes: the dockview reference is dist/cjs/dockview/components/tab/tab.js:72-73 (role='tab', aria-selected) and :189 (aria-selected update), or dist/esm/.../tab.js:45 — not 67-77/192. Also worth folding into the finding: the tablist violates aria-required-children independently of the missing role="tab", because index.html:17-42 puts a <span class="ribbon-brand"> and a role="toolbar" div directly inside the role="tablist" container.

**Priority rationale** — Verified the code: index.html:16-48 does declare `<div class="ribbon-tabstrip" role="tablist">` containing a `.ribbon-brand` span, a `role="toolbar"` QAT div, and six plain `<button class="ribbon-tab">` elements, and ribbon.ts:373-380 does signal the active tab only via `t.classList.toggle("active", t === tab)`. So the finding is technically accurate. But the failure it describes materializes only for a screen-reader user, and this is a locally-installed Tauri desktop tool with one developer and a known, sighted user base of Indonesian petrophysicists working their own DuckDB well data — no procurement or WCAG obligation, no accessibility claim in the product, and no planned work that trips on the missing attributes. For every actual user the ribbon already behaves correctly: the tabs are real `<button>`s so they are focusable and Enter/Space-activatable, the `.active` class is visible, and the panel swap is visible. Nothing is mis-reported as succeeding when it failed, so the app's cardinal data-honesty rule is untouched. The proposed fix is cheap and low-risk (six attributes in markup, one `setAttribute`, moving two nodes out of the tablist), but cheap-and-safe is not the same as worth a backlog slot when the observable payoff for this user population is zero — it would just sit there permanently un-actioned. If Jauhar ever edits the ribbon header markup for another reason, the cheapest honest move is to delete the bogus `role="tablist"` attribute (which today misdescribes a container holding a brand span and a toolbar) rather than wire up the full tab/tabpanel contract — but that is opportunistic cleanup, not scheduled work.

---

## Refuted

- **Status green #3c9a5f is written as a literal three times instead of one shared token** (`src/styles.css`) — The literals are quoted accurately (styles.css:4814-4815, 4836-4837, 5062-5063, all in the single src/styles.css), but the failure mechanism is refuted by the code's own design note at styles.css:4833-4835: "Status colors: ok=green, warned=amber, failed=app red, running=accent. Green/amber are fixed values that read on both light and dark panels." The greens are deliberately excluded from the 15-var palette contract, and the sibling declarations --proc-fail: var(--warn) / --proc-run: var(--accent) prove the author consciously chose which status colors track the palette. The named future change (retune ok-green to Pertamina brand green #a6c210) is therefore not a plausible edit — #a6c210 on the Pertamina --bg-panel #ffffff is ~1.9:1 contrast, unusable as badge text, which is the exact constraint the comment guards. The reporter's own proposed fix concedes the values are palette-independent, contradicting their own per-palette-retune scenario. Additionally, .catalog-badge.win is not the same slot as the panel status tokens: its siblings .shadow/.pin/.muted (styles.css:4816-4829) deliberately DO use var(--warn)/var(--accent)/var(--text-dim), so the green literal there is an intentional opt-out because the only green palette token --accent2 is graphite under Halliburton (styles.css:115) and blue under SLB (styles.css:134). Residual cost is 4 lines in one file, all found by a single grep of the literal — no concrete failure, no measurable cost, below the stated quality bar.

- **mlDialog is the last pane still hand-rolling a per-well checkbox list — using monteCarloDialog's orphaned `.mc-wells` class, with no height cap and no live-state subscription** (`D:\XX. Arshilla\src\ui\mlDialog.ts`) — The load-bearing failure scenario is mechanically false. wellScope.ts:48 captures `groupId` once from appState.activeWellGroup and wellScope.ts:41 fetches `groups` once; resolveIds() group mode (wellScope.ts:175-178) reads those captured locals, and groupId is mutated only by the dropdown's own change handler (112-115). The only subscriptions (wellScope.ts:249-250) are pinnedWellIds/multiSelectedWellIds, each gated on that mode. There is NO activeWellGroup subscription anywhere in src/. So switching the active group with the ML pane open re-resolves NEITHER picker — both stay on the old group and agree. "Trains on North Block, applies to South Block" is unreachable. Second, `.mc-wells` is not orphaned: mlDialog.ts:179 uses it for the Input-curves grid, so the proposed "delete the now-unused .mc-wells rule (styles.css:3621)" would break a control the reporter itself quoted. Third, the divergence is documented as intentional at mlDialog.ts:151-152 ("Apply wells (the run scope) come from the shared scope selector; Train wells stay a checklist below — a distinct labelled-data pick, not the run coverage"), and wellScope's own docstring scopes the convention to run coverage. Fourth, the height claim renders nothing wrong: .dock-panel (styles.css:649-656) is `height:100%; overflow:auto`, so the pane scrolls. Fifth, the "one-shot snapshot" criticism is not asymmetric: wellScope.ts:40/43 snapshot the well universe too and asyncPane (workspace.ts:610-632) never rebuilds either picker.

- **"Apply" means two opposite things: it closes the dialog in four places and keeps it open (with a separate OK) in Layout Properties** (`D:\XX. Arshilla\src\ui\layoutPropsDialog.ts`) — The code quotes are real (layoutPropsDialog.ts:342-347 is the only Cancel/Apply/OK footer; the four close-on-Apply sites are as quoted), but the finding fails as a defect on three counts. (1) The count is wrong: curveEditDialog.ts:78 -> close() at :130 is a sixth lone-Apply-closes dialog the reporter missed. (2) The divergence is principled and documented, not an inconsistency: layoutPropsDialog.ts:4-7 states "Operates on a deep clone; the caller receives the edited layout on Apply/OK" and :13 does structuredClone(layout) — it is the ONLY dialog with a deferred-commit working copy, so it is the only one that needs an explicit Cancel, and Cancel/Apply/OK is exactly the Windows/Techlog triad the target users know. The other five mutate the live opts object at click time (crossplotPanel.ts:1781-1806, logViewPanel.ts:445-449), where Cancel would be meaningless because the modal.ts close button and Escape already cancel. (3) The claimed measurable cost is a misread: "a 470 px form" is openModal's widthPx parameter (modal.ts:7,23), not a height; height is bounded by .modal-dialog max-height 84vh with .modal-body overflow-y auto (styles.css:1507,1771-1774), so the "six sections collapsed into scroll, re-scroll per iteration" cost is not demonstrated, and reopening loses no values because persist() (crossplotPanel.ts:1807) rebuilds the form from opts. The reverse "double-pushing the layout mutation" is also wrong in mechanism: applyLayoutEdit (logViewPanel.ts:847-854) is idempotent, so a second Apply corrupts nothing.

- **Composite Log and Report render the same CompositeResult but expose disjoint export sets (SVG+PDF vs PDF+PNG)** (`D:\XX. Arshilla\src\ui\compositeDialog.ts`) — The code quotes are accurate (compositeDialog.ts:90-100 = Render/Save SVG/Save PDF; reportDialog.ts:173-183 = Render/Save PDF/Save PNG), but the claimed cost is not reachable. src-tauri/src/report.rs:427-430 appends the composite pages into the report: `if !spec.tables_only { pages.extend(composite_pages.into_iter().map(|p| p.ops)); }`, built from spec.composite with the same well/layout/scale/page_size (reportDialog.ts:231-238), and `tablesOnly` defaults to unchecked (reportDialog.ts:132-133). render_report (report.rs:436-455) emits them via the same composite::svg_page with one shared page_width_mm/page_height_mm. So a slide-ready PNG of a composite log page is already obtainable in-app: render Report, page to the composite page, "Save PNG (page)…" — no PDF-then-screenshot, no external converter. The mirror half fails too: an editable vector of a report page is already produced by exportReportPdf (ipc.ts:293, vector PDF), and the proposed `export_report_svg` IPC command is unnecessary because svgExport.ts:311 `saveSvg()` already writes any SVG string frontend-only through the savePng byte transport. The sets also differ for a stated reason: plotExport.ts:8-11 designates the Composite pane as the log view's print-fidelity export route (WebGPU canvas cannot toDataURL), and it uniquely exposes depth_top/depth_bottom (compositeDialog.ts:137-138) that the report hardcodes to null (reportDialog.ts:234-235) — vector-for-plotting vs raster-for-slides. What is left is a two-click convenience gap with no concrete failure, below the stated quality bar.

- **Seven of 24 modal titles are sentence case against a Title Case majority that all 30 dock-pane titles follow** (`D:\XX. Arshilla\src\ui\pickettPanel.ts`) — The seven sentence-case openModal titles exist as quoted, but the defect as described does not. (a) The comparative premise collapses: "All dock-pane titles are Title Case without exception (workspace.ts:1203-1392)" is false inside the reporter's own cited range — workspace.ts:1354 is openSingleton("lorenz", "lorenz", "Lorenz Plot (flow units)", group). Worse, dock-pane titles are not a closed set of 30 frontend literals: workspace.ts:1376 does openSingleton(`module:${spec.name}`, "module", spec.title, group), so every backend manifest title becomes a tab, and many are sentence case — unconventional.rs:120 "Kerogen volume + OM-corrected porosity", :250 "Gas-in-place (free + Langmuir adsorbed)", :492 "Brittleness index (elastic / mineralogical)", rocktyping.rs:196 "Lucia Rock-Fabric Number (carbonate)", :238 "Rock Type from Cutoffs (electrofacies)", modules.rs:1414 "Gas Correction (density, iterated)". There is no universal convention being violated. (b) The named failure scenario is unreachable: modal.ts:11-13 comments "Only one dialog exists at a time (single #modal-root)" and calls activeClose?.(), so two modal headings can never render together; and "Crossplot Properties" is a modal title (crossplotPanel.ts:1773), never a tab — the crossplot tab is built at workspace.ts:1405-1409 as `Crossplot — <well_name>`. The claimed side-by-side reading of "Pickett properties" next to "Crossplot Properties" cannot occur. (c) The i18n cost contradicts i18n.ts's documented design (lines 8-11): unmatched phrases deliberately stay English and the module explicitly forbids "completing" the dictionary with jargon; lookup is exact-phrase (lines 588-591), so casing is a key-matching detail, not an inherited convention. The existing catalog already mixes cases freely (Title Case "Open Session" i18n.ts:154 alongside sentence-case "Set all" :37, "Session name" :156, "No recent projects" :33). No input panics, no state renders wrong, no measurable size/time cost, no named future change that trips on it — this is a pure style preference and fails the stated quality bar.

## Dimension summaries

### F3a — 9 raw

The 15-var contract itself is complete: all 15 colour vars (--bg-app, --bg-panel, --bg-panel-alt, --bg-hover, --border, --border-strong, --text, --text-dim, --accent, --accent-dim, --accent-soft, --accent2, --accent2-soft, --warn, --track-hd) are defined in every one of the 7 theme blocks in src/styles.css (:root, [data-theme=dark], the prefers-color-scheme fallback, pertamina, halliburton, schlumberger, lapi-itb, white). The two typography tokens (--font-canvas, --font-mono) live only in :root and correctly cascade into every skin. Of the 12 var names TS actually reads (--accent, --accent-dim, --accent2, --bg-app, --bg-panel, --bg-panel-alt, --border, --border-strong, --font-canvas, --text, --text-dim, --warn) not one is missing from any palette — so there is no High "var undefined in a palette" defect on the TS side, and the readTheme/root-fallback plumbing (plotCanvas.ts:32-49) plus the themeVersion repaint chain (logViewPanel.ts:332 -> renderer.repaint -> onFrameRendered -> highlightsOverlay/topsEditor/coreOverlay) is correct. The hex inventory I was given is stale: the actual count is 65 colour literals across 16 TS files (not 53/12) — the given list omits compositeDialog.ts:162, crossplotPanel.ts:1649, histogramPanel.ts:645, layoutPropsDialog.ts:313, and undercounts plotCanvas (20, of which 12 are the Tableau-10 FACIES_PALETTE) and highlightsOverlay (9). CLASSIFICATION — LEGITIMATE (57 of 65): plotCanvas.ts:793-794 FACIES_PALETTE (categorical data colours, Tableau-10, must be hue-stable across themes); plotCanvas.ts:41-47,56 and vegaPanel.ts:113-116,239 and monteCarloDialog.ts:802 and logViewPanel.ts:615,674,757 and LogCanvasRenderer.ts:690 and topsEditor.ts:88 and highlightsOverlay.ts:33-38 — dead defensive fallbacks after a live getComputedStyle read (every var resolves in all 7 palettes, so none of these strings can be reached); correlationPanel.ts:26-31 CONTACT_COLORS (OWC/GWC/GOC/GDT/ODT/FWL — fixed-meaning fluid-contact domain colours matching Petrel/Techlog convention) and :175 "#888" unknown-type fallback; pdfExport.ts:134-135 and svgExport.ts:67-68 fillStyle/strokeStyle "#000000" (the CanvasRenderingContext2D spec's own initial values in recorder shims, and CSS vars do not resolve in an export context); reportDialog.ts:373 "#ffffff" (matches the white page rect the Rust emitter writes at composite.rs:843) and styles.css:3082 .composite-preview svg background #fff (paper); compositeDialog.ts:162 / reportDialog.ts:259 "var(--border, #ccc)" (var fallback, unreachable); highlightsOverlay.ts:209,486 and topsEditor.ts:403 "#1a1a1a"/"#f5f5f5" (computed black-or-white contrast text, must not be themed); styles.css:4836-4837/5062-5063 --proc-ok/--proc-warn/--health-ok/--health-warn (deliberately palette-independent status semantics, declared as overridable tokens with a rationale comment). VIOLATIONS: 9, reported below — the two that matter most are not hex literals at all but phantom var names (--bg, --mono, --font-ui, --danger, --bg-active, --bg-inset are used in styles.css and defined nowhere) and a missing on-accent token.

### F3b — 6 raw

The five "flagship" Canvas-2D panes (log view, correlation, crossplot, histogram, Pickett) plus map, results-QC, unconventional, cutoff and Vega are all correctly wired to themeVersion, every subscription is released in dispose(), and no themeVersion handler re-queries the backend (logViewPanel.ts:330-338 and vegaPanel.ts:566-570 both document repainting from cache deliberately) — the reverse hazards you asked about are clean. The gap is in the second tier of docked analysis panes. Six panels registered in workspace.buildRenderer paint to canvas but have NO themeVersion subscription at all: HFU, Lorenz, Thomeer, SHF, SandiMin's reconstruction QC and Monte Carlo. Five of those fill the whole canvas with `theme.bg`, so after a theme switch they are a solid slab of the OLD palette sitting in a repainted panel; Thomeer and SHF have no resize-redraw fallback either, so the stale slab is permanent until you press Run again. Separately, themeVersion is emitted from exactly one place — the ribbon's <select> change handler — so the DEFAULT theme ("system") produces zero repaint signal when Windows flips to dark mode, and the log view's WebGPU clear colour is cached behind that signal. On the dataVersion side, the shared wellScope selector and a family of tool panes snapshot the well list and curve catalog at build time and never refresh, and the Vega panel is the one plot panel with no dataVersion subscription. Panels verified clean and needing nothing: dashboard (inline SVG styled by CSS classes, explicit Compute), wellsTops/tops/inspector/dbInspector (DOM + dataVersion via workspace), history (own pub/sub), health/processing (timers), zones/autocorr/composite/report (wellPane followData=true), and every openModal dialog (no canvas; layoutPropsDialog reads --accent at click time, not at build).

### F3c — 9 raw

The shared scaffolding is genuinely good: `buildWellScope` is used by 12 batch dialogs, `formRow`/`openModal` are universal, `plotExport.buildImageExportButtons` centralises plot export across all 6 plot panels, and `.mc-run-row` + `.mc-results` + `.mc-chain-note` give the analysis panes a common shape. The drift is concentrated in three places. (1) The primary Run button has FOUR competing class systems — `.form-run-btn` (6 panes), `.btn.btn-accent` (1), `.lp-btn primary` (4), and a bare `.primary` (6 panes) that matches no CSS rule at all, so half the analysis panes render their main call-to-action as an unstyled native button. (2) The VSH/PHIE/SWE/PERM cutoff quartet is hand-copied into five panes with divergent defaults (Monte Carlo uses PHIE≥0.08 / SWE≤0.50 where the other four use 0.10 / 0.60) and only one of the five reads the `cutoffs/__default__` document that Cutoff Sensitivity writes. (3) pickettPanel and mlDialog are single-pane holdouts of retired patterns — a `form-control`-classed gear button and a hand-rolled per-well checkbox list that `wellScope.ts`'s own header comment says was replaced. Lesser drift: Apply-means-close vs Apply-means-stay, decimal-place drift on Avg SWE / N/G across the three panes that render the same `PaySummaryRow`, and 7-of-24 modal titles in sentence case against a Title Case majority.

### F3d — 9 raw

The app has a genuinely strong empty/failure-state culture in most places: every dock pane has a "Select a well…" / "Failed to open X: err" fallback (workspace.ts:1027, 662, 624), every scoped dialog refuses to run with "No wells in scope — pick a group, pin/select wells, or choose All", the Processing panel groups per-well failures by reason with actionable advice, the SHF/cutoff/results-QC/Monte-Carlo/vega panels are exemplary at distinguishing "0 samples" from "0 result", and the all-NaN honesty guard is correctly wired into workflow.rs, equations.rs, python_engine.rs and multimin2.rs. The failures are all leaks around the edges of that same convention. The most serious is the pay-summary engine: a well whose VSH/PHIE/SWE were never computed still emits full SAND/RESERVOIR/PAY rows reading Net 0.0, N/G 0.00, HPV 0.00 — a fabricated clean zero that flows into the Field Dashboard grid, its CSV export, and the client-facing report PDF, with no field on PaySummaryRow that could tell the frontend the difference. Alongside that: the crossplot's "No valid data" message is unreachable for the nine mnemonics that are its own defaults; the Pickett panel has no empty-state message at all; the ML dialog writes a permanent History entry claiming N wells were written when only a subset were; the Field Dashboard states "FLAG curves written." on a code path that deliberately writes nothing; the batch report export runs for minutes as a single-item job whose Cancel button marks a fully-completed batch "Cancelled"; and the SQL console reports its LIMIT-truncated row count as the true total.

### F3e — 8 raw

Not clean. The 9D base did good work on the plot canvases (role=img, tabindex, keyboard pan/zoom, prefers-reduced-motion) and dockview 7.0.2 ships a correct WAI-ARIA tabs implementation for the dock tab strip (roving tabindex + Arrow/Home/End/Enter/Delete in tabs.js:457-500), so dock tabs, plot canvases, and focus rings are genuinely fine — there is no global `outline:none` reset, buttons keep the UA focus ring, and every `outline:none` in styles.css substitutes a border/box-shadow. The failures are concentrated in the hand-rolled DOM outside those two systems. Two are hard blockers: the Wells tree and Tops list — the ONLY interactive path to `appState.selectedWell` and `selectedInterval` — are click-only `<div>`s with no tabindex, role, or keydown, so a keyboard-only user cannot change the active well after startup; and `openModal` never moves focus into the dialog nor restores it on close, with `#modal-root` sitting last in index.html. Two more are cheap but wide: zero `for=`/`htmlFor` anywhere in the codebase against 179 `formRow()` call sites, and `#status-bar` (274 `setStatus()` calls, the app's entire feedback channel) carrying no `aria-live`. The one colour-only indicator is the Results-QC scorecard dot, and it is worse than a normal colour-only violation because the theme mapping makes "ok" render bright red under the Halliburton skin and green mean "warn" under the default skin — the codebase already does this correctly with ✓/⚠/✗ glyphs in processingPanel.ts. The drag-FWL handle, track resize/reorder, and scale editing all have keyboard equivalents and are NOT findings; only the curve visibility toggle lacks one.


