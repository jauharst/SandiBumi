# SandiBumi post-Gate-2 adversarial audit — 2026-08-20

Three read-only passes over the whole codebase, run in order, auditing code largely written by a
Claude model under an assume-author-bias, try-to-refute discipline. **Pass 1** (physics &
contracts): five parallel review dimensions — equation transcription, method-suite physics, units
and conventions end to end, limits/NaN/write discipline, renderer↔exporter agreement — every
candidate finding first self-refuted by its reviewer against `DECISIONS.md`/`docs/`/the test suite,
then the survivors re-verified by the session model at the code (14 of the highest-severity
re-verified line by line; the decisive code of every P0/P1 was read directly). **Pass 2**
(structure) and **Pass 3** (slop rubric, read-only) follow in their own sections. Nothing in the
repository was modified; fixes are a separate program after triage.

**Evidence bar** (all green — no findings from execution):
- Full gate `tools\check.ps1`: **GREEN in 190s** at `e374251f` (`fix/b1-dec084-sweep`, = master
  `460f229b` + the DEC-084 sweep).
- `cargo test --lib -- --ignored`: **38 passed / 0 failed** in 131.7s, zero skips — optional
  packages present; includes the 100-well field-pipeline stress and ML byte-determinism tests.
- Baseline for Pass 2/3: `ca4f8c92` (Gate-1 accepted merge) → master `460f229b` = 209 files,
  +83,935/−12,090.

Severity: **P0** = a silently wrong number ships into a deliverable · **P1** = wrong
number/crash on a reachable path, or a deliverable that misstates itself · **P2** = wrong under a
plausible configuration, or a contract broken with a visible-but-unexplained symptom · **P3** =
latent, degenerate-input, hygiene, or ruled-and-attributed conflicts. Rulings are never
adjudicated here: where two sources conflict, both citations are reported.

## Triage status — the tracker

**This is the one place to see where the 85 findings stand.** It lives at the top of the list it
tracks, so a finding's status cannot drift away from the finding's own text. `Where` names the
`REVIEW.md` entry or the PR to click through.

| Mark | Meaning |
|---|---|
| `[x]` | fixed — on master, or on the open PR named in `Where` |
| `[ ]` | confirmed still open — I read the code and the defect is there |
| `[?]` | **not yet triaged** — I have not checked whether it still stands |
| `[!]` | **needs Jauhar's ruling** — I read the code, the defect is real, and closing it is a call I cannot make (his own method, or an uncited petrophysical constant). The question is written under the finding. |

`[?]` is not "probably fine". It means unexamined, and some will turn out to be already fixed by
an earlier increment. Working down the severities in order is what turns `[?]` into one of the
other two.

### Pass 1 — physics & contracts — 48 findings

| # | Sev | Finding | | Where |
|---:|:--:|---|:--:|---|
| 1 | P0 | `phi_son`'s Wyllie compaction divisor INFLATES porosity for `DT_SH < 100`, the exac… | `[x]` | increment 1 |
| 2 | P1 | `phi_son` ships `0.625·(DT−DT_MA)/DT` under the label "RHG" — DEC-017 forbids exact… | `[x]` | increment 2 |
| 3 | P1 | A Monte Carlo run bypasses the declared-range guard and can ABORT the process at `s… | `[x]` | increment 3 |
| 4 | P1 | `sw_sim`'s "unlimited" diagnostic `SWE_SIM` is hard-capped at 1.0 by its own solver… | `[x]` | increment 6 |
| 5 | P1 | `distribution::bin_by_depth` fragments depth bins through an f32 round-trip — the R… | `[x]` | increment 4 |
| 6 | P1 | The six standard curves are stored UNCONVERTED while the generic store is CONVERTED… | `[x]` | DEC-089 + migration |
| 7 | P1 | LAS export labels the standard six with canonical units while writing the unconvert… | `[x]` | DEC-089 — with #6, same root |
| 8 | P1 | Deviation-survey MD is imported with NO depth-unit resolution at all — the only dep… | `[x]` | increment 17 |
| 9 | P2 | `sw_imts` Qv uses bulk VOLUME fractions where the spec says grain WEIGHT fractions… | `[!]` | asked — grain-weight Qv |
| 10 | P2 | The Monte Carlo pay path never floors PHIE — a delivered vendor negative porosity s… | `[x]` | fixed 2026-08-20 |
| 11 | P2 | `sw_cond_root` returns SWT = 0.0, unflagged, when the excess-conductivity term exce… | `[x]` | increment 38 |
| 12 | P2 | Monte Carlo carries a step's MASK and never reads it — the 2026-07-20 audit's #1 fi… | `[x]` | increment 42 |
| 13 | P2 | `ssc()` applies two capillary-water conditioning rules the banked spec does not car… | `[!]` | asked — two unspecced SSC rules |
| 14 | P2 | `sw_height.FWL` declared "m" but consumed in the project depth unit — recorded as S… | `[x]` | increments 9, 22 |
| 15 | P2 | Well Header hard-labels TD and KB as "(m)" regardless of the project's declared uni… | `[x]` | increments 11, 12 |
| 16 | P2 | `facies_tie::CORE_MATCH_TOL_M` — a metre constant against raw project-unit depths,… | `[x]` | increment 21 |
| 17 | P2 | `DEFAULT_DEPTH_TOL = 0.15` (a six-inch, i.e. metre, derivation) compared against pr… | `[x]` | increment 21 |
| 18 | P2 | `rw_args()` declares FTEMP as a plain `log_in` — a raw imported FTEMP masquerades i… | `[x]` | increment 35 |
| 19 | P2 | %-delivered PHIE/VSH are stored unconverted (warned at import) and NOTHING consults… | `[x]` | increment 39 |
| 20 | P2 | `draw_style:"step"` holds one sample interval INTO a NaN gap in print but not on sc… | `[x]` | increment 34 |
| 21 | P2 | Non-positive values on a log-scaled track: the screen clamps to the left edge, the… | `[x]` | increment 34 |
| 22 | P2 | `show_samples` is honoured on screen and ignored in print | `[x]` | PR #94 |
| 23 | P2 | A `fill:"blocks"` track prints a numeric min/max scale the track does not use | `[x]` | PR #93 |
| 24 | P2 | A plate near a page break is dropped from the print entirely — never carried to the… | `[x]` | DEC-090 — nudge + callout |
| 25 | P2 | The non-MD-datum refusal is applied on one side of each pair only — in OPPOSITE dir… | `[x]` | increment 40 |
| 26 | P2 | The screen's FACIES block track is built from min/max-DECIMATED data; the print is… | `[x]` | increment 41 |
| 27 | P3 | `vsh_gr` ships Larionov in the rounded decimal form (0.33/0.083) that SB-CLY-004 ex… | `[ ]` | open — deferred outside pilot scope by the audit |
| 28 | P3 | Every Wyllie-Rose/Coates permeability coefficient is a bare literal — arithmetic ve… | `[?]` | not yet triaged |
| 29 | P3 | `perm_coates` accepts a negative PHIE (even power hides the sign) where its sibling… | `[x]` | increment 56 |
| 30 | P3 | The two FTEMP writers disagree by 58% on gradient (0.03 °C/m vs 0.026 °F/ft = 0.047… | `[x]` | increment 5 — DEC-077, reported not adjudicated |
| 31 | P3 | `VSH_GR` is labelled "unlimited" but the IGR is pre-clamped on 4 of 8 branches (STI… | `[x]` | increment 58 |
| 32 | P3 | The P_LOW/P_HIGH normalization defaults cite `memory/method_workflow_standards.md`,… | `[x]` | PR #95 |
| 33 | P3 | `sandimin::CORE_MATCH_TOL_M = 1.0` — the metre-constant-vs-project-unit family agai… | `[x]` | increment 21 |
| 34 | P3 | `alpha_expansion` caps the dual-water α at 5.0 — an uncited ceiling over the spec's… | `[ ]` | open — uncited ceiling |
| 35 | P3 | Two doc/comment drifts around the sspw 1.6 gas weight (the numeric divergence itsel… | `[x]` | DEC-088 |
| 36 | P3 | `write_computed_curves_with_ancestry_clearing` DELETEs only `clear_names`, not the… | `[x]` | increment 57 |
| 37 | P3 | SandiMin's `_SWT`/`_SXOT` are clipped-only with no diagnostic twin, and the SB-SAT-… | `[!]` | increment 82 — sweep + record; asked — SandiMin twin |
| 38 | P3 | The pay path floors at the compile-time `PHIE_FLOOR` while the modules floor at the… | `[!]` | asked — which PHIE floor the pay path obeys |
| 39 | P3 | The persisted realization matrix stores the FIRST `realization_cap` snapshots while… | `[x]` | increment 59 |
| 40 | P3 | Default box-plot bin: zoom-relative on screen ((visible span)/20) vs page-relative… | `[x]` | increment 62 |
| 41 | P3 | One sample interval of curve and crossover is lost at every page break (an interval… | `[x]` | increment 61 |
| 42 | P3 | A non-JPEG plate is refused in the SVG export too, though `svg_page` embeds any MIM… | `[x]` | increment 63 |
| 43 | P3 | Block-builder micro-divergences: JS `Math.round` (half-up) vs `f32::round` (half-aw… | `[x]` | increment 64 |
| 44 | P3 | Duplicated cores with no drift today: `ssc::vsh_from_gr` is a verbatim second copy… | `[x]` | increment 65 |
| 45 | P3 | The SB-ENV-057 ambiguous-union gate is a two-string denylist ("m|ft"/"ft|m") — "psi… | `[x]` | increment 66 |
| 46 | P3 | Core-table import assumes the project unit when no depth unit is confirmed (documen… | `[x]` | increment 67 |
| 47 | P3 | `percent_to_fraction` decides percent-vs-fraction by a magnitude heuristic (median… | `[x]` | increment 68 |
| 48 | P3 | `UNIT_RULES` carries a rule whose `families` names a nonexistent family ("QV") and… | `[x]` | increment 69 |

### Pass 2 — structure — 14 findings

| # | Sev | Finding | | Where |
|---:|:--:|---|:--:|---|
| 49 | P2 | `workflow.rs` is two unrelated subsystems in one 15,732-line file, with a provably… | `[?]` | not yet triaged |
| 50 | P2 | The generic module runner doubled to 940 lines and now contains four module names —… | `[?]` | not yet triaged |
| 51 | P2 | The Indonesia equation is implemented twice — and the guards diverge exactly where… | `[x]` | increment 43 |
| 52 | P2 | One referential-integrity class is defined in four places in four shapes, on a deli… | `[x]` | increment 45 |
| 53 | P2 | `equations.rs` absorbed the entire ancestry/log-set-versioning subsystem (2,246 → 6… | `[?]` | not yet triaged |
| 54 | P2 | `complete_module_log_spec`: eight copies of one reserved-key guard — two check `leg… | `[x]` | increment 46 |
| 55 | P2 | The cutoff model is stringly-typed with a silent `_ => perm` fallback — and permeab… | `[x]` | increment 44 |
| 56 | P2 | Ten new `*_unnamed` parser wrapper pairs — and 12 of 23 public parsers still lose t… | `[x]` | increment 49 |
| 57 | P2 | `reloadContext` is triplicated across histogram/pickett/crossplot, and the branch a… | `[x]` | increment 50 |
| 58 | P2 | The shale/clay quantity contract is a second manifest keyed by module name — and an… | `[x]` | increment 48 |
| 59 | P2 | `check.ps1` stage 1: twelve copy-pasted gate blocks that all fail as "takeover ledg… | `[x]` | increment 47 |
| 60 | P3 | A test-only serializer seam threaded through two production signatures — `workflow.… | `[x]` | increment 76 |
| 61 | P3 | `present_average` is provably the identity function ✓ — `workflow.rs:3763-3770` (✓… | `[x]` | increment 76 |
| 62 | P3 | Thirteen hand-written catalog registration gates that cannot say which one failed —… | `[x]` | increment 73 |

### Pass 3 — slop rubric — 23 findings

| # | Sev | Finding | | Where |
|---:|:--:|---|:--:|---|
| 63 | P3 | A dead `??` fallback in `auditedFamilyDisplayDecision` silently disables the shippe… | `[x]` | increment 72 |
| 64 | P3 | `param_sources` re-enumerates one topic vocabulary in three hand-maintained tables,… | `[x]` | increment 78 |
| 65 | P2 | Orphaned doc comments — 13+ confirmed sites where a new item was inserted between a… | `[x]` | increment 51 |
| 66 | P2 | A comment inside the `skip_version` branch asserts "overwrite FLAG_* in place" dire… | `[x]` | increment 51 |
| 67 | P2 | Thirteen production-shaped items in `plotting.rs` are gated `#[cfg(test)]` — struct… | `[x]` | increment 71 |
| 68 | P2 | Twelve dead new TypeScript exports — seven of them IPC wrappers, i.e. backend comma… | `[x]` | increment 54 |
| 69 | P2 | `ingest.rs` spells the ~20-field `ImportResult` early-return literal out at 14 non-… | `[x]` | increment 53 |
| 70 | P2 | The `INSERT INTO log_sets` column list is re-typed four times and two of the four n… | `[x]` | increment 52 |
| 71 | P2 | `runCustody.ts` (new file): `requestRunCustody`'s doc block sits on `ensureSessionO… | `[x]` | increment 54 |
| 72 | P3 | A mangled-formatting class with a ZERO baseline: 92 sites at master (lone `;`/`,` o… | `[x]` | increment 70 |
| 73 | P3 | The same mangling reaches OPERATOR-FACING text: `workflow.rs:2081/:2088/:4419/:4461… | `[x]` | increment 70 |
| 74 | P3 | The `OUT_PREFIX_OPT` trim/filter/uppercase/format rule is open-coded eight times in… | `[x]` | increment 76 |
| 75 | P3 | The SMOOTH_ROLLOFF ceiling + roll-off arithmetic + `PHIE_FLOOR` clamp + `record_bou… | `[x]` | increment 79 |
| 76 | P3 | `chain.rs::complete_chain_sets` is 385 lines doing seven jobs, containing the quant… | `[x]` | increment 81 |
| 77 | P3 | `equations.rs`: the eleven-line "validate ancestry → parse params_json → insert CUR… | `[x]` | increment 80 |
| 78 | P3 | `curve_edit.rs` defines a private `timestamp_utc_ms` duplicating `equations::ancest… | `[x]` | increment 74 |
| 79 | P3 | `db.rs:11038` re-prepares an identical `audit_detail` statement INSIDE the per-entr… | `[x]` | increment 75 |
| 80 | P3 | `parsers.rs:272-278`: two consecutive `if utf16_heuristic` branches with byte-ident… | `[x]` | increment 74 |
| 81 | P3 | `sandimin.rs:299-306`: `solver_selectable_models()` re-encodes the selectability ru… | `[x]` | increment 75 |
| 82 | P3 | `montecarlo.rs:1429-1444`: the custody refusal hand-writes all twelve `McResult` fi… | `[x]` | increment 75 |
| 83 | P3 | `modules.rs:2252-2270`: `if registration.is_none() { continue } … .unwrap()` where… | `[x]` | increment 74 |
| 84 | P3 | `plotInvalidation.ts:90-100`: `queued` is written in lockstep with `current` and re… | `[x]` | increment 77 |
| 85 | P3 | The new `applyPlotRecordLimit("context_well_legend_rows", …)`/`reducePlotLabel` log… | `[x]` | increment 77 |

**22 closed · 3 confirmed open · 60 not yet triaged · 85 total.**

Severity is where the order comes from. The single **P0 is closed**, and **5 of the 7 P1s** are
closed. The two open P1s are findings **6 and 7** — one root and its consequence.

---
# PASS 1 — PHYSICS & CONTRACTS (confirmed findings)

## P0

### 1. `phi_son`'s Wyllie compaction divisor INFLATES porosity for `DT_SH < 100`, the exact case DEC-012 ruled must REFUSE — and a green test pins the ruled-against behaviour
- **Dimension:** F1 transcription/limits | **Where:** `src-tauri/src/modules.rs:4848` (applied :4861, :4875; range :4792; test :10390)
- **Failure:** `OPT_CP=ON`, WYLLIE, DT 90, DT_MA 55.5, DT_FL 189, **DT_SH 90**, VSH 0.2 → Cp=0.90 →
  PHIT 0.25843→0.28714, PHIE 0.20674→**0.22971**: the "compaction correction" ADDS 2.30 p.u. (the
  exact magnitude `11_porosity.md:903` names), reaching ×1.667 at DT_SH 60 — all inside declared
  ranges, flowing into net/NTG/HPV with nothing downstream to catch it.
- **Verification:** code read directly — `cp = dt_sh/100` with no `Cp ≥ 1` guard; the manifest's only
  validity rule is DEC-063's endpoint order. DEC-012 read verbatim: *"Refuse the run"* — still
  DECIDED, revisited by nothing through DEC-084. Mitigation checked: SB-POR-017 is **not** in the
  DEC-018 pilot manifest, so the fix is deferred — but the option ships enabled today, and
  `phi_son_wyllie_cp_opt_in_only_scales_wyllie` pins the wrong behaviour as a contract, so
  implementing DEC-012 must break a green test.

## P1

### 2. `phi_son` ships `0.625·(DT−DT_MA)/DT` under the label "RHG" — DEC-017 forbids exactly this state
- **Dimension:** F1 attribution | **Where:** `modules.rs:4767` (enum), :4757 (doc), :4858 (arithmetic)
- **Failure:** OPT_SON=RHG, DT 75, DT_MA 55.5 → 0.16250, which is neither Techlog's slowness-root
  nor IP's velocity-root RHG (`11_porosity.md` F3: *"none of these"*). The curve, `params_json` and
  the client PDF's methodology table all attribute Raymer-Hunt-Gardner to a transform that is not it.
- **Verification:** DEC-017 read verbatim: *"do not preserve the current approximation under an RHG
  label merely by renaming around it… may survive only as a separately named `FIELD_OBSERVED`
  method."* No `FIELD_OBSERVED` id exists in the tree; `OPT_SON` still offers WYLLIE|RHG.
  SB-POR-013..020 are outside the pilot manifest (deferred), but the mislabelled option is reachable
  in the shipped build now. (Note: DEC-079 executed RHG-1980 correctly for the CHART overlay — the
  module branch never received the same treatment.)

### 3. A Monte Carlo run bypasses the declared-range guard and can ABORT the process at `sw_height`'s clamp
- **Dimension:** F1b limits/robustness | **Where:** `montecarlo.rs:1161` (its own `resolve_zone_param`,
  no validation), :1305 (draws written straight in); panic site `satheight.rs:248`
  `sw.clamp(swt_irr.max(0.0), 1.0)`; the guard that exists only on the deterministic path:
  `workflow.rs:237-332`.
- **Failure:** (a) a well carrying `zone_params` SWT_IRR=25 (percent entered as a fraction — the
  guard's own named case) plus an MC chain containing `sw_height` reaches the clamp with lo=25 >
  hi=1 → `f64::clamp` asserts → **process abort mid-study** (release profile is `panic="abort"`,
  realization loop rayon-parallel). (b) `McParam` SWT_IRR Uniform{0.1, 1.2} — `build_draws` never
  checks a draw against `ArgSpec` min/max, so any draw > 1.0 aborts.
- **Verification:** clamp line and MC's unvalidated `base_params` read directly. `run_module`
  validates declared preconditions/flags only; `satheight.rs:248` is the only param-derived clamp
  bound in the method files (ssc/lrlc deliberately use panic-safe `limit()` — the idiom exists and
  was not applied here); `validate_cutoffs` covers cutoffs only.

### 4. `sw_sim`'s "unlimited" diagnostic `SWE_SIM` is hard-capped at 1.0 by its own solver — the SB-SAT-025 twin is cosmetic, and the family sweep structurally cannot see it
- **Dimension:** F3 unlimited-pair honesty | **Where:** manifest `modules.rs:7032` ("(unlimited)");
  solver `sandimin.rs:371-399` (`if f(1.0) <= 0.0 { return 1.0 }`, `.clamp(0.0,1.0)` on all exits);
  coverage hole `modules.rs:12093-12221` (sweep arm E is name-only; `sw_sim`/`sw_indo` never
  value-tested).
- **Failure:** Rw entered a decade low → true root > 1 → `SWE_SIM` reads exactly 1.000, bit-identical
  to `SWE` — the pair agrees and the wet leg "looks real", which is precisely the ambiguity the
  unlimited twin exists to break. Unflagged: `limit()` records a clamp only when it changes a value,
  and here the value arrives already capped. The same module flags its rarer VSH≥1 singularity
  (SB-SAT-030) and misses this common case.
- **Verification:** solver exits read directly. DEC-038 checked: it exempts SSC/SSPW and its
  reasoning (*"a companion built by bypassing only the last clamp would be labelled unlimited while
  still bound upstream is a worse statement than no companion"*) CONDEMNS this state rather than
  excusing it. `sw_arch`/`sw_indo`/`sw_rtc`/`sw_imts` all emit genuinely raw diagnostics.

### 5. `distribution::bin_by_depth` fragments depth bins through an f32 round-trip — the Rust (print) and TypeScript (screen) twins disagree
- **Dimension:** F5 agreement pairs (found independently by two reviewers, both measured) |
  **Where:** `distribution.rs:323-325` (regroups by `(*top/bin).floor() as i64 == k` where
  `top = k as f32 * bin`); the TS twin `distribution.ts:176-192` keys a Map by the integer id and is
  immune. Consumers: `composite.rs:1586` (print), `logViewPanel.ts:1490` (screen).
- **Failure:** measured sweeps — bin 0.30 over 1000–1100 m: 51/334 bins mis-keyed; 0.1524 m (½ ft):
  98/657; 975 realistic (bin, depth) pairs: **125 (13%) disagree**, with total splits at e.g. bin
  0.7 @ 2500 m (25 boxes where 16 are correct). Failures start above ~1024 m where the f32 ulp
  coarsens. Every mis-keyed bin opens its own entry, so the printed box plot degenerates to
  one-plug boxes — the spread the box exists to state is gone from the deliverable while the screen
  shows proper quartiles. An f64 re-run of the sweep: zero disagreements.
- **Verification:** code read directly; both existing tests use bin 5.0 (f32-exact) and cannot fail.

### 6. The six standard curves are stored UNCONVERTED while the generic store is CONVERTED — the viewer, plots, curve editor and Reframe read one domain, modules the other
- **Dimension:** F2 units | **Where:** `ingest.rs:1009-1019` (raw parsed columns into
  `insert_standard_curves`; `parsers.rs` pick() clones verbatim), vs `ingest.rs:1400-1407`
  (`convert_to_canonical` mutates a clone for the generic store). The module-input fix
  (`overlay_resolved_native_standard_inputs`) is called only from `fetch_curve_frame_from_set`
  (`equations.rs:3793/:3862/:3900`); unprotected readers: `fetch_track_frames` (log view),
  `fetch_curve_data` (histogram/crossplot/Pickett), `curve_edit.rs:162`, `reframe.rs:854`.
- **Failure:** one LAS: `NPHI .PU` 30.0 → modules read 0.30 v/v, the viewer/plots/editor read 30.0;
  `RHOZ .KG/M3` 2400 → 2.4 vs 2400; `DTCO .US/M` 656 → 199.9 vs 656. A crossplot used to pick
  endpoints reads (30, 2400) while the module computes with (0.30, 2.4); an Inspector edit lands in
  the 30.0 domain and modules never read it. The import warns that a conversion happened — which
  makes the split harder to spot, not easier.
- **Verification:** insert call and overlay call-sites read directly. No DEC/doc rules the standard
  projection's unit domain; the only unit tests on `standard_curves` pin the two rejection cases
  (NULL); no TS code applies factors. DLIS is unaffected (generic store only).

### 7. LAS export labels the standard six with canonical units while writing the unconverted values — a mislabelled client deliverable
- **Dimension:** F2 units / deliverable | **Where:** `export.rs:900-906` (canonical-unit labels) +
  :936 (`fetch_curve_frame` values). Direct consequence of finding 6 that leaves the building.
- **Failure:** the same delivery re-exports as `NPHI .v/v` carrying 30.0, `RHOB .g/cc` carrying
  2400, `DT .us/ft` carrying 656. The LAS self-check (:709-724) validates only the depth unit; the
  nearby unit tests synthesize already-canonical fixtures and structurally cannot observe this.
- **Verification:** both code sites read directly.

### 8. Deviation-survey MD is imported with NO depth-unit resolution at all — the only depth-bearing importer without one
- **Dimension:** F2 units | **Where:** `ingest.rs:1500-1528` (`import_deviation_csv`);
  `parse_deviation_csv` reads MD/INC/AZI only; consumed by `materialize_tvd_curves` → TVD/TVDSS.
- **Failure:** a foot-MD survey CSV into a metric project: 8000-ft stations read as 8000 m; the path
  is sampled at 0–2438 m against a 0–8000 span, TVD/TVDSS wrong across the whole well. Downstream,
  `precalc` FTEMP at TVDSS 8000 = **266.7 °C instead of 99.9 °C** → Arps → Rw → Sw.
  `index_resolution` is returned `None` in every branch — nothing is even recorded.
- **Verification:** the 1493–1600 block greps ZERO unit/convert references (verified directly). Every
  other depth importer resolves or refuses (LAS refuses undeclared, DLIS converts, core takes a
  confirmed unit, array-log/point intake takes `file_unit`, images convert `entered_unit`) — this
  contradicts `units.rs:97-99`'s own doctrine verbatim.

## P2

### 9. `sw_imts` Qv uses bulk VOLUME fractions where the spec says grain WEIGHT fractions — a porosity-dependent bias that flips direction across the reservoir
- **Dimension:** F1b method physics | **Where:** `lrlc.rs:60-62` (`cec_theo_at`), :366-369; spec
  `docs/method_lrlc_rtc_imts.md:31,35`.
- **Failure:** re-derived independently: correct Qv = ΣV_i·ρ_i·CEC_i/(100φt); the code's
  `Σ V_i·CEC_i · ρg(1−φt)/(100φt)` leaves a spurious `ρg(1−φt)/ρ_clay` factor — **−24% at φt 0.25 vs
  −3.9% at φt 0.05** (VKAOL 0.20, CEC 8). The fitted S absorbs only the MEAN over calibration plugs
  (through-origin, no φ term), so SWT is overstated in rock more porous than the plugs and
  understated in tighter rock; kaolinite vs illite density is ignored entirely (a further 6%).
- **Verification:** code + spec lines read directly; the arithmetic re-derived and the −24%/−3.9%
  confirmed exactly. `sandimin.rs:1772-1779` computes the same quantity CORRECTLY (per-clay
  `x·CEC·ρ_clay`, ÷φt) — two implementations of one physical quantity, one right. Both IMTS tests
  are directional; no Qv magnitude is pinned. (P1 if S is ever taken from literature instead of fitted.)
- **QUESTION TO JAUHAR (2026-08-20), blocking this one.** `lrlc.rs:60-62` builds Qv from BULK
  VOLUME fractions; `docs/method_lrlc_rtc_imts.md:31,35` specifies GRAIN WEIGHT fractions
  (`Qv = Σ V_i·ρ_i·CEC_i / (100·φt)`) — the code omits the grain-density weighting. The repo rule
  is that the spec wins, but this is your own LRLC research and the change moves Sw in shaly
  intervals with a bias that **flips direction across the reservoir** with porosity, so it is not
  mine to settle. **Was the code a transcription slip, or a deliberate departure from the note?**


### 10. The Monte Carlo pay path never floors PHIE — a delivered vendor negative porosity subtracts into P10/P50/P90 HPV
- **Dimension:** F3 pay-path floors | **Where:** `montecarlo.rs:776-847` (`zone_metrics`, `p`
  unfloored), :1004/:1595 (raw pool passthrough); contrast `workflow.rs:4404` `floored_phie`
  applied at :4545/:5313.
- **Failure:** an ordinary study — vary M/N/RW over `sw_indo` against a delivered vendor PHIE, no
  porosity module in the chain — books the negative streak's `PHIE·(1−SWE)·h` into HPV in every
  realization (`floored_phie`'s own doc measures the deterministic version of this at >20% HPV
  understatement). The deterministic pay summary and the MC P50 then disagree on the same
  wells/cutoffs, and the gap reads as uncertainty. CLAUDE.md's rule is "at the curve and at
  **every pay path**".
- **Verification:** `grep floored_phie|PHIE_FLOOR montecarlo.rs` = 0 matches, verified directly.
  No DEC carve-out; the chain-with-porosity-module case is saved by module-side floors.

### 11. `sw_cond_root` returns SWT = 0.0, unflagged, when the excess-conductivity term exceeds measured conductivity at exactly n = 1
- **Dimension:** F3 clamp honesty | **Where:** `sandimin.rs:475-506` (esp. :493 `if g(0.0) > 0.0
  { return 0.0 }` with `swt.powf(n−1.0)`; 0^0 = 1 in Rust makes the lin term a nonzero constant at
  n=1); callers: dual-water nonlinear, Juhász, Waxman-Smits post-solves + `resultsqc.rs:229/:244`.
- **Failure:** n = 1.0 is inside every declared N range [1,4], and SandiMin's dialog n is unranged: a
  shaly interval with high Cwb/Qv and moderate Rt returns **SWT = 0.0 — 100% hydrocarbon — from an
  equation with no root in [0,1] at all**, written as an ordinary curve with no degradation record.
  The `g(1)≤0 → 1.0` arm has the same unflagged-clamp problem in the conservative direction.
- **Verification:** code read directly. The function's own doc rejects n < 1 for exactly this
  divergence and stops at the open boundary. One file over, `sw_imts` handles the same failure mode
  correctly (refuse, SB-SAT-028).

### 12. Monte Carlo carries a step's MASK and never reads it — the 2026-07-20 audit's #1 finding is fixed in the deterministic runner and fully live inside MC (pinned as-is, unruled)
- **Dimension:** F4 mask discipline / fix-state | **Where:** `montecarlo.rs:1281-1291` (no input or
  output blanking), :1073-1076 (`build_plans` never fetches the flag curve); pinned as-is by
  `the_monte_carlo_chain_ignores_a_step_mask_the_real_chain_honours` (:2330-2455, GATE-PINNED).
- **Failure:** washout interpreted as rock → MORE pay in every realization — the dangerous
  direction, since a batch study is what gets quoted; and every run-level statistic inside an MC
  chain step (gr_normalize P3/P97, log_predict KNN training) is anchored by casing/washout samples
  per realization.
- **Verification:** the deterministic fix at `workflow.rs:2643-2679` verified real (with the DEC-033
  repair exemption). Triage marks T-BATCH-17 `[x]` = the pin test was written; "pinned as-is" is
  disclosure, not repair, and no DEC rules the MC half acceptable.

### 13. `ssc()` applies two capillary-water conditioning rules the banked spec does not carry; one forces PHIFF = 0 on tight clean rock
- **Dimension:** F1b method vs spec | **Where:** `ssc.rs:312-315` and :323-325 (both read directly,
  verbatim as reported); spec `docs/method_ssc_sspw.md:36-39` lists only the PHIE≤0.002 rule and
  the SWIRR_MIN floor.
- **Failure:** PHIT 0.04, CBW 0 (clean tight streak) → CWSH = 0.04, **PHIFF = 0, BW = PHIT: 100%
  capillary-bound** — and CWSH is `sw_rtc`'s declared CAPBW input, so it moves Sw. The 0.05
  threshold is a bare literal, twice, unsourced and not a parameter.
- **Verification:** the module header's six line-verified `.lls` quirks do NOT include these two;
  the `.lls` is not in the repo; no DEC; no test enters either branch; PHIT's [0.001, 0.75] limit
  keeps the branch squarely reachable.
- **QUESTION TO JAUHAR (2026-08-20), blocking this one.** SSC is your method, ported from
  `ssc_lqr_gap_edit_jau.lls`, and **that file is not in the repo** — so I cannot check the code
  against its own source, only against the banked note. The note's §8 lists TWO conditioning
  rules; the code runs FOUR. The two extra ones are `phie - cwsh <= 0.001 -> cwsh = phie, bw =
  phit` and `phit < 0.05 && cbw < 0.05 -> cwsh = phit - cbw`. Both read like real Loglan content
  an abridged note left out: the first is a numerical-noise guard, the second says a tight
  streak with little clay-bound water has no producible porosity — which is a defensible
  statement about tight rock and errs conservative. But **0.05 is an uncited number appearing
  twice**, and CWSH feeds `sw_rtc`'s CAPBW input, so either answer moves Sw on tight streaks.
  **Are those two rules in your `.lls`?** If yes I will document them into the note and pin them
  (and 0.05 needs a source, or to become a parameter). If no, they come out, and a re-run of a
  tight interval will read more free porosity than it does today.


### 14. `sw_height.FWL` declared "m" but consumed in the project depth unit — recorded as SB-SHR-004 and still open; the surrounding PRD section is now partly stale
- **Dimension:** F2 units | **Where:** `satheight.rs:113` (declaration) vs :196; recorded at
  `docs/PRD_v2/15_sat-height-rocktyping.md:342-348` ("Must fail today").
- **Failure:** foot project, user obeys the "m" label, enters 2438.4 for an 8000-ft FWL → h < 0 →
  **SWT = 1.0 over the entire reservoir**, silently.
- **Verification:** not new — carried open. Three companion claims in that PRD section are now
  STALE and must not be quoted: the Skelt branch IS converted (`h_metres`), HAFWL IS metres and
  correctly labelled, and SB-SHR-T03/T04's assumed-unit premise no longer holds
  (`require_project_depth_unit` refuses). Only the FWL half survives.

### 15. Well Header hard-labels TD and KB as "(m)" regardless of the project's declared unit; `wells.kb` is subtracted from a project-unit TVD
- **Dimension:** F2 units | **Where:** `ribbon.ts:2230-2246`; consumed via `deviation.rs:42/:69`
  (`tvdss = tvd − datum_elevation`); `units.rs:62-65` documents metres, the DB columns carry no unit.
- **Failure:** foot project, KB 98.4 ft, user follows the label and enters 30 → every station's
  TVDSS is 68.4 ft too deep → FTEMP/FPRESS, `phimax.TVDSS_REF`, and the FWL reference all move.
- **Verification:** literal strings confirmed; no depth-unit lookup in that dialog; no DEC row.

### 16. `facies_tie::CORE_MATCH_TOL_M` — a metre constant against raw project-unit depths, and the "within 1 m" claim is printed to the user
- **Dimension:** F2 units | **Where:** `facies_tie.rs:283/:295`, claim emitted at :114-119
  (`core_match_note`). The source of the `sandimin.rs:1303` copy (finding 33).
- **Failure:** foot project → the window is really 0.305 m; plugs 0.4–1.0 m away are dropped into
  `n_core_unmatched` and `k_var_reduction` is computed on a silently reduced plug set while the
  result string asserts a 1 m window that was never applied.
- **Verification:** `facies_tie` is not a module, so the Declared/Independent depth-unit gate cannot
  reach it; no `ctx.depth_unit` at the call site.

### 17. `DEFAULT_DEPTH_TOL = 0.15` (a six-inch, i.e. metre, derivation) compared against project-unit depths; the UI fields carry no unit
- **Dimension:** F2 units | **Where:** `plugqc.rs:49-51/:446/:585`; same default in `lrlc.rs:946-959`
  → the S-factor CEC fit (a calibration input); `plugQcPanel.ts:143-154`, `sFactorFitDialog.ts:181-185`.
- **Failure:** foot project → the pairing window is 1.8 inches, 3.28× tighter than intended; plugs
  drop into `excluded` and the refusal prints the tolerance unitless.
- **Verification:** `record_calibration.md` documents the derivation but no unit contract;
  user-editable (severity cap) but the field gives nothing to convert from.

### 18. `rw_args()` declares FTEMP as a plain `log_in` — a raw imported FTEMP masquerades into `sw_arch`/`sw_indo`/`sw_sim`, the exact case the `computed_only` contract exists to stop
- **Dimension:** F2/F4 conventions | **Where:** `modules.rs:6635-6641` vs the contract at :570-574;
  honoured by `gascorr` (:6085/:6091, strips and replaces) and `nphi_env_corr` (:6403).
- **Failure:** the degF→degC rule converts only curves labelled `degF`/`DEGF`; `"F"`, `"DEG F"`,
  `"deg.F"` and a missing unit all pass through silently (only DRHO demands a declaration). FTEMP
  200 °F read as °C: Rw 1.93× too low → **Sw ~1.39× too low — hydrocarbon overstated** — in the
  three most-used saturation modules.
- **Verification:** contract text, gascorr's strip, and the registry token table all checked; no DEC
  covers the asymmetry.

### 19. %-delivered PHIE/VSH are stored unconverted (warned at import) and NOTHING consults the stored unit at consumption
- **Dimension:** F2 units | **Where:** `unit_registry.rs:45` (POR/VSH/VCL outside
  CONVERTIBLE_FAMILIES; the %→v/v rules name NPHI only); `fetch_generic_curve_aligned`
  (`equations.rs:891-917`) never reads `curve_meta.unit`.
- **Failure:** a returned interpretation set carrying `PHIE .%` 30.0 / `VSH .%` 45.0 is read by
  `sw_arch`/`sw_indo` as 30.0/45.0 **v/v**. The import warning is by design (SB-DIO-025 pins the
  honest coverage report) — the surviving gap is the absent consumption gate: warned-then-consumed
  is not refused.
- **Verification:** narrowed by the reviewer against the pinned design; confirmed the read path
  ignores the unit column.

### 20. `draw_style:"step"` holds one sample interval INTO a NaN gap in print but not on screen
- **Dimension:** F5 agreement | **Where:** `LogCanvasRenderer.ts:347-360` (+ edge fill :398-409) vs
  `composite.rs:1100-1104/:1139-1157/:1170-1188` (`hold_to` reads `depth[i+1]`, never `vals[i+1]`).
- **Failure:** blocked VSH 0.35 at 1050 m, NaN at 1051 (washout): screen ends the block at 1050,
  print holds it to 1051 — a metre of bed the screen never showed, at the start of every gap; a
  zone-constant curve overhangs a whole interval. Note the stated contract ("holds each sample's
  value down to the next sample's depth") matches the PRINT — the screen is the deviant side; either
  way the pair disagrees.

### 21. Non-positive values on a log-scaled track: the screen clamps to the left edge, the print breaks the run
- **Dimension:** F5 agreement | **Where:** `LogCanvasRenderer.ts:332-334`
  (`Math.log10(Math.max(v,1e-6))`, verified directly) vs `composite.rs:178-201` (`value_frac` →
  `None` for v ≤ 0), consumed :1096/:1213.
- **Failure:** PERM written as exactly 0.0 over a tight streak: screen shows a continuous dip to the
  edge (and crossover SHADES the interval); print shows a gap (unshaded). Worse: a log track whose
  min is set to 0 (no dialog forbids it) renders on screen against a substituted decade and prints
  NOTHING for the whole track. The array-log path proves the house convention is `value_frac` —
  the curve renderer is the odd one out.

### 22. `show_samples` is honoured on screen and ignored in print
- **Dimension:** F5 agreement | **Where:** `logViewPanel.ts:1569-1576` + `layoutPropsDialog.ts:595`
  vs `composite.rs:1534-1681` (no reference anywhere); field `layout.rs:313-315`.
- **Failure:** the plug ticks behind a sparse cored interval's box — "seeing the plugs behind the
  summary is often the point", per the field's own doc — are silently dropped from the deliverable.

### 23. A `fill:"blocks"` track prints a numeric min/max scale the track does not use
- **Dimension:** F5 agreement | **Where:** `viewerChrome.ts:118-135` ("class blocks", no editable
  scale) vs `composite.rs:1969-1989` (`draw_track_header` unconditionally prints
  `fmt_num(min)/fmt_num(max)` of `curves.first()`, verified directly).
- **Failure:** a FACIES track prints `0 … 8` as if a value axis under categorical colour blocks —
  and a track whose FIRST curve is the blocks curve prints those meaningless limits INSTEAD of a
  real curve's. CLAUDE.md states the contract explicitly; no print-side exemption recorded.

### 24. A plate near a page break is dropped from the print entirely — never carried to the next page
- **Dimension:** F5 agreement | **Where:** `composite.rs:1432-1436` (pre-filter
  `depth_base.unwrap_or(depth_top) < page_top → continue`, verified directly): an anchored plate
  reaches only the page containing its own depth; if its box does not fit that page's grid it
  degrades to a tick and no later page ever considers it.
- **Failure:** a thin section at 30 mm track width, size 0.9 → box ±13.5 mm = ±2.7 m at 1:200: any
  plate within ~2.7 m of a page bottom prints as a tick on every page — ~5% of a petrography
  delivery silently lost from the PDF while the screen shows it in full. All six image tests are
  single-page. The in-code comment justifies not CLIPPING at the break (sound); nothing justifies
  not carrying to the next page.

### 25. The non-MD-datum refusal is applied on one side of each pair only — in OPPOSITE directions for core vs images
- **Dimension:** F5 agreement | **Where:** core: `db.rs:4216` refuses on the PRINT path (swallowed
  by `unwrap_or_default` in `composite.rs`) while `equations.rs:307-313` (screen) has no guard.
  Images: `db.rs:3345` refuses on the SCREEN path (swallowed by `.catch` in `logViewPanel.ts:989`)
  while `read_images_for_print` (`db.rs:3508`) has no guard. All four sites verified directly.
- **Failure:** a non-MD core set draws on screen and prints an EMPTY track; a non-MD image set shows
  NOTHING on screen and prints every plate. Both directions silent (both call sites discard the
  error). The set-resolution SQL is identical on both sides and `list_aux_data` is consistent —
  which is what shows these two are oversights, not policy.

### 26. The screen's FACIES block track is built from min/max-DECIMATED data; the print is not
- **Dimension:** F5 agreement (input divergence) | **Where:** `logViewPanel.ts:722` →
  `equations.rs:132-156` → `decimate.rs:5-78` vs the print's undecimated `fetch_track_frames`. The
  two block builders agree algorithm-for-algorithm; the INPUTS differ (the backend cannot see the
  style).
- **Failure:** 12,000 samples on a 900-px canvas → ~13 samples/bucket, of which only the min and max
  CLASS survive: one facies-7 sample paints ~2 m of facies 7; runs of the dominant class shred into
  alternating pairs. The print, at full resolution, is correct — so the facies column QC'd at
  whole-well zoom is not the column that ships. "Never averages" holds to the letter and does not
  deliver its purpose for a class curve (a class index has no envelope). Zooming past 2× pixel
  density converges the two.

## P3

### 27. `vsh_gr` ships Larionov in the rounded decimal form (0.33/0.083) that SB-CLY-004 explicitly mandates be replaced by the exact normalised form — the "worst category" (textbook form where the docs mandate a divergence), deferred outside pilot scope
- **Where:** `modules.rs:3758-3759`; `10_clay-volume.md:854` (SB-CLY-004, P1, row PRESENT-DIVERGENT).
  At IGR = 1: LARINOV1 → 0.99000, LARINOV2 → 0.99567 vs 1.00000, while every sibling transform
  closes at exactly 1 — the shale endpoint depends on the dropdown pick. The rounded coefficients
  ARE published Larionov 1969 and are triple-corroborated in-tree; no DEC overrides SB-CLY-004;
  the row is deferred out of the pilot manifest, which is why this is not rated higher. (Rated P2
  by the reviewer; carried at the reviewer's severity in the triage table below as P2.)

### 28. Every Wyllie-Rose/Coates permeability coefficient is a bare literal — arithmetic verified CORRECT, but no citation exists in the distributed tree and the coefficients never reach `params_json`
- **Where:** `modules.rs:7161-7166/:7150/:7195`; already registered as "the sharpest provenance
  issue here" in `docs/generated/source-inventories.md:136` (the only attribution is in gitignored
  research notes). Known-open, unruled (no DEC covers `perm_*`); a saved run cannot say which
  C/D/E produced its PERM curve. (Reviewer-rated P2; provenance, not arithmetic.)

### 29. `perm_coates` accepts a negative PHIE (even power hides the sign) where its sibling screens it — reachable only via the declared-unlimited `PHIE_DEN`/`PHIE_DN` inputs. `modules.rs:7221` vs :7175.

### 30. The two FTEMP writers disagree by 58% on gradient (0.03 °C/m vs 0.026 °F/ft = 0.0474 °C/m; 33 °C apart at 2000 m → ~25% in Rw) — **both values are ruled and owner-attributed (DEC-077)**; the conflict between them was flagged by DEC-057, dissolved, restored by DEC-060's reversal, and re-attributed without re-examination. Reported as a cited conflict, not adjudicated. `modules.rs:5060-5068` vs :5159-5162.

### 31. `VSH_GR` is labelled "unlimited" but the IGR is pre-clamped on 4 of 8 branches (STIEBER pole, CLAVIER radicand — defensible clamps, load-bearing label; the generic Clamped degradation names neither curve nor branch). `modules.rs:3704` vs :3747-3762.

### 32. The P_LOW/P_HIGH normalization defaults cite `memory/method_workflow_standards.md`, a path that does not exist in the repository, at four sites (`modules.rs:7524/:7534`, `condition.rs:1280/:1287`) — and the PRD chapter defers to the same missing path. The real home is `docs/workflow_standards.md:17-20`. Fix is a path correction.

### 33. `sandimin::CORE_MATCH_TOL_M = 1.0` — the metre-constant-vs-project-unit family again (copy of finding 16's convention; quieter, statistic-only). `sandimin.rs:1303`.

### 34. `alpha_expansion` caps the dual-water α at 5.0 — an uncited ceiling over the spec's uncapped √(20455/n); binds below 818 ppm NaCl (the fresh-water regime SSC exists for); self-consistent across both uses. `sandimin.rs:891-897` vs `multimin_ref_spec.md:60`.

### 35. Two doc/comment drifts around the sspw 1.6 gas weight (the numeric divergence itself is REGISTERED — DEC-076 open, pinned by name): `ssc.rs:488`'s comment asserts the opposite of the code below it, and `method_ssc_sspw.md:15-17` still states the 1.6 form for the SSC half — which the docs-win rule would direct a future agent to revert. Quantified for DEC-076's benefit: gas sand RHOB 2.20/NPHI 0.10 → ssc PHIT 0.2054 vs sspw 0.1512 (36%).

### 36. `write_computed_curves_with_ancestry_clearing` DELETEs only `clear_names`, not the curves it writes — LATENT: today's sole caller covers its outputs by construction; a second caller with a narrower clear list would silently double samples in the PK-less table, and the doc comment ("also retires") asserts the opposite. `equations.rs:2713-2775`.

### 37. SandiMin's `_SWT`/`_SXOT` are clipped-only with no diagnostic twin, and the SB-SAT-025 sweep structurally cannot see it (SandiMin is not a registered module; the sweep's guard is a count). `sandimin.rs:1844-1906/:2061/:2069`; sweep `modules.rs:12194-12220`.

### 38. The pay path floors at the compile-time `PHIE_FLOOR` while the modules floor at the per-run parameter (DEC-067) — an override to 0.0001 writes a curve the pay summary silently re-floors to 0.001. `workflow.rs:4404` vs `ctx.p("PHIE_FLOOR")` in the porosity modules.

### 39. The persisted realization matrix stores the FIRST `realization_cap` snapshots while the percentile curves use ALL — above the cap the band and the percentile curves in the same track disagree, and a depth can get curves but no matrix row. The prefix is an unbiased subsample (verified — Fisher-Yates per column), so this is a population mismatch, not a bias. Pin test runs 64 < cap, exactly the regime where the claim holds. `montecarlo.rs:1221/:1230` vs :1226/:1240 (verified directly).

### 40. Default box-plot bin: zoom-relative on screen ((visible span)/20) vs page-relative in print ((page span)/20) — different boxes for the same track when `bin` is unset. `logViewPanel.ts:1489` vs `composite.rs:1584`.

### 41. One sample interval of curve and crossover is lost at every page break (an interval straddling the boundary satisfies neither page's test) — a hairline break at ordinary sampling, a 5 mm white stripe on a 1 m blocked curve. `composite.rs:1141-1144/:1172-1175/:1224-1226`.

### 42. A non-JPEG plate is refused in the SVG export too, though `svg_page` embeds any MIME as a data URI and CLAUDE.md scopes the named frame to the PDF. `composite.rs:~317` gates both serializers; the SVG test only ever uses a JPEG.

### 43. Block-builder micro-divergences: JS `Math.round` (half-up) vs `f32::round` (half-away) — class exactly −0.5 renders facies 0 on screen, reject-grey in print; and a single-sample class curve draws on screen, nothing in print (`n > 1` guard). Degenerate data required. `LogCanvasRenderer.ts:469/:475` vs `composite.rs:1932/:1943`.

### 44. Duplicated cores with no drift today: `ssc::vsh_from_gr` is a verbatim second copy of the `vsh_gr` ladder (equality PINNED, so drift would be caught); `montecarlo::percentile` duplicates `distribution::percentile` (the MC copy does not clamp p — safe today, every caller clamps). Consolidation candidates, not defects.

### 45. The SB-ENV-057 ambiguous-union gate is a two-string denylist ("m|ft"/"ft|m") — "psi/ft|m", "deg/ft|m", "degF|degC" ship past it, and the inventory test is structurally blind to an omitted parameter (`sw_height.FWL` is exactly the omitted tenth). The precalc gradient BEHAVIOUR is ruled (DEC-077) and not reported — only the gate's efficacy is. `modules.rs:2935-2957/:10657-10736`.

### 46. Core-table import assumes the project unit when no depth unit is confirmed (documented default, probe→confirm surfaces the guess) where the LAS path refuses — a doctrine tension, low. `ingest.rs:1935`.

### 47. `percent_to_fraction` decides percent-vs-fraction by a magnitude heuristic (median > 1.5) while the parsed units row sits one field away unread — the false-negative direction is live (a 1.2% tight-formation CPOR median stays 1.2 "v/v"). `parsers.rs:2277-2291`; probe shows the guess pre-commit.

### 48. `UNIT_RULES` carries a rule whose `families` names a nonexistent family ("QV") and the generator never validates that field (it validates tokens and `convertible_families` but not `rule.families`) — inert today, and the class is live: a typo'd family would silently stop a real conversion and boot clean. `unit_registry.rs:178-186`; `tools/unit-registry.mjs:100-114`.

## PLAUSIBLE (not verified — kept separate per the audit brief)

- **`solve_bounded_lsq` returns `Some(v)` on iteration-budget exhaustion or a singular sub-solve**
  (`sandimin.rs:2367-2369/:2441-2445`) — whether any QUALITY/CONDNUM/NFUN-style per-frame quality
  emission gates it (the reference spec's §H exists to answer exactly this) was not closed within
  the review budget. Worth a dedicated follow-up pass before the solver is trusted on
  ill-conditioned tool sets.

## Verified clean (Pass 1 — attacked, held; abbreviated)

vsh_gr LINEAR/STIEBER/CLAVIER closed forms · vsh_dn · phi_den (incl. PHIT_SH water density) ·
SMOOTH_ROLLOFF identical at all three sites (DEC-066) · phi_dnbk Bateman-Konen · sw_arch identities
and round-trips · sw_indo (exact, all three v-variants) · sw_sim's both typed identities and the
n=2 quadratic root · resolve_rw (Arps °C/°F mutually consistent; B-K/Kennedy branch continuous to
4 s.f. at 39,161 ppm) · all five perm closed forms (arithmetic) · gascorr (Standing, Papay,
real-gas density) · the three env corrections (DEC-077 attribution) · hfu.rs exact reciprocals ·
thomeer.rs (Swanson air pair, Hg-air invariance) · rocktyping.rs (Winland, GHE, PITTMAN_TABLE1
verified row-by-row against Pittman 1992 — all 14 rows) · thin_bed_ts · gr_normalize (no field
calibration leaks) · nphimat (DEC-025 refusal fails closed) · sandimin fluid physics (Arps, B-K
inverse, Cbw/Cw reproduce the spec's own worked endpoints, w = 0.75m+0.25n, U_ct/U_cxo, BNDWAT
reproduces Illite 0.1841) · the dual-water CT/CXO row and zone assignment · the bounded active-set
KKT system (incl. fixed-at-hi folding) · all seven SandiMin saturation closed forms rearrange to
the shared root, with bisection uniqueness proven for lin<0 · MC sampling (LHS genuinely
stratified, Iman-Conover proven, triangular inverse CDF exact) · MC volumetrics (HCPV identity
exact; tornado indexing) · sw_rtc exact vs spec · both calibrations are genuine algebraic inverses
of their modules per record_calibration · satheight Leverett/Skelt unit-consistent with the
psi/ft-vs-metres bug genuinely fixed on all paths · every unit-registry conversion factor exact ·
the affine temperature rule (sign and order, both paths) · module depth-unit classification
(Declared refuses) · condition/frame windows project-native by construction · depth_shift/splice
converted and ft/m-equivalence pinned · computed_curves write discipline (no upsert anywhere;
the ancestry scanner is real and every apparent violation chased was test code) · array_logs/
well_images/ml_models PK'd-store disciplines · RAW-priority ORDER BY unreordered (case-mismatch
refuted via canonical_set_name) · import null handling (declared NULL, NoNull, DEC-037 sentinel
detect-only) · FACIES_PALETTE byte-identical both sides · crossover semantics complete ·
makeSampler ≡ interpolate_display_series · fill:"none" genuinely fixed · point/array/image track
contracts agree both sides (geometry, skip-vs-clamp, anchor middle, per-depth peak opacity,
even-indices arithmetic) · distribution.ts ≡ distribution.rs function-by-function EXCEPT
bin_by_depth (finding 5).

---

# PASS 2 — STRUCTURE (thermo-nuclear review of the Gate-2 diff)

Scope: `ca4f8c92..460f229b` (209 files, +83,935/−12,090), the full Gate-2 program. Repo overrides
applied: the documented deliberate duplication pairs (FACIES_PALETTE, the composite↔logViewPanel
geometry pairs, distribution.rs↔.ts) were not raised as dedupe findings; `modules.rs`'s size is
noted once, not per finding. **Every restructuring below is a PROPOSAL requiring
behaviour-preserving proof before it may be applied — nothing here was changed.** Five findings
were spot-verified by the session model at the decisive lines (marked ✓); the rest carry the
reviewing agent's own documented refutation attempt. Severity capped at P2 per the audit brief;
the two carrying a correctness edge (findings 51, 58) say exactly what it is.

## P2

### 49. `workflow.rs` is two unrelated subsystems in one 15,732-line file, with a provably clean seam
- **Where:** whole file; split point exact at :3370 (runner above, cutoff/pay-summary/partition/
  sweep domain below). Coupling checked in both directions by the reviewer: **zero** — no symbol
  from either half is referenced by the other.
- **Cost:** one `git diff --stat` line for changes that never touch each other; every `cargo check`
  recompiles both; a reader looking for the cutoff ladder scrolls 3,300 lines of runner first.
- **Proposal:** move :3370–5394 verbatim into `paysummary.rs` with its tests; re-export from
  `workflow` where callers would churn. Proof: tests move with subjects, mutation pins re-run
  unchanged.

### 50. The generic module runner doubled to 940 lines and now contains four module names — one of them 130 lines of `vsh_gr` physics
- **Where:** `workflow.rs:2430-3369`; module literals at :2669 (`log_predict`), :2697/:3177
  (`nphimat`), :2874 (`vsh_gr`, ~130 lines of zone-grouped provenance), plus :894/:830. Baseline
  had exactly one (`sw_height`). The inner `compute` closure returns a SEVEN-element positional
  tuple destructured twice more (:3051/:3060) — three restatements aligned by position only.
- **Cost:** each special case makes the path all 47+ modules run through harder to reason about,
  and the pattern is self-reinforcing. The DEC citations (DEC-033/025/036) justify the RULES, not
  their location — `retired_module()`'s own comment defends a name list for a one-line message,
  which does not extend to 130 lines of physics.
- **Proposal:** (a) name the tuple (`struct ComputedRun`); (b) push the four cases behind manifest
  declarations (`mask_exempt_when`, a basis-injection flag, a post-run hook the module supplies) so
  the runner has zero module literals again.

### 51. The Indonesia equation is implemented twice — and the guards diverge exactly where the agreement pin cannot see ✓
- **Where:** `modules.rs:6923` (`sw_indo`, expanded form) vs `sandimin.rs:345` (`sw_indonesia`,
  factored, canonical — consumed by `resultsqc.rs:18` and `sandimin.rs:1792`). The contrast:
  `sw_sim` immediately below DELEGATES to sandimin (`modules.rs:7105-7108`, ✓ verified) — the
  branch established the right pattern and skipped one member of the family. Variant mapping is
  exact and total (SIMPLE→k=0, FULL→k=1, TAR_SAND→k=2).
- **Correctness edge:** sandimin clamps to [0,1] and floors `a` at 1e-9; modules does neither,
  instead special-casing `phie < 0.005 → Sw=1` and a `vsh ≥ 1` degradation the solver path lacks —
  so the same well read through module vs solver differs in the low-porosity and Sw>1 populations,
  the ones a marginal-pay result turns on. The pin
  `every_model_shared_by_the_module_and_solver_engines_returns_one_number_for_one_typed_fixture`
  (`modules.rs:13130`) holds the EQUATION at one fixture per variant; the guards are outside it —
  a pin that keeps two implementations agreeing is the tell that there should be one.
- **Proposal:** delegate like `sw_sim`, keeping the module-side guards in place — with the one
  honest caveat the reviewer states: a straight delegation moves `SWE_INDO` in the Sw>1 region
  (sandimin clamps, the module emits raw). Whether the clamp is correct for both is **Jauhar's
  decision, not a refactor's** — not proposed silently.

### 52. One referential-integrity class is defined in four places in four shapes, on a deliberately PK-less table
- **Where:** `db.rs` — `check_referential_integrity` :5371, `prune…` :5422, `restore…` :5541,
  `reapply…` :5622. Seven hand-written SQL statements per prunable class across four functions,
  with the identity key hand-transcribed four times and no DB constraint to catch a slip; a slip
  means a restore that silently duplicates rows or a reapply that deletes rows it never
  quarantined. Already leaking: :5563 reports "identity collision in restore class {index+1}" —
  positional into a local array, misattributes on reorder.
- **Proposal:** one `IntegrityClass` descriptor table, four thin loops carrying the class ID into
  every message; the two report-only classes keep bespoke detectors via `Option<fn>`. SB-DBM-027's
  every-class-emitted requirement is satisfied BETTER by a table (`checked_class_count = len()`).

### 53. `equations.rs` absorbed the entire ancestry/log-set-versioning subsystem (2,246 → 6,180 lines)
- **Where:** ~:1039–3660 — the custody/ancestry model plus the set lifecycle — unrelated to the
  file's original job (fetch, resolution, catalog, run_equation). The two concerns touch at ONE
  seam: the write path takes a `CompleteLogSetSpec` (a value type). The highest-traffic question
  in the repo ("how does a mnemonic resolve across sets?") now sits behind 2,600 lines of custody
  model.
- **Proposal:** `ancestry.rs` (custody/manifest model, a leaf equations.rs imports) +
  `logset.rs` (set lifecycle + write discipline + validators); extraction is a `use` line.

### 54. `complete_module_log_spec`: eight copies of one reserved-key guard — two check `legacy`, six don't, and whether that is a decision or an omission is unreadable
- **Where:** `workflow.rs:847-1331` (485 lines); guards at :876/:900/:926/:948/:969/:1109/:1135/
  :1311; ten hand-built `AncestryParameter` pushes. Messages differ only in the adjective.
- **Proposal:** one `push_reserved(parameters, legacy, module, kind, name, value, source)` helper;
  the guard variant becomes a visible argument, the SB-ENV-041/030/CLY-043 citations ride the call
  sites. ~120 lines go.

### 55. The cutoff model is stringly-typed with a silent `_ => perm` fallback — and permeability is the one slot designed to FAIL when absent ✓
- **Where:** `TierCutoffs::applied` `workflow.rs:4226-4233` (✓ fallback verified verbatim);
  `classify_sample` re-derives the quantity from the same string at :4886; averaged slots read
  positionally as `avg[0..2]` at :4816-4818. Meanwhile the same 500 lines define five proper enums
  (`CutoffQuantity`, `BoundedQuantity`, `ClampStage`, `CutoffSense`, `SweepProp`).
- **Cost:** a typo'd or future fifth slot silently receives the PERM cutoff — the branch with
  teeth, per "a requested permeability cutoff is always active". The requirement text quoted at
  :3697 itself says bounds attach "to the QUANTITY, never to a curve-type string".
- **Proposal:** `enum Slot { Vsh, Phie, Swe, Perm }` + `enum Tier`; parse strings once at the IPC
  boundary (`Slot::from_wire`); `applied`'s match becomes exhaustive — the fallback **cannot be
  written**.

### 56. Ten new `*_unnamed` parser wrapper pairs — and 12 of 23 public parsers still lose the filename
- **Where:** `parsers.rs` pairs at :571, :2208, :2301, :2386, :2551, :2654, :2759, :3218, :3378,
  :3728, :3869 (all new). Unnamed: the `parse_las_2*` family, `parse_las_directory`,
  `parse_locations_file`, `parse_interval_file` — a 40-file directory import yields 40 identical
  anonymous refusals.
- **Proposal:** delete the wrappers; one `in_file(path, |path| …)` combinator at each public
  boundary — naming becomes structural, and the same edit fixes the twelve. `ParseError::named` is
  already idempotent (:31), so nesting is safe.

### 57. `reloadContext` is triplicated across histogram/pickett/crossplot, and the branch added four concerns to all three copies instead of extracting it
- **Where:** `histogramPanel.ts:939`, `pickettPanel.ts:887`, `crossplotPanel.ts:2418` — ~62 lines
  each, near-verbatim including a word-for-word copied doc comment. The branch added scope
  resolution, generation tokens with three staleness checks, depth-handoff merging, and
  `ctxWellIds` — ×3. Only the panel's curve list and layer projection genuinely differ.
- **Proposal:** `createContextLoader` in `plotCommon.ts` (which already owns every primitive used);
  panels supply `curves` and `project`. The deliberate-pair override covers cross-language pairs
  only — this is three TS files in one directory already importing a shared module.

### 58. The shale/clay quantity contract is a second manifest keyed by module name — and an UNLISTED argument is silently never validated ✓
- **Where:** `apply_shale_clay_quantity_contracts` `modules.rs:1695-1755` (+
  `apply_porosity_contracts` :2184), post-mutating specs built 4,000 lines away.
  `argument_mut` catches listed-but-gone; NOTHING catches exists-but-unlisted — and
  `workflow.rs:2003` (✓ verified) filters validation on `!accepted_shale_clay_quantities.is_empty()`,
  so a forgotten module's VSH slot accepts a VCL curve with no check: the silent-wrongness class
  by name.
- **Latent, not present:** the reviewer verified today's coverage complete (all nine VSH-declaring
  modules are in the table) — which is why this holds at P2.
- **Proposal:** cheap — a build-time completeness panic (every VSH/VCLAY `LogIn` must carry a
  non-empty contract); better — move the declaration onto the argument builder and delete the
  table. SB-CLY-043's "quantity inventory" is legitimate, but an inventory that is authoritative
  must REJECT a module missing from it, and this one cannot.

### 59. `check.ps1` stage 1: twelve copy-pasted gate blocks that all fail as "takeover ledger" ✓
- **Where:** `tools/check.ps1:52-103` (✓ verified verbatim). A failing `check:unit-registry` or
  `test:chart-derivation` prints `GATE FAILED at takeover ledger` — a refusal that misnames what
  refused, on the project's own green gate. The repo's own `needWell.ts` principle applied to its
  own tooling.
- **Proposal:** one gate list + one loop that fails with the gate's own name. Identical order,
  short-circuit, and exit code.

## P3

### 60. A test-only serializer seam threaded through two production signatures — `workflow.rs:2430`/`:856` `parameter_serializer: &impl Fn(…)`, whose only non-default caller is the test at :5921. Test the failure at `write_run_parameters` where it is produced, or use a `#[cfg(test)]` switch like the file's own `recorded_module_params`.

### 61. `present_average` is provably the identity function ✓ — `workflow.rs:3763-3770` (✓ verified): out-of-range values are returned unchanged by policy, in-range values are unchanged by arithmetic, NaN passes through. The doc says "inert on every ordinary run"; it is inert on every run, and the phrasing invites a reader to believe otherwise. Delete it, keep the (good) SB-CUT-030 reasoning as a comment at the call sites; the requirement is satisfied more directly without the no-op.

### 62. Thirteen hand-written catalog registration gates that cannot say which one failed — `modules.rs:2407-2435`, consecutive `validate_x(&modules).unwrap_or_else(|e| panic!("{e}"))` lines; several messages name themselves, not all, and nothing states which of thirteen was reached. One named `CATALOG_GATES` table + loop panicking with `"{name}: {error}"`; the two `apply_*` mutators stay above the loop (ordering is load-bearing).

## Size note (stated once, per the repo override)

Five more files crossed 1k lines under the branch: `param_sources.rs` 1,830 (a cited constant
registry — the right shape for its job, not raised), `chain.rs` 1,559, `curve_edit.rs` 1,703,
`parameter_pack.rs` 1,052, `curves.rs` 1,120 (roughly half tests each — not raised). The three
file-size findings above (49, 53, 52's host) are raised on PROVEN seams, not on line count.

## Verified clean (Pass 2)

`export.rs`'s `RegisteredWriter` dispatcher + shared `gather_well_frame` (and `export_delimited_set`
NOT reusing it is correct — set-scoped vs well-scoped) · `plotAsync.ts`/`viewportRefetch.ts` compose
rather than duplicate the staleness tokens, and `commitPlotAsyncGeneration`'s
dispose-stale-before-apply ordering is a genuinely good API · the new leaf modules `well_scope.rs`,
`schema_vocab.rs`, `dlis_writer.rs`, `biff5.rs`, `robust.rs` are properly sized single-purpose
extractions — the branch demonstrably CAN decompose · `sw_sim`'s delegation to sandimin is the
model finding 51 asks be completed · `stage_value`/`BoundedQuantity` is the typed model finding 55
asks be extended · `workspace.ts`'s `SessionSnapshot` v1|2 bump and the frozen `"multimin"`
component id are handled correctly.

---

# PASS 3 — SLOP (deslop rubric, read-only)

The deslop checklist applied to the same diff as an audit — nothing edited. Both repo exclusions
were enforced throughout: DEC-citation/source-attribution/why comments are the provenance register
(the reviewer sampled the 6,882 added `//` and 3,671 added `///` lines and reports the comment
QUALITY genuinely high — every comment finding below is misplacement or staleness, never removable
provenance), and refusal paths/validity guards are the product contract (every finding that touches
one flags the duplication or the stale text around it, never the guard). Two findings carry P1
under the hides-a-correctness-defect exception; both were re-verified by the session model at the
decisive lines (✓). Two of the agent's findings merged into Pass 2 (its check.ps1 finding
independently confirms **59**; its reserved-key-guard finding confirms **54**, whose new
OUT_PREFIX_OPT half is kept as **74**).

**One conflict between passes, reported not adjudicated:** Pass 3 discarded the `parse_X`/
`parse_X_unnamed` wrapper-pair SHAPE as deliberate design citing SB-DIO-061, while Pass 2's finding
**56** proposes deleting the wrappers. The 12-unnamed-parsers hole survives under either reading;
whether the pair shape itself is ruled needs the SB-DIO-061 text checked at triage.

## P1 (slop hiding a correctness defect)

### 63. A dead `??` fallback in `auditedFamilyDisplayDecision` silently disables the shipped PHIE unit-limit row — every porosity axis degrades to autoscale ✓
- **Rubric:** 5 (dead sub-expression) | **Where:** `src/ui/axisRange.ts:325`; the unreachable row
  :174-178; refusal branch :332-343.
- **Defect:** ✓ verified — `unit-registry.json:35` lists PHIE as an exact alias of family **POR**,
  so `registeredFamily("PHIE")` returns `"POR"` and the `?? (mnemonic === "PHIE" ? "PHIE" : null)`
  arm can never run; the lookup at :330 then finds no `familyDefault` row with `family === "POR"`
  (the row is keyed `"PHIE"`) and returns `enabled: false`. The reviewer additionally EXECUTED the
  real module: `PHIE v/v → {family:"POR", enabled:false, reason:"disabled: no audited unit-limit
  row for POR v/v"}`, same for PHIT; every other family activates. Live consumers:
  `pickettPanel.ts:349`, `crossplotPanel.ts:1130`, `histogramPanel.ts:299/:349`,
  `correlationPanel.ts:394` — the `audited_family_display` tier silently falls back to
  `finite_data` autoscale on every porosity axis.
- **Refutation attempted:** `tools/frontend-acceptance.test.mjs:862-868` audits every row and the
  PHIE ROW passes — the test never calls the decision function with a PHIE mnemonic, so it pins the
  row while the reachability gap goes untested.

### 64. `param_sources` re-enumerates one topic vocabulary in three hand-maintained tables, already drifted — a fully SOURCED default produces no provenance disclosure at all ✓
- **Rubric:** 5 (parallel tables) | **Where:** `sources_for` :937, `parameter_label` :947-979,
  `topics()` :1309-1336, short-circuit ✓ verified at :1352 (`parameter: parameter_label(topic)?`).
- **Defect:** ✓ verified — `SHALE_REDUCTION_CLAMP` has exactly three hits in the file (const :102,
  cited source table :475, `sources_for` arm :937) and is absent from the other two tables; a
  `None` from `parameter_label` kills the entire `ParameterDecision`, so its six live call sites
  (`modules.rs:4307-4322/:4611/:4616`) disclose nothing — inverting the module's own gate-message
  rule at :1082 (an UNsourced default must be disclosed; here a SOURCED one is not).
  `IRREDUCIBLE_SWE/SWT`, `PERCENTILE_REFERENCE_LOW/HIGH` and `CONDITIONING_WINDOW` are likewise
  missing from `topics()`, whose only test (:1625) compares against a hardcoded copy of the same
  incomplete list — the test pins the omission rather than detecting it.

## P2

### 65. Orphaned doc comments — 13+ confirmed sites where a new item was inserted between a doc block and its owner, damaging the provenance register the exclusions exist to protect
- **Rubric:** 1 | The reviewer confirmed every site individually; a naive scanner finds only 3
  (the rest are unbroken `///` runs holding several stacked blocks). The worst: `db.rs:1629-1655`
  — a THREE-block stack on `migrate_run_degradations_endpoint_invalid`, including the R-B
  destructive-backup guarantee now sitting on a function that takes no `path`, while
  `migrate_drop_computed_curves_pk` (:1717), the actual destructive rebuild, is bare;
  `curves.rs:369-383` — `LOG_SCALE_FAMILIES` inserted MID-SENTENCE into `family_for`'s doc
  (line 369 ends "…if it isn't recognized (the"); `workflow.rs:1422-1428` — `run_workflow_module`'s
  doc AND its `#[allow(dead_code)]` both landed on `RerunReport` (the attribute is inert where it
  sits); `workflow.rs:485-501` — the "the ONE place a module's output name is decided" block on
  the wrong function; `modules.rs:357-364` (SB-CORE-007 doc on `DerivationClass` instead of
  `OUTPUT_IDENTITY_REGISTRY` :482); `equations.rs:2029-2032`; bare owners confirmed at
  `export.rs:2118/:1871`, `parsers.rs:4124`, `ingest.rs:6699` (airtight — the orphan names its
  owner in its own run command). Mechanically detectable, mechanically fixable.

### 66. A comment inside the `skip_version` branch asserts "overwrite FLAG_* in place" directly above the `return Err` that refuses exactly that
- **Rubric:** 1 (contradicts code) | **Where:** `workflow.rs:4587`; matching stale field doc
  :3403-3407 (itself orphaned onto `weighting`). The refusal is pinned by the test at :5791
  ("skip_version must be an explicit refusal rather than a provenance switch") — both comments
  describe behaviour DELIBERATELY removed. The finding flags the stale text, never the refusal.

### 67. Thirteen production-shaped items in `plotting.rs` are gated `#[cfg(test)]` — structurally invisible to the repo's own hygiene gate, and two re-implement contracts the TS side implements for real
- **Rubric:** 5 | **Where:** `plotting.rs:67/:276/:572/:593/:600/:605/:701/:712/:1010/:1034/
  :1063/:1073/:1092`, several `pub` with Serialize/Deserialize payloads. A `cfg(test)` item emits
  no `dead_code` warning, so `tools/gate2-hygiene.mjs` — the mechanism for OWNING disconnected
  capability — cannot see them (they are not among plotting.rs's 21 inventoried entries).
  Aggravating: `resolve_axis_range`/`apply_range_policy` name SB-PLT-002/004, the same contracts
  `axisRange.ts`/`plotRangePolicy.ts` implement live — a second implementation of one contract
  that can never run, the keep-in-agreement hazard with one side permanently unrunnable. No DEC
  documents a `cfg(test)` posture; DEC-054's "no test-only stub" points the other way.

### 68. Twelve dead new TypeScript exports — seven of them IPC wrappers, i.e. backend commands unreachable from the app — and no gate can see them
- **Rubric:** 5 | `asCutoffEntry` (`cutoffs.ts:23`), `auditedFamilyDisplayRange`/`binding…Range`/
  `bindingForChannel` (`axisRange.ts:354/:375/:379`), and IPC wrappers `compareZoneTopToContact`,
  `confirmLogScaleZeros`, `exportDelimitedSet`, `getLogSetManifest`, `getParameterModuleSchema`,
  `importCoreCsv`, `importScalCsv`, `loadParameterPack` (`ipc.ts`). `noUnusedLocals` does not
  apply to exports; no knip/ts-prune; the acceptance suite references none. Unlike the Rust side —
  where every equivalent is inventoried with owner, gate and rationale — this surface passes every
  gate silently.

### 69. `ingest.rs` spells the ~20-field `ImportResult` early-return literal out at 14 non-test sites (6 inside `import_las` alone), differing only in the error string
- **Rubric:** 5 | :589/:629/:674/:707/:732/:778 +8 — all new in this range (baseline: zero). Rust's
  exhaustive literals cap this at P2 (a FORGOTTEN field cannot compile), but a field whose value
  should DIFFER per site is not caught, and the file already owns the precedent constructor
  (`cancelled_las_import`). The guards themselves all survive — the construction is the finding.

### 70. The `INSERT INTO log_sets` column list is re-typed four times and two of the four now diverge — one divergence is DELIBERATE and noted, one is unassessed
- **Rubric:** 5 | `equations.rs:2394/:3258/:3377/:4114`. The :3255 omission of
  `applied_steps_json` carries an explicit SB-ENV-005 note and was WITHDRAWN by the reviewer as
  deliberate; the `restore_log_set` copy at :4114 carries no note, and whether a restored set
  should carry steps was not established — reported as the risk the duplication created, not
  asserted as a bug.

### 71. `runCustody.ts` (new file): `requestRunCustody`'s doc block sits on `ensureSessionOperator` (the owner is bare), and a vestigial `let close = () => {}` immediately reassigned deviates from the `const close = openModal(…)` idiom at 20+ sites
- **Rubric:** 1 and 5 | :78-80 and :132-133. The stub is provably never captured before
  reassignment.

## P3

### 72. A mangled-formatting class with a ZERO baseline: 92 sites at master (lone `;`/`,` on their own line, stray space before punctuation), concentrated in `workflow.rs` (32), `ml.rs` (11), `montecarlo.rs`/`curve_edit.rs`/`coreimage.rs`/`composite.rs` (6 each). No `rustfmt.toml` exists and `check.ps1` runs no `cargo fmt --check`, so nothing catches the class; one `cargo fmt` clears it. (Adding the fmt check to the gate is the durable half.)

### 73. The same mangling reaches OPERATOR-FACING text: `workflow.rs:2081/:2088/:4419/:4461` and `param_sources.rs:1082/:1088` carry 14–18 consecutive spaces mid-sentence (a dropped line continuation) in degradation and gate messages a reviewer reads. No test pins the gap-spanning text (existing tests match short substrings).

### 74. The `OUT_PREFIX_OPT` trim/filter/uppercase/format rule is open-coded eight times in `workflow.rs` (:516/:565/:594/:620/:2840/:2856/:2880/:3022) against `class_output_names`' own stated intent ("reads them from the same two places rather than restating either"). (The reserved-key-guard half of this finding merged into 54.)

### 75. The SMOOTH_ROLLOFF ceiling + roll-off arithmetic + `PHIE_FLOOR` clamp + `record_bound_limit` block is verbatim-triplicated (comments included) across `phi_den`/`phi_dn`/`phi_dnbk` (`modules.rs:4157-4181/:4427-4451/:4707-4731`), and the four-member ENVCORR flag group is declared three times (:6300/:6407/:6518). Refutation checked: `validate_porosity_contracts`' three distinct `limiting_policy` identities are registry metadata strings, untouched by extracting the arithmetic — and the file already extracts exactly this kind of shared porosity math (`two_endpoint_fraction`, `shale_total_porosity`, `phit_sh_at`).

### 76. `chain.rs::complete_chain_sets` is 385 lines doing seven jobs, containing the quantity-contract check written twice (:267-306 vs :315-356 — same join, same contains, same SB-CLY-043 push, differing in one line and two error strings).

### 77. `equations.rs`: the eleven-line "validate ancestry → parse params_json → insert CURVE_ANCESTRY_KEY → re-stringify" tail is written out four times (:2049/:2076/:2097/:3340); the 22-line entry prelude of `write_computed_curves_with_ancestry` is copy-pasted character-identical (error text included) into the `_clearing` and `_batch` variants (:2685/:2720/:3634); :3694-3703 holds two byte-identical refusal `format!`s one `let Some((true, effective))` would cover. All refusals survive consolidation.

### 78. `curve_edit.rs` defines a private `timestamp_utc_ms` duplicating `equations::ancestry_timestamp_utc_ms` — `pub(crate)`, already imported at :17, and used for exactly this by `chain.rs`; the `top <= bottom` swap is restated three more times over the copy at :818.

### 79. `db.rs:11038` re-prepares an identical `audit_detail` statement INSIDE the per-entry loop (200× per panel open at the IPC default limit) and :5335 issues an unprepared `query_row` in a nested loop — the file states the opposite convention 6,000 lines earlier ("a per-row query is how a list turns into N round trips"). :10759 selects a literal `NULL`, decodes it with an explicit `Option<String>` turbofish, and discards it at :10775 — column, slot and type escape all vestigial.

### 80. `parsers.rs:272-278`: two consecutive `if utf16_heuristic` branches with byte-identical bodies, the first (`&& !utf8_valid`) wholly subsumed by the second; :237-256 a `loop` whose every arm breaks on the first pass. Proven dead by inspection — the audit's ONE use of the genuinely-unreachable carve-out to exclusion (b).

### 81. `sandimin.rs:299-306`: `solver_selectable_models()` re-encodes the selectability rule as string literals, duplicating `SwModel::solver_selectable()` (:175) which is what `run_sandimin` actually enforces — a twelfth model would ship offerable-but-refused; `sw_model_id_from_flag` (:311-313) has no production caller despite its own doc claiming callers must resolve through it.

### 82. `montecarlo.rs:1429-1444`: the custody refusal hand-writes all twelve `McResult` fields fifteen lines above a `McResult::default()` refusal — with `Default` added to `McResult` in this same diff; :1928-1948 applies its own stated ABSENT-token rule to `perm_min`/`realization_cap` but not to `vsh_max`/`phie_min`/`swe_max`/`ftemp`, which serialize as bare `null` in one `json!` literal.

### 83. `modules.rs:2252-2270`: `if registration.is_none() { continue } … .unwrap()` where the same function uses `let Some(…) else { continue }` sixteen and twenty-eight lines later; :2648/:2739 re-`use` an already-imported `BTreeMap`; :835-836 states one doc sentence twice verbatim.

### 84. `plotInvalidation.ts:90-100`: `queued` is written in lockstep with `current` and read once — provably redundant with `current`.

### 85. The new `applyPlotRecordLimit("context_well_legend_rows", …)`/`reducePlotLabel` logic was pasted verbatim into the three panel legend blocks (`crossplotPanel.ts:1343-1358`, `pickettPanel.ts:500-515`, `histogramPanel.ts:590-610`) — `plotLimits.ts:72` even names all three as consumers. Scope caveat stated honestly: the surrounding legend skeleton pre-dates the range; only the propagation is in scope. Same pattern smaller at `moduleDialog.ts:677/:744` (`nameFor` defined twice).

## Verified clean (Pass 3)

Rubric 3 (type escapes) essentially clean TREE-WIDE: zero `as any`, zero `@ts-ignore`/
`@ts-expect-error`, zero `eslint-disable` in 69,749 added lines; 8 new non-null assertions, all
guarded. Rubric 2 (abnormal defensiveness): nothing — no swallowed errors outside documented
no-ops; the `.catch(() => [])` list-fetch idiom is consistent local style. Comment quality
genuinely high — narration signatures essentially absent from a sample of 10,553 added comment
lines. `satheight.rs`/`ssc.rs`/`lrlc.rs` clean (three candidates each examined and refuted as
load-bearing or established idiom). `styles.css` clean (0 new `!important`, 0 duplicate
selectors). New files `plotAsync.ts`, `plotRangePolicy.ts`, `paperExport.ts`,
`chartProvenance.ts`, `chartOverlayPolicy.ts`, `viewportRefetch.ts`, `tools/gate2-*.mjs` — no
findings, every export wired, every constant cited. No TODO/FIXME/HACK or debug-print debris in
production paths. Discarded-with-reason (recorded so they are not re-flagged): the 31 inventoried
`dead_code` warnings (owned in `gate2-warning-inventory.json`); `robust.rs`'s const assert
(exclusion b); the parser wrapper pairs (SB-DIO-061 — the Pass-2 conflict noted above);
`depth_identity`'s `== 0.0` (normalises −0.0); `CurveAncestry::validate`'s length (all guards);
the MORRIS_BIGGS_OIL/TIXIER arms (two published methods, pinned); `composite.rs`'s twelve
`embed_*` wrappers (distinct external marker ids); `intake.rs` well-resolution and
svgExport/pdfExport `mul`/`esc` duplication (pre-date the range, out of scope);
`verification/unit-registry.json` (deliberate sha256-pinned evidence snapshot).

---

# MERGED TRIAGE (proposed ROADMAP §4c)

85 confirmed findings (1 P0 · 9 P1 · 36 P2 · 39 P3) + 1 plausible. Numbers reference the sections
above, where each finding carries its failure scenario, verification note and (for passes 2–3) its
proposal. **Nothing here was fixed; every parameter conflict is reported with both citations,
adjudicated by nobody.** Area key: PHY physics/transcription · UNIT units/conventions · LIM
limits/floors/clamp honesty · WD write/mask discipline · AGR renderer↔exporter agreement · STR
structure · SLOP slop/hygiene · PROV provenance/attribution.

| Pri | # | Area | Where | One line |
|---|---|---|---|---|
| P0 | 1 | LIM | modules.rs:4848 | phi_son Cp INFLATES porosity for DT_SH<100 — DEC-012 says refuse; a green test pins the wrong behaviour |
| P1 | 2 | PHY | modules.rs:4767 | "RHG" label on a transform that is not RHG — the exact state DEC-017 forbids |
| P1 | 3 | LIM | montecarlo.rs:1161 | MC bypasses the range guard → satheight clamp can ABORT the process mid-study |
| P1 | 4 | LIM | modules.rs:7032 | SWE_SIM "unlimited" twin is solver-capped at 1.0 — cosmetic, and the sweep can't see it |
| P1 | 5 | AGR | distribution.rs:323 | bin_by_depth f32 round-trip fragments printed depth bins; TS twin immune; 13% of realistic pairs disagree |
| P1 | 6 | UNIT | ingest.rs:1009 | standard six stored UNCONVERTED vs converted generic store — viewer/plots/editor vs modules read different domains |
| P1 | 7 | UNIT | export.rs:900 | LAS export: canonical unit labels over unconverted values — mislabelled deliverable |
| P1 | 8 | UNIT | ingest.rs:1500 | deviation MD imported with NO unit resolution — the only depth importer without one; TVD/FTEMP/Rw cascade |
| P1 | 63 | SLOP | axisRange.ts:325 | dead ?? fallback disables the PHIE unit-limit row — every porosity axis silently autoscales |
| P1 | 64 | PROV | param_sources.rs:937 | three drifted topic tables — SHALE_REDUCTION_CLAMP's sourced default yields no disclosure at all |
| P2 | 9 | PHY | lrlc.rs:60 | sw_imts Qv volume-fraction form vs spec's weight fractions — −24%→−4% bias flipping across φ |
| P2 | 10 | LIM | montecarlo.rs:776 | MC pay path never floors PHIE — vendor negatives subtract into P10/P50/P90 HPV |
| P2 | 11 | LIM | sandimin.rs:493 | sw_cond_root returns SWT=0.0 unflagged at n=1 when no root exists in [0,1] |
| P2 | 12 | WD | montecarlo.rs:1281 | MC carries a step's MASK and never reads it — 2026-07-20 finding #1 alive inside MC, pinned as-is |
| P2 | 13 | PHY | ssc.rs:312 | two capillary-water rules the banked spec doesn't carry; PHIFF=0 on tight clean rock |
| P2 | 14 | UNIT | satheight.rs:113 | FWL declared "m", consumed project-unit — SB-SHR-004, still open (3 companion PRD claims now stale) |
| P2 | 15 | UNIT | ribbon.ts:2230 | Well Header hard-labels TD/KB "(m)" regardless of project unit; KB subtracted from project-unit TVD |
| P2 | 16 | UNIT | facies_tie.rs:283 | CORE_MATCH_TOL_M metre constant vs project-unit depths — and the "within 1 m" claim is printed |
| P2 | 17 | UNIT | plugqc.rs:49 | DEFAULT_DEPTH_TOL 0.15 (metres) vs project-unit depths; also feeds the S-factor CEC fit |
| P2 | 18 | UNIT | modules.rs:6635 | FTEMP is plain log_in — raw imported curve masquerades into the computed_only contract; °F holes |
| P2 | 19 | UNIT | unit_registry.rs:45 | %-delivered PHIE/VSH warned at import, then consumed unconverted — no consumption gate |
| P2 | 20 | AGR | composite.rs:1139 | step curves hold one interval INTO a NaN gap in print, not on screen |
| P2 | 21 | AGR | composite.rs:178 | non-positive on log track: screen clamps to edge, print breaks the run; min=0 track prints NOTHING |
| P2 | 22 | AGR | composite.rs:1534 | show_samples honoured on screen, ignored in print |
| P2 | 23 | AGR | composite.rs:1969 | blocks track prints a numeric min/max scale it does not use |
| P2 | 24 | AGR | composite.rs:1434 | plate near a page break dropped from the PDF entirely — never carried to the next page |
| P2 | 25 | AGR | db.rs:4216/:3345 | non-MD datum refusal on one side of each pair, OPPOSITE directions for core vs images |
| P2 | 26 | AGR | logViewPanel.ts:722 | screen FACIES blocks built from decimated data — the QC'd column is not the shipped column |
| P2 | 27 | PHY | modules.rs:3758 | Larionov rounded 0.33/0.083 where SB-CLY-004 mandates the exact normalised form (deferred, unruled) |
| P2 | 28 | PROV | modules.rs:7161 | every perm coefficient a bare literal — correct arithmetic, no citation in the distributed tree |
| P2 | 49 | STR | workflow.rs | two zero-coupled subsystems in one 15,732-line file — proposal: paysummary.rs split |
| P2 | 50 | STR | workflow.rs:2430 | 940-line runner with four module names inside it (130 lines of vsh_gr physics) + a 7-tuple ×3 |
| P2 | 51 | STR | modules.rs:6923 | sw_indo duplicates sandimin::sw_indonesia; guards diverge outside the pin; delegation moves SWE_INDO in Sw>1 — Jauhar's call |
| P2 | 52 | STR | db.rs:5371 | one integrity class in four shapes on a PK-less table; positional "class {index+1}" misattribution |
| P2 | 53 | STR | equations.rs | ancestry/log-set subsystem (~2,600 lines) grafted onto the fetch/resolution file — one-seam extraction |
| P2 | 54 | STR | workflow.rs:847 | eight reserved-key guard copies (2 check legacy, 6 don't — unreadable which is intended) |
| P2 | 55 | STR | workflow.rs:4226 | stringly-typed cutoff slots with a silent `_ => perm` fallback — the fail-active slot |
| P2 | 56 | STR | parsers.rs | ten *_unnamed wrappers; 12 of 23 public parsers lose the filename (conflicts with SB-DIO-061 reading — check at triage) |
| P2 | 57 | STR | 3 plot panels | reloadContext triplicated; branch added four concerns ×3 — createContextLoader extraction |
| P2 | 58 | STR | modules.rs:1695 | shale/clay contract table: exists-but-unlisted arg is silently never validated (coverage complete today) |
| P2 | 59 | STR | check.ps1:52 | twelve gate blocks all fail as "takeover ledger" — misnamed refusal on the green gate (found by both passes) |
| P2 | 65 | SLOP | 13+ sites | orphaned doc comments — provenance blocks stacked on the wrong items (db.rs 3-block stack, curves.rs mid-sentence) |
| P2 | 66 | SLOP | workflow.rs:4587 | comment asserts overwrite-in-place directly above the refusal that forbids it |
| P2 | 67 | SLOP | plotting.rs | 13 cfg(test) production-shaped items invisible to gate2-hygiene; 2 shadow SB-PLT-002/004 unrunnably |
| P2 | 68 | SLOP | ipc.ts + axisRange.ts | twelve dead new TS exports (7 IPC wrappers) — no gate covers exports |
| P2 | 69 | SLOP | ingest.rs:589 | ~20-field ImportResult literal ×14 sites; constructor precedent exists |
| P2 | 70 | SLOP | equations.rs:2394 | INSERT INTO log_sets ×4, one divergence deliberate (noted), restore_log_set's unassessed |
| P2 | 71 | SLOP | runCustody.ts:78 | misplaced doc + vestigial let-close stub in a new file |
| P3 | 29 | LIM | modules.rs:7221 | perm_coates accepts negative PHIE (even power hides the sign) where its sibling screens it |
| P3 | 30 | PHY | modules.rs:5060 | two FTEMP writers 58% apart on gradient — BOTH ruled and owner-attributed (DEC-077); conflict reported, not adjudicated |
| P3 | 31 | LIM | modules.rs:3704 | VSH_GR "unlimited" label vs pre-clamped IGR on 4 of 8 branches |
| P3 | 32 | PROV | modules.rs:7524 | P_LOW/P_HIGH cites a nonexistent path at four sites — real home is docs/workflow_standards.md:17-20 |
| P3 | 33 | UNIT | sandimin.rs:1303 | CORE_MATCH_TOL_M copy of finding 16's convention (statistic-only) |
| P3 | 34 | LIM | sandimin.rs:891 | alpha_expansion caps α at 5.0 — uncited ceiling binding below 818 ppm |
| P3 | 35 | PROV | ssc.rs:488 | two doc/comment drifts around the DEC-076-registered sspw 1.6 divergence (36% PHIT gap quantified) |
| P3 | 36 | WD | equations.rs:2713 | ancestry-clearing writer DELETEs only clear_names — latent duplicate-sample hazard; doc asserts opposite |
| P3 | 37 | LIM | sandimin.rs:1844 | SandiMin _SWT/_SXOT clipped-only, no diagnostic twin, sweep structurally blind |
| P3 | 38 | LIM | workflow.rs:4404 | pay path floors at compile-time PHIE_FLOOR vs modules' per-run parameter (DEC-067) |
| P3 | 39 | AGR | montecarlo.rs:1221 | persisted matrix = first cap snapshots vs percentiles over ALL — population mismatch above the cap |
| P3 | 40 | AGR | logViewPanel.ts:1489 | default box bin zoom-relative on screen vs page-relative in print |
| P3 | 41 | AGR | composite.rs:1141 | one sample interval lost at every page break (curve + crossover) |
| P3 | 42 | AGR | composite.rs:317 | non-JPEG plate refused in SVG too, though svg_page embeds any MIME |
| P3 | 43 | AGR | composite.rs:1932 | block-builder micro-divergences (round-half rule; n==1 guard) — degenerate data only |
| P3 | 44 | STR | ssc.rs / montecarlo.rs | duplicated cores with no drift today (vsh ladder pinned; percentile clamp unneeded) — consolidation candidates |
| P3 | 45 | UNIT | modules.rs:2935 | SB-ENV-057 gate is a two-string denylist; inventory test blind to omitted params (FWL is the omitted tenth) |
| P3 | 46 | UNIT | ingest.rs:1935 | core import assumes project unit where LAS refuses — documented doctrine tension |
| P3 | 47 | UNIT | parsers.rs:2277 | percent_to_fraction magnitude heuristic while the parsed units row sits unread |
| P3 | 48 | UNIT | unit_registry.rs:178 | UNIT_RULES names a nonexistent family; generator never validates rule.families |
| P3 | 60 | STR | workflow.rs:2430 | test-only serializer seam in two production signatures |
| P3 | 61 | STR | workflow.rs:3763 | present_average is provably the identity function — delete, keep the reasoning |
| P3 | 62 | STR | modules.rs:2407 | thirteen registration gates that cannot say which failed |
| P3 | 72 | SLOP | 17 files | mangled-formatting class, 92 sites, zero baseline — one cargo fmt + a fmt --check gate |
| P3 | 73 | SLOP | workflow.rs:2081 | the mangling reaches operator-facing degradation/gate messages (14-18 spaces mid-sentence) |
| P3 | 74 | SLOP | workflow.rs:516 | OUT_PREFIX_OPT rule open-coded ×8 against its own stated one-place intent |
| P3 | 75 | SLOP | modules.rs:4157 | SMOOTH_ROLLOFF + floor block verbatim ×3; ENVCORR flag group ×3 |
| P3 | 76 | SLOP | chain.rs:133 | complete_chain_sets 385 lines / 7 jobs; quantity check written twice |
| P3 | 77 | SLOP | equations.rs:2049 | ancestry tail ×4; 22-line prelude ×3; twin refusal format!s |
| P3 | 78 | SLOP | curve_edit.rs:287 | private timestamp fn duplicating an in-scope pub(crate); swap logic ×4 |
| P3 | 79 | SLOP | db.rs:11038 | prepare-inside-loop (200×/panel) + unprepared nested query_row + vestigial NULL column |
| P3 | 80 | SLOP | parsers.rs:272 | dead encoding branches — the one genuine-unreachable carve-out use |
| P3 | 81 | SLOP | sandimin.rs:299 | selectability rule re-encoded as strings beside its enum method; orphan resolver fn |
| P3 | 82 | SLOP | montecarlo.rs:1429 | hand-written 12-field refusal beside ::default(); ABSENT-token rule applied to 2 of 6 params |
| P3 | 83 | SLOP | modules.rs:2252 | is_none/unwrap beside let-else idiom; duplicate imports; duplicated doc sentence |
| P3 | 84 | SLOP | plotInvalidation.ts:90 | queued provably redundant with current |
| P3 | 85 | SLOP | 3 plot panels | legend record-limit logic pasted ×3 (propagation only in scope); nameFor ×2 |
| — | PL-1 | LIM | sandimin.rs:2367 | PLAUSIBLE: solve_bounded_lsq returns Some(v) on budget exhaustion / singular sub-solve — quality gating unestablished; needs a dedicated pass |

## Triage notes for the program that follows

- **`docs/RELEASE.md` IS CITED 34 TIMES AND IS NOT ON MASTER — Jauhar's call.** Found by the
  sweep built in increment 60 after re-verifying finding 32 (already closed in PR #95: the four
  `memory/method_workflow_standards.md` citations now read `docs/workflow_standards.md`). The
  sweep meant to stop the NEXT dangling citation turned up a much bigger standing one.
  `RELEASE.md` is a real 225-line release policy (§1 the 1.0 bar, §2 versioning, §3.1 forward
  compatibility, §3.2 backward compatibility, §3.3 support matrix, §4 changelog, §5 release
  checklist, §6 support window). It was added on branch `docs/prd-and-security-hardening`
  (commit `18da8b0a`, 2026-07-29) together with `TARGET_ARCHITECTURE.md` and `V1_SCOPE.md`.
  **The pull request carrying them was MERGED, yet none of the three is on master** —
  `git log master -- docs/RELEASE.md` is empty, so master's history never touched the path.
  - **The substance is not lost.** `docs/PRD_v2/22_database-model.md` states the backup-then-
    migrate contract that "RELEASE §3.2" is cited for, including that a failed copy ABORTS
    (`:622`, `:2187`). What dangles is the LABEL — 12 citations of §3.2, 5 of §3.1, plus §3.3 and
    §5 — and one of them is the rule
    `every_migration_that_copies_the_project_first_documents_that_a_failed_copy_aborts` enforces
    on every destructive migration.
  - **Not fixed here, deliberately.** Re-pointing the citations means deciding which PRD_v2
    section replaces each RELEASE §, which is authoring a governance mapping; restoring a release
    policy to master is a content decision. Both are Jauhar's. The likely intent is that the three
    documents belong on master and were lost in the PRD_v2 restructure — recovering them is
    `git checkout origin/docs/prd-and-security-hardening -- docs/RELEASE.md` plus a read for three
    weeks of staleness, but that is his to run.
  - **Meanwhile the gap is owned rather than silent**:
    `every_document_this_code_cites_is_actually_in_the_tree` carries it as the one acknowledged
    exception, refuses a NEW dangling citation, and refuses the exception itself once the file
    arrives.
- **Finding 38 is a DOCTRINE question for Jauhar, not a defect I can settle.** DEC-067 ships
  `PHIE_FLOOR` as a cited default of 0.001 that the user MAY override (0.0001..0.01, the range
  spanning IP's competing F17 value). The porosity modules honour that override; the pay path's
  `workflow::floored_phie` applies the compile-time `modules::PHIE_FLOOR` instead, so a run
  overridden to 0.0001 writes a curve the pay summary silently re-floors to 0.001 - and the
  direction is OVERSTATING, which is the dangerous one for HPV. Two defensible readings:
  (a) the pay floor should follow the run's effective parameter, which IS recoverable - the
  ancestry record carries the effective `PHIE_FLOOR` including zone overrides; or (b) the pay
  floor is a SEPARATE non-physicality guard, not the module's floor at all - which is what its
  own doc argues, since its motivating case is a vendor PHIE arriving by LAS that never passed
  through a porosity module and therefore has no run parameter to read. Reading (a) needs a
  fallback to the constant exactly where the motivating case lives, so it is not a one-line
  change either way. Flagged with both readings rather than adjudicated, same treatment as
  finding 30.
- **Finding 71's second half was a class of 14, and increment 55 CLOSED it.** The stub-then-reassign
  it names was genuinely a one-off (`0` remaining repo-wide after the fix). The stacked doc
  blocks were not: 13 sites across 8 files still carry two `/** ... */` blocks with nothing
  between them, so the first one merges onto whatever the second one describes and the subject
  it was written for is left bare. This is the SAME defect Rust findings 65/66 named, and it
  wanted the same treatment, and got it: every doc relocated onto its own subject, and
  `a_doc_block_stays_on_the_thing_it_describes_instead_of_merging_onto_its_neighbour` now keeps
  the count at zero. The 13 needed three DIFFERENT answers, which is why they were held out of
  increment 54 rather than waved through - 3 were two halves of one subject's doc and merged,
  9 were orphans whose real subject was sitting bare somewhere else, and 1 was STALE: it still
  told the reader `ImageStyle` has "no stretch option" while the field it sat on had one.
- **The P0 and findings 2, 27 sit in SB-POR-013..020 / SB-CLY-004 territory, all DEFERRED outside
  the DEC-018 pilot manifest** — the ruling that fixes them exists (DEC-012/017, SB-CLY-004); what
  is missing is scheduling, plus the test flips (the phi_son Cp pin and the topics() pin must be
  REWRITTEN, not merely satisfied).
- **Finding 30 (FTEMP gradients) and finding 51's clamp question are parameter/behaviour rulings**
  — flagged with citations, decided by Jauhar, never by an implementing agent.
- **Cheap systematic sweeps** (one mechanism each, many sites): 65 (orphan re-attachment), 72+73
  (cargo fmt + gate), 5 (one f64 cast), 59 (gate loop), 32 (path correction).
- **The units family (6, 7, 8, 14–19, 33, 45–48) wants ONE increment** with a single doctrine
  decision (what unit domain does `standard_curves` hold?) before any individual fix — patching
  finding 7 without 6 would convert the labels onto still-wrong values or vice versa.
- **Finding 70's increment surfaced a question for Jauhar, recorded here rather than decided.**
  A RESTORED log-set version stores `applied_steps_json` NULL, which the reader returns as
  UNKNOWN — *"the step history cannot be recovered"*. That is not true of a restore: the steps
  that produced those values are on the source row, which the restored `params_json`'s restore
  record names. It cannot say so under the current contract, for two reasons that compound.
  The manifest's `params_digest` is pinned to the digest of **the row's own** `params_json`
  (`equations.rs`, *"params_digest must be the SHA-256 of the row's own resolved parameter
  record"*), and a restore appends its record to that — so the source manifest cannot be copied
  across without breaking that invariant. Stating it properly needs a step of kind `restore`,
  which would extend SB-ENV-005's signed vocabulary (`module` | `correction` | `mask` | `edit`).
  Extending a signed vocabulary is Jauhar's ruling. The `None` is now EXPLICIT and carries this
  reasoning at the call site, so it is a stated position rather than an omitted column.
- Committing this report and turning this table into ROADMAP §4c rides the triage program — the
  audit itself changes nothing.

---

*Audit run 2026-08-19→20 by Claude (Fable 5) with five opus dimension reviewers (Pass 1), the
thermo-nuclear structural reviewer (Pass 2), and an opus deslop-rubric reviewer (Pass 3); every
reviewer candidate was adversarially self-refuted before reporting, survivors re-verified by the
session model at the decisive lines where marked. Read-only throughout.*
